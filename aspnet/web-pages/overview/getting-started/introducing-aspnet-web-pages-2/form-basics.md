---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: HTML formu temelleri ASP.NET Web sayfaları - giriş | Microsoft Docs
author: tfitzmac
description: Bu öğreticide, bir giriş formun nasıl oluşturulacağını ve ASP.NET Web sayfaları (Razor) kullandığınızda kullanıcının giriş nasıl ele alınacağını temelleri gösterilir. Ve şimdi...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: 6f44f74774c2fa6338524987779e15f3940d1830
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/06/2018
ms.locfileid: "30898927"
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="0b979-104">ASP.NET Web sayfalarını - HTML formu temelleri tanıtma</span><span class="sxs-lookup"><span data-stu-id="0b979-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="0b979-105">tarafından [zel FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="0b979-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="0b979-106">Bu öğreticide, bir giriş formun nasıl oluşturulacağını ve ASP.NET Web sayfaları (Razor) kullandığınızda kullanıcının giriş nasıl ele alınacağını temelleri gösterilir.</span><span class="sxs-lookup"><span data-stu-id="0b979-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="0b979-107">Ve bir veritabanı olduğuna göre veritabanında belirli filmler bulmalarına olanak form yeteneklerinizi kullanacaksınız.</span><span class="sxs-lookup"><span data-stu-id="0b979-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="0b979-108">Seri aracılığıyla tamamladığınızdan varsayar [giriş için görüntüleme verileri kullanarak ASP.NET Web sayfaları](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="0b979-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="0b979-109">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="0b979-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="0b979-110">Standart HTML öğeleri kullanılarak bir form oluşturma</span><span class="sxs-lookup"><span data-stu-id="0b979-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="0b979-111">Kullanıcı okumak nasıl bir formda giriş anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="0b979-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="0b979-112">Seçmeli olarak bir arama kullanarak verileri, kullanıcı terimi, bir SQL sorgusu oluşturmak nasıl sağlar.</span><span class="sxs-lookup"><span data-stu-id="0b979-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="0b979-113">Sayfasında "ne kullanıcının girdiği unutmayın" alanları nasıl.</span><span class="sxs-lookup"><span data-stu-id="0b979-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="0b979-114">Özellikler/teknolojilerini ele alınan:</span><span class="sxs-lookup"><span data-stu-id="0b979-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="0b979-115">`Request` Nesnesi.</span><span class="sxs-lookup"><span data-stu-id="0b979-115">The `Request` object.</span></span>
> - <span data-ttu-id="0b979-116">SQL `Where` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="0b979-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="0b979-117">Ne oluşturacağınız</span><span class="sxs-lookup"><span data-stu-id="0b979-117">What You'll Build</span></span>

<span data-ttu-id="0b979-118">Önceki öğreticide, bir veritabanı oluşturuldu, veri kendisine eklenmiş ve ardından kullanılan `WebGrid` verileri görüntülemek için yardımcı.</span><span class="sxs-lookup"><span data-stu-id="0b979-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="0b979-119">Bu öğreticide, belirli bir tarzını filmler bulmanıza sağlayan veya girdiğiniz ne olursa olsun word, başlık içeren bir arama kutusu ekleyeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="0b979-120">(Örneğin, size tarzı "Eylem" veya "Harry" veya "Adventure.", başlık içerdiği tüm filmler bulamıyor olması)</span><span class="sxs-lookup"><span data-stu-id="0b979-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="0b979-121">Bu öğreticiyi tamamladığınızda, bunun gibi bir sayfa sahip olursunuz:</span><span class="sxs-lookup"><span data-stu-id="0b979-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Tarz ve başlık arama filmler sayfası](form-basics/_static/image1.png)

<span data-ttu-id="0b979-123">Son öğretici ile aynı sayfa listeleme parçası olan &mdash; bir kılavuz.</span><span class="sxs-lookup"><span data-stu-id="0b979-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="0b979-124">Fark için arama kılavuz yalnızca filmler gösterecektir olacaktır.</span><span class="sxs-lookup"><span data-stu-id="0b979-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="0b979-125">HTML formları hakkında</span><span class="sxs-lookup"><span data-stu-id="0b979-125">About HTML Forms</span></span>

<span data-ttu-id="0b979-126">(Deneyimi HTML formları oluşturma ile arasındaki fark var olmadığını `GET` ve `POST`, bu bölümü atlayabilirsiniz.)</span><span class="sxs-lookup"><span data-stu-id="0b979-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="0b979-127">Kullanıcı girişi öğelerinin formun sahip &mdash; metin kutuları, düğmeleri, radyo düğmeleri, onay kutularını, açılan listeleri ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="0b979-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="0b979-128">Kullanıcılar bu denetimlerin doldurun veya seçim yapın ve ardından bir düğmeyi tıklatarak formu göndermeden.</span><span class="sxs-lookup"><span data-stu-id="0b979-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="0b979-129">Bu örnek tarafından formun temel HTML sözdizimi gösterilmiştir:</span><span class="sxs-lookup"><span data-stu-id="0b979-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="0b979-130">Bu biçimlendirme bir sayfa çalıştığında, bu çizim gibi görünen basit bir form oluşturur:</span><span class="sxs-lookup"><span data-stu-id="0b979-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Tarayıcıda işlenmiş olarak temel HTML formu](form-basics/_static/image2.png)

<span data-ttu-id="0b979-132">`<form>` Öğesi gönderilecek HTML öğeleri alır.</span><span class="sxs-lookup"><span data-stu-id="0b979-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="0b979-133">(Öğeleri sayfasına ekleyin, ancak bunları içine yerleştirin unuttunuz yapmak için kolay bir hata olduğundan bir `<form>` öğesi.</span><span class="sxs-lookup"><span data-stu-id="0b979-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="0b979-134">Bu durumda, hiçbir şey gönderilir.) `method` Öznitelik kullanıcı girişini gönderme tarayıcı söyler.</span><span class="sxs-lookup"><span data-stu-id="0b979-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="0b979-135">Bu ayar `post` bir güncelleştirme sunucusu veya çok gerçekleştiriyorsanız `get` yalnızca sunucudan veri getirme durumunda.</span><span class="sxs-lookup"><span data-stu-id="0b979-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="0b979-136">**GET, POST ve HTTP fiili güvenliği**</span><span class="sxs-lookup"><span data-stu-id="0b979-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="0b979-137">HTTP, tarayıcılar ve sunucuları bilgi, exchange kullanmasını protokolü temel işlemlerde çok kolaydır.</span><span class="sxs-lookup"><span data-stu-id="0b979-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="0b979-138">Tarayıcılar istekleri sunucularına yapmak için yalnızca birkaç fiilleri kullanın.</span><span class="sxs-lookup"><span data-stu-id="0b979-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="0b979-139">Web için kodu yazarken bu fiilleri ve bunları nasıl tarayıcı ve sunucu kullanılacağını anlamak faydalıdır.</span><span class="sxs-lookup"><span data-stu-id="0b979-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="0b979-140">En yaygın olarak kullanılan fiillerden bunlar ve uzakta:</span><span class="sxs-lookup"><span data-stu-id="0b979-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="0b979-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="0b979-141">`GET`.</span></span> <span data-ttu-id="0b979-142">Tarayıcı, bir sunucudan getirmek için bu fiil kullanır.</span><span class="sxs-lookup"><span data-stu-id="0b979-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="0b979-143">Örneğin, bir URL tarayıcınıza yazın, tarayıcı gerçekleştirdiğinde bir `GET` istediğiniz sayfasını istemek için işlemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="0b979-144">Sayfa grafik içeriyorsa, tarayıcı ek gerçekleştirir `GET` görüntüleri alma işlemleri.</span><span class="sxs-lookup"><span data-stu-id="0b979-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="0b979-145">Varsa `GET` işlemi sahip bilgi sunucuya geçirmek, bilgileri sorgu dizesinde URL'SİNİN bir parçası olarak geçirilir.</span><span class="sxs-lookup"><span data-stu-id="0b979-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="0b979-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="0b979-146">`POST`.</span></span> <span data-ttu-id="0b979-147">Tarayıcı gönderir bir `POST` eklenemez veya sunucu üzerinde değiştirilmiş için veri göndermek için istek.</span><span class="sxs-lookup"><span data-stu-id="0b979-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="0b979-148">Örneğin, `POST` fiili bir veritabanında kayıtları oluşturmak veya var olanları değiştirmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="0b979-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="0b979-149">Çoğu zaman, ne zaman bir formu doldurun ve Gönder düğmesine tıklayın, tarayıcı gerçekleştiren bir `POST` işlemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="0b979-150">İçinde bir `POST` sunucuya aktarılan veriler işlemdir sayfasının gövdesinde.</span><span class="sxs-lookup"><span data-stu-id="0b979-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="0b979-151">Bu eylemler arasında önemli bir fark olan bir `GET` işlemi sunucu üzerindeki herhangi bir şey değiştirmek için beklenen değildir — veya biraz daha soyut bir şekilde koymak için bir `GET` işlem sunucusunda durumda bir değişiklik neden olmaz.</span><span class="sxs-lookup"><span data-stu-id="0b979-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="0b979-152">Gerçekleştirebileceğiniz bir `GET` aynı kaynakları sayıda istediğiniz ve bu kaynakları değiştirme işlemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="0b979-153">(A `GET` işlemi genellikle denirse "safe" ya da teknik bir terim kullanılacak olan *ıdempotent*.) Buna karşılık, doğal olarak, bir `POST` isteği sunucuda bir şey işlemi gerçekleştirdiğiniz her zaman değiştirir.</span><span class="sxs-lookup"><span data-stu-id="0b979-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="0b979-154">İki örnek Bu ayrım göstermeye yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="0b979-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="0b979-155">Bing veya Google, gibi bir altyapısını kullanarak bir arama yaparken bir metin kutusunun oluşan bir formu doldurun ve ardından arama düğmesini tıklatın.</span><span class="sxs-lookup"><span data-stu-id="0b979-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="0b979-156">Tarayıcı gerçekleştiren bir `GET` işlemiyle URL'SİNİN bir parçası geçirilen kutusuna girdiğiniz değer.</span><span class="sxs-lookup"><span data-stu-id="0b979-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="0b979-157">Kullanarak bir `GET` işlemi sunucudaki tüm kaynakların bir arama işlemi değişmediğinden bu tür bir form uygundur için bu yalnızca bilgi getirir.</span><span class="sxs-lookup"><span data-stu-id="0b979-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="0b979-158">Şimdi, çevrimiçi bir şey sıralama işlemi göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="0b979-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="0b979-159">Sipariş ayrıntılarını doldurun ve ardından Gönder düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="0b979-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="0b979-160">Bu işlem bir `POST` isteği işlemi sunucuda yeni bir sipariş kaydı, bir değişiklik hesap bilgilerinizi ve belki de diğer birçok değişikliği gibi değişiklikler neden.</span><span class="sxs-lookup"><span data-stu-id="0b979-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="0b979-161">Farklı `GET` işlemi, yinelenemez, `POST` isteği — olsaydı, istek yeniden gönderildi her zaman, sunucuda yeni bir sıra oluşturur.</span><span class="sxs-lookup"><span data-stu-id="0b979-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="0b979-162">(Bu gibi durumlarda, Web siteleri genellikle bir gönderme düğmesi birden çok kez'i sizi uyarır ve böylece form yanlışlıkla yeniden yok Gönder düğmesi devre dışı bırakır.)</span><span class="sxs-lookup"><span data-stu-id="0b979-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="0b979-163">Bu öğretici esnasında her ikisi de kullanacağınız bir `GET` işlemi ve `POST` HTML formlarla çalışmak için işlemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="0b979-164">Uygun olanı kullandığınız fiil her servis talebi neden olan açıklayacağız.</span><span class="sxs-lookup"><span data-stu-id="0b979-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="0b979-165">(HTTP fiilleri hakkında daha fazla bilgi için bkz: [yöntemi tanımları](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) W3C sitesinde makale.)</span><span class="sxs-lookup"><span data-stu-id="0b979-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="0b979-166">Çoğu kullanıcı girişi öğelerinin HTML'dir `<input>` öğeleri.</span><span class="sxs-lookup"><span data-stu-id="0b979-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="0b979-167">Neye benzediğini gösteren `<input type="type" name="name">,` nerede *türü* istediğiniz kullanıcı giriş denetiminin türünü gösterir.</span><span class="sxs-lookup"><span data-stu-id="0b979-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="0b979-168">Bu öğeleri ortak olanlardır:</span><span class="sxs-lookup"><span data-stu-id="0b979-168">These elements are the common ones:</span></span>

- <span data-ttu-id="0b979-169">Metin kutusu: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="0b979-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="0b979-170">Onay kutusu: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="0b979-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="0b979-171">Radyo düğmesi: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="0b979-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="0b979-172">Button: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="0b979-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="0b979-173">Gönder düğmesi: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="0b979-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="0b979-174">De kullanabilirsiniz `<textarea>` öğenin çok satırlı metin kutusu oluşturma ve `<select>` aşağı açılan listesi veya kaydırılabilir listesi oluşturmak için öğesi.</span><span class="sxs-lookup"><span data-stu-id="0b979-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="0b979-175">(HTML hakkında daha fazla öğeleri oluşturmak için bkz: [HTML formları ve giriş](http://www.w3schools.com/html/html_forms.asp) W3Schools sitesinde.)</span><span class="sxs-lookup"><span data-stu-id="0b979-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="0b979-176">`name` Özniteliği çok önemlidir, kısa süre içinde anlatıldığı gibi daha sonra öğenin değeri nasıl elde edersiniz çünkü adı.</span><span class="sxs-lookup"><span data-stu-id="0b979-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="0b979-177">Kullanıcının girişle ne sayfasında geliştirici olarak size ilginç parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="0b979-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="0b979-178">Bu öğelerle ilişkili hiçbir yerleşik davranış yoktur.</span><span class="sxs-lookup"><span data-stu-id="0b979-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="0b979-179">Bunun yerine, kullanıcı girilen veya seçilen değerleri alır ve bunlarla bir şeyler gerekir.</span><span class="sxs-lookup"><span data-stu-id="0b979-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="0b979-180">Bu öğreticide öğrenecekleriniz olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="0b979-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="0b979-181">**HTML5 ve girdi biçimleri**</span><span class="sxs-lookup"><span data-stu-id="0b979-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="0b979-182">Biliyor olabilirsiniz HTML geçiş durumunda ve en son sürümünü (HTML5) bilgi girmek kullanıcılar için daha sezgisel yolları için destek içerir.</span><span class="sxs-lookup"><span data-stu-id="0b979-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="0b979-183">Örneğin, HTML5 ile bir tarih girmesini istediğiniz, (sayfa Geliştirici) sayfa anlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="0b979-184">Tarayıcı daha sonra otomatik olarak bir tarih el ile girmek kullanıcının gerektiren yerine bir takvim görüntüleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="0b979-185">Ancak, HTML5 yenidir ve tüm tarayıcılarda henüz desteklenmiyor.</span><span class="sxs-lookup"><span data-stu-id="0b979-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="0b979-186">ASP.NET Web sayfaları, kullanıcının tarayıcısının mu toplasa bile giriş HTML5 destekler.</span><span class="sxs-lookup"><span data-stu-id="0b979-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="0b979-187">Yeni öznitelikler için hakkında bir fikir için `<input>` HTML5, öğesinde bkz [HTML &lt;giriş&gt; öznitelik türü](http://www.w3schools.com/html/html_form_input_types.asp) W3Schools sitesinde.</span><span class="sxs-lookup"><span data-stu-id="0b979-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="0b979-188">Form Oluşturma</span><span class="sxs-lookup"><span data-stu-id="0b979-188">Creating the Form</span></span>

<span data-ttu-id="0b979-189">WebMatrix içinde içinde **dosyaları** çalışma alanında, açık *Movies.cshtml* sayfası.</span><span class="sxs-lookup"><span data-stu-id="0b979-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="0b979-190">Kapatma sonrasında `</h1>` etiketi ve açmadan önce `<div>` etiket `grid.GetHtml` çağrısı, aşağıdaki biçimlendirmeyi ekleyin:</span><span class="sxs-lookup"><span data-stu-id="0b979-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="0b979-191">Bu biçimlendirme adlı bir metin kutusu olan bir form oluşturur `searchGenre` ve bir gönderme düğmesi.</span><span class="sxs-lookup"><span data-stu-id="0b979-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="0b979-192">Metin kutusu ve düğmesi içine alınmıştır göndermek bir `<form>` öğesi, `method` özniteliği `get`.</span><span class="sxs-lookup"><span data-stu-id="0b979-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="0b979-193">(Metin kutusu put yoksa ve Gönder düğmesi içinde unutmayın bir `<form>` öğesi, düğmeye tıkladığınızda bir şey gönderilecek.) Kullandığınız `GET` fiil form oluşturmakta olduğunuz çünkü Burada, herhangi bir değişiklik sunucuda yapmaz — yalnızca bir arama sonuçları.</span><span class="sxs-lookup"><span data-stu-id="0b979-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="0b979-194">(Önceki öğreticide kullandığınız bir `post` değişiklikleri sunucuya gönderme nasıl olduğu yöntemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="0b979-195">Sonraki öğreticide yeniden göreceksiniz.)</span><span class="sxs-lookup"><span data-stu-id="0b979-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="0b979-196">Sayfayı çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="0b979-196">Run the page.</span></span> <span data-ttu-id="0b979-197">Form için herhangi bir davranış tanımlanan henüz rağmen nasıl göründüğünü görebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="0b979-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Arama kutusuna Tarz için filmler sayfası](form-basics/_static/image3.png)

<span data-ttu-id="0b979-199">"Komedi." gibi bir metin kutusuna bir değer girin</span><span class="sxs-lookup"><span data-stu-id="0b979-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="0b979-200">Ardından **arama Tarz**.</span><span class="sxs-lookup"><span data-stu-id="0b979-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="0b979-201">Sayfanın URL'sini not edin.</span><span class="sxs-lookup"><span data-stu-id="0b979-201">Take note of the URL of the page.</span></span> <span data-ttu-id="0b979-202">Ayarladığınız çünkü `<form>` öğenin `method` özniteliğini `get`, girdiğiniz değer artık böyle URL sorgu dizesinde parçasıdır:</span><span class="sxs-lookup"><span data-stu-id="0b979-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="0b979-203">Okuma Form değerleri</span><span class="sxs-lookup"><span data-stu-id="0b979-203">Reading Form Values</span></span>

<span data-ttu-id="0b979-204">Veritabanı verileri alır ve sonuçları kılavuzda görüntüleyen biraz kod sayfası zaten var.</span><span class="sxs-lookup"><span data-stu-id="0b979-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="0b979-205">Şimdi arama terimi içeren bir SQL sorgusu çalıştırabilmeniz için metin kutusunun değerini okuma biraz kod eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="0b979-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="0b979-206">Formun yöntemi kümesine çünkü `get`, aşağıdaki gibi kod kullanarak metin kutusuna girilen değer okuyabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="0b979-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="0b979-207">`Request.QueryString` Nesne ( `QueryString` özelliği `Request` nesnesi) bir parçası olarak gönderildikleri öğelerinin değerlerini içerir `GET` işlemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="0b979-208">`Request.QueryString` Özelliği içeren bir *koleksiyonu* (listesi) biçiminde gönderilen değerler.</span><span class="sxs-lookup"><span data-stu-id="0b979-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="0b979-209">Herhangi bir tek değer almak için istediğiniz öğenin adını belirtin.</span><span class="sxs-lookup"><span data-stu-id="0b979-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="0b979-210">İşte bu nedenle sahip olmanız bir `name` özniteliği `<input>` öğesi (`searchTerm`), metin kutusu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="0b979-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="0b979-211">(Hakkında daha fazla bilgi için `Request` nesne için bkz: [kenar](#BKMK_TheRequestObject) daha sonra.)</span><span class="sxs-lookup"><span data-stu-id="0b979-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="0b979-212">Metin kutusunun değerini okumaya basit bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="0b979-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="0b979-213">Ancak kullanıcı herhangi bir şey hiç metin kutusunda girmediniz ancak tıklattınız **arama** yine de olduğundan aramak için hiçbir şey o tıklatma göz ardı edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="0b979-214">Aşağıdaki kod bu koşullar uygulamak nasıl oluşturulduğunu gösteren bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="0b979-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="0b979-215">(Bu kodu henüz eklemeniz gerekmez; bu birazdan gerçekleştirirsiniz.)</span><span class="sxs-lookup"><span data-stu-id="0b979-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="0b979-216">Bu şekilde test parçalara ayırır:</span><span class="sxs-lookup"><span data-stu-id="0b979-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="0b979-217">Değerini alın `Request.QueryString["searchGenre"]`, yani içine girilen değer `<input>` adlı öğe `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="0b979-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="0b979-218">Bunu kullanarak boş olup olmadığını öğrenmek `IsEmpty` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="0b979-219">Bu yöntem, bir şey (örneğin, bir form öğesi) bir değer içerip içermediğini belirlemek için standart bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="0b979-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="0b979-220">Ancak yalnızca varsa, gerçekten, verdiğiniz *değil* boş, bu nedenle...</span><span class="sxs-lookup"><span data-stu-id="0b979-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="0b979-221">Ekleme `!` önüne işleci `IsEmpty` sınayın.</span><span class="sxs-lookup"><span data-stu-id="0b979-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="0b979-222">( `!` İşleci mantıksal değil anlamına gelir).</span><span class="sxs-lookup"><span data-stu-id="0b979-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="0b979-223">Düz İngilizce, tüm `if` koşul aşağıdaki çevirir: *formun searchGenre öğesi sonra boş değilse...*</span><span class="sxs-lookup"><span data-stu-id="0b979-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="0b979-224">Bu bloğu aşama arama terimi kullanan bir sorgu oluşturmak için ayarlar.</span><span class="sxs-lookup"><span data-stu-id="0b979-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="0b979-225">Bu sonraki bölümde gerçekleştirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="0b979-226">**İstek nesnesi**</span><span class="sxs-lookup"><span data-stu-id="0b979-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="0b979-227">`Request` Nesnesi bir sayfa istenen veya gönderildiğinde, uygulamanız için tarayıcı gönderen tüm bilgileri içerir.</span><span class="sxs-lookup"><span data-stu-id="0b979-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="0b979-228">Bu nesne kullanıcı sağlar metin kutusu değerleri veya karşıya yüklemek için bir dosya gibi herhangi bir bilgi içerir.</span><span class="sxs-lookup"><span data-stu-id="0b979-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="0b979-229">Ayrıca, her türlü tanımlama bilgileri, URL sorgu dizesi (varsa), değerleri gibi ek bilgileri içeren, kullanıcının kullanarak tarayıcıda ayarlanmış olan dillerin listesini tarayıcı türü çalıştıran sayfanın dosya yolu ve çok daha fazlası.</span><span class="sxs-lookup"><span data-stu-id="0b979-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="0b979-230">`Request` Nesne bir *koleksiyonu* (listesi) değerleri.</span><span class="sxs-lookup"><span data-stu-id="0b979-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="0b979-231">Tek bir değer koleksiyonu dışında adını belirterek alın:</span><span class="sxs-lookup"><span data-stu-id="0b979-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="0b979-232">`Request` Nesne gerçekten birkaç alt kümelerini kullanıma sunar.</span><span class="sxs-lookup"><span data-stu-id="0b979-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="0b979-233">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="0b979-233">For example:</span></span>
> 
> - <span data-ttu-id="0b979-234">`Request.Form` gönderilen içindeki öğelerden değerleri verir `<form>` istek ise öğesi bir `POST` isteği.</span><span class="sxs-lookup"><span data-stu-id="0b979-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="0b979-235">`Request.QueryString` URL'nin sorgu dizesinde yalnızca değerleri verir.</span><span class="sxs-lookup"><span data-stu-id="0b979-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="0b979-236">(Bir URL'de gibi `http://mysite/myapp/page?searchGenre=action&page=2`, `?searchGenre=action&page=2` bölümdür URL sorgu dizesi.)</span><span class="sxs-lookup"><span data-stu-id="0b979-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="0b979-237">`Request.Cookies` Koleksiyon tarayıcı gönderdi tanımlama bilgilerini erişmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="0b979-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="0b979-238">Bildiğiniz bir değer almak için gönderilen biçimindedir, kullanabileceğiniz `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="0b979-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="0b979-239">Alternatif olarak, daha belirli sürümlerini kullanabilirsiniz `Request.Form["name"]` (için `POST` istekleri) veya `Request.QueryString["name"]` (için `GET` istekleri).</span><span class="sxs-lookup"><span data-stu-id="0b979-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="0b979-240">Elbette, *adı* alınacak öğenin adıdır.</span><span class="sxs-lookup"><span data-stu-id="0b979-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="0b979-241">Almak istediğiniz öğenin adını kullandığınız koleksiyonu içinde benzersiz olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="0b979-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="0b979-242">İşte bu nedenle `Request` nesnesi sağlar kümeleri ister `Request.Form` ve `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="0b979-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="0b979-243">Sayfanız adlı bir form öğesi içeriyor varsayalım `userName` ve *de* adlı bir tanımlama bilgisi içeren `userName`.</span><span class="sxs-lookup"><span data-stu-id="0b979-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="0b979-244">Alırsanız `Request["userName"]`, form değer veya tanımlama bilgisinin isteyip istemediğiniz belirsiz.</span><span class="sxs-lookup"><span data-stu-id="0b979-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="0b979-245">Ancak, alırsanız `Request.Form["userName"]` veya `Request.Cookie["userName"]`, hangi değerini almak için açık.</span><span class="sxs-lookup"><span data-stu-id="0b979-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="0b979-246">Belirli ve alt kümesini kullanmak için iyi bir uygulamadır `Request` , gibi ilginizi `Request.Form` veya `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="0b979-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="0b979-247">Bu öğreticide oluşturduğunuz basit sayfaları için bu büyük olasılıkla gerçekten farklılığı yapmaz.</span><span class="sxs-lookup"><span data-stu-id="0b979-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="0b979-248">Ancak, daha karmaşık sayfaları oluştururken, açık sürüm kullanarak `Request.Form` veya `Request.QueryString` sayfa bir form (veya birden çok form) varsa ortaya çıkabilecek sorunları önlemenize yardımcı olabilir tanımlama bilgileri, sorgu dizesi değerlerini ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="0b979-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="0b979-249">Bir arama terimi kullanarak bir sorgu oluşturma</span><span class="sxs-lookup"><span data-stu-id="0b979-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="0b979-250">Kullanıcının girdiği arama terimi alma bildiğinize göre bunu kullanan bir sorgu oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="0b979-251">Veritabanı dışında tüm film öğeleri almak için bu bildirimi gibi görünen bir SQL sorgusu kullanmakta olduğunuz unutmayın:</span><span class="sxs-lookup"><span data-stu-id="0b979-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="0b979-252">İçeren bir sorgu kullanmak zorunda yalnızca belirli filmler almak için bir `Where` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="0b979-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="0b979-253">Bu yan tümcesi satırları sorgu tarafından döndürülen bir koşul ayarlamanıza olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="0b979-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="0b979-254">Örnek buradadır:</span><span class="sxs-lookup"><span data-stu-id="0b979-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="0b979-255">Temel biçim `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="0b979-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="0b979-256">Bunun yanında, yalnızca farklı işleçleri kullanabilirsiniz `=`gibi `>` (büyük), `<` (küçüktür), `<>` (eşit değildir), `<=` (küçük veya eşittir), vs. aradığınızı bağlı olarak.</span><span class="sxs-lookup"><span data-stu-id="0b979-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="0b979-257">SQL deyimleri, merak durumunda büyük küçük harfe duyarlı olmayan &mdash; `SELECT` aynı `Select` (veya hatta `select`).</span><span class="sxs-lookup"><span data-stu-id="0b979-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="0b979-258">Ancak, kişiler genellikle SQL deyimindeki anahtar sözcükler gibi büyük harfe çevirme `SELECT` ve `WHERE`okunmasını kolaylaştırmak için.</span><span class="sxs-lookup"><span data-stu-id="0b979-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="0b979-259">Arama terimi parametre olarak geçirme</span><span class="sxs-lookup"><span data-stu-id="0b979-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="0b979-260">Belirli bir tarzını yeterince kolay için arama (`WHERE Genre = 'Action'`), ancak kullanıcının girdiği herhangi bir tarzını için arama yapmak istiyorsanız.</span><span class="sxs-lookup"><span data-stu-id="0b979-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="0b979-261">Bunu yapmak için arama yapmak için bir yer tutucu değerini içeren SQL sorgusu oluşturun.</span><span class="sxs-lookup"><span data-stu-id="0b979-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="0b979-262">Bu komutu gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="0b979-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="0b979-263">Yer tutucusu `@` karakter sıfıra izlemelidir.</span><span class="sxs-lookup"><span data-stu-id="0b979-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="0b979-264">Tahmin gibi bir sorgu birden fazla yer tutucuları içerebilir ve bunların adlı `@0`, `@1`, `@2`vb.</span><span class="sxs-lookup"><span data-stu-id="0b979-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="0b979-265">Sorgusu ayarlama ve gerçekte değeri geçirmek için aşağıdaki gibi bir kod kullanın:</span><span class="sxs-lookup"><span data-stu-id="0b979-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="0b979-266">Bu kod, ne zaten veri ızgarada gösterilecek yaptığınızı için benzer.</span><span class="sxs-lookup"><span data-stu-id="0b979-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="0b979-267">Yalnızca farklılıklar şunlardır:</span><span class="sxs-lookup"><span data-stu-id="0b979-267">The only differences are:</span></span>

- <span data-ttu-id="0b979-268">Sorgu bir yer tutucu içerir (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="0b979-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="0b979-269">Sorgu bir değişkene konur (`selectCommand`); sorgu doğrudan geçirilen önce `db.Query` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="0b979-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="0b979-270">Çağırdığınızda `db.Query` yöntemi, geçirdiğiniz sorgu ve yer tutucusu için kullanılacak bir değer.</span><span class="sxs-lookup"><span data-stu-id="0b979-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="0b979-271">(Birden çok yer tutucu sorgu sahipse, bunları geçip geçmeyeceğini yöntemi için ayrı değerleri olarak tüm.)</span><span class="sxs-lookup"><span data-stu-id="0b979-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="0b979-272">Tüm bu öğeler bir araya getirme, aşağıdaki kodu alın:</span><span class="sxs-lookup"><span data-stu-id="0b979-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="0b979-273">**Önemli!**</span><span class="sxs-lookup"><span data-stu-id="0b979-273">**Important!**</span></span> <span data-ttu-id="0b979-274">Yer tutucuları kullanma (gibi `@0`) bir SQL komutu değerleri geçirmektir *son derece önemli* güvenlik için.</span><span class="sxs-lookup"><span data-stu-id="0b979-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="0b979-275">Burada, değişken veri yer tutucuları olan karşınıza SQL komutlarını oluşturmalısınız tek yolu yoludur.</span><span class="sxs-lookup"><span data-stu-id="0b979-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="0b979-276">Metin (birleştirme) ve kullanıcıdan Al değerleri birlikte koyarak hiçbir zaman bir SQL deyimi oluşturmak.</span><span class="sxs-lookup"><span data-stu-id="0b979-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="0b979-277">Kullanıcı bir SQL deyimi girişine birleştirme açar, sitenize bir *SQL ekleme saldırısında* burada kötü niyetli bir kullanıcının veritabanınızı korsan saldırılarına değerleri sayfanıza gönderir.</span><span class="sxs-lookup"><span data-stu-id="0b979-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="0b979-278">(Daha fazla bilgiyi makalesinde [SQL ekleme](https://msdn.microsoft.com/library/ms161953.aspx) MSDN Web.)</span><span class="sxs-lookup"><span data-stu-id="0b979-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="0b979-279">Arama koduyla filmler sayfa güncelleştiriliyor</span><span class="sxs-lookup"><span data-stu-id="0b979-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="0b979-280">Kodda güncelleştirebilirsiniz artık *Movies.cshtml* dosya.</span><span class="sxs-lookup"><span data-stu-id="0b979-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="0b979-281">Başlamak için sayfanın üst kısmındaki kod bloğundaki kod bu kod ile değiştirin:</span><span class="sxs-lookup"><span data-stu-id="0b979-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="0b979-282">Burada sorguyu yerleştirdiğiniz farktır `selectCommand` için geçirdiğiniz Değişken `db.Query` daha sonra.</span><span class="sxs-lookup"><span data-stu-id="0b979-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="0b979-283">Bir değişken SQL deyimine koyma arama yapmak için gerçekleştirirsiniz olduğundan deyim değiştirmenize olanak verir.</span><span class="sxs-lookup"><span data-stu-id="0b979-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="0b979-284">Ayrıca geri daha sonra içine bu iki satır kaldırdınız:</span><span class="sxs-lookup"><span data-stu-id="0b979-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="0b979-285">Sorgu henüz çalıştırmak istemediğiniz (diğer bir deyişle, çağrı `db.Query`) ve başlatmak istemiyorsanız `WebGrid` Yardımcısı henüz ya da.</span><span class="sxs-lookup"><span data-stu-id="0b979-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="0b979-286">Çalıştırmak hangi SQL deyimini olduğunu dahil edilir sonra bunlar gerçekleştirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="0b979-287">Bu yeniden bloğundan sonra arama işlemek için yeni mantık ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="0b979-288">Tamamlanan kodu aşağıdaki gibi görünür.</span><span class="sxs-lookup"><span data-stu-id="0b979-288">The completed code will look like the following.</span></span> <span data-ttu-id="0b979-289">Bu örnek eşleşecek şekilde sayfanıza kodunu güncelleştirin:</span><span class="sxs-lookup"><span data-stu-id="0b979-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="0b979-290">Sayfa şimdi gibi çalışır.</span><span class="sxs-lookup"><span data-stu-id="0b979-290">The page now works like this.</span></span> <span data-ttu-id="0b979-291">Sayfayı her çalıştığında veritabanını kodu açar ve `selectCommand` değişken olarak ayarlanmış tüm kayıtları alır SQL deyimine `Movies` tablo.</span><span class="sxs-lookup"><span data-stu-id="0b979-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="0b979-292">Kod ayrıca başlatır `searchTerm` değişkeni.</span><span class="sxs-lookup"><span data-stu-id="0b979-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="0b979-293">Ancak, geçerli istek için bir değer içeriyorsa, `searchGenre` öğesi, kod kümeleri `selectCommand` farklı bir sorgu için — yani, birine içeren `Where` bir tarzını için aranacak yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="0b979-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="0b979-294">Ayrıca ayarlar `searchTerm` için ne olursa olsun (hiçbir şey olabilir) arama kutusu için geçirildi.</span><span class="sxs-lookup"><span data-stu-id="0b979-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="0b979-295">Bağımsız olarak SQL deyimi yer `selectCommand`, kod sonra çağırır `db.Query` sorguyu çalıştırmak için hangi geçirme yer `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="0b979-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="0b979-296">Yoksa hiçbir şey `searchTerm`, bu durumda olduğu için hiçbir parametre değerine geçirmek için önemli değildir `selectCommand` yine de.</span><span class="sxs-lookup"><span data-stu-id="0b979-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="0b979-297">Son olarak, kod başlatır `WebGrid` önceden olduğu gibi sorgu sonuçları kullanarak Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="0b979-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="0b979-298">SQL deyimi koyarak görebilirsiniz ve değişkenleri, arama terimini koda esneklik ekledik.</span><span class="sxs-lookup"><span data-stu-id="0b979-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="0b979-299">Daha sonra Bu öğreticide gördüğünüz gibi bu temel çerçevesini kullanabilirsiniz ve aramaları farklı türleri için mantığı ekleme tutun.</span><span class="sxs-lookup"><span data-stu-id="0b979-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="0b979-300">Türe göre arama özelliğini test etme</span><span class="sxs-lookup"><span data-stu-id="0b979-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="0b979-301">Webmatrix'te Çalıştır *Movies.cshtml* sayfası.</span><span class="sxs-lookup"><span data-stu-id="0b979-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="0b979-302">Tarz metin kutusu sayfasına bakın.</span><span class="sxs-lookup"><span data-stu-id="0b979-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="0b979-303">Bir test kayıtlarınız için girdikten sonra'ı tıklatın bir tarzını girin **arama**.</span><span class="sxs-lookup"><span data-stu-id="0b979-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="0b979-304">Bu süre, o Tarz eşleşen filmler listesini görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="0b979-304">This time you see a listing of just the movies that match that genre:</span></span>

![Tarz 'Comedies' aradıktan sonra listeleyen filmler sayfası](form-basics/_static/image4.png)

<span data-ttu-id="0b979-306">Farklı bir tarzını girin ve yeniden arayın.</span><span class="sxs-lookup"><span data-stu-id="0b979-306">Enter a different genre and search again.</span></span> <span data-ttu-id="0b979-307">Arama büyük küçük harfe duyarlı değildir görebilmeniz için tüm küçük harfler veya tüm büyük harfleri kullanarak Tarz girmeyi deneyin.</span><span class="sxs-lookup"><span data-stu-id="0b979-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="0b979-308">"Ne kullanıcının girdiği hatırlamak"</span><span class="sxs-lookup"><span data-stu-id="0b979-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="0b979-309">Bir tarzını girilen ve tıklandığında sonra fark etmiş olabileceğiniz **arama Tarz**, bir listesi için bu tarz gördünüz.</span><span class="sxs-lookup"><span data-stu-id="0b979-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="0b979-310">Ancak, arama metin kutusuna boş &mdash; girilen başka bir deyişle, unutmayın alamadık.</span><span class="sxs-lookup"><span data-stu-id="0b979-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="0b979-311">Bu davranış neden meydana geldiğini anlamak önemlidir.</span><span class="sxs-lookup"><span data-stu-id="0b979-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="0b979-312">Bir sayfa gönderdiğinizde, tarayıcı web sunucusuna bir istek gönderir.</span><span class="sxs-lookup"><span data-stu-id="0b979-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="0b979-313">ASP.NET istek aldığında, sayfa yeni bir örneğini oluşturur, kod içinde çalışır ve tarayıcı sayfasına yeniden oluşturur.</span><span class="sxs-lookup"><span data-stu-id="0b979-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="0b979-314">Etkin, ancak sayfa kendisinin önceki bir sürümüyle yalnızca çalıştığınız bilmiyor.</span><span class="sxs-lookup"><span data-stu-id="0b979-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="0b979-315">Tüm olan bazı olan isteği aldı bilir veri içinde oluşturur.</span><span class="sxs-lookup"><span data-stu-id="0b979-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="0b979-316">Bir sayfa isteği her zaman &mdash; ilk kez mi göndermeden tarafından &mdash; yeni bir sayfa alıyorsunuz.</span><span class="sxs-lookup"><span data-stu-id="0b979-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="0b979-317">Web sunucusu son isteğinizin belleğe sahip değil.</span><span class="sxs-lookup"><span data-stu-id="0b979-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="0b979-318">ASP.NET, yapar ve tarayıcı, yapar.</span><span class="sxs-lookup"><span data-stu-id="0b979-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="0b979-319">Yalnızca bu sayfayı ayrı örnekleri arasında aralarında iletme herhangi bir veri bağlantısıdır.</span><span class="sxs-lookup"><span data-stu-id="0b979-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="0b979-320">Örneğin, bir sayfa gönderirseniz, yeni sayfa örneği önceki örneği tarafından gönderildiği form verileri alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="0b979-321">(Sayfaları arasında veri aktarmak için başka bir yolu, tanımlama bilgileri kullanmaktır.)</span><span class="sxs-lookup"><span data-stu-id="0b979-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="0b979-322">Bu durumu tanımlamak için bir resmi web sayfaları olduğunu söylemek için yoludur *durum bilgisiz*.</span><span class="sxs-lookup"><span data-stu-id="0b979-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="0b979-323">Web sunucuları ve sayfalarını ve sayfa öğeleri sayfa önceki durumuyla ilgili herhangi bir bilgi korumak değil.</span><span class="sxs-lookup"><span data-stu-id="0b979-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="0b979-324">Web istekleri ayrı ayrı durumu koruma hızla genellikle binlerce, belki de işlemek web sunucuları, kaynakları bile yüz binlerce, saniye başına istek tüketebilir çünkü bu şekilde tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="0b979-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="0b979-325">Neden olacak şekilde metin kutusu boş.</span><span class="sxs-lookup"><span data-stu-id="0b979-325">So that's why the text box was empty.</span></span> <span data-ttu-id="0b979-326">Sayfa gönderilen sonra ASP.NET sayfa yeni bir örneğini oluşturulan ve kod ve biçimlendirme çalıştırılmıştır.</span><span class="sxs-lookup"><span data-stu-id="0b979-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="0b979-327">Bir şey yoktu metin kutusuna bir değer almaya ASP.NET söylediyse bu kodu.</span><span class="sxs-lookup"><span data-stu-id="0b979-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="0b979-328">ASP.NET herhangi bir nedenle tepki vermedi ve metin kutusuna bir değer olmadan işlenir.</span><span class="sxs-lookup"><span data-stu-id="0b979-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="0b979-329">Bu sorunu çözmek almak için gerçekten kolay bir yol yoktur.</span><span class="sxs-lookup"><span data-stu-id="0b979-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="0b979-330">Metin kutusuna girdiğiniz Tarz *olan* kodda kullanabileceğiniz &mdash; içinde `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="0b979-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="0b979-331">Metin kutusu için biçimlendirme güncelleştirme böylece `value` öznitelik değerini alır `searchTerm`, bu örnek ister:</span><span class="sxs-lookup"><span data-stu-id="0b979-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="0b979-332">Bu sayfada ayrıca ayarladığınız `value` özniteliğini `searchTerm` girdiğiniz değişkeni, bu değişkeni ayrıca Tarz içerdiğinden.</span><span class="sxs-lookup"><span data-stu-id="0b979-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="0b979-333">Ancak kullanarak `Request` ayarlamak için nesne `value` özniteliği bu görevi gerçekleştirmek için standart yol gösterildiği gibi.</span><span class="sxs-lookup"><span data-stu-id="0b979-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="0b979-334">(Hatta istediğiniz Bunu yapmak varsayılarak &mdash; bazı durumlarda, sayfayı oluşturmak isteyebilirsiniz *olmadan* alanlarında değer.</span><span class="sxs-lookup"><span data-stu-id="0b979-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="0b979-335">Hiç uygulamanız ile neler olup bittiğini bağlıdır.)</span><span class="sxs-lookup"><span data-stu-id="0b979-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="0b979-336">"Parola için kullanılan bir metin kutusunun değerini unutmayın olamaz".</span><span class="sxs-lookup"><span data-stu-id="0b979-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="0b979-337">Kod kullanarak bir parola alanına doldurulacak izin vermek için bir güvenlik açığı olacaktır.</span><span class="sxs-lookup"><span data-stu-id="0b979-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="0b979-338">Sayfayı yeniden çalıştırın, bir tarzını girin ve tıklatın **arama Tarz**.</span><span class="sxs-lookup"><span data-stu-id="0b979-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="0b979-339">Bu zaman kalmaz, arama sonuçlarını görebilirsiniz, ancak son zamanı girilen metin kutusunda hatırlıyor:</span><span class="sxs-lookup"><span data-stu-id="0b979-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Metin kutusu 'önceki girdi hatırlanan olduğunu' gösteren sayfası](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="0b979-341">Başlıkta herhangi bir sözcük arama</span><span class="sxs-lookup"><span data-stu-id="0b979-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="0b979-342">Artık herhangi bir tarzını için arama yapabilirsiniz, ancak aynı zamanda için bir başlık arama yapmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="0b979-343">Bunun yerine bir başlık herhangi bir yerde görünür bir sözcük arayabilirsiniz arama yaparken bir başlık tam doğru elde etmek zordur.</span><span class="sxs-lookup"><span data-stu-id="0b979-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="0b979-344">SQL'de yapmak için kullandığınız `LIKE` işleci ve sözdizimi aşağıdaki gibi:</span><span class="sxs-lookup"><span data-stu-id="0b979-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="0b979-345">Bu komut, "adventure" başlıkları içeren tüm filmler alır.</span><span class="sxs-lookup"><span data-stu-id="0b979-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="0b979-346">Kullandığınızda `LIKE` işleci, joker karakteri içeren `%` arama terimi bir parçası olarak.</span><span class="sxs-lookup"><span data-stu-id="0b979-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="0b979-347">Arama `LIKE 'adventure%'` "'adventure' ile başlayan" anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="0b979-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="0b979-348">(Teknik olarak, bu "Dize 'tarafından herhangi bir şey ardından adventure'." anlamına gelir) Benzer şekilde, arama terimi `LIKE '%adventure'` "hiçbir şey 'adventure' dizesi tarafından izlenen", "Bitiş 'adventure' ile" söylemek için başka bir yol olduğu anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="0b979-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="0b979-349">Arama terimi `LIKE '%adventure%'` bu nedenle "ile"adventure' başlığı başka bir yerindeki."anlamına gelir</span><span class="sxs-lookup"><span data-stu-id="0b979-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="0b979-350">(Teknik olarak, "herhangi bir şey tarafından herhangi bir şey ardından adventure', arkasından başlığında.")</span><span class="sxs-lookup"><span data-stu-id="0b979-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="0b979-351">İçinde `<form>` öğesi, kapatma sağ altında aşağıdaki biçimlendirmeleri eklemek `</div>` etiketi Tarz arama için (yalnızca kapatmadan önce `</form>` öğesi):</span><span class="sxs-lookup"><span data-stu-id="0b979-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="0b979-352">Bir araya getirmek zorunda bu arama işlemek üzere kod Tarz arama koda benzerdir `LIKE` arama.</span><span class="sxs-lookup"><span data-stu-id="0b979-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="0b979-353">Bu sayfanın en üstündeki kod bloğunun içine ekleyin `if` hemen sonra engelleme `if` bloğu Tarz arama için:</span><span class="sxs-lookup"><span data-stu-id="0b979-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="0b979-354">Bu kodu, daha önce gördüğünüz aynı mantığı kullanır, arama kullanır ancak bu bir `LIKE` işleci ve kod geçirir "`%`" önce ve sonra arama terimi.</span><span class="sxs-lookup"><span data-stu-id="0b979-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="0b979-355">Başka bir arama eklemek kolay nasıl dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="0b979-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="0b979-356">Tüm yapmak zorunda oluştu:</span><span class="sxs-lookup"><span data-stu-id="0b979-356">All you had to do was:</span></span>

- <span data-ttu-id="0b979-357">Oluşturma bir `if` ilgili arama kutusunda bir değere sahip olup olmadığını görmek için test bloğu.</span><span class="sxs-lookup"><span data-stu-id="0b979-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="0b979-358">Ayarlama `selectCommand` yeni bir SQL deyimi için değişken.</span><span class="sxs-lookup"><span data-stu-id="0b979-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="0b979-359">Ayarlama `searchTerm` sorgusuna geçirilecek değerine değişken.</span><span class="sxs-lookup"><span data-stu-id="0b979-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="0b979-360">Bir başlık araması için yeni mantığı içeren tam kod bloğu şöyledir:</span><span class="sxs-lookup"><span data-stu-id="0b979-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="0b979-361">Bu kodun yaptığı bir özeti aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="0b979-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="0b979-362">Değişkenleri `searchTerm` ve `selectCommand` en üstünde başlatılır.</span><span class="sxs-lookup"><span data-stu-id="0b979-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="0b979-363">Bu değişkenler (varsa) ilgili arama terim kümesi olacak ve kullanıcı sayfasında yaptığı üzerinde uygun SQL komutu dayanır.</span><span class="sxs-lookup"><span data-stu-id="0b979-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="0b979-364">Varsayılan arama tüm filmler veritabanından alma basit bir durumdur.</span><span class="sxs-lookup"><span data-stu-id="0b979-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="0b979-365">İçin testlerinde `searchGenre` ve `searchTitle`, kod kümeleri `searchTerm` aramak istediğiniz değeri.</span><span class="sxs-lookup"><span data-stu-id="0b979-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="0b979-366">Bu kod blokları da ayarlamanız `selectCommand` bu arama için uygun bir SQL komutu.</span><span class="sxs-lookup"><span data-stu-id="0b979-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="0b979-367">`db.Query` Yöntemi çağrıldığında yalnızca bir kez içinde ne olursa olsun SQL komutunu kullanarak `selectedCommand` ve ne olursa olsun değer `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="0b979-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="0b979-368">(Bir tarzını ve başlık sözcük yok), arama terimi yok ise değerini `searchTerm` boş bir dize.</span><span class="sxs-lookup"><span data-stu-id="0b979-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="0b979-369">Bu durumda sorgu parametresi gerektirmediğinden ancak, bu, önemli değildir.</span><span class="sxs-lookup"><span data-stu-id="0b979-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="0b979-370">Başlık arama özelliğini test etme</span><span class="sxs-lookup"><span data-stu-id="0b979-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="0b979-371">Artık tamamlanmış arama sayfası test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-371">Now you can test your completed search page.</span></span> <span data-ttu-id="0b979-372">Çalıştırma *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0b979-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="0b979-373">Bir tarzını girin ve tıklayın **arama Tarz**.</span><span class="sxs-lookup"><span data-stu-id="0b979-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="0b979-374">Kılavuz önce gibi bu tarz filmler görüntüler.</span><span class="sxs-lookup"><span data-stu-id="0b979-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="0b979-375">Bir başlık sözcüğü girin ve tıklayın **arama başlık**.</span><span class="sxs-lookup"><span data-stu-id="0b979-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="0b979-376">Kılavuzun bu word başlığında filmler görüntüler.</span><span class="sxs-lookup"><span data-stu-id="0b979-376">The grid displays movies that have that word in the title.</span></span>

![Başlık '' için arama yaptıktan sonra listeleyen filmler sayfası](form-basics/_static/image6.png)

<span data-ttu-id="0b979-378">Her iki metin kutusunu boş bırakın ve her iki düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="0b979-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="0b979-379">Kılavuz tüm filmler görüntüler.</span><span class="sxs-lookup"><span data-stu-id="0b979-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="0b979-380">Sorguları birleştirme</span><span class="sxs-lookup"><span data-stu-id="0b979-380">Combining the Queries</span></span>

<span data-ttu-id="0b979-381">Gerçekleştirebileceğiniz aramaları dışlar fark edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="0b979-382">Her iki arama kutusu değerleri bunları olsa bile aynı anda başlığı ve bir tarzını arama yapamazsınız.</span><span class="sxs-lookup"><span data-stu-id="0b979-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="0b979-383">Örneğin, "Adventure", başlık içeren tüm eylem filmler için arama yapamazsınız.</span><span class="sxs-lookup"><span data-stu-id="0b979-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="0b979-384">(Başlık araması tarz ve başlığı için değerleri girerseniz, sayfa şimdi kodlanmış olarak öncelik alır.) Koşullar birleştiren bir arama oluşturmak için aşağıdaki gibi söz dizimi olan bir SQL sorgusu oluşturmak gerekir:</span><span class="sxs-lookup"><span data-stu-id="0b979-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="0b979-385">Ve aşağıdaki gibi bir deyimi kullanarak sorguyu çalıştırmanız gerekir (kabaca konuşulur):</span><span class="sxs-lookup"><span data-stu-id="0b979-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="0b979-386">Gördüğünüz gibi çok sayıda permütasyon arama ölçütü izin vermek için mantık oluşturma biraz karmaşık alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0b979-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="0b979-387">Bu nedenle, biz burada durdurmanız.</span><span class="sxs-lookup"><span data-stu-id="0b979-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="0b979-388">Sıradaki gelen</span><span class="sxs-lookup"><span data-stu-id="0b979-388">Coming Up Next</span></span>

<span data-ttu-id="0b979-389">Sonraki öğreticide filmler veritabanına eklemek kullanıcıların izin vermek için bir form kullanan bir sayfa oluşturacaksınız.</span><span class="sxs-lookup"><span data-stu-id="0b979-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="0b979-390">Tam listesi için (arama güncelleştirilmiş) film sayfası</span><span class="sxs-lookup"><span data-stu-id="0b979-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="0b979-391">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="0b979-391">Additional Resources</span></span>

- [<span data-ttu-id="0b979-392">Razor sözdizimini kullanan ASP.NET Web programlamaya giriş</span><span class="sxs-lookup"><span data-stu-id="0b979-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="0b979-393">[SQL WHERE yan tümcesi](http://www.w3schools.com/sql/sql_where.asp) W3Schools sitesinde</span><span class="sxs-lookup"><span data-stu-id="0b979-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="0b979-394">[Yöntem tanımlarını](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) W3C sitesinde makale</span><span class="sxs-lookup"><span data-stu-id="0b979-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="0b979-395">[Önceki](displaying-data.md)
> [sonraki](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="0b979-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>

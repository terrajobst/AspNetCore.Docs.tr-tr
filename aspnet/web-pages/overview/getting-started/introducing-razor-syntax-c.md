---
uid: web-pages/overview/getting-started/introducing-razor-syntax-c
title: Razor sözdizimini (C#) kullanarak ASP.NET Web programlamaya giriş | Microsoft Docs
author: tfitzmac
description: Bu bölümde Razor sözdizimini kullanarak ASP.NET Web sayfaları ile programlama genel bir bakış sağlar. ASP.NET dinamik web pa çalıştırmak için Microsoft'un teknolojidir...
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: aa67d304-583b-4bf8-a231-195656cfb587
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-c
msc.type: authoredcontent
ms.openlocfilehash: 430033c06df74cc3661c40ca7f7bd9244cd257c9
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/06/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-c"></a><span data-ttu-id="c6249-104">Razor sözdizimini (C#) kullanarak ASP.NET Web programlamaya giriş</span><span class="sxs-lookup"><span data-stu-id="c6249-104">Introduction to ASP.NET Web Programming Using the Razor Syntax (C#)</span></span>
====================
<span data-ttu-id="c6249-105">tarafından [zel FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="c6249-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="c6249-106">Bu makalede Razor sözdizimini kullanarak ASP.NET Web sayfaları ile programlama genel bir bakış sağlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-106">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax.</span></span> <span data-ttu-id="c6249-107">ASP.NET, dinamik web sayfaları web sunucuları üzerinde çalışan Microsoft teknolojisidir.</span><span class="sxs-lookup"><span data-stu-id="c6249-107">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span> <span data-ttu-id="c6249-108">C# programlama dilini kullanarak bu makaleler odaklar.</span><span class="sxs-lookup"><span data-stu-id="c6249-108">This articles focuses on using the C# programming language.</span></span>
> 
> <span data-ttu-id="c6249-109">**Öğrenecekleriniz**:</span><span class="sxs-lookup"><span data-stu-id="c6249-109">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="c6249-110">Programlama ipuçları Razor sözdizimini kullanan ASP.NET Web sayfaları programlama ile çalışmaya başlama için üst 8.</span><span class="sxs-lookup"><span data-stu-id="c6249-110">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="c6249-111">Temel programlama kavramları, gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-111">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="c6249-112">ASP.NET sunucu kodu nedir ve Razor sözdizimi olan tüm hakkında.</span><span class="sxs-lookup"><span data-stu-id="c6249-112">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="c6249-113">Yazılım sürümleri</span><span class="sxs-lookup"><span data-stu-id="c6249-113">Software versions</span></span>
> 
> 
> - <span data-ttu-id="c6249-114">ASP.NET Web sayfaları (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="c6249-114">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="c6249-115">Bu öğreticide, ASP.NET Web Pages 2 ile de çalışır.</span><span class="sxs-lookup"><span data-stu-id="c6249-115">This tutorial also works with ASP.NET Web Pages 2.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="c6249-116">8 programlama ipuçları</span><span class="sxs-lookup"><span data-stu-id="c6249-116">The Top 8 Programming Tips</span></span>

<span data-ttu-id="c6249-117">Bu bölümde kesinlikle Razor sözdizimini kullanan ASP.NET sunucusu kod yazmaya başladığınızda bilmeniz gereken birkaç ipucu listelenir.</span><span class="sxs-lookup"><span data-stu-id="c6249-117">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

> [!NOTE]
> <span data-ttu-id="c6249-118">Razor sözdizimi C# programlama dilini üzerinde temel alır ve ASP.NET Web sayfaları ile en sık kullanılan dil olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-118">The Razor syntax is based on the C# programming language, and that's the language that's used most often with ASP.NET Web Pages.</span></span> <span data-ttu-id="c6249-119">Ancak, Razor sözdizimini Visual Basic dili ve Visual Basic'te da yapabilirsiniz gördüğünüz her şeyi destekler.</span><span class="sxs-lookup"><span data-stu-id="c6249-119">However, the Razor syntax also supports the Visual Basic language, and everything you see you can also do in Visual Basic.</span></span> <span data-ttu-id="c6249-120">Ek ayrıntılar için bkz [Visual Basic dili ve sözdizimi](https://go.microsoft.com/fwlink/?LinkId=202908).</span><span class="sxs-lookup"><span data-stu-id="c6249-120">For details, see the appendix [Visual Basic Language and Syntax](https://go.microsoft.com/fwlink/?LinkId=202908).</span></span>


<span data-ttu-id="c6249-121">Bu programlama tekniklerinin çoğunu hakkında daha fazla bilgi makalenin sonraki bölümlerinde bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-121">You can find more details about most of these programming techniques later in the article.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="c6249-122">1. Kod kullanarak bir sayfa ekleyin @ karakteri</span><span class="sxs-lookup"><span data-stu-id="c6249-122">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="c6249-123">`@` Karakter satır içi ifadeler, tek bir deyimde blokları ve birden fazla deyim blokları başlatır:</span><span class="sxs-lookup"><span data-stu-id="c6249-123">The `@` character starts inline expressions, single statement blocks, and multi-statement blocks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample1.html)]

<span data-ttu-id="c6249-124">Bu sayfa bir tarayıcıda çalıştığında bu deyimleri nasıl göründüğünü oluşur:</span><span class="sxs-lookup"><span data-stu-id="c6249-124">This is what these statements look like when the page runs in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-c/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="c6249-126">**HTML Encoding**</span><span class="sxs-lookup"><span data-stu-id="c6249-126">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="c6249-127">Kullanarak bir sayfa içeriği görüntülediğinizde `@` karakter, önceki örneklerde olduğu gibi çıktı ASP.NET HTML olarak kodlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-127">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="c6249-128">Bu ayrılmış HTML karakterlerini değiştirir (gibi `<` ve `>` ve `&`) HTML etiketleri veya varlıklar olarak yorumlanmak yerine bir web sayfasında karakter olarak görüntülenecek karakterleri etkinleştirmek kodlarıyla.</span><span class="sxs-lookup"><span data-stu-id="c6249-128">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="c6249-129">HTML kodlaması olmadan sunucu kodunuzu çıktısını düzgün görüntülenmeyebilir ve sayfa güvenlik risklerine maruz bırakabileceğinden.</span><span class="sxs-lookup"><span data-stu-id="c6249-129">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="c6249-130">Amacınız biçimlendirme olarak etiketlerini işler HTML biçimlendirmesi çıkış olup olmadığını (örneğin `<p></p>` paragrafı için veya `<em></em>` metin vurgulamak için), bölümüne bakın [birleştirme metin, biçimlendirme ve kod blokları kodda](#BM_CombiningTextMarkupAndCode) bu makalenin ilerisinde yer.</span><span class="sxs-lookup"><span data-stu-id="c6249-130">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="c6249-131">Daha fazla bilgiyi, HTML kodlaması hakkında [formlarla çalışmak](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="c6249-131">You can read more about HTML encoding in [Working with Forms](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-in-braces"></a><span data-ttu-id="c6249-132">2. Kod blokları içinde köşeli parantez içine</span><span class="sxs-lookup"><span data-stu-id="c6249-132">2. You enclose code blocks in braces</span></span>

<span data-ttu-id="c6249-133">A *kod bloğunu* ayraç içine ve bir veya daha fazla kod deyimleri içerir.</span><span class="sxs-lookup"><span data-stu-id="c6249-133">A *code block* includes one or more code statements and is enclosed in braces.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample2.html)]

<span data-ttu-id="c6249-134">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="c6249-134">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-c/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-semicolon"></a><span data-ttu-id="c6249-136">3. Bir blok içinde her kod açıklaması noktalı virgül ile bitmelidir</span><span class="sxs-lookup"><span data-stu-id="c6249-136">3. Inside a block, you end each code statement with a semicolon</span></span>

<span data-ttu-id="c6249-137">Kod bloğu içinde her tam kod deyimi noktalı virgül ile bitmelidir.</span><span class="sxs-lookup"><span data-stu-id="c6249-137">Inside a code block, each complete code statement must end with a semicolon.</span></span> <span data-ttu-id="c6249-138">Satır içi ifadeler noktalı virgül ile sona ermez.</span><span class="sxs-lookup"><span data-stu-id="c6249-138">Inline expressions don't end with a semicolon.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample3.html)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="c6249-139">4. Değerlerini depolamak için değişkenleri kullanma</span><span class="sxs-lookup"><span data-stu-id="c6249-139">4. You use variables to store values</span></span>

<span data-ttu-id="c6249-140">Değerleri depolayabilir bir *değişkeni*dizeler, sayılar ve tarihleri, vb. dahil olmak üzere. Kullanarak yeni bir değişken oluşturmak `var` anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="c6249-140">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `var` keyword.</span></span> <span data-ttu-id="c6249-141">Doğrudan sayfasını kullanarak bir değişken değerleri ekleyebilirsiniz `@`.</span><span class="sxs-lookup"><span data-stu-id="c6249-141">You can insert variable values directly in a page using `@`.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample4.html)]

<span data-ttu-id="c6249-142">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="c6249-142">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-c/_static/image3.jpg)

<a id="ID_StringLiterals"></a>
### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="c6249-144">5. Değişmez değer dize değerleri çift tırnak işaretleri içine alın</span><span class="sxs-lookup"><span data-stu-id="c6249-144">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="c6249-145">A *dize* metin olarak kabul edilir karakter sırasıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-145">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="c6249-146">Bir dize belirtmek için çift tırnak işaretleri içine alın:</span><span class="sxs-lookup"><span data-stu-id="c6249-146">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample5.cshtml)]

<span data-ttu-id="c6249-147">Görüntülemek istediğiniz dizesi bir ters bölü karakteri içeriyorsa ( `\` ) veya çift tırnak işareti ( `"` ), kullanan bir *verbatim dize sabit değeri* ile önek `@` işleci.</span><span class="sxs-lookup"><span data-stu-id="c6249-147">If the string that you want to display contains a backslash character ( `\` ) or double quotation marks ( `"` ), use a *verbatim string literal* that's prefixed with the `@` operator.</span></span> <span data-ttu-id="c6249-148">(C# ' ta, \ verbatim dize sabit değeri kullanmadığınız sürece özel bir anlamı olan karakter.)</span><span class="sxs-lookup"><span data-stu-id="c6249-148">(In C#, the \ character has special meaning unless you use a verbatim string literal.)</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample6.html)]

<span data-ttu-id="c6249-149">Çift tırnak işaretleri eklemek için verbatim dize sabit değeri kullanın ve tırnak işaretleri yineleyin:</span><span class="sxs-lookup"><span data-stu-id="c6249-149">To embed double quotation marks, use a verbatim string literal and repeat the quotation marks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample7.html)]

<span data-ttu-id="c6249-150">Bu örneklerin her ikisinin bir sayfasında kullanmanın sonucu şöyledir:</span><span class="sxs-lookup"><span data-stu-id="c6249-150">Here's the result of using both of these examples in a page:</span></span>

![Razor-Img4](introducing-razor-syntax-c/_static/image4.jpg)

> [!NOTE]
> <span data-ttu-id="c6249-152">Dikkat `@` karakter verbatim dize değişmez değerleri C# işaretlemek ve ASP.NET sayfaları kodda işaretlemek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-152">Notice that the `@` character is used both to mark verbatim string literals in C# and to mark code in ASP.NET pages.</span></span>


### <a name="6-code-is-case-sensitive"></a><span data-ttu-id="c6249-153">6. Kodu büyük küçük harfe duyarlı.</span><span class="sxs-lookup"><span data-stu-id="c6249-153">6. Code is case sensitive</span></span>

<span data-ttu-id="c6249-154">C#, anahtar sözcükler (gibi `var`, `true`, ve `if`) ve değişken adları büyük küçük harfe duyarlı.</span><span class="sxs-lookup"><span data-stu-id="c6249-154">In C#, keywords (like `var`, `true`, and `if`) and variable names are case sensitive.</span></span> <span data-ttu-id="c6249-155">Aşağıdaki kod satırlarını iki farklı değişkenleri oluşturma `lastName` ve `LastName.`</span><span class="sxs-lookup"><span data-stu-id="c6249-155">The following lines of code create two different variables, `lastName` and `LastName.`</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample8.cshtml)]

<span data-ttu-id="c6249-156">Bir değişken olarak bildirirseniz `var lastName = "Smith";` ve sayfanız olarak bu değişken başvuru çalışırsanız `@LastName`, bir hata nedeniyle oluşur `LastName` tanınmaz.</span><span class="sxs-lookup"><span data-stu-id="c6249-156">If you declare a variable as `var lastName = "Smith";` and if you try to reference that variable in your page as `@LastName`, an error results because `LastName` won't be recognized.</span></span>

> [!NOTE]
> <span data-ttu-id="c6249-157">Visual Basic anahtar sözcükleri ve değişkenleri olan *değil* büyük küçük harfe duyarlı.</span><span class="sxs-lookup"><span data-stu-id="c6249-157">In Visual Basic, keywords and variables are *not* case sensitive.</span></span>


### <a name="7-much-of-your-coding-involves-objects"></a><span data-ttu-id="c6249-158">7. Kodlama çoğunu nesneleri içerir</span><span class="sxs-lookup"><span data-stu-id="c6249-158">7. Much of your coding involves objects</span></span>

<span data-ttu-id="c6249-159">Bir *nesne* ile program bir şeyi temsil eden &#8212; bir sayfa, bir metin kutusu, bir dosya, görüntü, bir web isteği, bir e-posta iletisi, bir müşteri kaydı (veritabanı satır) vb. Nesnelerin özelliklerini açıklayan özellikleri vardır ve okuma değiştirmek veya, &#8212; bir metin kutusu nesnesi bir `Text` istek nesnesi özelliği (diğerlerinin yanı sıra) sahip bir `Url` özelliği, e-posta iletisine sahip bir `From` özelliği ve müşteri nesnesi bir `FirstName` özelliği.</span><span class="sxs-lookup"><span data-stu-id="c6249-159">An *object* represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics and that you can read or change &#8212; a text box object has a `Text` property (among others), a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="c6249-160">Nesneleri olan yöntemlerini de &quot;fiiller&quot; yapabilirler.</span><span class="sxs-lookup"><span data-stu-id="c6249-160">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="c6249-161">Örnekler bir dosya nesnesinin `Save` yöntemi, bir görüntü nesnenin `Rotate` yöntemi ve e-posta nesnenin `Send` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c6249-161">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="c6249-162">Genellikle ile karşılaşmayacağınızı `Request` , metin kutuları (form alanları) değerleri gibi bilgiler sağlayan tarayıcının ne tür, sayfa, kullanıcı kimliği, vb. URL'sini istekte sayfasında, nesne. Aşağıdaki örnek özelliklerine erişmek nasıl gösterir `Request` nesne ve nasıl çağrılacağını `MapPath` yöntemi `Request` sayfasının mutlak yolu sunucuda verir nesnesi:</span><span class="sxs-lookup"><span data-stu-id="c6249-162">You'll often work with the `Request` object, which gives you information like the values of text boxes (form fields) on the page, what type of browser made the request, the URL of the page, the user identity, etc. The following example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample9.html)]

<span data-ttu-id="c6249-163">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="c6249-163">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-c/_static/image5.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="c6249-165">8. Kararları kod yazma</span><span class="sxs-lookup"><span data-stu-id="c6249-165">8. You can write code that makes decisions</span></span>

<span data-ttu-id="c6249-166">Bir anahtar dinamik web sayfaları koşullara dayanarak ne yapacağınıza belirlemek özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="c6249-166">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="c6249-167">Bunu yapmak için en yaygın yolu `if` deyimi (ve isteğe bağlı `else` deyimi).</span><span class="sxs-lookup"><span data-stu-id="c6249-167">The most common way to do this is with the `if` statement (and optional `else` statement).</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample10.cshtml)]

<span data-ttu-id="c6249-168">Deyim `if(IsPost)` kestirme yol yazma, `if(IsPost == true)`.</span><span class="sxs-lookup"><span data-stu-id="c6249-168">The statement `if(IsPost)` is a shorthand way of writing `if(IsPost == true)`.</span></span> <span data-ttu-id="c6249-169">İle birlikte `if` deyimleri, çeşitli yollarla koşullarda, kod, yineleme bloklarını test etmek için vardır ve vb., bu makalenin sonraki bölümlerinde açıklanmıştır.</span><span class="sxs-lookup"><span data-stu-id="c6249-169">Along with `if` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="c6249-170">Bir tarayıcıda görüntülenen sonuç (tıkladıktan sonra **gönderme**):</span><span class="sxs-lookup"><span data-stu-id="c6249-170">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-c/_static/image6.jpg)

> [!TIP] 
> 
> <a id="SB_HttpGetPost"></a>
> ### <a name="http-get-and-post-methods-and-the-ispost-property"></a><span data-ttu-id="c6249-172">HTTP GET ve POST yöntemleri ve IsPost özelliği</span><span class="sxs-lookup"><span data-stu-id="c6249-172">HTTP GET and POST Methods and the IsPost Property</span></span>
> 
> <span data-ttu-id="c6249-173">Web sayfaları (HTTP) için kullanılan protokol çok sınırlı sayıda sunucuya isteği yapmak için kullanılan yöntemleri (fiiller) destekler.</span><span class="sxs-lookup"><span data-stu-id="c6249-173">The protocol used for web pages (HTTP) supports a very limited number of methods (verbs) that are used to make requests to the server.</span></span> <span data-ttu-id="c6249-174">İki en yaygın bir sayfa okumak için kullanılan GET ve sayfa göndermek için kullanılan POST olanlardır.</span><span class="sxs-lookup"><span data-stu-id="c6249-174">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="c6249-175">Genel olarak, bir kullanıcı bir sayfa istekleri ilk kez sayfa GET kullanarak istendi.</span><span class="sxs-lookup"><span data-stu-id="c6249-175">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="c6249-176">Kullanıcı bir formda doldurur ve Gönder düğmesine tıkladığında, tarayıcı sunucuya bir POST isteği yapar.</span><span class="sxs-lookup"><span data-stu-id="c6249-176">If the user fills in a form and then clicks a submit button, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="c6249-177">Programlama web sayfasını işlemek nasıl bilmesi bir sayfa bir GET veya POST olarak istenen olup olmadığını öğrenmek yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-177">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="c6249-178">ASP.NET Web Pages'da kullandığınız `IsPost` bir isteğin bir GET veya POST olup olmadığını görmek için özellik.</span><span class="sxs-lookup"><span data-stu-id="c6249-178">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="c6249-179">Bir POST isteğiyse `IsPost` özelliği true döndürür ve metin kutuları bir form üzerinde değerlerini okuma gibi şeyler yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-179">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="c6249-180">Birçok örnek göreceksiniz sayfanın farklı değerine bağlı olarak işlemek nasıl Göster `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="c6249-180">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="c6249-181">Basit bir kod örneğidir</span><span class="sxs-lookup"><span data-stu-id="c6249-181">A Simple Code Example</span></span>

<span data-ttu-id="c6249-182">Bu yordam temel programlama tekniklerinin gösteren bir sayfa oluşturulacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="c6249-182">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="c6249-183">Örnekte, kullanıcıların bunları ekler ve sonucu görüntüler sonra iki sayıyı girin olanak sağlayan bir sayfa oluşturun.</span><span class="sxs-lookup"><span data-stu-id="c6249-183">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="c6249-184">Düzenleyicinizde, yeni bir dosya oluşturun ve adlandırın *AddNumbers.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c6249-184">In your editor, create a new file and name it *AddNumbers.cshtml*.</span></span>
2. <span data-ttu-id="c6249-185">Aşağıdaki kod ve biçimlendirme zaten sayfasındaki herhangi bir şey değiştirme sayfanın kopyalayın.</span><span class="sxs-lookup"><span data-stu-id="c6249-185">Copy the following code and markup into the page, replacing anything already in the page.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample11.cshtml)]

    <span data-ttu-id="c6249-186">Dikkat edilecek bazı öğeleri şunlardır:</span><span class="sxs-lookup"><span data-stu-id="c6249-186">Here are some things for you to note:</span></span>

    - <span data-ttu-id="c6249-187">`@` Karakter sayfasında ilk kod bloğunu başlar ve kendisinden `totalMessage` sayfanın altı katıştırılmış değişkeni.</span><span class="sxs-lookup"><span data-stu-id="c6249-187">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable that's embedded near the bottom of the page.</span></span>
    - <span data-ttu-id="c6249-188">Sayfanın üstündeki blok kaşlı ayraçlar içinde yer.</span><span class="sxs-lookup"><span data-stu-id="c6249-188">The block at the top of the page is enclosed in braces.</span></span>
    - <span data-ttu-id="c6249-189">Üst bloğunda noktalı virgül tüm satırların bitiş.</span><span class="sxs-lookup"><span data-stu-id="c6249-189">In the block at the top, all lines end with a semicolon.</span></span>
    - <span data-ttu-id="c6249-190">Değişkenleri `total`, `num1`, `num2`, ve `totalMessage` birkaç numarası ve bir dize depolar.</span><span class="sxs-lookup"><span data-stu-id="c6249-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="c6249-191">Atanan değişmez dize değeri `totalMessage` çift tırnak işaretleri değişkenidir.</span><span class="sxs-lookup"><span data-stu-id="c6249-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="c6249-192">Kod zaman büyük küçük harfe duyarlı, olduğundan `totalMessage` değişkeni sayfanın altı kullanıldığında, adını üst değişken tam olarak eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="c6249-192">Because the code is case-sensitive, when the `totalMessage` variable is used near the bottom of the page, its name must match the variable at the top exactly.</span></span>
    - <span data-ttu-id="c6249-193">İfade `num1.AsInt() + num2.AsInt()` nesneleri ve yöntemleri ile nasıl çalışılacağı gösterir.</span><span class="sxs-lookup"><span data-stu-id="c6249-193">The expression `num1.AsInt() + num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="c6249-194">`AsInt` Yöntemi her değişken, aritmetik üzerinde gerçekleştirebilmeleri için bir sayı (tamsayı) bir kullanıcı tarafından girilen dize dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="c6249-194">The `AsInt` method on each variable converts the string entered by a user to a number (an integer) so that you can perform arithmetic on it.</span></span>
    - <span data-ttu-id="c6249-195">`<form>` Etiketi de içeren bir `method="post"` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="c6249-195">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="c6249-196">Bu belirtir kullanıcı tıklattığında **Ekle**, sayfa HTTP POST yöntemini kullanarak sunucuya gönderilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-196">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="c6249-197">Sayfa gönderildiğinde `if(IsPost)` test değerlendirir true ve koşullu kod sayıları ekleme sonucu görüntülenirken çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="c6249-197">When the page is submitted, the `if(IsPost)` test evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="c6249-198">Sayfayı kaydedin ve tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="c6249-198">Save the page and run it in a browser.</span></span> <span data-ttu-id="c6249-199">(Emin olun sayfa seçildiğinde, **dosyaları** çalıştırmadan önce onu çalışma.) İki tam sayılar girin ve ardından **Ekle** düğmesi.</span><span class="sxs-lookup"><span data-stu-id="c6249-199">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span> 

    ![Razor-Img7](introducing-razor-syntax-c/_static/image7.jpg)

## <a name="basic-programming-concepts"></a><span data-ttu-id="c6249-201">Temel programlama kavramları</span><span class="sxs-lookup"><span data-stu-id="c6249-201">Basic Programming Concepts</span></span>

<span data-ttu-id="c6249-202">Bu makalede, ASP.NET web programlama bir bakış sağlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-202">This article provides you with an overview of ASP.NET web programming.</span></span> <span data-ttu-id="c6249-203">Yalnızca hızlı bir tur en sık kullanacağınız programlama kavramları aracılığıyla ayrıntılı bir incelemesi değil.</span><span class="sxs-lookup"><span data-stu-id="c6249-203">It isn't an exhaustive examination, just a quick tour through the programming concepts you'll use most often.</span></span> <span data-ttu-id="c6249-204">Buna rağmen neredeyse ASP.NET Web sayfaları ile çalışmaya başlamak için gereken her şeyi kapsar.</span><span class="sxs-lookup"><span data-stu-id="c6249-204">Even so, it covers almost everything you'll need to get started with ASP.NET Web Pages.</span></span>

<span data-ttu-id="c6249-205">Ancak ilk olarak, küçük bir teknik arka plan.</span><span class="sxs-lookup"><span data-stu-id="c6249-205">But first, a little technical background.</span></span>

### <a name="the-razor-syntax-server-code-and-aspnet"></a><span data-ttu-id="c6249-206">Razor sözdizimi, sunucu kodu ve ASP.NET</span><span class="sxs-lookup"><span data-stu-id="c6249-206">The Razor Syntax, Server Code, and ASP.NET</span></span>

<span data-ttu-id="c6249-207">Razor sözdizimi kod sunucu tabanlı bir web sayfasında katıştırmak için basit bir programlama sözdizimi şeklindedir.</span><span class="sxs-lookup"><span data-stu-id="c6249-207">Razor syntax is a simple programming syntax for embedding server-based code in a web page.</span></span> <span data-ttu-id="c6249-208">Razor sözdizimini kullanan bir web sayfası içeriği iki tür vardır: istemci içeriği ve sunucu kodu.</span><span class="sxs-lookup"><span data-stu-id="c6249-208">In a web page that uses the Razor syntax, there are two kinds of content: client content and server code.</span></span> <span data-ttu-id="c6249-209">İstemci içeriktir için kullandığınız web sayfalarında şeyler: HTML biçimlendirmesi (öğeleri), stil CSS gibi bilgileri belki JavaScript ve düz metin gibi bazı istemci komut dosyası.</span><span class="sxs-lookup"><span data-stu-id="c6249-209">Client content is the stuff you're used to in web pages: HTML markup (elements), style information such as CSS, maybe some client script such as JavaScript, and plain text.</span></span>

<span data-ttu-id="c6249-210">Razor sözdizimi, bu istemci içerik için sunucu kodu eklemenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-210">Razor syntax lets you add server code to this client content.</span></span> <span data-ttu-id="c6249-211">Sayfa tarayıcıya göndermeden önce sunucunun, bu kod sayfasında sunucu kodu varsa, ilk olarak, çalışır.</span><span class="sxs-lookup"><span data-stu-id="c6249-211">If there's server code in the page, the server runs that code first, before it sends the page to the browser.</span></span> <span data-ttu-id="c6249-212">Sunucu üzerinde çalışan tarafından kodu sunucu tabanlı veritabanlarına erişme gibi tek başına, istemci içeriği kullanarak yapmak için çok daha karmaşık olabilir görevleri gerçekleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-212">By running on the server, the code can perform tasks that can be a lot more complex to do using client content alone, like accessing server-based databases.</span></span> <span data-ttu-id="c6249-213">En önemlisi, sunucu kodu istemci içeriği dinamik olarak oluşturabilirsiniz &#8212; HTML biçimlendirmesi veya diğer içerik kolay bir şekilde oluşturmak ve sayfa içerebilecek herhangi bir statik HTML birlikte tarayıcı gönderin.</span><span class="sxs-lookup"><span data-stu-id="c6249-213">Most importantly, server code can dynamically create client content &#8212; it can generate HTML markup or other content on the fly and then send it to the browser along with any static HTML that the page might contain.</span></span> <span data-ttu-id="c6249-214">Tarayıcının açısından bakıldığında, sunucu kodunuz tarafından oluşturulan istemci içeriği herhangi bir istemci içerik farklı değildir.</span><span class="sxs-lookup"><span data-stu-id="c6249-214">From the browser's perspective, client content that's generated by your server code is no different than any other client content.</span></span> <span data-ttu-id="c6249-215">Önceden gördüğünüz gibi gerekli olan sunucu kodu oldukça basittir.</span><span class="sxs-lookup"><span data-stu-id="c6249-215">As you've already seen, the server code that's required is quite simple.</span></span>

<span data-ttu-id="c6249-216">Razor sözdizimi içeren ASP.NET web sayfaları bir özel dosya uzantısına sahip (*.cshtml* veya *.vbhtml*).</span><span class="sxs-lookup"><span data-stu-id="c6249-216">ASP.NET web pages that include the Razor syntax have a special file extension (*.cshtml* or *.vbhtml*).</span></span> <span data-ttu-id="c6249-217">Sunucu bu uzantıları tanır, Razor sözdizimi ile işaretlenmiş ve ardından sayfanın tarayıcıya gönderir. kodu çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="c6249-217">The server recognizes these extensions, runs the code that's marked with Razor syntax, and then sends the page to the browser.</span></span>

### <a name="where-does-aspnet-fit-in"></a><span data-ttu-id="c6249-218">ASP.NET nerelerde uygundur?</span><span class="sxs-lookup"><span data-stu-id="c6249-218">Where does ASP.NET fit in?</span></span>

<span data-ttu-id="c6249-219">Razor sözdizimi sırayla Microsoft .NET Framework temel alınarak ASP.NET adlı Microsoft teknolojisini temel alır.</span><span class="sxs-lookup"><span data-stu-id="c6249-219">Razor syntax is based on a technology from Microsoft called ASP.NET, which in turn is based on the Microsoft .NET Framework.</span></span> <span data-ttu-id="c6249-220">Neredeyse her türlü bilgisayarı uygulama geliştirmek için büyük, kapsamlı bir programlama çerçevesi Microsoft.NET Framework var.</span><span class="sxs-lookup"><span data-stu-id="c6249-220">The.NET Framework is a big, comprehensive programming framework from Microsoft for developing virtually any type of computer application.</span></span> <span data-ttu-id="c6249-221">ASP.NET web uygulamaları oluşturmak için özel olarak tasarlanmıştır .NET Framework'ün parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-221">ASP.NET is the part of the .NET Framework that's specifically designed for creating web applications.</span></span> <span data-ttu-id="c6249-222">Geliştiriciler dünyanın en büyük ve en yüksek trafik Web siteleri çoğunu oluşturmak için ASP.NET kullanmış.</span><span class="sxs-lookup"><span data-stu-id="c6249-222">Developers have used ASP.NET to create many of the largest and highest-traffic websites in the world.</span></span> <span data-ttu-id="c6249-223">(Her zaman dosya adı uzantısı bkz *.aspx* URL bir sitedeki bir parçası olarak, site ASP.NET kullanılarak yazılmıştır anlarsınız.)</span><span class="sxs-lookup"><span data-stu-id="c6249-223">(Any time you see the file-name extension *.aspx* as part of the URL in a site, you'll know that the site was written using ASP.NET.)</span></span>

<span data-ttu-id="c6249-224">Razor sözdizimi, ASP.NET, ancak bir başlangıç iseniz ve size daha üretken yapıyorsa uzmanı olup olmadığınızı öğrenmek daha kolay bir Basitleştirilmiş söz dizimini kullanarak tüm güç sağlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-224">The Razor syntax gives you all the power of ASP.NET, but using a simplified syntax that's easier to learn if you're a beginner and that makes you more productive if you're an expert.</span></span> <span data-ttu-id="c6249-225">Bu sözdiziminin kullanmak basit olsa bile, ASP.NET ve .NET Framework ailesi ilişkisini sitelerinizi daha karmaşık hale geldikçe kullanabileceğiniz daha büyük çerçeveleri güç olduğu anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="c6249-225">Even though this syntax is simple to use, its family relationship to ASP.NET and the .NET Framework means that as your websites become more sophisticated, you have the power of the larger frameworks available to you.</span></span>

![Razor-Img8](introducing-razor-syntax-c/_static/image8.jpg)

> [!TIP] 
> 
> <span data-ttu-id="c6249-227">**Sınıfları ve örnekleri**</span><span class="sxs-lookup"><span data-stu-id="c6249-227">**Classes and Instances**</span></span>
> 
> <span data-ttu-id="c6249-228">ASP.NET sunucusu kod sınıfları hakkında fikir üzerinde sırayla yerleşik nesneleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="c6249-228">ASP.NET server code uses objects, which are in turn built on the idea of classes.</span></span> <span data-ttu-id="c6249-229">Sınıf tanımı ve şablonu bir nesne için ' dir.</span><span class="sxs-lookup"><span data-stu-id="c6249-229">The class is the definition or template for an object.</span></span> <span data-ttu-id="c6249-230">Örneğin, bir uygulama içerebilir bir `Customer` sınıfı özellikleri ve herhangi bir müşteri nesnesi gereken yöntemleri tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-230">For example, an application might contain a `Customer` class that defines the properties and methods that any customer object needs.</span></span>
> 
> <span data-ttu-id="c6249-231">Uygulamayı gerçek müşteri bilgileri ile çalışmak gerektiğinde bir örneğini oluşturur (veya *başlatır*) bir müşteri nesnesi.</span><span class="sxs-lookup"><span data-stu-id="c6249-231">When the application needs to work with actual customer information, it creates an instance of (or *instantiates*) a customer object.</span></span> <span data-ttu-id="c6249-232">Her müşterinin ayrı bir örneği olan `Customer` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="c6249-232">Each individual customer is a separate instance of the `Customer` class.</span></span> <span data-ttu-id="c6249-233">Her örnek aynı özellikleri ve yöntemleri destekler, ancak her müşteri nesnesi benzersiz olduğundan her örneği için özellik değerlerini genellikle farklı.</span><span class="sxs-lookup"><span data-stu-id="c6249-233">Every instance supports the same properties and methods, but the property values for each instance are typically different, because each customer object is unique.</span></span> <span data-ttu-id="c6249-234">Bir müşteri nesnesindeki `LastName` özelliği "Smith" olabilir; başka bir müşteri nesnesindeki `LastName` özelliği "Can" olabilir</span><span class="sxs-lookup"><span data-stu-id="c6249-234">In one customer object, the `LastName` property might be "Smith"; in another customer object, the `LastName` property might be "Jones."</span></span>
> 
> <span data-ttu-id="c6249-235">Benzer şekilde, tüm tek tek web sitenizde sayfasıdır bir `Page` örneği nesne `Page` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="c6249-235">Similarly, any individual web page in your site is a `Page` object that's an instance of the `Page` class.</span></span> <span data-ttu-id="c6249-236">Düğme sayfasında bir `Button` örneği nesne `Button` sınıfı ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="c6249-236">A button on the page is a `Button` object that is an instance of the `Button` class, and so on.</span></span> <span data-ttu-id="c6249-237">Her örneğinin kendi özellikleri vardır, ancak bunlar tüm nesnenin sınıf tanımında belirtilen üzerinde temel alır.</span><span class="sxs-lookup"><span data-stu-id="c6249-237">Each instance has its own characteristics, but they all are based on what's specified in the object's class definition.</span></span>


## <a name="basic-syntax"></a><span data-ttu-id="c6249-238">Temel sözdizimi</span><span class="sxs-lookup"><span data-stu-id="c6249-238">Basic Syntax</span></span>

<span data-ttu-id="c6249-239">Daha önce bir ASP.NET Web Pages sayfası oluşturma ve sunucu kodunu HTML biçimlendirmesi nasıl ekleyebileceğiniz temel bir örneği gördünüz.</span><span class="sxs-lookup"><span data-stu-id="c6249-239">Earlier you saw a basic example of how to create an ASP.NET Web Pages page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="c6249-240">Burada Razor sözdizimini kullanarak ASP.NET sunucusu kod yazmaya temel bilgileri öğreneceksiniz &#8212; diğer bir deyişle, programlama dili kuralları.</span><span class="sxs-lookup"><span data-stu-id="c6249-240">Here you'll learn the basics of writing ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="c6249-241">(Özellikle, C, C++, C#, Visual Basic veya JavaScript kullandıysanız) programlama ile deneyimli değilseniz, ne burada okuma çoğunu tanıdık gelecektir.</span><span class="sxs-lookup"><span data-stu-id="c6249-241">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="c6249-242">Büyük olasılıkla yalnızca nasıl sunucu kodu biçimlendirmede eklenen ile öğrenmeniz gerekir *.cshtml* dosyaları.</span><span class="sxs-lookup"><span data-stu-id="c6249-242">You'll probably need to familiarize yourself only with how server code is added to markup in *.cshtml* files.</span></span>

<a id="BM_CombiningTextMarkupAndCode"></a>
### <a name="combining-text-markup-and-code-in-code-blocks"></a><span data-ttu-id="c6249-243">Metin, biçimlendirme ve kod blokları içinde kodu birleştirme</span><span class="sxs-lookup"><span data-stu-id="c6249-243">Combining Text, Markup, and Code in Code Blocks</span></span>

<span data-ttu-id="c6249-244">Sunucu kod bloğu, genellikle çıkış metni biçimlendirme (veya her ikisi de) sayfasına istersiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-244">In server code blocks, you often want to output text or markup (or both) to the page.</span></span> <span data-ttu-id="c6249-245">Sunucu kod bloğu kodu değil ve, bunun yerine olarak işleneceğini metin içeriyorsa, ASP.NET metnin kodunu ayırt etmek gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-245">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="c6249-246">Bunu yapmanın birkaç yolu vardır.</span><span class="sxs-lookup"><span data-stu-id="c6249-246">There are several ways to do this.</span></span>

- <span data-ttu-id="c6249-247">Gibi bir HTML öğesindeki metni içine `<p></p>` veya `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="c6249-247">Enclose the text in an HTML element like `<p></p>` or `<em></em>`:</span></span>   

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample12.cshtml)]

    <span data-ttu-id="c6249-248">HTML öğesi, metin, ek HTML öğeleri ve sunucu kodu ifadeleri içerebilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-248">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="c6249-249">ASP.NET, HTML etiketi açılış gördüğünde (örneğin, `<p>`), öğe dahil her şeyi işler ve bunun içeriği olarak gidiyor gibi sunucu kodu ifadeleri çözme tarayıcıya.</span><span class="sxs-lookup"><span data-stu-id="c6249-249">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything including the element and its content as is to the browser, resolving server-code expressions as it goes.</span></span>
- <span data-ttu-id="c6249-250">Kullanım `@:` işleci veya `<text>` öğesi.</span><span class="sxs-lookup"><span data-stu-id="c6249-250">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="c6249-251">`@:` Tek satırlık bir düz metin veya eşleşmeyen HTML etiketleri; içeren içerik çıkarır `<text>` öğesi çıktısını almak için birden fazla satır alır.</span><span class="sxs-lookup"><span data-stu-id="c6249-251">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="c6249-252">Bu seçenekler, çıkışı bir parçası olarak bir HTML öğesini işlemek istemediğiniz zaman yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-252">These options are useful when you don't want to render an HTML element as part of the output.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample13.cshtml)]

    <span data-ttu-id="c6249-253">Çok satırlı metin ya da eşleşmeyen HTML etiketleri çıktı istiyorsanız, her satırın önünde `@:`, veya satırda içine bir `<text>` öğesi.</span><span class="sxs-lookup"><span data-stu-id="c6249-253">If you want to output multiple lines of text or unmatched HTML tags, you can precede each line with `@:`, or you can enclose the line in a `<text>` element.</span></span> <span data-ttu-id="c6249-254">Gibi `@:` işleci,`<text>` etiketleri metin içeriği tanımlamak için ASP.NET tarafından kullanılan ve sayfa çıktıda hiçbir zaman işlenir.</span><span class="sxs-lookup"><span data-stu-id="c6249-254">Like the `@:` operator,`<text>` tags are used by ASP.NET to identify text content and are never rendered in the page output.</span></span>

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample14.cshtml)]

    <span data-ttu-id="c6249-255">İlk örnek önceki örnek yineler ancak tek bir çift kullanır `<text>` etiketleri oluşturmak için metni alın.</span><span class="sxs-lookup"><span data-stu-id="c6249-255">The first example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span> <span data-ttu-id="c6249-256">İkinci örnekteki `<text>` ve `</text>` etiketlerini içine üç satır, bunların tümü sahip bazı uncontained metin ve eşleşmeyen HTML etiketleri (`<br />`), sunucu kodu ve eşleşen HTML etiketleri yanı sıra.</span><span class="sxs-lookup"><span data-stu-id="c6249-256">In the second example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="c6249-257">Yine de her satırın tek tek koyun `@:` işleci; her iki şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="c6249-257">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    > [!NOTE]
    > <span data-ttu-id="c6249-258">Olduğunda, çıktı metin bu bölümde gösterilen &#8212; bir HTML öğesi kullanarak `@:` işleci veya `<text>` öğesi &#8212; ASP.NET olmayan HTML olarak kodlanacak çıktı.</span><span class="sxs-lookup"><span data-stu-id="c6249-258">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="c6249-259">(Daha önce belirtildiği gibi ASP.NET sunucu kodu ifadeleri ve tarafından öncesinde sunucu kod blokları çıktısını kodlamak `@`, bu bölümde belirtildiği özel durumlar hariç.)</span><span class="sxs-lookup"><span data-stu-id="c6249-259">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="c6249-260">Boşluk</span><span class="sxs-lookup"><span data-stu-id="c6249-260">Whitespace</span></span>

<span data-ttu-id="c6249-261">Ek boşluk deyimi içinde (ve bir değişmez dize değeri dışında) deyim etkilemez:</span><span class="sxs-lookup"><span data-stu-id="c6249-261">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample15.cshtml)]

<span data-ttu-id="c6249-262">Satır sonu deyimi içinde deyim üzerinde hiçbir etkisi olmaz ve Okunabilirlik için deyimleri kayabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-262">A line break in a statement has no effect on the statement, and you can wrap statements for readability.</span></span> <span data-ttu-id="c6249-263">Aşağıdaki deyimleri aynıdır:</span><span class="sxs-lookup"><span data-stu-id="c6249-263">The following statements are the same:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample16.cshtml)]

<span data-ttu-id="c6249-264">Ancak, bir değişmez dize değeri ortasında bir satır kaydırılamıyor.</span><span class="sxs-lookup"><span data-stu-id="c6249-264">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="c6249-265">Aşağıdaki örnek çalışmıyor:</span><span class="sxs-lookup"><span data-stu-id="c6249-265">The following example doesn't work:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample17.cshtml)]

<span data-ttu-id="c6249-266">Yukarıdaki kod gibi birden çok satır sarmalar uzun bir dize birleştirmek için iki seçenek vardır.</span><span class="sxs-lookup"><span data-stu-id="c6249-266">To combine a long string that wraps to multiple lines like the above code, there are two options.</span></span> <span data-ttu-id="c6249-267">Birleştirme işleci kullanabilirsiniz (`+`), hangi bu makalenin sonraki bölümlerinde görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="c6249-267">You can use the concatenation operator (`+`), which you'll see later in this article.</span></span> <span data-ttu-id="c6249-268">Aynı zamanda `@` bu makalede anlatıldığı gibi aynen dize değişmez değeri oluşturmak için karakter.</span><span class="sxs-lookup"><span data-stu-id="c6249-268">You can also use the `@` character to create a verbatim string literal, as you saw earlier in this article.</span></span> <span data-ttu-id="c6249-269">Harfi harfine dize değişmez değerleri satırlara bölünebilir:</span><span class="sxs-lookup"><span data-stu-id="c6249-269">You can break verbatim string literals across lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample18.cshtml)]

### <a name="code-and-markup-comments"></a><span data-ttu-id="c6249-270">Kod (ve biçimlendirme) açıklamaları</span><span class="sxs-lookup"><span data-stu-id="c6249-270">Code (and Markup) Comments</span></span>

<span data-ttu-id="c6249-271">Açıklamalar, kendiniz veya başkaları için Notlar bırakın olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="c6249-271">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="c6249-272">Devre dışı bırakmak de sağlarlar (*çıkışı açıklama*) bir bölümünü kodu veya çalıştırmak istediğiniz yoktur, ancak şimdilik sayfanıza tutmak istediğiniz biçimlendirme.</span><span class="sxs-lookup"><span data-stu-id="c6249-272">They also allow you to disable (*comment out*) a section of code or markup that you don't want to run but want to keep in your page for the time being.</span></span>

<span data-ttu-id="c6249-273">Sözdizimi HTML biçimlendirmesi ve Razor kod için yorum oluşturma farklı yoktur.</span><span class="sxs-lookup"><span data-stu-id="c6249-273">There's different commenting syntax for Razor code and for HTML markup.</span></span> <span data-ttu-id="c6249-274">Tüm Razor kod gibi ile Razor açıklamalar işlenen (ve sonra kaldırılan) sayfa tarayıcıya gönderilmeden önce sunucuda.</span><span class="sxs-lookup"><span data-stu-id="c6249-274">As with all Razor code, Razor comments are processed (and then removed) on the server before the page is sent to the browser.</span></span> <span data-ttu-id="c6249-275">Bu nedenle, Razor yorum sözdizimi, dosyayı düzenlemeniz ancak kullanıcıların görmüyorum, hatta sayfa kaynağında görebilirsiniz açıklamaları kodu (veya hatta işaretleme) yerleştirmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-275">Therefore, the Razor commenting syntax lets you put comments into the code (or even into the markup) that you can see when you edit the file, but that users don't see, even in the page source.</span></span>

<span data-ttu-id="c6249-276">ASP.NET Razor açıklamaları için açıklama ile başlangıç `@*` ve ile bitmelidir `*@`.</span><span class="sxs-lookup"><span data-stu-id="c6249-276">For ASP.NET Razor comments, you start the comment with `@*` and end it with `*@`.</span></span> <span data-ttu-id="c6249-277">Açıklama, tek bir çizgi veya birden çok satır olabilir:</span><span class="sxs-lookup"><span data-stu-id="c6249-277">The comment can be on one line or multiple lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample19.cshtml)]

<span data-ttu-id="c6249-278">Kod bloğu içinde bir yorum şöyledir:</span><span class="sxs-lookup"><span data-stu-id="c6249-278">Here is a comment within a code block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample20.cshtml)]

<span data-ttu-id="c6249-279">Böylece çalışmaz burada kod, aynı blok kod satırı ile kılınmıştır:</span><span class="sxs-lookup"><span data-stu-id="c6249-279">Here is the same block of code, with the line of code commented out so that it won't run:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample21.cshtml)]

<span data-ttu-id="c6249-280">Kod bloğunun içine Razor açıklama sözdizimi kullanılarak alternatif olarak, C# gibi kullandığınız programlama dili yorum söz dizimini kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c6249-280">Inside a code block, as an alternative to using Razor comment syntax, you can use the commenting syntax of the programming language you're using, such as C#:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample22.cshtml)]

<span data-ttu-id="c6249-281">C# ' ta tek satırlı yorumlar öncesinde `//` karakterler ve çok satırlı açıklamaları ile başlar `/*` ve sonunda `*/`.</span><span class="sxs-lookup"><span data-stu-id="c6249-281">In C#, single-line comments are preceded by the `//` characters, and multi-line comments begin with `/*` and end with `*/`.</span></span> <span data-ttu-id="c6249-282">(Razor yorumlarla gibi C# açıklamaları tarayıcıya işlenmez.)</span><span class="sxs-lookup"><span data-stu-id="c6249-282">(As with Razor comments, C# comments are not rendered to the browser.)</span></span>

<span data-ttu-id="c6249-283">İşaretleme için bildiğiniz gibi bir HTML açıklaması oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c6249-283">For markup, as you probably know, you can create an HTML comment:</span></span>

[!code-xml[Main](introducing-razor-syntax-c/samples/sample23.xml)]

<span data-ttu-id="c6249-284">HTML açıklamaları başlayarak `<!--` karakterler ve ile biten `-->`.</span><span class="sxs-lookup"><span data-stu-id="c6249-284">HTML comments start with `<!--` characters and end with `-->`.</span></span> <span data-ttu-id="c6249-285">Yalnızca metin ancak sayfasında tutmak isteyebilirsiniz, ancak işlemek istemediğiniz ayrıca herhangi bir HTML biçimlendirmesi surround için HTML açıklamaları kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-285">You can use HTML comments to surround not only text, but also any HTML markup that you may want to keep in the page but don't want to render.</span></span> <span data-ttu-id="c6249-286">Bu HTML açıklama, etiketler ve içerdikleri metin tüm içeriğini gizlenir:</span><span class="sxs-lookup"><span data-stu-id="c6249-286">This HTML comment will hide the entire content of the tags and the text they contain:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample24.html)]

<span data-ttu-id="c6249-287">Razor açıklama, HTML açıklamaları aksine *olan* sayfasına işlenmiş ve kullanıcı onları sayfa kaynağı görüntüleyerek görebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-287">Unlike Razor comments, HTML comments *are* rendered to the page and the user can see them by viewing the page source.</span></span>

<span data-ttu-id="c6249-288">Razor iç içe geçmiş bloklarını C# üzerinde sınırlamalara sahiptir.</span><span class="sxs-lookup"><span data-stu-id="c6249-288">Razor has limitations on nested blocks of C#.</span></span> <span data-ttu-id="c6249-289">Daha fazla bilgi için bkz: [adlı C# değişkenleri ve iç içe geçmiş blokları bozuk kodu oluştur](http://aspnetwebstack.codeplex.com/workitem/1914)</span><span class="sxs-lookup"><span data-stu-id="c6249-289">For more information see [Named C# Variables and Nested Blocks Generate Broken Code](http://aspnetwebstack.codeplex.com/workitem/1914)</span></span>

## <a name="variables"></a><span data-ttu-id="c6249-290">Değişkenler</span><span class="sxs-lookup"><span data-stu-id="c6249-290">Variables</span></span>

<span data-ttu-id="c6249-291">Bir değişken verilerini depolamak için kullandığınız adlandırılmış bir nesnedir.</span><span class="sxs-lookup"><span data-stu-id="c6249-291">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="c6249-292">Herhangi bir şey değişkenleri adı verebilirsiniz ancak adı alfabetik bir karakter ile başlamalı ve boşluk ya da ayrılmış karakterler içeremez.</span><span class="sxs-lookup"><span data-stu-id="c6249-292">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="c6249-293">Değişkenleri ve veri türleri</span><span class="sxs-lookup"><span data-stu-id="c6249-293">Variables and Data Types</span></span>

<span data-ttu-id="c6249-294">Bir değişken ne tür veriler değişkende depolanır gösteren bir özel veri türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-294">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="c6249-295">Dize değerlerini depolayan dize değişkenleri olabilir (gibi &quot;Merhaba Dünya&quot;), tam sayı değerleri (örneğin, 3 veya 79) depolamak tamsayı değişkenleri ve biçimleri (4/12/2012 veya Mart 2009 gibi çeşitli tarih değerlerini depolama tarih değişkenleri ).</span><span class="sxs-lookup"><span data-stu-id="c6249-295">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="c6249-296">Ve kullanabileceğiniz birçok diğer veri türleri vardır.</span><span class="sxs-lookup"><span data-stu-id="c6249-296">And there are many other data types you can use.</span></span>

<span data-ttu-id="c6249-297">Ancak, genellikle bir değişken için bir tür belirtmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="c6249-297">However, you generally don't have to specify a type for a variable.</span></span> <span data-ttu-id="c6249-298">Çoğu zaman, ASP.NET değişkeni verileri nasıl kullanıldığını temel türü tahmin.</span><span class="sxs-lookup"><span data-stu-id="c6249-298">Most of the time, ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="c6249-299">(Bazen bir türü belirtmeniz gerekir; bu doğru olduğu örnekler görürsünüz.)</span><span class="sxs-lookup"><span data-stu-id="c6249-299">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="c6249-300">Bir değişken kullanarak bildirme `var` (bir tür belirtmek istemiyorsanız) anahtar sözcüğü veya türünün adını kullanarak:</span><span class="sxs-lookup"><span data-stu-id="c6249-300">You declare a variable using the `var` keyword (if you don't want to specify a type) or by using the name of the type:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample25.cshtml)]

<span data-ttu-id="c6249-301">Aşağıdaki örnek, bir web sayfasında bazı tipik kullanımları değişkenlerin gösterir:</span><span class="sxs-lookup"><span data-stu-id="c6249-301">The following example shows some typical uses of variables in a web page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample26.cshtml)]

<span data-ttu-id="c6249-302">Bir sayfa önceki örneklerde birleştiriyorsanız, bu bir tarayıcıda görüntülenen bakın:</span><span class="sxs-lookup"><span data-stu-id="c6249-302">If you combine the previous examples in a page, you see this displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-c/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="c6249-304">Dönüştürme ve veri türleri test etme</span><span class="sxs-lookup"><span data-stu-id="c6249-304">Converting and Testing Data Types</span></span>

<span data-ttu-id="c6249-305">Bazen ASP.NET genellikle bir veri türü otomatik olarak belirlemek de, işlem gerçekleştirilemiyor.</span><span class="sxs-lookup"><span data-stu-id="c6249-305">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="c6249-306">Bu nedenle, açık bir dönüştürme gerçekleştirerek ASP.NET yardımcı olması gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-306">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="c6249-307">Bazen türleri dönüştürme gerekmez olsa bile, veri türünü, birlikte çalışabilir görmek için test etmek yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="c6249-307">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="c6249-308">En yaygın olarak bir tam sayı veya tarih gibi bir dizeyi başka bir türüne dönüştürmek sahip olur.</span><span class="sxs-lookup"><span data-stu-id="c6249-308">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="c6249-309">Aşağıdaki örnek, burada, bir dizeyi sayıya dönüştürme gerekir tipik bir durumu gösterir.</span><span class="sxs-lookup"><span data-stu-id="c6249-309">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample27.cshtml)]

<span data-ttu-id="c6249-310">Bir kural olarak, kullanıcı girişi için dize olarak gelir.</span><span class="sxs-lookup"><span data-stu-id="c6249-310">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="c6249-311">Bir sayı girin kullanıcılardan olsa bile ve bir rakam girmiş olduğunuz da kullanıcı girişi gönderildiğinde ve kodda okuma olsa bile, veri dize biçiminde sağlanır.</span><span class="sxs-lookup"><span data-stu-id="c6249-311">Even if you've prompted users to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="c6249-312">Bu nedenle, dize bir sayıya dönüştürmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-312">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="c6249-313">Bunları, dönüştürmeden değerlerine aritmetik gerçekleştirmeye çalışırsanız ASP.NET iki dizeyi eklediğinden örnekte, aşağıdaki hata, sonuçları:</span><span class="sxs-lookup"><span data-stu-id="c6249-313">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

<span data-ttu-id="c6249-314">*Örtük olarak 'int' için ' string' türü dönüştürülemiyor.*</span><span class="sxs-lookup"><span data-stu-id="c6249-314">*Cannot implicitly convert type 'string' to 'int'.*</span></span>

<span data-ttu-id="c6249-315">Tamsayılara değerleri dönüştürmek için arama `AsInt` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c6249-315">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="c6249-316">Dönüştürme başarılı olursa, sayıları daha sonra ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-316">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="c6249-317">Aşağıdaki tabloda bazı yaygın dönüştürme ve test yöntemleri değişkenleri listeler.</span><span class="sxs-lookup"><span data-stu-id="c6249-317">The following table lists some common conversion and test methods for variables.</span></span>


|   <span data-ttu-id="c6249-318"><strong>Yöntemi</strong></span><span class="sxs-lookup"><span data-stu-id="c6249-318"><strong>Method</strong></span></span>    |                                                                              <span data-ttu-id="c6249-319"><strong>Açıklama</strong></span><span class="sxs-lookup"><span data-stu-id="c6249-319"><strong>Description</strong></span></span>                                                                              |                         <span data-ttu-id="c6249-320"><strong>Örnek</strong></span><span class="sxs-lookup"><span data-stu-id="c6249-320"><strong>Example</strong></span></span>                         |
|------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
|      `AsInt(), IsInt()`      |                                                      <span data-ttu-id="c6249-321">Bir tam sayı (örneğin, "593") bir tamsayı olarak temsil eden bir dize dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="c6249-321">Converts a string that represents a whole number (like "593") to an integer.</span></span>                                                      |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample28.cs)]   |
|     `AsBool(), IsBool()`     |                                                    <span data-ttu-id="c6249-322">Gibi bir dizeyi dönüştürür &quot;true&quot; veya &quot;false&quot; bir Boolean türü.</span><span class="sxs-lookup"><span data-stu-id="c6249-322">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span>                                                     |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample29.cs)]   |
|    `AsFloat(), IsFloat()`    |                                    <span data-ttu-id="c6249-323">Gibi ondalık bir değeri olan bir dize dönüştürür &quot;1.3&quot; veya &quot;7.439&quot; bir kayan noktalı sayı.</span><span class="sxs-lookup"><span data-stu-id="c6249-323">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span>                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample30.cs)]   |
|  `AsDecimal(), IsDecimal()`  | <span data-ttu-id="c6249-324">Gibi ondalık bir değeri olan bir dize dönüştürür &quot;1.3&quot; veya &quot;7.439&quot; ondalık sayıya.</span><span class="sxs-lookup"><span data-stu-id="c6249-324">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="c6249-325">(ASP.NET, ondalık sayı bir kayan nokta numarasından daha kesin.)</span><span class="sxs-lookup"><span data-stu-id="c6249-325">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample31.cs)]   |
| `AsDateTime(), IsDateTime()` |                                                <span data-ttu-id="c6249-326">ASP.NET için bir tarih ve saat değerini temsil eden bir dize dönüştürür `DateTime` türü.</span><span class="sxs-lookup"><span data-stu-id="c6249-326">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span>                                                 |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample32.cs)]   |
|         `ToString()`         |                                                                       <span data-ttu-id="c6249-327">Başka bir veri türü bir dizeye dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="c6249-327">Converts any other data type to a string.</span></span>                                                                        | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample33.js)] |

## <a name="operators"></a><span data-ttu-id="c6249-328">İşleçler</span><span class="sxs-lookup"><span data-stu-id="c6249-328">Operators</span></span>

<span data-ttu-id="c6249-329">Bir işleç bir anahtar sözcük veya ASP ne tür bir ifadede gerçekleştirmek için komutu bir karakter değil.</span><span class="sxs-lookup"><span data-stu-id="c6249-329">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="c6249-330">C# dili (ve bunu temel alan Razor sözdizimi) birçok işleçleri destekler, ancak yalnızca başlamak için birkaç tanıması gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-330">The C# language (and the Razor syntax that's based on it) supports many operators, but you only need to recognize a few to get started.</span></span> <span data-ttu-id="c6249-331">Aşağıdaki tabloda, en yaygın işleçleri özetler.</span><span class="sxs-lookup"><span data-stu-id="c6249-331">The following table summarizes the most common operators.</span></span>


|   <span data-ttu-id="c6249-332"><strong>Operator</strong></span><span class="sxs-lookup"><span data-stu-id="c6249-332"><strong>Operator</strong></span></span>    |                                                                     <span data-ttu-id="c6249-333"><strong>Açıklama</strong></span><span class="sxs-lookup"><span data-stu-id="c6249-333"><strong>Description</strong></span></span>                                                                     |                        <span data-ttu-id="c6249-334"><strong>Örnekler</strong></span><span class="sxs-lookup"><span data-stu-id="c6249-334"><strong>Examples</strong></span></span>                         |
|--------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
|        <span data-ttu-id="c6249-335">`+` `-` `*` `/`</span><span class="sxs-lookup"><span data-stu-id="c6249-335">`+` `-` `*` `/`</span></span>         |                                                            <span data-ttu-id="c6249-336">Matematik işleçleri sayısal ifadelerde kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-336">Math operators used in numerical expressions.</span></span>                                                             |    [!code-css[Main](introducing-razor-syntax-c/samples/sample34.css)]    |
|              `=`               |                                    <span data-ttu-id="c6249-337">Atama.</span><span class="sxs-lookup"><span data-stu-id="c6249-337">Assignment.</span></span> <span data-ttu-id="c6249-338">Sol tarafındaki nesnesine sağ tarafında deyiminin değeri atar.</span><span class="sxs-lookup"><span data-stu-id="c6249-338">Assigns the value on the right side of a statement to the object on the left side.</span></span>                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample35.cs)]   |
|              `==`              |                      <span data-ttu-id="c6249-339">Eşitlik.</span><span class="sxs-lookup"><span data-stu-id="c6249-339">Equality.</span></span> <span data-ttu-id="c6249-340">Döndürür `true` değerleri aynıysa.</span><span class="sxs-lookup"><span data-stu-id="c6249-340">Returns `true` if the values are equal.</span></span> <span data-ttu-id="c6249-341">(Arasında ayrım fark `=` işleci ve `==` işleci.)</span><span class="sxs-lookup"><span data-stu-id="c6249-341">(Notice the distinction between the `=` operator and the `==` operator.)</span></span>                      |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample36.cs)]   |
|              `!=`              |                                                       <span data-ttu-id="c6249-342">Eşitsizlik.</span><span class="sxs-lookup"><span data-stu-id="c6249-342">Inequality.</span></span> <span data-ttu-id="c6249-343">Döndürür `true` değerler eşit değilse.</span><span class="sxs-lookup"><span data-stu-id="c6249-343">Returns `true` if the values are not equal.</span></span>                                                        |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample37.cs)]   |
|          `< > <= >=`           |                                               <span data-ttu-id="c6249-344">Daha az-büyük daha-daha, daha az-daha-veya-eşittir ve büyük-daha-veya-eşittir.</span><span class="sxs-lookup"><span data-stu-id="c6249-344">Less-than, greater-than, less-than-or-equal, and greater-than-or-equal.</span></span>                                                |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample38.cs)]   |
|              `+`               | <span data-ttu-id="c6249-345">Birleştirme dizeleri eklemek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-345">Concatenation, which is used to join strings.</span></span> <span data-ttu-id="c6249-346">ASP.NET bu işleci ifade veri türüne göre toplama işleci arasındaki fark bilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-346">ASP.NET knows the difference between this operator and the addition operator based on the data type of the expression.</span></span> |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample39.cs)]   |
|           <span data-ttu-id="c6249-347">`+=``-=`</span><span class="sxs-lookup"><span data-stu-id="c6249-347">`+=` `-=`</span></span>            |                                   <span data-ttu-id="c6249-348">Ekleme ve 1 (sırasıyla) bir değişkeninden artırma ve azaltma işleçleri.</span><span class="sxs-lookup"><span data-stu-id="c6249-348">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span>                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample40.cs)]   |
|              `.`               |                                                  <span data-ttu-id="c6249-349">Nokta.</span><span class="sxs-lookup"><span data-stu-id="c6249-349">Dot.</span></span> <span data-ttu-id="c6249-350">Nesneleri ve özellikleri ve yöntemleri ayırt etmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-350">Used to distinguish objects and their properties and methods.</span></span>                                                  |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample41.cs)]   |
|              `()`              |                                              <span data-ttu-id="c6249-351">Parantez.</span><span class="sxs-lookup"><span data-stu-id="c6249-351">Parentheses.</span></span> <span data-ttu-id="c6249-352">Grup ifadeleri ve parametreleri yöntemlere geçirmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-352">Used to group expressions and to pass parameters to methods.</span></span>                                               | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample42.js)] |
|              `[]`              |                                                    <span data-ttu-id="c6249-353">Köşeli ayraçlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-353">Brackets.</span></span> <span data-ttu-id="c6249-354">Diziler veya koleksiyonlar değerleri erişmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-354">Used for accessing values in arrays or collections.</span></span>                                                     |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample43.cs)]   |
|              `!`               |               <span data-ttu-id="c6249-355">Değil.</span><span class="sxs-lookup"><span data-stu-id="c6249-355">Not.</span></span> <span data-ttu-id="c6249-356">Tersine çevirir bir `true` değeri `false` tersi.</span><span class="sxs-lookup"><span data-stu-id="c6249-356">Reverses a `true` value to `false` and vice versa.</span></span> <span data-ttu-id="c6249-357">Sınamak için bir toplu şekilde genelde kullanılan `false` (diğer bir deyişle, için değil `true`).</span><span class="sxs-lookup"><span data-stu-id="c6249-357">Typically used as a shorthand way to test for `false` (that is, for not `true`).</span></span>               |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample44.cs)]   |
| <span data-ttu-id="c6249-358">`&&`<code>&#124;&#124;</code></span><span class="sxs-lookup"><span data-stu-id="c6249-358">`&&` <code>&#124;&#124;</code></span></span> |                                                   <span data-ttu-id="c6249-359">Mantıksal AND ve OR koşulları birlikte hangi bağlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-359">Logical AND and OR, which are used to link conditions together.</span></span>                                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample45.cs)]   |

<a id="ID_WorkingWithFileAndFolderPaths"></a>
## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="c6249-360">Dosya ve klasör yollarında kodu ile çalışma</span><span class="sxs-lookup"><span data-stu-id="c6249-360">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="c6249-361">Genellikle, kodunuzda iş ile dosya ve klasör yolları.</span><span class="sxs-lookup"><span data-stu-id="c6249-361">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="c6249-362">Burada, geliştirme bilgisayarınızda görünebilir gibi bir Web sitesi için fiziksel klasör yapısı örneği verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="c6249-362">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="c6249-363">URL'ler ve yollar hakkında bazı temel ayrıntıları aşağıdadır:</span><span class="sxs-lookup"><span data-stu-id="c6249-363">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="c6249-364">Bir URL ile ya da bir etki alanı adı başlar (`http://www.example.com`) veya bir sunucu adı (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="c6249-364">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="c6249-365">Bir URL bir ana bilgisayardaki fiziksel bir yola karşılık gelir.</span><span class="sxs-lookup"><span data-stu-id="c6249-365">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="c6249-366">Örneğin, `http://myserver` klasörüne karşılık gelebilir *C:\websites\mywebsite* sunucusunda.</span><span class="sxs-lookup"><span data-stu-id="c6249-366">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="c6249-367">Sanal yol tam yolunu belirtmek zorunda kalmadan kod yolları temsil etmek için toplu özelliktir.</span><span class="sxs-lookup"><span data-stu-id="c6249-367">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="c6249-368">Etki alanı veya sunucu adı bir URL bölümünü içerir.</span><span class="sxs-lookup"><span data-stu-id="c6249-368">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="c6249-369">Sanal yol kullandığınızda, yolları güncelleştirmek zorunda kalmadan farklı etki alanı veya sunucu için kodunuzu taşıyabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-369">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="c6249-370">Farkları anlamanıza yardımcı olması için örnek aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="c6249-370">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="c6249-371">Tam URL</span><span class="sxs-lookup"><span data-stu-id="c6249-371">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="c6249-372">Sunucu adı</span><span class="sxs-lookup"><span data-stu-id="c6249-372">Server name</span></span> | <span data-ttu-id="c6249-373">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="c6249-373">*mycompanyserver*</span></span> |
| <span data-ttu-id="c6249-374">Sanal yol</span><span class="sxs-lookup"><span data-stu-id="c6249-374">Virtual path</span></span> | <span data-ttu-id="c6249-375">*/HumanResources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="c6249-375">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="c6249-376">Fiziksel yol</span><span class="sxs-lookup"><span data-stu-id="c6249-376">Physical path</span></span> | <span data-ttu-id="c6249-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="c6249-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="c6249-378">Sanal kök / yalnızca C: aynı kök gibi sürücüdür \.</span><span class="sxs-lookup"><span data-stu-id="c6249-378">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="c6249-379">(Sanal klasör yolları her zaman eğik kullanın.) Sanal bir klasör yolu fiziksel klasör olarak aynı ada sahip gerekmez; bir diğer ad olabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-379">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="c6249-380">(Üretim sunucularında, sanal yolu nadiren tam fiziksel yolunu eşleşir.)</span><span class="sxs-lookup"><span data-stu-id="c6249-380">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="c6249-381">Bazen kodu bulunan dosya ve klasörleri ile çalışırken, fiziksel yol ve bazen çalıştığınız hangi nesneleri bağlı olarak sanal bir yol başvuru gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-381">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="c6249-382">ASP.NET size bu araçları kodu dosya ve klasör yollarında ile çalışmak için: `Server.MapPath` yöntemi ve `~` işleci ve `Href` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c6249-382">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="c6249-383">Fiziksel için sanal yol dönüştürme: Server.MapPath yöntemi</span><span class="sxs-lookup"><span data-stu-id="c6249-383">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="c6249-384">`Server.MapPath` Yöntemi dönüştürür sanal bir yol (gibi */default.cshtml*) mutlak fiziksel yola (gibi *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="c6249-384">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="c6249-385">Tam fiziksel yolunu gerek duyduğunuz her zaman bu yöntemi kullanın.</span><span class="sxs-lookup"><span data-stu-id="c6249-385">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="c6249-386">Okunurken ya da bir metin dosyası veya web sunucusunda görüntü dosyası yazılırken tipik bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="c6249-386">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="c6249-387">Mutlak fiziksel yol barındırma site sunucusunda, sitenizin genellikle bilmiyorsanız, bu yöntem yolu dönüştürebilirsiniz şekilde bildiğiniz — sanal yolu —, sunucuda karşılık gelen yolu.</span><span class="sxs-lookup"><span data-stu-id="c6249-387">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="c6249-388">Sanal yol bir dosya veya klasör yöntemine geçirdiğiniz ve fiziksel yolu döndürür:</span><span class="sxs-lookup"><span data-stu-id="c6249-388">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample46.cshtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="c6249-389">Sanal kök başvuran: ~ işleci ve Href yöntemi</span><span class="sxs-lookup"><span data-stu-id="c6249-389">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="c6249-390">İçinde bir *.cshtml* veya *.vbhtml* dosyası, sanal kök yolu kullanarak başvuru `~` işleci.</span><span class="sxs-lookup"><span data-stu-id="c6249-390">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="c6249-391">Bu bir sitede sayfaları taşıyabilirsiniz ve diğer sayfalara içerdikleri tüm bağlantılar bozuk olmayacaktır çünkü çok kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-391">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="c6249-392">Ayrıca, her zamankinden Web sitenizi farklı bir konuma taşımanız durumunda kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-392">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="c6249-393">Bazı örnekler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="c6249-393">Here are some examples:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample47.cshtml)]

<span data-ttu-id="c6249-394">Web sitesi ise `http://myserver/myapp`, işte sayfa çalıştığında ASP.NET bu yollar nasıl kabul eder:</span><span class="sxs-lookup"><span data-stu-id="c6249-394">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="c6249-395">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="c6249-395">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="c6249-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="c6249-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="c6249-397">(Aslında bu yollar değişkeni değerleri olarak görmez, ancak, ne oldukları ise ASP.NET yollar kabul eder.)</span><span class="sxs-lookup"><span data-stu-id="c6249-397">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="c6249-398">Kullanabileceğiniz `~` işleci (yukarıdaki gibi) sunucu kodu hem de bu gibi biçimlendirme:</span><span class="sxs-lookup"><span data-stu-id="c6249-398">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample48.html)]

<span data-ttu-id="c6249-399">Biçimlendirme içinde kullandığınız `~` görüntü dosyaları, diğer web sayfalarını ve CSS dosyaları gibi kaynaklarına yollar oluşturmak için işleç.</span><span class="sxs-lookup"><span data-stu-id="c6249-399">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="c6249-400">Sayfa çalıştığında, ASP.NET (kod ve biçimlendirme) sayfadan arar ve tüm çözümler `~` uygun yolu başvuruları.</span><span class="sxs-lookup"><span data-stu-id="c6249-400">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="c6249-401">Koşullu mantık ve döngüler</span><span class="sxs-lookup"><span data-stu-id="c6249-401">Conditional Logic and Loops</span></span>

<span data-ttu-id="c6249-402">ASP.NET sunucusu kod koşullara göre görevlerini gerçekleştirmek ve belirli bir sayıda deyimleri yineler kod yazmayı sağlar (diğer bir deyişle, bir döngü çalıştırır kodu).</span><span class="sxs-lookup"><span data-stu-id="c6249-402">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times (that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="c6249-403">Koşullar test etme</span><span class="sxs-lookup"><span data-stu-id="c6249-403">Testing Conditions</span></span>

<span data-ttu-id="c6249-404">Kullandığınız basit bir koşulu test etmek için `if` true veya false değerini döndürür, belirttiğiniz bir testi temel alan deyimi:</span><span class="sxs-lookup"><span data-stu-id="c6249-404">To test a simple condition you use the `if` statement, which returns true or false based on a test you specify:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample49.cshtml)]

<span data-ttu-id="c6249-405">`if` Anahtar sözcüğü bir blok başlatır.</span><span class="sxs-lookup"><span data-stu-id="c6249-405">The `if` keyword starts a block.</span></span> <span data-ttu-id="c6249-406">Gerçek test (koşul) parantez içinde olduğundan ve true veya false döndürür.</span><span class="sxs-lookup"><span data-stu-id="c6249-406">The actual test (condition) is in parentheses and returns true or false.</span></span> <span data-ttu-id="c6249-407">Test true ise çalıştırılan deyimleri ayraç içine alınır.</span><span class="sxs-lookup"><span data-stu-id="c6249-407">The statements that run if the test is true are enclosed in braces.</span></span> <span data-ttu-id="c6249-408">Bir `if` deyimi dahil edebileceğiniz bir `else` koşul yanlış ise, çalıştırılacak deyimleri belirtir engelle:</span><span class="sxs-lookup"><span data-stu-id="c6249-408">An `if` statement can include an `else` block that specifies statements to run if the condition is false:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample50.cshtml)]

<span data-ttu-id="c6249-409">Kullanarak birden çok koşul ekleyebileceğiniz bir `else if` engelle:</span><span class="sxs-lookup"><span data-stu-id="c6249-409">You can add multiple conditions using an `else if` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample51.cshtml)]

<span data-ttu-id="c6249-410">Eğer ilk koşul, bu örnekte, blok true değil `else if` koşul denetlenir.</span><span class="sxs-lookup"><span data-stu-id="c6249-410">In this example, if the first condition in the if block is not true, the `else if` condition is checked.</span></span> <span data-ttu-id="c6249-411">Bu koşul karşılandığında deyimlerinde `else if` blok çalıştırılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-411">If that condition is met, the statements in the `else if` block are executed.</span></span> <span data-ttu-id="c6249-412">Hiç bir koşul karşılanıyorsa deyimlerinde `else` blok çalıştırılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-412">If none of the conditions are met, the statements in the `else` block are executed.</span></span> <span data-ttu-id="c6249-413">Else IF herhangi bir sayıda ekleyebilirsiniz engeller ve ile kapatın bir `else` olarak engellemek &quot;şey&quot; koşulu.</span><span class="sxs-lookup"><span data-stu-id="c6249-413">You can add any number of else if blocks, and then close with an `else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="c6249-414">Çok sayıda koşullar sınamak için kullanın bir `switch` engelle:</span><span class="sxs-lookup"><span data-stu-id="c6249-414">To test a large number of conditions, use a `switch` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample52.cshtml)]

<span data-ttu-id="c6249-415">Test etmek için parantez içinde değerdir (örnekte `weekday` değişkeni).</span><span class="sxs-lookup"><span data-stu-id="c6249-415">The value to test is in parentheses (in the example, the `weekday` variable).</span></span> <span data-ttu-id="c6249-416">Tek tek her test kullanan bir `case` iki nokta (:) ile biter deyimi.</span><span class="sxs-lookup"><span data-stu-id="c6249-416">Each individual test uses a `case` statement that ends with a colon (:).</span></span> <span data-ttu-id="c6249-417">Varsa değerini bir `case` deyimi eşleşen test değeri, bu örneği bloğundaki kod yürütülür.</span><span class="sxs-lookup"><span data-stu-id="c6249-417">If the value of a `case` statement matches the test value, the code in that case block is executed.</span></span> <span data-ttu-id="c6249-418">Her case deyimiyle kapatmak bir `break` deyimi.</span><span class="sxs-lookup"><span data-stu-id="c6249-418">You close each case statement with a `break` statement.</span></span> <span data-ttu-id="c6249-419">(Her sonu eklenecek unutursanız `case` engellemek, sonraki koddan `case` deyimi de çalışır.) A `switch` blok genellikle sahip bir `default` deyimi için son durum olarak bir &quot;şey&quot; diğer durumlarda hiçbiri doğruysa, çalıştırılan seçeneği.</span><span class="sxs-lookup"><span data-stu-id="c6249-419">(If you forget to include break in each `case` block, the code from the next `case` statement will run also.) A `switch` block often has a `default` statement as the last case for an &quot;everything else&quot; option that runs if none of the other cases are true.</span></span>

<span data-ttu-id="c6249-420">Bir tarayıcıda görüntülenen son iki koşullu blokları sonucu:</span><span class="sxs-lookup"><span data-stu-id="c6249-420">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-c/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="c6249-422">Döngü kodu</span><span class="sxs-lookup"><span data-stu-id="c6249-422">Looping Code</span></span>

<span data-ttu-id="c6249-423">Genellikle aynı deyimleri tekrar tekrar çalıştırmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-423">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="c6249-424">Bunun için döngüyle.</span><span class="sxs-lookup"><span data-stu-id="c6249-424">You do this by looping.</span></span> <span data-ttu-id="c6249-425">Örneğin, genellikle aynı deyimlerini her öğe için bir veri koleksiyonunda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="c6249-425">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="c6249-426">Tam döngü istediğiniz kaç kez biliyorsanız, kullanabileceğiniz bir `for` döngü.</span><span class="sxs-lookup"><span data-stu-id="c6249-426">If you know exactly how many times you want to loop, you can use a `for` loop.</span></span> <span data-ttu-id="c6249-427">Bu tür bir döngü sayım veya sayım için özellikle yararlı olur:</span><span class="sxs-lookup"><span data-stu-id="c6249-427">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample53.html)]

<span data-ttu-id="c6249-428">Döngü ile başlayan `for` ayraç içinde üç deyimleri ve ardından bir anahtar sözcük, her sonlandırıldı noktalı virgül.</span><span class="sxs-lookup"><span data-stu-id="c6249-428">The loop begins with the `for` keyword, followed by three statements in parentheses, each terminated with a semicolon.</span></span>

- <span data-ttu-id="c6249-429">İlk ifade parantez içinde (`var i=10;`) bir sayaç oluşturur ve 10 başlatır.</span><span class="sxs-lookup"><span data-stu-id="c6249-429">Inside the parentheses, the first statement (`var i=10;`) creates a counter and initializes it to 10.</span></span> <span data-ttu-id="c6249-430">Sayaç adı gerekmez `i` &#8212; herhangi bir değişkeni kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-430">You don't have to name the counter `i` &#8212; you can use any variable.</span></span> <span data-ttu-id="c6249-431">Zaman `for` döngüsü çalıştırıldığında, sayaç otomatik olarak artırılır.</span><span class="sxs-lookup"><span data-stu-id="c6249-431">When the `for` loop runs, the counter is automatically incremented.</span></span>
- <span data-ttu-id="c6249-432">İkinci ifade (`i < 21;`) saymak istediğiniz kadar koşulu ayarlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-432">The second statement (`i < 21;`) sets the condition for how far you want to count.</span></span> <span data-ttu-id="c6249-433">Bu durumda, en fazla 20 için Git istediğiniz (sayaç değerinden 21 durumdayken başka bir deyişle, gidiyor).</span><span class="sxs-lookup"><span data-stu-id="c6249-433">In this case, you want it to go to a maximum of 20 (that is, keep going while the counter is less than 21).</span></span>
- <span data-ttu-id="c6249-434">Üçüncü ifade (`i++` ) yalnızca sayacı döngünün her çalıştığında eklenene 1 olmalıdır belirten bir artış işleci kullanır.</span><span class="sxs-lookup"><span data-stu-id="c6249-434">The third statement (`i++` ) uses an increment operator, which simply specifies that the counter should have 1 added to it each time the loop runs.</span></span>

<span data-ttu-id="c6249-435">Köşeli parantez her döngü için çalışacak kodudur.</span><span class="sxs-lookup"><span data-stu-id="c6249-435">Inside the braces is the code that will run for each iteration of the loop.</span></span> <span data-ttu-id="c6249-436">Yeni bir paragraf biçimlendirme oluşturur (`<p>` öğesi) her zaman ve değerini görüntüleme çıktısı için bir satır ekler `i` (sayaç).</span><span class="sxs-lookup"><span data-stu-id="c6249-436">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of `i` (the counter).</span></span> <span data-ttu-id="c6249-437">Bu sayfayı çalıştırdığınızda, örnek öğe sayısını gösteren her satırın metinle çıkış görüntüleme 11 satırları oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c6249-437">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-c/_static/image11.jpg)

<span data-ttu-id="c6249-439">Bir koleksiyonu veya dizisi ile çalışıyorsanız, sık kullandığınız bir `foreach` döngü.</span><span class="sxs-lookup"><span data-stu-id="c6249-439">If you're working with a collection or array, you often use a `foreach` loop.</span></span> <span data-ttu-id="c6249-440">Bir koleksiyon benzer nesneleri oluşan bir gruptur ve `foreach` döngü koleksiyondaki her öğe bir görevi yerine getirmek sağlar.</span><span class="sxs-lookup"><span data-stu-id="c6249-440">A collection is a group of similar objects, and the `foreach` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="c6249-441">Bu tür bir döngü koleksiyonlar için uygun olan çünkü aksine bir `for` döngüsü, sınırı sayaç artırın veya zorunda değilsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-441">This type of loop is convenient for collections, because unlike a `for` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="c6249-442">Bunun yerine, `foreach` döngü kodunu işlemi tamamlanana kadar toplulukta yalnızca geçer.</span><span class="sxs-lookup"><span data-stu-id="c6249-442">Instead, the `foreach` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="c6249-443">Örneğin, aşağıdaki kod öğeleri döndürür `Request.ServerVariables` web sunucunuz hakkında bilgi içeren bir nesne koleksiyonu.</span><span class="sxs-lookup"><span data-stu-id="c6249-443">For example, the following code returns the items in the `Request.ServerVariables` collection, which is an object that contains information about your web server.</span></span> <span data-ttu-id="c6249-444">Kullandığı bir `foreac` yeni oluşturarak her öğenin adını görüntülemek için h döngü `<li>` öğesi bir HTML madde işaretli listede.</span><span class="sxs-lookup"><span data-stu-id="c6249-444">It uses a `foreac` h loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample54.html)]

<span data-ttu-id="c6249-445">`foreach` Anahtar sözcüğü ardından parantez tarafından nerede koleksiyonunda tek bir öğeyi temsil eden bir değişken bildirme (örnekte `var item`), ardından `in` döngü istediğiniz koleksiyonu ve ardından bir anahtar sözcük,.</span><span class="sxs-lookup"><span data-stu-id="c6249-445">The `foreach` keyword is followed by parentheses where you declare a variable that represents a single item in the collection (in the example, `var item`), followed by the `in` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="c6249-446">Gövdesinde `foreach` döngü, daha önce bildirilen değişkeni kullanarak geçerli öğesi erişebilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-446">In the body of the `foreach` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-c/_static/image12.jpg)

<span data-ttu-id="c6249-448">Daha fazla genel amaçlı bir döngü oluşturmak üzere kullanmanız `while` deyimi:</span><span class="sxs-lookup"><span data-stu-id="c6249-448">To create a more general-purpose loop, use the `while` statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample55.cshtml)]

<span data-ttu-id="c6249-449">A `while` döngü başlıyorsa `while` parantez tarafından ne kadar süreyle döngünün devam belirlediğiniz anahtar sözcüğünü, (burada için sürece `countNum` değerinden 50'dir), yinelenecek sonra bloğu.</span><span class="sxs-lookup"><span data-stu-id="c6249-449">A `while` loop begins with the `while` keyword, followed by parentheses where you specify how long the loop continues (here, for as long as `countNum` is less than 50), then the block to repeat.</span></span> <span data-ttu-id="c6249-450">Döngüler genellikle Artır (Ekle) veya azaltma (Çıkart) bir değişken veya sayım için kullanılan nesne.</span><span class="sxs-lookup"><span data-stu-id="c6249-450">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="c6249-451">Örnekte, `+=` işleci ekler 1 `countNum` döngünün her çalıştığında.</span><span class="sxs-lookup"><span data-stu-id="c6249-451">In the example, the `+=` operator adds 1 to `countNum` each time the loop runs.</span></span> <span data-ttu-id="c6249-452">(Bir değişken sayıları aşağı Döngüdeki düşürmek için azaltma işleci kullanırsınız `-=`).</span><span class="sxs-lookup"><span data-stu-id="c6249-452">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`).</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="c6249-453">Nesneleri ve koleksiyonları</span><span class="sxs-lookup"><span data-stu-id="c6249-453">Objects and Collections</span></span>

<span data-ttu-id="c6249-454">ASP.NET Web sitesi, neredeyse her şeyi web sayfası dahil olmak üzere bir nesnesidir.</span><span class="sxs-lookup"><span data-stu-id="c6249-454">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="c6249-455">Bu bölümde, ile sık kodunuzda karşılaşmayacağınızı bazı önemli nesneleri anlatılmaktadır.</span><span class="sxs-lookup"><span data-stu-id="c6249-455">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="c6249-456">Sayfa nesneleri</span><span class="sxs-lookup"><span data-stu-id="c6249-456">Page Objects</span></span>

<span data-ttu-id="c6249-457">En temel ASP.NET sayfası nesnedir.</span><span class="sxs-lookup"><span data-stu-id="c6249-457">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="c6249-458">Hiçbir belirleme nesnesi olmadan doğrudan sayfa nesnesinin özellikleri erişebilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-458">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="c6249-459">Aşağıdaki kod sayfanın dosya yolunu alır kullanarak `Request` sayfasının nesnesi:</span><span class="sxs-lookup"><span data-stu-id="c6249-459">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample56.cshtml)]

<span data-ttu-id="c6249-460">Bunu yapmak için Temizle özellikleri ve yöntemleri geçerli sayfa nesnesinde başvuran, isteğe bağlı olarak anahtar sözcüğünü kullanabilirsiniz `this` kodunuzda sayfa nesnesini temsil eden.</span><span class="sxs-lookup"><span data-stu-id="c6249-460">To make it clear that you're referencing properties and methods on the current page object, you can optionally use the keyword `this` to represent the page object in your code.</span></span> <span data-ttu-id="c6249-461">İle önceki kod örneğinde, işte `this` sayfa temsil etmek için eklendi:</span><span class="sxs-lookup"><span data-stu-id="c6249-461">Here is the previous code example, with `this` added to represent the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample57.cshtml)]

<span data-ttu-id="c6249-462">Özelliklerini kullanabilirsiniz `Page` nesne gibi bir çok sayıda bilgi, almak için:</span><span class="sxs-lookup"><span data-stu-id="c6249-462">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="c6249-463">`Request`.</span><span class="sxs-lookup"><span data-stu-id="c6249-463">`Request`.</span></span> <span data-ttu-id="c6249-464">Bu önceden gördüğünüz gibi tarayıcının ne tür, sayfa, kullanıcı kimliği, vb. URL'sini istekte dahil olmak üzere geçerli istek hakkındaki bilgiler koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="c6249-464">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="c6249-465">`Response`.</span><span class="sxs-lookup"><span data-stu-id="c6249-465">`Response`.</span></span> <span data-ttu-id="c6249-466">Bu, sunucu kodu çalışmasını bitirdikten sonra tarayıcıya gönderilen yanıt (sayfa) hakkında bilgi koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="c6249-466">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="c6249-467">Örneğin, bilgi yanıtına yazmak için bu özelliği kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-467">For example, you can use this property to write information into the response.</span></span> 

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample58.cshtml)]

<a id="ID_CollectionsAndObjects"></a>
### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="c6249-468">Koleksiyon nesnesi (diziler ve sözlükler)</span><span class="sxs-lookup"><span data-stu-id="c6249-468">Collection Objects (Arrays and Dictionaries)</span></span>

<span data-ttu-id="c6249-469">A *koleksiyonu* aynı türde bir koleksiyonu gibi nesneleri oluşan bir gruptur `Customer` nesnelerini bir veritabanından.</span><span class="sxs-lookup"><span data-stu-id="c6249-469">A *collection* is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="c6249-470">ASP.NET içeren birçok yerleşik koleksiyonlar gibi `Request.Files` koleksiyonu.</span><span class="sxs-lookup"><span data-stu-id="c6249-470">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="c6249-471">Koleksiyonlarda verileri genellikle çalışmak.</span><span class="sxs-lookup"><span data-stu-id="c6249-471">You'll often work with data in collections.</span></span> <span data-ttu-id="c6249-472">İki ortak koleksiyon türü *dizi* ve *sözlük*.</span><span class="sxs-lookup"><span data-stu-id="c6249-472">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="c6249-473">Bir dizi benzer öğeleri koleksiyonu depolamak istiyorsanız, ancak her bir öğeyi tutmak için ayrı bir değişken oluşturmak istemediğiniz durumlarda yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="c6249-473">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample59.cshtml)]

<span data-ttu-id="c6249-474">Dizilerle, bir özel veri türü gibi bildirdiğiniz `string`, `int`, veya `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="c6249-474">With arrays, you declare a specific data type, such as `string`, `int`, or `DateTime`.</span></span> <span data-ttu-id="c6249-475">Bir dizi değişkeni içerebilir, köşeli bildirimine eklemek belirtmek için (gibi `string[]` veya `int[]`).</span><span class="sxs-lookup"><span data-stu-id="c6249-475">To indicate that the variable can contain an array, you add brackets to the declaration (such as `string[]` or `int[]`).</span></span> <span data-ttu-id="c6249-476">Öğeleri konumlarını (dizin) kullanarak bir dizi veya kullanarak erişebilirsiniz `foreach` deyimi.</span><span class="sxs-lookup"><span data-stu-id="c6249-476">You can access items in an array using their position (index) or by using the `foreach` statement.</span></span> <span data-ttu-id="c6249-477">Dizi dizinleri sıfır tabanlı &#8212; diğer bir deyişle, ilk öğe adresindeki konumlandırın 0, ikinci öğesi konum 1 ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="c6249-477">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample60.cshtml)]

<span data-ttu-id="c6249-478">Alarak dizideki öğelerin sayısını belirleyebilirsiniz kendi `Length` özelliği.</span><span class="sxs-lookup"><span data-stu-id="c6249-478">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="c6249-479">Belirli bir öğenin (array aramak için) dizideki konumunu almak için `Array.IndexOf` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c6249-479">To get the position of a specific item in the array (to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="c6249-480">Bir dizinin içeriğini da ters gibi şeyler yapabilirsiniz ( `Array.Reverse` yöntemi) veya içeriği sıralama ( `Array.Sort` yöntemi).</span><span class="sxs-lookup"><span data-stu-id="c6249-480">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="c6249-481">Bir tarayıcıda görüntülenen dize dizisi kodu çıktı:</span><span class="sxs-lookup"><span data-stu-id="c6249-481">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-c/_static/image13.jpg)

<span data-ttu-id="c6249-483">Bir anahtar (veya ad) karşılık gelen değeri ayarlayamaz veya Burada sağladığınız anahtar/değer çiftleri koleksiyonu dictionary'si:</span><span class="sxs-lookup"><span data-stu-id="c6249-483">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample61.cshtml)]

<span data-ttu-id="c6249-484">Bir sözlük oluşturmak için kullandığınız `new` yeni bir sözlük nesnesi oluşturduğunuzu göstermek için anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="c6249-484">To create a dictionary, you use the `new` keyword to indicate that you're creating a new dictionary object.</span></span> <span data-ttu-id="c6249-485">Bir değişken kullanarak bir sözlük atayabilirsiniz `var` anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="c6249-485">You can assign a dictionary to a variable using the `var` keyword.</span></span> <span data-ttu-id="c6249-486">Köşeli kullanarak sözlükteki öğe veri türlerini gösterir ( `< >` ).</span><span class="sxs-lookup"><span data-stu-id="c6249-486">You indicate the data types of the items in the dictionary using angle brackets ( `< >` ).</span></span> <span data-ttu-id="c6249-487">Bu yeni bir sözlük oluşturur yöntemi gerçekte olduğundan bildirimi sonunda parantez çifti eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-487">At the end of the declaration, you must add a pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="c6249-488">Sözlüğe öğeler eklemek için çağırabilirsiniz `Add` sözlük değişkenin yöntemi (`myScores` bu durumda) ve ardından bir anahtar ve bir değer belirtin.</span><span class="sxs-lookup"><span data-stu-id="c6249-488">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="c6249-489">Alternatif olarak, anahtarı belirtmek ve aşağıdaki örnekteki gibi basit bir atama yapmak için köşeli ayraç kullanın:</span><span class="sxs-lookup"><span data-stu-id="c6249-489">Alternatively, you can use square brackets to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample62.cs)]

<span data-ttu-id="c6249-490">Sözlükten bir değer almak için köşeli anahtarı belirtin:</span><span class="sxs-lookup"><span data-stu-id="c6249-490">To get a value from the dictionary, you specify the key in brackets:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample63.cs)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="c6249-491">Parametrelerle yöntemleri çağırma</span><span class="sxs-lookup"><span data-stu-id="c6249-491">Calling Methods with Parameters</span></span>

<span data-ttu-id="c6249-492">Bu makalenin önceki bölümlerinde okuma gibi ile program nesneleri yöntemleri olabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-492">As you read earlier in this article, the objects that you program with can have methods.</span></span> <span data-ttu-id="c6249-493">Örneğin, bir `Database` nesnesi olabilir bir `Database.Connect` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c6249-493">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="c6249-494">Birçok yöntem de bir veya daha fazla parametre vardır.</span><span class="sxs-lookup"><span data-stu-id="c6249-494">Many methods also have one or more parameters.</span></span> <span data-ttu-id="c6249-495">A *parametresi* bir yönteme geçirin bir değer, görevini tamamlamak üzere yöntemini etkinleştirmek için.</span><span class="sxs-lookup"><span data-stu-id="c6249-495">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="c6249-496">Örneğin, bir bildirimi bakın `Request.MapPath` üç parametre alır yöntemi:</span><span class="sxs-lookup"><span data-stu-id="c6249-496">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample64.cs)]

<span data-ttu-id="c6249-497">(Satır daha okunabilir yapmak için sarmalanmış.</span><span class="sxs-lookup"><span data-stu-id="c6249-497">(The line has been wrapped to make it more readable.</span></span> <span data-ttu-id="c6249-498">İç dizeleri dışında yerleştirin neredeyse tüm satır sonlarını koyabilirsiniz unutmayın tırnak işaretleri içine.)</span><span class="sxs-lookup"><span data-stu-id="c6249-498">Remember that you can put line breaks almost any place except inside strings that are enclosed in quotation marks.)</span></span>

<span data-ttu-id="c6249-499">Bu yöntem, belirtilen sanal yolu sunucuda karşılık gelen fiziksel yolu döndürür.</span><span class="sxs-lookup"><span data-stu-id="c6249-499">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="c6249-500">Yöntem için üç parametreleri `virtualPath`, `baseVirtualDir`, ve `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="c6249-500">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="c6249-501">(Kabul verileri veri türleriyle parametreleri bildiriminde listelenen dikkat edin.) Bu yöntemi çağırdığınızda, tüm üç parametreleri için değerler girmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="c6249-501">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="c6249-502">Razor sözdizimi için bir yöntem parametreleri geçirme için iki seçenek sunar: *konumsal parametreler* ve *adlandırılmış parametreleri*.</span><span class="sxs-lookup"><span data-stu-id="c6249-502">The Razor syntax gives you two options for passing parameters to a method: *positional parameters* and *named parameters*.</span></span> <span data-ttu-id="c6249-503">Konumsal parametreler kullanarak bir yöntemi çağırmak için yöntem bildiriminde belirtilen katı bir sırada parametreleri geçirin.</span><span class="sxs-lookup"><span data-stu-id="c6249-503">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="c6249-504">(Genellikle bu sırada yöntemi belgelerine okuyarak bilmeniz.) Sırayı izler ve parametrelerinden herhangi birini atlayamazsınız &#8212; gerekirse, boş bir dize geçirdiğiniz (`""`) veya `null` için bir değer yoksa konumsal bir parametre için.</span><span class="sxs-lookup"><span data-stu-id="c6249-504">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or `null` for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="c6249-505">Aşağıdaki örnek adlı bir klasör olduğunu varsayar *betikleri* Web sitenizde.</span><span class="sxs-lookup"><span data-stu-id="c6249-505">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="c6249-506">Kod çağrıları `Request.MapPath` üç parametre doğru sırada yöntemi ve geçişleri değerlerini.</span><span class="sxs-lookup"><span data-stu-id="c6249-506">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="c6249-507">Ardından, elde edilen eşlenen yolun görüntüler.</span><span class="sxs-lookup"><span data-stu-id="c6249-507">It then displays the resulting mapped path.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample65.cshtml)]

<span data-ttu-id="c6249-508">Bir yöntem birçok parametreleri sahip olduğunda, kodunuzu daha okunabilir adlandırılmış parametreler kullanılarak kullanmaya devam edebilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-508">When a method has many parameters, you can keep your code more readable by using named parameters.</span></span> <span data-ttu-id="c6249-509">Adlandırılmış parametreler kullanılarak bir yöntemi çağırmak için iki nokta üst üste (:), ardından değeri tarafından ardından parametre adı belirtin.</span><span class="sxs-lookup"><span data-stu-id="c6249-509">To call a method using named parameters, you specify the parameter name followed by a colon (:), and then the value.</span></span> <span data-ttu-id="c6249-510">Adlandırılmış parametreler avantajı, bunları istediğiniz herhangi bir sırada geçirebilirsiniz sağlamasıdır.</span><span class="sxs-lookup"><span data-stu-id="c6249-510">The advantage of named parameters is that you can pass them in any order you want.</span></span> <span data-ttu-id="c6249-511">(Bir dezavantajı yöntem çağrısının olarak compact olmamasıdır.)</span><span class="sxs-lookup"><span data-stu-id="c6249-511">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="c6249-512">Aşağıdaki örnek yukarıdaki gibi aynı yöntemini çağırır, ancak adlı parametre değerlerini sağlamak için kullanır:</span><span class="sxs-lookup"><span data-stu-id="c6249-512">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample66.cshtml)]

<span data-ttu-id="c6249-513">Gördüğünüz gibi parametreleri farklı bir sırayla geçirilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-513">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="c6249-514">Ancak, önceki örnekte ve bu örnek çalıştırırsanız, aynı değere döneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="c6249-514">However, if you run the previous example and this example, they'll return the same value.</span></span>

<a id="ID_HandlingErrors"></a>
## <a name="handling-errors"></a><span data-ttu-id="c6249-515">Hataları işleme</span><span class="sxs-lookup"><span data-stu-id="c6249-515">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="c6249-516">Try-Catch deyimleri</span><span class="sxs-lookup"><span data-stu-id="c6249-516">Try-Catch Statements</span></span>

<span data-ttu-id="c6249-517">Genellikle, denetimi dışında kalan nedeniyle başarısız olabilir, kodunuzda deyimleri sahip olacaksınız.</span><span class="sxs-lookup"><span data-stu-id="c6249-517">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="c6249-518">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="c6249-518">For example:</span></span>

- <span data-ttu-id="c6249-519">Kodunuzu oluşturmak veya bir dosyaya erişmek çalışırsa, her türlü hataları oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-519">If your code tries to create or access a file, all sorts of errors might occur.</span></span> <span data-ttu-id="c6249-520">İstediğiniz dosya var olmayabilir, kilitli olabilir, kodu değil izinlere sahip ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="c6249-520">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="c6249-521">Benzer şekilde, kodunuzu veritabanındaki kayıtları güncelleştirme çalışırsa, izin sorunları olabilir, veritabanı bağlantısı bırakılabilir, kaydetmek için verileri geçersiz vb. olabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-521">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="c6249-522">Bu durumlarda programlama dilinde denir *özel durumları*.</span><span class="sxs-lookup"><span data-stu-id="c6249-522">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="c6249-523">Kodunuzu bir özel durum karşılaşırsa (atar) oluşturan bir hata iletisi o, en iyi kullanıcılara sinir bozucu:</span><span class="sxs-lookup"><span data-stu-id="c6249-523">If your code encounters an exception, it generates (throws) an error message that's, at best, annoying to users:</span></span>

![Razor-Img14](introducing-razor-syntax-c/_static/image14.jpg)

<span data-ttu-id="c6249-525">Burada kodunuzu karşılaşabileceğiniz özel durumlar durumlarda ve hata iletileri bu tür önlemek için kullanabileceğiniz `try/catch` deyimleri.</span><span class="sxs-lookup"><span data-stu-id="c6249-525">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `try/catch` statements.</span></span> <span data-ttu-id="c6249-526">İçinde `try` deyimi, denetimi kod çalıştırma.</span><span class="sxs-lookup"><span data-stu-id="c6249-526">In the `try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="c6249-527">Bir veya daha `catch` deyimleri için özel konum (belirli tür özel durumlar) hatalar oluşmuş olabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-527">In one or more `catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="c6249-528">Kadar içerebilir `catch` deyimleri yazarken gereken bekleme hataları aramak.</span><span class="sxs-lookup"><span data-stu-id="c6249-528">You can include as many `catch` statements as you need to look for errors that you are anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="c6249-529">Kullanmaktan kaçının öneririz `Response.Redirect` yönteminde `try/catch` deyimleri, çünkü bu bir özel durum sayfanızda neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="c6249-529">We recommend that you avoid using the `Response.Redirect` method in `try/catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="c6249-530">Aşağıdaki örnek ilk istek üzerinde bir metin dosyası oluşturur ve kullanıcının dosyayı açma olanak sağlayan bir düğme görüntüleyen bir sayfada görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="c6249-530">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="c6249-531">Örnek kasıtlı olarak hatalı dosya adı kullanır, böylece bir özel durumuna neden olur.</span><span class="sxs-lookup"><span data-stu-id="c6249-531">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="c6249-532">Kod içeren `catch` deyimleri iki olası özel durumlar için: `FileNotFoundException`, dosya adı hatalı olması durumunda gerçekleşir ve `DirectoryNotFoundException`, ASP.NET bile klasörünü bulamazsanız oluşur.</span><span class="sxs-lookup"><span data-stu-id="c6249-532">The code includes `catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="c6249-533">(, Örnek bir deyimde her şeyin doğru şekilde çalışırken, nasıl çalıştığı görmek için açıklamadan çıkarın.)</span><span class="sxs-lookup"><span data-stu-id="c6249-533">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="c6249-534">Kodunuzu özel durum işleme alamadık, önceki ekran görüntüsü gibi hata sayfasını görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="c6249-534">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="c6249-535">Ancak, `try/catch` bölüm, kullanıcı bu tür hataların görmemesi yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="c6249-535">However, the `try/catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample67.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="c6249-536">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="c6249-536">Additional Resources</span></span>

<span data-ttu-id="c6249-537">**Visual Basic ile programlama**</span><span class="sxs-lookup"><span data-stu-id="c6249-537">**Programming with Visual Basic**</span></span>


[<span data-ttu-id="c6249-538">Ek: Visual Basic dili ve sözdizimi</span><span class="sxs-lookup"><span data-stu-id="c6249-538">Appendix: Visual Basic Language and Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkId=202908)


<span data-ttu-id="c6249-539">**Başvuru belgeleri**</span><span class="sxs-lookup"><span data-stu-id="c6249-539">**Reference Documentation**</span></span>


[<span data-ttu-id="c6249-540">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="c6249-540">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)

[<span data-ttu-id="c6249-541">C# dili</span><span class="sxs-lookup"><span data-stu-id="c6249-541">C# Language</span></span>](https://msdn.microsoft.com/library/kx37x362.aspx)

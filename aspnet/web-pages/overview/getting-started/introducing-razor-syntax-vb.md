---
uid: web-pages/overview/getting-started/introducing-razor-syntax-vb
title: "Razor sözdizimini (Visual Basic) kullanarak ASP.NET Web programlamaya giriş | Microsoft Docs"
author: tfitzmac
description: "Bu ekte Razor sözdizimini kullanarak Visual Basic'te, ASP.NET Web sayfaları ile programlama genel bir bakış sağlar."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: 5da59646-e973-41cd-88a9-c6b2c0594027
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-vb
msc.type: authoredcontent
ms.openlocfilehash: 8f5d223a5944d8adb9fe65c89e87829d18d1c7ee
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-visual-basic"></a><span data-ttu-id="98190-103">Razor sözdizimini (Visual Basic) kullanarak ASP.NET Web programlamaya giriş</span><span class="sxs-lookup"><span data-stu-id="98190-103">Introduction to ASP.NET Web Programming Using the Razor Syntax (Visual Basic)</span></span>
====================
<span data-ttu-id="98190-104">tarafından [zel FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="98190-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="98190-105">Bu makalede Visual Basic ve Razor sözdizimini kullanan ASP.NET Web sayfaları ile programlama genel bir bakış sağlar.</span><span class="sxs-lookup"><span data-stu-id="98190-105">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax and Visual Basic.</span></span> <span data-ttu-id="98190-106">ASP.NET, dinamik web sayfaları web sunucuları üzerinde çalışan Microsoft teknolojisidir.</span><span class="sxs-lookup"><span data-stu-id="98190-106">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span>
> 
> <span data-ttu-id="98190-107">**Öğrenecekleriniz**:</span><span class="sxs-lookup"><span data-stu-id="98190-107">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="98190-108">Programlama ipuçları Razor sözdizimini kullanan ASP.NET Web sayfaları programlama ile çalışmaya başlama için üst 8.</span><span class="sxs-lookup"><span data-stu-id="98190-108">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="98190-109">Temel programlama kavramları, gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-109">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="98190-110">ASP.NET sunucu kodu nedir ve Razor sözdizimi olan tüm hakkında.</span><span class="sxs-lookup"><span data-stu-id="98190-110">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="98190-111">Yazılım sürümleri</span><span class="sxs-lookup"><span data-stu-id="98190-111">Software versions</span></span>
> 
> 
> - <span data-ttu-id="98190-112">ASP.NET Web sayfaları (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="98190-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="98190-113">Bu öğreticide, ASP.NET Web Pages 2 ile de çalışır.</span><span class="sxs-lookup"><span data-stu-id="98190-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>


<span data-ttu-id="98190-114">Razor sözdizimi ile ASP.NET Web sayfalarını kullanarak çoğu örnekler C# kullanın.</span><span class="sxs-lookup"><span data-stu-id="98190-114">Most examples of using ASP.NET Web Pages with Razor syntax use C#.</span></span> <span data-ttu-id="98190-115">Ancak, Razor sözdizimini Visual Basic de destekler.</span><span class="sxs-lookup"><span data-stu-id="98190-115">But the Razor syntax also supports Visual Basic.</span></span> <span data-ttu-id="98190-116">Bir web sayfası ile oluşturduğunuz program Visual Basic'te bir ASP.NET web sayfası için bir *.vbhtml* dosya adı uzantısı ve Visual Basic kodu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="98190-116">To program an ASP.NET web page in Visual Basic, you create a web page with a *.vbhtml* filename extension, and then add Visual Basic code.</span></span> <span data-ttu-id="98190-117">Bu makalede Visual Basic dili ve ASP.NET Web sayfaları oluşturmak için sözdizimi ile çalışmaya genel bir bakış sağlar.</span><span class="sxs-lookup"><span data-stu-id="98190-117">This article gives you an overview of working with the Visual Basic language and syntax to create ASP.NET Webpages.</span></span>

> [!NOTE]
> <span data-ttu-id="98190-118">Microsoft WebMatrix için varsayılan Web sitesi şablonları (**Firincilik**, **Fotoğraf Galerisi**, ve **başlangıç sitesi**, vs.) C# ve Visual Basic sürümlerinde kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-118">The default website templates for Microsoft WebMatrix (**Bakery**, **Photo Gallery**, and **Starter Site**, etc.) are available in C# and Visual Basic versions.</span></span> <span data-ttu-id="98190-119">Visual Basic şablon NuGet paketlerini yükleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-119">You can install the Visual Basic templates by as NuGet packages.</span></span> <span data-ttu-id="98190-120">Web sitesi şablonları adlı bir klasörde, sitenizin kök klasörüne yüklenir *Microsoft Templates*.</span><span class="sxs-lookup"><span data-stu-id="98190-120">Website templates are installed in the root folder of your site in a folder named *Microsoft Templates*.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="98190-121">8 programlama ipuçları</span><span class="sxs-lookup"><span data-stu-id="98190-121">The Top 8 Programming Tips</span></span>

<span data-ttu-id="98190-122">Bu bölümde kesinlikle Razor sözdizimini kullanan ASP.NET sunucusu kod yazmaya başladığınızda bilmeniz gereken birkaç ipucu listelenir.</span><span class="sxs-lookup"><span data-stu-id="98190-122">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="98190-123">1. Kod kullanarak bir sayfa ekleyin @ karakteri</span><span class="sxs-lookup"><span data-stu-id="98190-123">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="98190-124">`@` Karakter satır içi ifadeler, tek deyimli blokları ve birden fazla deyim blokları başlatır:</span><span class="sxs-lookup"><span data-stu-id="98190-124">The `@` character starts inline expressions, single-statement blocks, and multi-statement blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample1.vbhtml)]

<span data-ttu-id="98190-125">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="98190-125">The result displayed in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-vb/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="98190-127">**HTML Encoding**</span><span class="sxs-lookup"><span data-stu-id="98190-127">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="98190-128">Kullanarak bir sayfa içeriği görüntülediğinizde `@` karakter, önceki örneklerde olduğu gibi çıktı ASP.NET HTML olarak kodlar.</span><span class="sxs-lookup"><span data-stu-id="98190-128">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="98190-129">Bu ayrılmış HTML karakterlerini değiştirir (gibi `<` ve `>` ve `&`) HTML etiketleri veya varlıklar olarak yorumlanmak yerine bir web sayfasında karakter olarak görüntülenecek karakterleri etkinleştirmek kodlarıyla.</span><span class="sxs-lookup"><span data-stu-id="98190-129">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="98190-130">HTML kodlaması olmadan sunucu kodunuzu çıktısını düzgün görüntülenmeyebilir ve sayfa güvenlik risklerine maruz bırakabileceğinden.</span><span class="sxs-lookup"><span data-stu-id="98190-130">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="98190-131">Amacınız biçimlendirme olarak etiketlerini işler HTML biçimlendirmesi çıkış olup olmadığını (örneğin `<p></p>` paragrafı için veya `<em></em>` metin vurgulamak için), bölümüne bakın [birleştirme metin, biçimlendirme ve kod blokları kodda](#BM_CombiningTextMarkupAndCode) bu makalenin ilerisinde yer.</span><span class="sxs-lookup"><span data-stu-id="98190-131">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="98190-132">Daha fazla bilgiyi, HTML kodlaması hakkında [ASP.NET Web Pages sitelerinde HTML formları ile çalışma](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="98190-132">You can read more about HTML encoding in [Working with HTML Forms in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-with-codeend-code"></a><span data-ttu-id="98190-133">2. Kod blokları koduyla içine... Bitiş kodu</span><span class="sxs-lookup"><span data-stu-id="98190-133">2. You enclose code blocks with Code...End Code</span></span>

<span data-ttu-id="98190-134">Kod bloğu bir veya daha fazla kod deyimleri içerir ve anahtar sözcükleriyle içine `Code` ve `End Code`.</span><span class="sxs-lookup"><span data-stu-id="98190-134">A code block includes one or more code statements and is enclosed with the keywords `Code` and `End Code`.</span></span> <span data-ttu-id="98190-135">Açılış yerleştirin `Code` anahtar sözcüğü hemen sonra `@` karakter &#8212; olamaz boşluk aralarında.</span><span class="sxs-lookup"><span data-stu-id="98190-135">Place the opening `Code` keyword immediately after the `@` character &#8212; there can't be whitespace between them.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample2.vbhtml)]

<span data-ttu-id="98190-136">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="98190-136">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-vb/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-line-break"></a><span data-ttu-id="98190-138">3. Bir blok içinde satır sonu her kod açıklaması bitiş</span><span class="sxs-lookup"><span data-stu-id="98190-138">3. Inside a block, you end each code statement with a line break</span></span>

<span data-ttu-id="98190-139">Visual Basic kod bloğunda her deyimi bir satır sonu ile sona erer.</span><span class="sxs-lookup"><span data-stu-id="98190-139">In a Visual Basic code block, each statement ends with a line break.</span></span> <span data-ttu-id="98190-140">(Makalenin sonraki bölümlerinde uzun kod açıklaması birden çok satıra gerekirse sarmalamak için bir yol görürsünüz.)</span><span class="sxs-lookup"><span data-stu-id="98190-140">(Later in the article you'll see a way to wrap a long code statement into multiple lines if needed.)</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample3.vbhtml)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="98190-141">4. Değerlerini depolamak için değişkenleri kullanma</span><span class="sxs-lookup"><span data-stu-id="98190-141">4. You use variables to store values</span></span>

<span data-ttu-id="98190-142">Değerleri depolayabilir bir *değişkeni*dizeler, sayılar ve tarihleri, vb. dahil olmak üzere. Kullanarak yeni bir değişken oluşturmak `Dim` anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="98190-142">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `Dim` keyword.</span></span> <span data-ttu-id="98190-143">Doğrudan sayfasını kullanarak bir değişken değerleri ekleyebilirsiniz `@`.</span><span class="sxs-lookup"><span data-stu-id="98190-143">You can insert variable values directly in a page using `@`.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample4.vbhtml)]

<span data-ttu-id="98190-144">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="98190-144">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-vb/_static/image3.jpg)

### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="98190-146">5. Değişmez değer dize değerleri çift tırnak işaretleri içine alın</span><span class="sxs-lookup"><span data-stu-id="98190-146">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="98190-147">A *dize* metin olarak kabul edilir karakter sırasıdır.</span><span class="sxs-lookup"><span data-stu-id="98190-147">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="98190-148">Bir dize belirtmek için çift tırnak işaretleri içine alın:</span><span class="sxs-lookup"><span data-stu-id="98190-148">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample5.vbhtml)]

<span data-ttu-id="98190-149">Bir dize değeri çift tırnak işaretleri eklemek için iki çift tırnak işareti karakteri ekleyin.</span><span class="sxs-lookup"><span data-stu-id="98190-149">To embed double quotation marks within a string value, insert two double quotation mark characters.</span></span> <span data-ttu-id="98190-150">Sayfa çıktısının bir kez görünmesi çift tırnak karakteri istiyorsanız olarak girin `""` tırnak işaretli içinde dize ve iki kez görünmesini istiyorsanız olarak girin `""""` tırnak işaretli dizesi içinde.</span><span class="sxs-lookup"><span data-stu-id="98190-150">If you want the double quotation character to appear once in the page output, enter it as `""` within the quoted string, and if you want it to appear twice, enter it as `""""` within the quoted string.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample6.vbhtml)]

<span data-ttu-id="98190-151">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="98190-151">The result displayed in a browser:</span></span>

![Razor-Img4](introducing-razor-syntax-vb/_static/image4.jpg)

### <a name="6-visual-basic-code-is-not-case-sensitive"></a><span data-ttu-id="98190-153">6. Visual Basic kodu büyük küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="98190-153">6. Visual Basic code is not case sensitive</span></span>

<span data-ttu-id="98190-154">Visual Basic Dil büyük küçük harfe duyarlı değil.</span><span class="sxs-lookup"><span data-stu-id="98190-154">The Visual Basic language is not case sensitive.</span></span> <span data-ttu-id="98190-155">Programlama anahtar sözcükler (gibi `Dim`, `If`, ve `True`) ve değişken adları (gibi `myString`, veya `subTotal`) herhangi bir durumda yazılabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-155">Programming keywords (like `Dim`, `If`, and `True`) and variable names (like `myString`, or `subTotal`) can be written in any case.</span></span>

<span data-ttu-id="98190-156">Aşağıdaki kod satırlarını değişkene bir değer atamak `lastname` küçük harf kullanarak ad ve bir büyük harf adını kullanarak sayfasına değişken değeri çıktı.</span><span class="sxs-lookup"><span data-stu-id="98190-156">The following lines of code assign a value to the variable `lastname` using a lowercase name, and then output the variable value to the page using an uppercase name.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample7.vbhtml)]

<span data-ttu-id="98190-157">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="98190-157">The result displayed in a browser:</span></span>

![vb-syntax-5](introducing-razor-syntax-vb/_static/image5.jpg)

### <a name="7-much-of-your-coding-involves-working-with-objects"></a><span data-ttu-id="98190-159">7. Kodlama çoğunu nesnelerle çalışmayı içerir</span><span class="sxs-lookup"><span data-stu-id="98190-159">7. Much of your coding involves working with objects</span></span>

<span data-ttu-id="98190-160">Bir nesne ile program bir şey &#8212;temsil eder; bir sayfa, bir metin kutusu, bir dosya, görüntü, bir web isteği, bir e-posta iletisi, bir müşteri kaydı (veritabanı satır), vb. Nesnelerin özelliklerine &#8212;açıklayan özelliklere sahip; bir metin kutusu nesnesi bir `Text` istek nesnesi özelliğine sahip bir `Url` bir e-posta iletisi özelliğine sahip bir `From` özelliğine ve bir müşteri nesnesi sahiptir bir `FirstName` özelliği.</span><span class="sxs-lookup"><span data-stu-id="98190-160">An object represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics &#8212; a text box object has a `Text` property, a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="98190-161">Nesneleri olan yöntemlerini de &quot;fiiller&quot; yapabilirler.</span><span class="sxs-lookup"><span data-stu-id="98190-161">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="98190-162">Örnekler bir dosya nesnesinin `Save` yöntemi, bir görüntü nesnenin `Rotate` yöntemi ve e-posta nesnenin `Send` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="98190-162">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="98190-163">Genellikle ile karşılaşmayacağınızı `Request` form değerleri gibi bilgileri verir nesne alanları sayfasında (metin kutuları, vb.), tarayıcının ne tür, sayfa, kullanıcı kimliği, vb. URL'sini istekte. Bu örnek özelliklerine erişmek nasıl gösterir `Request` nesne ve nasıl çağrılacağını `MapPath` yöntemi `Request` sayfasının mutlak yolu sunucuda verir nesnesi:</span><span class="sxs-lookup"><span data-stu-id="98190-163">You'll often work with the `Request` object, which gives you information like the values of form fields on the page (text boxes, etc.), what type of browser made the request, the URL of the page, the user identity, etc. This example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample8.html)]

<span data-ttu-id="98190-164">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="98190-164">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-vb/_static/image6.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="98190-166">8. Kararları kod yazma</span><span class="sxs-lookup"><span data-stu-id="98190-166">8. You can write code that makes decisions</span></span>

<span data-ttu-id="98190-167">Bir anahtar dinamik web sayfaları koşullara dayanarak ne yapacağınıza belirlemek özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="98190-167">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="98190-168">Bunu yapmak için en yaygın yolu `If` deyimi (ve isteğe bağlı `Else` deyimi).</span><span class="sxs-lookup"><span data-stu-id="98190-168">The most common way to do this is with the `If` statement (and optional `Else` statement).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample9.vbhtml)]

<span data-ttu-id="98190-169">Deyim `If IsPost` kestirme yol yazma, `If IsPost = True`.</span><span class="sxs-lookup"><span data-stu-id="98190-169">The statement `If IsPost` is a shorthand way of writing `If IsPost = True`.</span></span> <span data-ttu-id="98190-170">İle birlikte `If` deyimleri, çeşitli yollarla koşullarda, kod, yineleme bloklarını test etmek için vardır ve vb., bu makalenin sonraki bölümlerinde açıklanmıştır.</span><span class="sxs-lookup"><span data-stu-id="98190-170">Along with `If` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="98190-171">Bir tarayıcıda görüntülenen sonuç (tıkladıktan sonra **gönderme**):</span><span class="sxs-lookup"><span data-stu-id="98190-171">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-vb/_static/image7.jpg)

> [!TIP] 
> 
> <span data-ttu-id="98190-173">**HTTP GET ve POST yöntemleri ve IsPost özelliği**</span><span class="sxs-lookup"><span data-stu-id="98190-173">**HTTP GET and POST Methods and the IsPost Property**</span></span>
> 
> <span data-ttu-id="98190-174">Web sayfaları (HTTP) için kullanılan protokol yöntemleri çok sınırlı sayıda destekler (&quot;fiiller&quot;) sunucuya isteği yapmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="98190-174">The protocol used for web pages (HTTP) supports a very limited number of methods (&quot;verbs&quot;) that are used to make requests to the server.</span></span> <span data-ttu-id="98190-175">İki en yaygın bir sayfa okumak için kullanılan GET ve sayfa göndermek için kullanılan POST olanlardır.</span><span class="sxs-lookup"><span data-stu-id="98190-175">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="98190-176">Genel olarak, bir kullanıcı bir sayfa istekleri ilk kez sayfa GET kullanarak istendi.</span><span class="sxs-lookup"><span data-stu-id="98190-176">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="98190-177">Kullanıcı bir formda doldurur ve ardından tıklattığında **gönderme**, tarayıcı sunucuya bir POST isteği yapar.</span><span class="sxs-lookup"><span data-stu-id="98190-177">If the user fills in a form and then clicks **Submit**, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="98190-178">Programlama web sayfasını işlemek nasıl bilmesi bir sayfa bir GET veya POST olarak istenen olup olmadığını öğrenmek yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="98190-178">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="98190-179">ASP.NET Web Pages'da kullandığınız `IsPost` bir isteğin bir GET veya POST olup olmadığını görmek için özellik.</span><span class="sxs-lookup"><span data-stu-id="98190-179">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="98190-180">Bir POST isteğiyse `IsPost` özelliği true döndürür ve metin kutuları bir form üzerinde değerlerini okuma gibi şeyler yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-180">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="98190-181">Birçok örnek göreceksiniz sayfanın farklı değerine bağlı olarak işlemek nasıl Göster `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="98190-181">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="98190-182">Basit bir kod örneğidir</span><span class="sxs-lookup"><span data-stu-id="98190-182">A Simple Code Example</span></span>

<span data-ttu-id="98190-183">Bu yordam temel programlama tekniklerinin gösteren bir sayfa oluşturulacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="98190-183">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="98190-184">Örnekte, kullanıcıların bunları ekler ve sonucu görüntüler sonra iki sayıyı girin olanak sağlayan bir sayfa oluşturun.</span><span class="sxs-lookup"><span data-stu-id="98190-184">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="98190-185">Düzenleyicinizde, yeni bir dosya oluşturun ve adlandırın *AddNumbers.vbhtml*.</span><span class="sxs-lookup"><span data-stu-id="98190-185">In your editor, create a new file and name it *AddNumbers.vbhtml*.</span></span>
2. <span data-ttu-id="98190-186">Aşağıdaki kod ve biçimlendirme zaten sayfasındaki herhangi bir şey değiştirme sayfanın kopyalayın.</span><span class="sxs-lookup"><span data-stu-id="98190-186">Copy the following code and markup into the page, replacing anything already in the page.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample10.vbhtml)]

    <span data-ttu-id="98190-187">Dikkat edilecek bazı öğeleri şunlardır:</span><span class="sxs-lookup"><span data-stu-id="98190-187">Here are some things for you to note:</span></span>

    - <span data-ttu-id="98190-188">`@` Karakter sayfasında ilk kod bloğunu başlar ve kendisinden `totalMessage` değişkeni katıştırılmış altına.</span><span class="sxs-lookup"><span data-stu-id="98190-188">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable embedded near the bottom.</span></span>
    - <span data-ttu-id="98190-189">Sayfanın üstündeki blok sınırlanan `Code...End Code`.</span><span class="sxs-lookup"><span data-stu-id="98190-189">The block at the top of the page is enclosed in `Code...End Code`.</span></span>
    - <span data-ttu-id="98190-190">Değişkenleri `total`, `num1`, `num2`, ve `totalMessage` birkaç numarası ve bir dize depolar.</span><span class="sxs-lookup"><span data-stu-id="98190-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="98190-191">Atanan değişmez dize değeri `totalMessage` çift tırnak işaretleri değişkenidir.</span><span class="sxs-lookup"><span data-stu-id="98190-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="98190-192">Visual Basic kodu zaman büyük/küçük harfe duyarlı olmadığı için `totalMessage` değişkeni sayfanın altı kullanıldığında, adı yalnızca sayfanın üst kısmındaki değişken bildirimi yazımını ile eşleşmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-192">Because Visual Basic code is not case sensitive, when the `totalMessage` variable is used near the bottom of the page, its name only needs to match the spelling of the variable declaration at the top of the page.</span></span> <span data-ttu-id="98190-193">Büyük küçük harf önemli değildir.</span><span class="sxs-lookup"><span data-stu-id="98190-193">The casing doesn't matter.</span></span>
    - <span data-ttu-id="98190-194">İfade `num1.AsInt()`  +  `num2.AsInt()` nesneleri ve yöntemleri ile nasıl çalışılacağı gösterir.</span><span class="sxs-lookup"><span data-stu-id="98190-194">The expression `num1.AsInt()` + `num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="98190-195">`AsInt` Yöntemi her değişken bir eklenebilecek tamsayıya (tamsayı) bir kullanıcı tarafından girilen dize dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="98190-195">The `AsInt` method on each variable converts the string entered by a user to a whole number (an integer) that can be added.</span></span>
    - <span data-ttu-id="98190-196">`<form>` Etiketi de içeren bir `method="post"` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="98190-196">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="98190-197">Bu belirtir kullanıcı tıklattığında **Ekle**, sayfa HTTP POST yöntemini kullanarak sunucuya gönderilir.</span><span class="sxs-lookup"><span data-stu-id="98190-197">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="98190-198">Ne zaman sayfa gönderildiğinde, kodu `If IsPost` true ve koşullu değerlendirir kod sayıları ekleme sonucu görüntülenirken çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="98190-198">When the page is submitted, the code `If IsPost` evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="98190-199">Sayfayı kaydedin ve tarayıcıda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="98190-199">Save the page and run it in a browser.</span></span> <span data-ttu-id="98190-200">(Emin olun sayfa seçildiğinde, **dosyaları** çalıştırmadan önce onu çalışma.) İki tam sayılar girin ve ardından **Ekle** düğmesi.</span><span class="sxs-lookup"><span data-stu-id="98190-200">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span>

    ![Razor-Img7](introducing-razor-syntax-vb/_static/image8.jpg)

## <a name="visual-basic-language-and-syntax"></a><span data-ttu-id="98190-202">Visual Basic dili ve sözdizimi</span><span class="sxs-lookup"><span data-stu-id="98190-202">Visual Basic Language and Syntax</span></span>

<span data-ttu-id="98190-203">Daha önce bir ASP.NET web sayfası oluşturma ve sunucu kodunu HTML biçimlendirmesi nasıl ekleyebileceğiniz temel bir örneği gördünüz.</span><span class="sxs-lookup"><span data-stu-id="98190-203">Earlier you saw a basic example of how to create an ASP.NET web page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="98190-204">Burada Razor sözdizimi &#8212;kullanarak ASP.NET sunucusu kod yazmak için Visual Basic kullanma hakkında temel bilgileri öğreneceksiniz; diğer bir deyişle, programlama dili kuralları.</span><span class="sxs-lookup"><span data-stu-id="98190-204">Here you'll learn the basics of using Visual Basic to write ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="98190-205">(Özellikle, C, C++, C#, Visual Basic veya JavaScript kullandıysanız) programlama ile deneyimli değilseniz, ne burada okuma çoğunu tanıdık gelecektir.</span><span class="sxs-lookup"><span data-stu-id="98190-205">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="98190-206">Büyük olasılıkla yalnızca nasıl WebMatrix kod biçimlendirmede eklenen ile öğrenmeniz gerekir *.vbhtml* dosyaları.</span><span class="sxs-lookup"><span data-stu-id="98190-206">You'll probably need to familiarize yourself only with how WebMatrix code is added to markup in *.vbhtml* files.</span></span>

### <a id="BM_CombiningTextMarkupAndCode"></a><span data-ttu-id="98190-207">Metin, biçimlendirme ve kod blokları içinde kodu birleştirme</span><span class="sxs-lookup"><span data-stu-id="98190-207">Combining text, markup, and code in code blocks</span></span>

<span data-ttu-id="98190-208">Sunucu kod bloğu, genellikle metin ve biçimlendirme sayfasına çıkış istersiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-208">In server code blocks, you'll often want to output text and markup to the page.</span></span> <span data-ttu-id="98190-209">Sunucu kod bloğu kodu değil ve, bunun yerine olarak işleneceğini metin içeriyorsa, ASP.NET metnin kodunu ayırt etmek gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-209">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="98190-210">Bunu yapmanın birkaç yolu vardır.</span><span class="sxs-lookup"><span data-stu-id="98190-210">There are several ways to do this.</span></span>

- <span data-ttu-id="98190-211">Gibi bir HTML blok öğesindeki metni içine `<p></p>` veya `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="98190-211">Enclose the text in an HTML block element like `<p></p>` or `<em></em>`:</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample11.vbhtml)]

    <span data-ttu-id="98190-212">HTML öğesi, metin, ek HTML öğeleri ve sunucu kodu ifadeleri içerebilir.</span><span class="sxs-lookup"><span data-stu-id="98190-212">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="98190-213">ASP.NET, HTML etiketi açılış gördüğünde (örneğin, `<p>`), her şeyi öğesi oluşturur ve bunun içeriği olarak tarayıcı (ve çözümler için sunucu kodu ifadeleri).</span><span class="sxs-lookup"><span data-stu-id="98190-213">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything the element and its content as is to the browser (and resolves the server-code expressions).</span></span>

- <span data-ttu-id="98190-214">Kullanım `@:` işleci veya `<text>` öğesi.</span><span class="sxs-lookup"><span data-stu-id="98190-214">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="98190-215">`@:` Tek satırlık bir düz metin veya eşleşmeyen HTML etiketleri; içeren içerik çıkarır `<text>` öğesi çıktısını almak için birden fazla satır alır.</span><span class="sxs-lookup"><span data-stu-id="98190-215">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="98190-216">Bu seçenekler, çıkışı bir parçası olarak bir HTML öğesini işlemek istemediğiniz zaman yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="98190-216">These options are useful when you don't want to render an HTML element as part of the output.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample12.vbhtml)]

    <span data-ttu-id="98190-217">Aşağıdaki örnek, önceki örnekte yineler ancak tek bir çift kullanan `<text>` etiketleri oluşturmak için metni alın.</span><span class="sxs-lookup"><span data-stu-id="98190-217">The following example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample13.vbhtml)]

    <span data-ttu-id="98190-218">Aşağıdaki örnekte, `<text>` ve `</text>` etiketlerini içine üç satır, bunların tümü sahip bazı uncontained metin ve eşleşmeyen HTML etiketleri (`<br />`), sunucu kodu ve eşleşen HTML etiketleri yanı sıra.</span><span class="sxs-lookup"><span data-stu-id="98190-218">In the following example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="98190-219">Yine de her satırın tek tek koyun `@:` işleci; her iki şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="98190-219">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample14.vbhtml)]

    > [!NOTE]
    > <span data-ttu-id="98190-220">Ne zaman bu bölümde &#8212;gösterildiği gibi metin çıktısını; bir HTML öğesi kullanarak `@:` işleci veya `<text>` öğesi &#8212; ASP.NET çıktı HTML olarak kodlanacak değil.</span><span class="sxs-lookup"><span data-stu-id="98190-220">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="98190-221">(Daha önce belirtildiği gibi ASP.NET sunucu kodu ifadeleri ve tarafından öncesinde sunucu kod blokları çıktısını kodlamak `@`, bu bölümde belirtildiği özel durumlar hariç.)</span><span class="sxs-lookup"><span data-stu-id="98190-221">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="98190-222">Boşluk</span><span class="sxs-lookup"><span data-stu-id="98190-222">Whitespace</span></span>

<span data-ttu-id="98190-223">Ek boşluk deyimi içinde (ve bir değişmez dize değeri dışında) deyim etkilemez:</span><span class="sxs-lookup"><span data-stu-id="98190-223">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample15.vbhtml)]

### <a name="breaking-long-statements-into-multiple-lines"></a><span data-ttu-id="98190-224">Birden çok satırlara uzun deyimleri ayırma</span><span class="sxs-lookup"><span data-stu-id="98190-224">Breaking long statements into multiple lines</span></span>

<span data-ttu-id="98190-225">Uzun kod açıklaması alt çizgi karakterini kullanarak birden çok çizgiye bölün `_` (Visual Basic'te adlandırılan *devamlılığı karakteri*) her kod satırının sonra.</span><span class="sxs-lookup"><span data-stu-id="98190-225">You can break a long code statement into multiple lines by using the underscore character `_` (which in Visual Basic is called the *continuation character*) after each line of code.</span></span> <span data-ttu-id="98190-226">Break deyimi sonraki satıra üzerine satırın sonuna bir boşluk ve devamlılığı karakteri ekleyin.</span><span class="sxs-lookup"><span data-stu-id="98190-226">To break a statement onto the next line, at the end of the line add a space and then the continuation character.</span></span> <span data-ttu-id="98190-227">Deyim bir sonraki satırında devam edin.</span><span class="sxs-lookup"><span data-stu-id="98190-227">Continue the statement on the next line.</span></span> <span data-ttu-id="98190-228">Deyimleri okunabilirliğini artırmak gerektiği kadar satır üzerine kayabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-228">You can wrap statements onto as many lines as you need to improve readability.</span></span> <span data-ttu-id="98190-229">Aşağıdaki deyimleri aynıdır:</span><span class="sxs-lookup"><span data-stu-id="98190-229">The following statements are the same:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample16.vbhtml)]

<span data-ttu-id="98190-230">Ancak, bir değişmez dize değeri ortasında bir satır kaydırılamıyor.</span><span class="sxs-lookup"><span data-stu-id="98190-230">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="98190-231">Aşağıdaki örnek çalışmıyor:</span><span class="sxs-lookup"><span data-stu-id="98190-231">The following example doesn't work:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample17.vbhtml)]

<span data-ttu-id="98190-232">Yukarıdaki kod gibi birden çok satır sarmalar uzun bir dize birleştirmek için size kullanmanız gerekir *birleştirme işleci* (`&`), hangi bu makalenin sonraki bölümlerinde görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="98190-232">To combine a long string that wraps to multiple lines like the above code, you would need to use the *concatenation operator* (`&`), which you'll see later in this article.</span></span>

### <a name="code-comments"></a><span data-ttu-id="98190-233">Kod açıklamaları</span><span class="sxs-lookup"><span data-stu-id="98190-233">Code comments</span></span>

<span data-ttu-id="98190-234">Açıklamalar, kendiniz veya başkaları için Notlar bırakın olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="98190-234">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="98190-235">Razor sözdizimi açıklamaları öneki ile `@*` ve sonunda `*@`.</span><span class="sxs-lookup"><span data-stu-id="98190-235">Razor syntax comments are prefixed with `@*` and end with `*@`.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-vb/samples/sample18.cshtml)]

<span data-ttu-id="98190-236">Kod blokları içinde Razor sözdizimi açıklamaları kullanabilirsiniz ya da tek tırnak işareti olan sıradan Visual Basic açıklama karakterini kullanabilirsiniz (`'`) her satır için önek.</span><span class="sxs-lookup"><span data-stu-id="98190-236">Within code blocks you can use the Razor syntax comments, or you can use ordinary Visual Basic comment character, which is a single quote (`'`) prefixed to each line.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample19.vbhtml)]

## <a name="variables"></a><span data-ttu-id="98190-237">Değişkenler</span><span class="sxs-lookup"><span data-stu-id="98190-237">Variables</span></span>

<span data-ttu-id="98190-238">Bir değişken verilerini depolamak için kullandığınız adlandırılmış bir nesnedir.</span><span class="sxs-lookup"><span data-stu-id="98190-238">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="98190-239">Herhangi bir şey değişkenleri adı verebilirsiniz ancak adı alfabetik bir karakter ile başlamalı ve boşluk ya da ayrılmış karakterler içeremez.</span><span class="sxs-lookup"><span data-stu-id="98190-239">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span> <span data-ttu-id="98190-240">Visual Basic'te, daha önce gördüğünüz gibi büyük/küçük harf bir değişken adı önemli değildir.</span><span class="sxs-lookup"><span data-stu-id="98190-240">In Visual Basic, as you saw earlier, the case of the letters in a variable name doesn't matter.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="98190-241">Değişkenleri ve veri türleri</span><span class="sxs-lookup"><span data-stu-id="98190-241">Variables and data types</span></span>

<span data-ttu-id="98190-242">Bir değişken ne tür veriler değişkende depolanır gösteren bir özel veri türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-242">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="98190-243">Dize değerlerini depolayan dize değişkenleri olabilir (gibi &quot;Merhaba Dünya&quot;), tam sayı değerleri (örneğin, 3 veya 79) depolamak tamsayı değişkenleri ve biçimleri (4/12/2012 veya Mart 2009 gibi çeşitli tarih değerlerini depolama tarih değişkenleri ).</span><span class="sxs-lookup"><span data-stu-id="98190-243">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="98190-244">Ve kullanabileceğiniz birçok diğer veri türleri vardır.</span><span class="sxs-lookup"><span data-stu-id="98190-244">And there are many other data types you can use.</span></span>

<span data-ttu-id="98190-245">Ancak, bir değişken için bir tür belirtmek zorunda değilsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-245">However, you don't have to specify a type for a variable.</span></span> <span data-ttu-id="98190-246">Çoğu durumda ASP.NET değişkeni verileri nasıl kullanıldığını temel türü tahmin.</span><span class="sxs-lookup"><span data-stu-id="98190-246">In most cases ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="98190-247">(Bazen bir türü belirtmeniz gerekir; bu doğru olduğu örnekler görürsünüz.)</span><span class="sxs-lookup"><span data-stu-id="98190-247">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="98190-248">Bir değişken türü belirtmeden bildirmek için kullanmak `Dim` değişken adı artı (örneğin, `Dim myVar`).</span><span class="sxs-lookup"><span data-stu-id="98190-248">To declare a variable without specifying a type, use `Dim` plus the variable name (for instance, `Dim myVar`).</span></span> <span data-ttu-id="98190-249">Bir değişken türü ile bildirmek için kullanmak `Dim` ve ardından değişkeni artı `As` ve ardından tür adını (örneği için `Dim myVar As String`).</span><span class="sxs-lookup"><span data-stu-id="98190-249">To declare a variable with a type, use `Dim` plus the variable name, followed by `As` and then the type name (for instance, `Dim myVar As String`).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample20.vbhtml)]

<span data-ttu-id="98190-250">Aşağıdaki örnek, bir web sayfasında değişkenleri kullanan bazı satır içi ifadeler gösterir.</span><span class="sxs-lookup"><span data-stu-id="98190-250">The following example shows some inline expressions that use the variables in a web page.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample21.vbhtml)]

<span data-ttu-id="98190-251">Bir tarayıcıda görüntülenen sonuç:</span><span class="sxs-lookup"><span data-stu-id="98190-251">The result displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-vb/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="98190-253">Dönüştürme ve veri türleri test etme</span><span class="sxs-lookup"><span data-stu-id="98190-253">Converting and testing data types</span></span>

<span data-ttu-id="98190-254">Bazen ASP.NET genellikle bir veri türü otomatik olarak belirlemek de, işlem gerçekleştirilemiyor.</span><span class="sxs-lookup"><span data-stu-id="98190-254">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="98190-255">Bu nedenle, açık bir dönüştürme gerçekleştirerek ASP.NET yardımcı olması gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="98190-255">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="98190-256">Bazen türleri dönüştürme gerekmez olsa bile, veri türünü, birlikte çalışabilir görmek için test etmek yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="98190-256">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="98190-257">En yaygın olarak bir tam sayı veya tarih gibi bir dizeyi başka bir türüne dönüştürmek sahip olur.</span><span class="sxs-lookup"><span data-stu-id="98190-257">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="98190-258">Aşağıdaki örnek, burada, bir dizeyi sayıya dönüştürme gerekir tipik bir durumu gösterir.</span><span class="sxs-lookup"><span data-stu-id="98190-258">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample22.vbhtml)]

<span data-ttu-id="98190-259">Bir kural olarak, kullanıcı girişi için dize olarak gelir.</span><span class="sxs-lookup"><span data-stu-id="98190-259">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="98190-260">Bir sayı girin kullanıcıya sorulur olsa bile ve bir rakam girmiş olduğunuz da kullanıcı girişi gönderildiğinde ve kodda okuma olsa bile, veri dize biçiminde sağlanır.</span><span class="sxs-lookup"><span data-stu-id="98190-260">Even if you've prompted the user to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="98190-261">Bu nedenle, dize bir sayıya dönüştürmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-261">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="98190-262">Bunları, dönüştürmeden değerlerine aritmetik gerçekleştirmeye çalışırsanız ASP.NET iki dizeyi eklediğinden örnekte, aşağıdaki hata, sonuçları:</span><span class="sxs-lookup"><span data-stu-id="98190-262">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

`Cannot implicitly convert type 'string' to 'int'.`

<span data-ttu-id="98190-263">Tamsayılara değerleri dönüştürmek için arama `AsInt` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="98190-263">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="98190-264">Dönüştürme başarılı olursa, sayıları daha sonra ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-264">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="98190-265">Aşağıdaki tabloda bazı yaygın dönüştürme ve test yöntemleri değişkenleri listeler.</span><span class="sxs-lookup"><span data-stu-id="98190-265">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="98190-266">**Yöntemi**</span><span class="sxs-lookup"><span data-stu-id="98190-266">**Method**</span></span> | <span data-ttu-id="98190-267">**Açıklama**</span><span class="sxs-lookup"><span data-stu-id="98190-267">**Description**</span></span> | <span data-ttu-id="98190-268">**Örnek**</span><span class="sxs-lookup"><span data-stu-id="98190-268">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="98190-269">Bir tam sayı temsil eden bir dize dönüştürür (gibi &quot;593&quot;) bir tamsayı.</span><span class="sxs-lookup"><span data-stu-id="98190-269">Converts a string that represents a whole number (like &quot;593&quot;) to an integer.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample23.vb)] |
| `AsBool(), IsBool()` | <span data-ttu-id="98190-270">Gibi bir dizeyi dönüştürür &quot;true&quot; veya &quot;false&quot; bir Boolean türü.</span><span class="sxs-lookup"><span data-stu-id="98190-270">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample24.vb)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="98190-271">Gibi ondalık bir değeri olan bir dize dönüştürür &quot;1.3&quot; veya &quot;7.439&quot; bir kayan noktalı sayı.</span><span class="sxs-lookup"><span data-stu-id="98190-271">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample25.vb)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="98190-272">Gibi ondalık bir değeri olan bir dize dönüştürür &quot;1.3&quot; veya &quot;7.439&quot; ondalık sayıya.</span><span class="sxs-lookup"><span data-stu-id="98190-272">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="98190-273">(ASP.NET, ondalık sayı bir kayan nokta numarasından daha kesin.)</span><span class="sxs-lookup"><span data-stu-id="98190-273">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample26.vb)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="98190-274">ASP.NET için bir tarih ve saat değerini temsil eden bir dize dönüştürür `DateTime` türü.</span><span class="sxs-lookup"><span data-stu-id="98190-274">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample27.vb)] |
| `ToString()` | <span data-ttu-id="98190-275">Başka bir veri türü bir dizeye dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="98190-275">Converts any other data type to a string.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample28.vb)] |

## <a name="operators"></a><span data-ttu-id="98190-276">İşleçler</span><span class="sxs-lookup"><span data-stu-id="98190-276">Operators</span></span>

<span data-ttu-id="98190-277">Bir işleç bir anahtar sözcük veya ASP ne tür bir ifadede gerçekleştirmek için komutu bir karakter değil.</span><span class="sxs-lookup"><span data-stu-id="98190-277">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="98190-278">Visual Basic birçok işleçleri destekler, ancak yalnızca ASP.NET web sayfaları geliştirmeye başlamak için birkaç tanıması gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-278">Visual Basic supports many operators, but you only need to recognize a few to get started developing ASP.NET web pages.</span></span> <span data-ttu-id="98190-279">Aşağıdaki tabloda, en yaygın işleçleri özetler.</span><span class="sxs-lookup"><span data-stu-id="98190-279">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="98190-280">**Operator**</span><span class="sxs-lookup"><span data-stu-id="98190-280">**Operator**</span></span> | <span data-ttu-id="98190-281">**Açıklama**</span><span class="sxs-lookup"><span data-stu-id="98190-281">**Description**</span></span> | <span data-ttu-id="98190-282">**Örnekler**</span><span class="sxs-lookup"><span data-stu-id="98190-282">**Examples**</span></span> |
| --- | --- | --- |
| `+ - * /` | <span data-ttu-id="98190-283">Matematik işleçleri sayısal ifadelerde kullanılır.</span><span class="sxs-lookup"><span data-stu-id="98190-283">Math operators used in numerical expressions.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample29.vb)] |
| `=` | <span data-ttu-id="98190-284">Atama ve eşitlik.</span><span class="sxs-lookup"><span data-stu-id="98190-284">Assignment and equality.</span></span> <span data-ttu-id="98190-285">Bağlam, bağlı olarak ya da sol tarafında nesnede deyiminin sağ taraftaki değer atayan veya eşitlik için değerleri denetler.</span><span class="sxs-lookup"><span data-stu-id="98190-285">Depending on context, either assigns the value on the right side of a statement to the object on the left side, or checks the values for equality.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample30.vb)] |
| `<>` | <span data-ttu-id="98190-286">Eşitsizlik.</span><span class="sxs-lookup"><span data-stu-id="98190-286">Inequality.</span></span> <span data-ttu-id="98190-287">Döndürür `True` değerler eşit değilse.</span><span class="sxs-lookup"><span data-stu-id="98190-287">Returns `True` if the values are not equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample31.vb)] |
| `< > <= >=` | <span data-ttu-id="98190-288">Küçük, büyük, küçük veya eşit ve büyüktür veya eşittir.</span><span class="sxs-lookup"><span data-stu-id="98190-288">Less than, greater than, less than or equal, and greater than or equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample32.vb)] |
| `&` | <span data-ttu-id="98190-289">Birleştirme dizeleri eklemek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="98190-289">Concatenation, which is used to join strings.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample33.vbhtml)] |
| `+= -=` | <span data-ttu-id="98190-290">Ekleme ve 1 (sırasıyla) bir değişkeninden artırma ve azaltma işleçleri.</span><span class="sxs-lookup"><span data-stu-id="98190-290">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample34.vb)] |
| `.` | <span data-ttu-id="98190-291">Nokta.</span><span class="sxs-lookup"><span data-stu-id="98190-291">Dot.</span></span> <span data-ttu-id="98190-292">Nesneleri ve özellikleri ve yöntemleri ayırt etmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="98190-292">Used to distinguish objects and their properties and methods.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample35.vb)] |
| `()` | <span data-ttu-id="98190-293">Parantez.</span><span class="sxs-lookup"><span data-stu-id="98190-293">Parentheses.</span></span> <span data-ttu-id="98190-294">Parametreleri yöntemleri ve erişim üyeleri dizileri ve koleksiyonları geçirmek için Grup ifadeleri için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="98190-294">Used to group expressions, to pass parameters to methods, and to access members of arrays and collections.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample36.vbhtml)] |
| `Not` | <span data-ttu-id="98190-295">Değil.</span><span class="sxs-lookup"><span data-stu-id="98190-295">Not.</span></span> <span data-ttu-id="98190-296">True değeri false ve tersi yönde tersine çevirir.</span><span class="sxs-lookup"><span data-stu-id="98190-296">Reverses a true value to false and vice versa.</span></span> <span data-ttu-id="98190-297">Sınamak için bir toplu şekilde genelde kullanılan `False` (diğer bir deyişle, için değil `True`).</span><span class="sxs-lookup"><span data-stu-id="98190-297">Typically used as a shorthand way to test for `False` (that is, for not `True`).</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample37.vb)] |
| `AndAlso OrElse` | <span data-ttu-id="98190-298">Mantıksal AND ve OR koşulları birlikte hangi bağlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="98190-298">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample38.vb)] |

## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="98190-299">Dosya ve klasör yollarında kodu ile çalışma</span><span class="sxs-lookup"><span data-stu-id="98190-299">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="98190-300">Genellikle, kodunuzda iş ile dosya ve klasör yolları.</span><span class="sxs-lookup"><span data-stu-id="98190-300">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="98190-301">Burada, geliştirme bilgisayarınızda görünebilir gibi bir Web sitesi için fiziksel klasör yapısı örneği verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="98190-301">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="98190-302">URL'ler ve yollar hakkında bazı temel ayrıntıları aşağıdadır:</span><span class="sxs-lookup"><span data-stu-id="98190-302">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="98190-303">Bir URL ile ya da bir etki alanı adı başlar (`http://www.example.com`) veya bir sunucu adı (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="98190-303">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="98190-304">Bir URL bir ana bilgisayardaki fiziksel bir yola karşılık gelir.</span><span class="sxs-lookup"><span data-stu-id="98190-304">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="98190-305">Örneğin, `http://myserver` klasörüne karşılık gelebilir *C:\websites\mywebsite* sunucusunda.</span><span class="sxs-lookup"><span data-stu-id="98190-305">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="98190-306">Sanal yol tam yolunu belirtmek zorunda kalmadan kod yolları temsil etmek için toplu özelliktir.</span><span class="sxs-lookup"><span data-stu-id="98190-306">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="98190-307">Etki alanı veya sunucu adı bir URL bölümünü içerir.</span><span class="sxs-lookup"><span data-stu-id="98190-307">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="98190-308">Sanal yol kullandığınızda, yolları güncelleştirmek zorunda kalmadan farklı etki alanı veya sunucu için kodunuzu taşıyabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-308">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="98190-309">Farkları anlamanıza yardımcı olması için örnek aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="98190-309">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="98190-310">Tam URL</span><span class="sxs-lookup"><span data-stu-id="98190-310">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="98190-311">Sunucu adı</span><span class="sxs-lookup"><span data-stu-id="98190-311">Server name</span></span> | <span data-ttu-id="98190-312">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="98190-312">*mycompanyserver*</span></span> |
| <span data-ttu-id="98190-313">Sanal yol</span><span class="sxs-lookup"><span data-stu-id="98190-313">Virtual path</span></span> | <span data-ttu-id="98190-314">*/HumanResources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="98190-314">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="98190-315">Fiziksel yol</span><span class="sxs-lookup"><span data-stu-id="98190-315">Physical path</span></span> | <span data-ttu-id="98190-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="98190-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="98190-317">Sanal kök / yalnızca C: aynı kök gibi sürücüdür \.</span><span class="sxs-lookup"><span data-stu-id="98190-317">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="98190-318">(Sanal klasör yolları her zaman eğik kullanın.) Sanal bir klasör yolu fiziksel klasör olarak aynı ada sahip gerekmez; bir diğer ad olabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-318">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="98190-319">(Üretim sunucularında, sanal yolu nadiren tam fiziksel yolunu eşleşir.)</span><span class="sxs-lookup"><span data-stu-id="98190-319">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="98190-320">Bazen kodu bulunan dosya ve klasörleri ile çalışırken, fiziksel yol ve bazen çalıştığınız hangi nesneleri bağlı olarak sanal bir yol başvuru gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-320">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="98190-321">ASP.NET size bu araçları kodu dosya ve klasör yollarında ile çalışmak için: `Server.MapPath` yöntemi ve `~` işleci ve `Href` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="98190-321">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="98190-322">Fiziksel için sanal yol dönüştürme: Server.MapPath yöntemi</span><span class="sxs-lookup"><span data-stu-id="98190-322">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="98190-323">`Server.MapPath` Yöntemi dönüştürür sanal bir yol (gibi */default.cshtml*) mutlak fiziksel yola (gibi *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="98190-323">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="98190-324">Tam fiziksel yolunu gerek duyduğunuz her zaman bu yöntemi kullanın.</span><span class="sxs-lookup"><span data-stu-id="98190-324">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="98190-325">Okunurken ya da bir metin dosyası veya web sunucusunda görüntü dosyası yazılırken tipik bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="98190-325">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="98190-326">Mutlak fiziksel yol barındırma site sunucusunda, sitenizin genellikle bilmiyorsanız, bu yöntem yolu dönüştürebilirsiniz şekilde bildiğiniz — sanal yolu —, sunucuda karşılık gelen yolu.</span><span class="sxs-lookup"><span data-stu-id="98190-326">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="98190-327">Sanal yol bir dosya veya klasör yöntemine geçirdiğiniz ve fiziksel yolu döndürür:</span><span class="sxs-lookup"><span data-stu-id="98190-327">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample39.vbhtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="98190-328">Sanal kök başvuran: ~ işleci ve Href yöntemi</span><span class="sxs-lookup"><span data-stu-id="98190-328">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="98190-329">İçinde bir *.cshtml* veya *.vbhtml* dosyası, sanal kök yolu kullanarak başvuru `~` işleci.</span><span class="sxs-lookup"><span data-stu-id="98190-329">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="98190-330">Bu bir sitede sayfaları taşıyabilirsiniz ve diğer sayfalara içerdikleri tüm bağlantılar bozuk olmayacaktır çünkü çok kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="98190-330">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="98190-331">Ayrıca, her zamankinden Web sitenizi farklı bir konuma taşımanız durumunda kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="98190-331">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="98190-332">Bazı örnekler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="98190-332">Here are some examples:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample40.vbhtml)]

<span data-ttu-id="98190-333">Web sitesi ise `http://myserver/myapp`, işte sayfa çalıştığında ASP.NET bu yollar nasıl kabul eder:</span><span class="sxs-lookup"><span data-stu-id="98190-333">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="98190-334">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="98190-334">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="98190-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="98190-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="98190-336">(Aslında bu yollar değişkeni değerleri olarak görmez, ancak, ne oldukları ise ASP.NET yollar kabul eder.)</span><span class="sxs-lookup"><span data-stu-id="98190-336">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="98190-337">Kullanabileceğiniz `~` işleci (yukarıdaki gibi) sunucu kodu hem de bu gibi biçimlendirme:</span><span class="sxs-lookup"><span data-stu-id="98190-337">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample41.html)]

<span data-ttu-id="98190-338">Biçimlendirme içinde kullandığınız `~` görüntü dosyaları, diğer web sayfalarını ve CSS dosyaları gibi kaynaklarına yollar oluşturmak için işleç.</span><span class="sxs-lookup"><span data-stu-id="98190-338">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="98190-339">Sayfa çalıştığında, ASP.NET (kod ve biçimlendirme) sayfadan arar ve tüm çözümler `~` uygun yolu başvuruları.</span><span class="sxs-lookup"><span data-stu-id="98190-339">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="98190-340">Koşullu mantık ve döngüler</span><span class="sxs-lookup"><span data-stu-id="98190-340">Conditional Logic and Loops</span></span>

<span data-ttu-id="98190-341">ASP.NET sunucusu kod koşullar ve bir döngü çalışan başka bir deyişle, belirli sayıda kod deyimleri yineler yazma kod temelinde görevleri gerçekleştirmenize olanak sağlar).</span><span class="sxs-lookup"><span data-stu-id="98190-341">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="98190-342">Koşullar test etme</span><span class="sxs-lookup"><span data-stu-id="98190-342">Testing conditions</span></span>

<span data-ttu-id="98190-343">Kullandığınız basit bir koşulu test etmek için `If...Then` döndürür deyimi `True` veya `False` belirttiğiniz bir testi temel alan:</span><span class="sxs-lookup"><span data-stu-id="98190-343">To test a simple condition you use the `If...Then` statement, which returns `True` or `False` based on a test you specify:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample42.vbhtml)]

<span data-ttu-id="98190-344">`If` Anahtar sözcüğü bir blok başlatır.</span><span class="sxs-lookup"><span data-stu-id="98190-344">The `If` keyword starts a block.</span></span> <span data-ttu-id="98190-345">Gerçek test (koşul) izleyen `If` anahtar sözcüğü ve true veya false değerini döndürür.</span><span class="sxs-lookup"><span data-stu-id="98190-345">The actual test (condition) follows the `If` keyword and returns true or false.</span></span> <span data-ttu-id="98190-346">`If` Deyimi sona erdirir ile `Then`.</span><span class="sxs-lookup"><span data-stu-id="98190-346">The `If` statement ends with `Then`.</span></span> <span data-ttu-id="98190-347">Test true ise, çalıştırılacak deyimleri kapsadığı `If` ve `End If`.</span><span class="sxs-lookup"><span data-stu-id="98190-347">The statements that will run if the test is true are enclosed by `If` and `End If`.</span></span> <span data-ttu-id="98190-348">Bir `If` deyimi dahil edebileceğiniz bir `Else` koşul yanlış ise, çalıştırılacak deyimleri belirtir engelle:</span><span class="sxs-lookup"><span data-stu-id="98190-348">An `If` statement can include an `Else` block that specifies statements to run if the condition is false:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample43.vbhtml)]

<span data-ttu-id="98190-349">Varsa bir `If` deyimi kod bloğu başlatır ve normal kullanmak zorunda değilsiniz `Code...End Code` blokları içerecek şekilde deyimleri.</span><span class="sxs-lookup"><span data-stu-id="98190-349">If an `If` statement starts a code block, you don't have to use the normal `Code...End Code` statements to include the blocks.</span></span> <span data-ttu-id="98190-350">Eklemeniz yeterlidir `@` bloğuna ve çalışır.</span><span class="sxs-lookup"><span data-stu-id="98190-350">You can just add `@` to the block, and it will work.</span></span> <span data-ttu-id="98190-351">Bu yaklaşım çalışır `If` diğer Visual Basic dahil olmak üzere kod blokları tarafından izlenen anahtar sözcükleri programlama yanı sıra `For`, `For Each`, `Do While`vb.</span><span class="sxs-lookup"><span data-stu-id="98190-351">This approach works with `If` as well as other Visual Basic programming keywords that are followed by code blocks, including `For`, `For Each`, `Do While`, etc.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample44.vbhtml)]

<span data-ttu-id="98190-352">Bir veya daha fazla kullanarak birden çok koşul ekleyebilirsiniz `ElseIf` engeller:</span><span class="sxs-lookup"><span data-stu-id="98190-352">You can add multiple conditions using one or more `ElseIf` blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample45.vbhtml)]

<span data-ttu-id="98190-353">Bu örnekte, içinde ilk koşul, `If` blok true değil `ElseIf` koşul denetlenir.</span><span class="sxs-lookup"><span data-stu-id="98190-353">In this example, if the first condition in the `If` block is not true, the `ElseIf` condition is checked.</span></span> <span data-ttu-id="98190-354">Bu koşul karşılandığında deyimlerinde `ElseIf` blok çalıştırılır.</span><span class="sxs-lookup"><span data-stu-id="98190-354">If that condition is met, the statements in the `ElseIf` block are executed.</span></span> <span data-ttu-id="98190-355">Hiç bir koşul karşılanıyorsa deyimlerinde `Else` blok çalıştırılır.</span><span class="sxs-lookup"><span data-stu-id="98190-355">If none of the conditions are met, the statements in the `Else` block are executed.</span></span> <span data-ttu-id="98190-356">Herhangi bir sayıda ekleyebilirsiniz `ElseIf` engeller ve ile kapatın bir `Else` olarak engellemek &quot;şey&quot; koşulu.</span><span class="sxs-lookup"><span data-stu-id="98190-356">You can add any number of `ElseIf` blocks, and then close with an `Else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="98190-357">Çok sayıda koşullar sınamak için kullanın bir `Select Case` engelle:</span><span class="sxs-lookup"><span data-stu-id="98190-357">To test a large number of conditions, use a `Select Case` block:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample46.vbhtml)]

<span data-ttu-id="98190-358">Test etmek için parantez içinde (örnekte haftanın günü değişkeni) değerdir.</span><span class="sxs-lookup"><span data-stu-id="98190-358">The value to test is in parentheses (in the example, the weekday variable).</span></span> <span data-ttu-id="98190-359">Tek tek her test kullanan bir `Case` bir değer listeler deyimi.</span><span class="sxs-lookup"><span data-stu-id="98190-359">Each individual test uses a `Case` statement that lists a value.</span></span> <span data-ttu-id="98190-360">Varsa değerini bir `Case` deyimi, kodda test değeri ile eşleşen `Case` blok gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="98190-360">If the value of a `Case` statement matches the test value, the code in that `Case` block is executed.</span></span>

<span data-ttu-id="98190-361">Bir tarayıcıda görüntülenen son iki koşullu blokları sonucu:</span><span class="sxs-lookup"><span data-stu-id="98190-361">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-vb/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="98190-363">Döngü kodu</span><span class="sxs-lookup"><span data-stu-id="98190-363">Looping code</span></span>

<span data-ttu-id="98190-364">Genellikle aynı deyimleri tekrar tekrar çalıştırmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-364">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="98190-365">Bunun için döngüyle.</span><span class="sxs-lookup"><span data-stu-id="98190-365">You do this by looping.</span></span> <span data-ttu-id="98190-366">Örneğin, genellikle aynı deyimlerini her öğe için bir veri koleksiyonunda çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="98190-366">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="98190-367">Tam döngü istediğiniz kaç kez biliyorsanız, kullanabileceğiniz bir `For` döngü.</span><span class="sxs-lookup"><span data-stu-id="98190-367">If you know exactly how many times you want to loop, you can use a `For` loop.</span></span> <span data-ttu-id="98190-368">Bu tür bir döngü sayım veya sayım için özellikle yararlı olur:</span><span class="sxs-lookup"><span data-stu-id="98190-368">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample47.vbhtml)]

<span data-ttu-id="98190-369">Döngü ile başlayan `For` anahtar sözcüğü, üç öğeleri tarafından takip:</span><span class="sxs-lookup"><span data-stu-id="98190-369">The loop begins with the `For` keyword, followed by three elements:</span></span>

- <span data-ttu-id="98190-370">Hemen sonra `For` deyimi, bir sayaç değişken bildirme (kullanmak zorunda değilsiniz `Dim`) ve ardından olarak aralık belirtmek `i = 10 to 20`.</span><span class="sxs-lookup"><span data-stu-id="98190-370">Immediately after the `For` statement, you declare a counter variable (you don't have to use `Dim`) and then indicate the range, as in `i = 10 to 20`.</span></span> <span data-ttu-id="98190-371">Bu değişkeni anlamına gelir `i` 10 sayım başlatmak ve 20 (dahil) ulaşana kadar devam edin.</span><span class="sxs-lookup"><span data-stu-id="98190-371">This means the variable `i` will start counting at 10 and continue until it reaches 20 (inclusive).</span></span>
- <span data-ttu-id="98190-372">Arasında `For` ve `Next` deyimleri olduğu blok içeriği.</span><span class="sxs-lookup"><span data-stu-id="98190-372">Between the `For` and `Next` statements is the content of the block.</span></span> <span data-ttu-id="98190-373">Bu, çalıştırılacak bir veya daha fazla kod deyimleri her döngü ile içerebilir.</span><span class="sxs-lookup"><span data-stu-id="98190-373">This can contain one or more code statements that execute with each loop.</span></span>
- <span data-ttu-id="98190-374">`Next i` Deyimi döngü sona erdirir.</span><span class="sxs-lookup"><span data-stu-id="98190-374">The `Next i` statement ends the loop.</span></span> <span data-ttu-id="98190-375">Bu sayaç artırılır ve sonraki döngü başlatır.</span><span class="sxs-lookup"><span data-stu-id="98190-375">It increments the counter and starts the next iteration of the loop.</span></span>

<span data-ttu-id="98190-376">Kod arasında satır `For` ve `Next` satırlarını her döngü için çalışan bir kod içerir.</span><span class="sxs-lookup"><span data-stu-id="98190-376">The line of code between the `For` and `Next` lines contains the code that runs for each iteration of the loop.</span></span> <span data-ttu-id="98190-377">Yeni bir paragraf biçimlendirme oluşturur (`<p>` öğesi) her zaman ve değerini görüntüleme çıktısı için bir satır ekler i (sayaç).</span><span class="sxs-lookup"><span data-stu-id="98190-377">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of i (the counter).</span></span> <span data-ttu-id="98190-378">Bu sayfayı çalıştırdığınızda, örnek öğe sayısını gösteren her satırın metinle çıkış görüntüleme 11 satırları oluşturur.</span><span class="sxs-lookup"><span data-stu-id="98190-378">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-vb/_static/image11.jpg)

<span data-ttu-id="98190-380">Bir koleksiyonu veya dizisi ile çalışıyorsanız, sık kullandığınız bir `For Each` döngü.</span><span class="sxs-lookup"><span data-stu-id="98190-380">If you're working with a collection or array, you often use a `For Each` loop.</span></span> <span data-ttu-id="98190-381">Bir koleksiyon benzer nesneleri oluşan bir gruptur ve `For Each` döngü koleksiyondaki her öğe bir görevi yerine getirmek sağlar.</span><span class="sxs-lookup"><span data-stu-id="98190-381">A collection is a group of similar objects, and the `For Each` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="98190-382">Bu tür bir döngü koleksiyonlar için uygun olan çünkü aksine bir `For` döngüsü, sınırı sayaç artırın veya zorunda değilsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-382">This type of loop is convenient for collections, because unlike a `For` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="98190-383">Bunun yerine, `For Each` döngü kodunu işlemi tamamlanana kadar toplulukta yalnızca geçer.</span><span class="sxs-lookup"><span data-stu-id="98190-383">Instead, the `For Each` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="98190-384">Bu örnek öğeleri döndürür `Request.ServerVariables` (web sunucunuz hakkında bilgi içeren) koleksiyon.</span><span class="sxs-lookup"><span data-stu-id="98190-384">This example returns the items in the `Request.ServerVariables` collection (which contains information about your web server).</span></span> <span data-ttu-id="98190-385">Kullandığı bir `For Each` yeni oluşturarak her öğenin adını görüntülemek için döngü `<li>` öğesi bir HTML madde işaretli listede.</span><span class="sxs-lookup"><span data-stu-id="98190-385">It uses a `For Each` loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample48.vbhtml)]

<span data-ttu-id="98190-386">`For Each` Anahtar sözcüğü koleksiyonunda tek bir öğeyi temsil eden bir değişken arkasından (örnekte `myItem`), ardından `In` döngü istediğiniz koleksiyonu ve ardından bir anahtar sözcük,.</span><span class="sxs-lookup"><span data-stu-id="98190-386">The `For Each` keyword is followed by a variable that represents a single item in the collection (in the example, `myItem`), followed by the `In` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="98190-387">Gövdesinde `For Each` döngü, daha önce bildirilen değişkeni kullanarak geçerli öğesi erişebilir.</span><span class="sxs-lookup"><span data-stu-id="98190-387">In the body of the `For Each` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-vb/_static/image12.jpg)

<span data-ttu-id="98190-389">Daha fazla genel amaçlı bir döngü oluşturmak üzere kullanmanız `Do While` deyimi:</span><span class="sxs-lookup"><span data-stu-id="98190-389">To create a more general-purpose loop, use the `Do While` statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample49.vbhtml)]

<span data-ttu-id="98190-390">Bu döngü ile başlayan `Do While` anahtar sözcüğünü bir koşul tarafından ardından yinelenecek bloğu tarafından.</span><span class="sxs-lookup"><span data-stu-id="98190-390">This loop begins with the `Do While` keyword, followed by a condition, followed by the block to repeat.</span></span> <span data-ttu-id="98190-391">Döngüler genellikle Artır (Ekle) veya azaltma (Çıkart) bir değişken veya sayım için kullanılan nesne.</span><span class="sxs-lookup"><span data-stu-id="98190-391">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="98190-392">Örnekte, `+=` işleci döngünün her çalıştığında bir değişken değerine 1 ekler.</span><span class="sxs-lookup"><span data-stu-id="98190-392">In the example, the `+=` operator adds 1 to the value of a variable each time the loop runs.</span></span> <span data-ttu-id="98190-393">(Bir değişken sayıları aşağı Döngüdeki düşürmek için azaltma işleci kullanırsınız `-=`.)</span><span class="sxs-lookup"><span data-stu-id="98190-393">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`.)</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="98190-394">Nesneleri ve koleksiyonları</span><span class="sxs-lookup"><span data-stu-id="98190-394">Objects and Collections</span></span>

<span data-ttu-id="98190-395">ASP.NET Web sitesi, neredeyse her şeyi web sayfası dahil olmak üzere bir nesnesidir.</span><span class="sxs-lookup"><span data-stu-id="98190-395">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="98190-396">Bu bölümde, ile sık kodunuzda karşılaşmayacağınızı bazı önemli nesneleri anlatılmaktadır.</span><span class="sxs-lookup"><span data-stu-id="98190-396">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="98190-397">Sayfa nesneleri</span><span class="sxs-lookup"><span data-stu-id="98190-397">Page objects</span></span>

<span data-ttu-id="98190-398">En temel ASP.NET sayfası nesnedir.</span><span class="sxs-lookup"><span data-stu-id="98190-398">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="98190-399">Hiçbir belirleme nesnesi olmadan doğrudan sayfa nesnesinin özellikleri erişebilir.</span><span class="sxs-lookup"><span data-stu-id="98190-399">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="98190-400">Aşağıdaki kod sayfanın dosya yolunu alır kullanarak `Request` sayfasının nesnesi:</span><span class="sxs-lookup"><span data-stu-id="98190-400">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample50.vbhtml)]

<span data-ttu-id="98190-401">Özelliklerini kullanabilirsiniz `Page` nesne gibi bir çok sayıda bilgi, almak için:</span><span class="sxs-lookup"><span data-stu-id="98190-401">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="98190-402">`Request`.</span><span class="sxs-lookup"><span data-stu-id="98190-402">`Request`.</span></span> <span data-ttu-id="98190-403">Bu önceden gördüğünüz gibi tarayıcının ne tür, sayfa, kullanıcı kimliği, vb. URL'sini istekte dahil olmak üzere geçerli istek hakkındaki bilgiler koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="98190-403">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="98190-404">`Response`.</span><span class="sxs-lookup"><span data-stu-id="98190-404">`Response`.</span></span> <span data-ttu-id="98190-405">Bu, sunucu kodu çalışmasını bitirdikten sonra tarayıcıya gönderilen yanıt (sayfa) hakkında bilgi koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="98190-405">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="98190-406">Örneğin, bilgi yanıtına yazmak için bu özelliği kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-406">For example, you can use this property to write information into the response.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample51.vbhtml)]

### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="98190-407">Koleksiyon nesnesi (diziler ve sözlükler)</span><span class="sxs-lookup"><span data-stu-id="98190-407">Collection objects (arrays and dictionaries)</span></span>

<span data-ttu-id="98190-408">Aynı türde bir koleksiyonu gibi bir grup koleksiyonudur `Customer` nesnelerini bir veritabanından.</span><span class="sxs-lookup"><span data-stu-id="98190-408">A collection is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="98190-409">ASP.NET içeren birçok yerleşik koleksiyonlar gibi `Request.Files` koleksiyonu.</span><span class="sxs-lookup"><span data-stu-id="98190-409">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="98190-410">Koleksiyonlarda verileri genellikle çalışmak.</span><span class="sxs-lookup"><span data-stu-id="98190-410">You'll often work with data in collections.</span></span> <span data-ttu-id="98190-411">İki ortak koleksiyon türü *dizi* ve *sözlük*.</span><span class="sxs-lookup"><span data-stu-id="98190-411">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="98190-412">Bir dizi benzer öğeleri koleksiyonu depolamak istiyorsanız, ancak her bir öğeyi tutmak için ayrı bir değişken oluşturmak istemediğiniz durumlarda yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="98190-412">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample52.vbhtml)]

<span data-ttu-id="98190-413">Dizilerle, bir özel veri türü gibi bildirdiğiniz `String`, `Integer`, veya `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="98190-413">With arrays, you declare a specific data type, such as `String`, `Integer`, or `DateTime`.</span></span> <span data-ttu-id="98190-414">Bir dizi değişkeni içerebilir, değişken adı bildiriminde parantez eklemek belirtmek için (gibi `Dim myVar() As String`).</span><span class="sxs-lookup"><span data-stu-id="98190-414">To indicate that the variable can contain an array, you add parentheses to the variable name in the declaration (such as `Dim myVar() As String`).</span></span> <span data-ttu-id="98190-415">Öğeleri konumlarını (dizin) kullanarak bir dizi veya kullanarak erişebilirsiniz `For Each` deyimi.</span><span class="sxs-lookup"><span data-stu-id="98190-415">You can access items in an array using their position (index) or by using the `For Each` statement.</span></span> <span data-ttu-id="98190-416">Dizinin sıfır tabanlı olan &#8212;dizinler; diğer bir deyişle, ilk öğe adresindeki konumlandırın 0, ikinci öğesi konum 1 ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="98190-416">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample53.vbhtml)]

<span data-ttu-id="98190-417">Alarak dizideki öğelerin sayısını belirleyebilirsiniz kendi `Length` özelliği.</span><span class="sxs-lookup"><span data-stu-id="98190-417">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="98190-418">Belirli bir öğenin dizideki konumunu almak için (diğer bir deyişle, dizi aramak için), kullanın `Array.IndexOf` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="98190-418">To get the position of a specific item in the array (that is, to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="98190-419">Bir dizinin içeriğini da ters gibi şeyler yapabilirsiniz ( `Array.Reverse` yöntemi) veya içeriği sıralama ( `Array.Sort` yöntemi).</span><span class="sxs-lookup"><span data-stu-id="98190-419">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="98190-420">Bir tarayıcıda görüntülenen dize dizisi kodu çıktı:</span><span class="sxs-lookup"><span data-stu-id="98190-420">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-vb/_static/image13.jpg)

<span data-ttu-id="98190-422">Bir anahtar (veya ad) karşılık gelen değeri ayarlayamaz veya Burada sağladığınız anahtar/değer çiftleri koleksiyonu dictionary'si:</span><span class="sxs-lookup"><span data-stu-id="98190-422">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample54.vbhtml)]

<span data-ttu-id="98190-423">Bir sözlük oluşturmak için kullandığınız `New` yeni oluşturuyorsanız göstermek için anahtar sözcüğü `Dictionary` nesnesi.</span><span class="sxs-lookup"><span data-stu-id="98190-423">To create a dictionary, you use the `New` keyword to indicate that you're creating a new `Dictionary` object.</span></span> <span data-ttu-id="98190-424">Bir değişken kullanarak bir sözlük atayabilirsiniz `Dim` anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="98190-424">You can assign a dictionary to a variable using the `Dim` keyword.</span></span> <span data-ttu-id="98190-425">Parantez kullanılarak sözlükteki öğe veri türlerini gösterir ( `( )` ).</span><span class="sxs-lookup"><span data-stu-id="98190-425">You indicate the data types of the items in the dictionary using parentheses ( `( )` ).</span></span> <span data-ttu-id="98190-426">Bildirim sonunda, bu yeni bir sözlük oluşturur yöntemi gerçekte olduğundan başka bir çift ayraç, eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-426">At the end of the declaration, you must add another pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="98190-427">Sözlüğe öğeler eklemek için çağırabilirsiniz `Add` sözlük değişkenin yöntemi (`myScores` bu durumda) ve ardından bir anahtar ve bir değer belirtin.</span><span class="sxs-lookup"><span data-stu-id="98190-427">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="98190-428">Alternatif olarak, anahtarı belirtmek ve aşağıdaki örnekteki gibi basit bir atama yapmak için parantezleri kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="98190-428">Alternatively, you can use parentheses to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample55.vbhtml)]

<span data-ttu-id="98190-429">Sözlükten bir değer almak için parantez içinde anahtarı belirtin:</span><span class="sxs-lookup"><span data-stu-id="98190-429">To get a value from the dictionary, you specify the key in parentheses:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample56.vbhtml)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="98190-430">Parametrelerle yöntemleri çağırma</span><span class="sxs-lookup"><span data-stu-id="98190-430">Calling Methods with Parameters</span></span>

<span data-ttu-id="98190-431">Bu makalede anlatıldığı gibi ile program nesnelerin yöntemleri vardır.</span><span class="sxs-lookup"><span data-stu-id="98190-431">As you saw earlier in this article, the objects that you program with have methods.</span></span> <span data-ttu-id="98190-432">Örneğin, bir `Database` nesnesi olabilir bir `Database.Connect` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="98190-432">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="98190-433">Birçok yöntem de bir veya daha fazla parametre vardır.</span><span class="sxs-lookup"><span data-stu-id="98190-433">Many methods also have one or more parameters.</span></span> <span data-ttu-id="98190-434">A *parametresi* bir yönteme geçirin bir değer, görevini tamamlamak üzere yöntemini etkinleştirmek için.</span><span class="sxs-lookup"><span data-stu-id="98190-434">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="98190-435">Örneğin, bir bildirimi bakın `Request.MapPath` üç parametre alır yöntemi:</span><span class="sxs-lookup"><span data-stu-id="98190-435">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-vb[Main](introducing-razor-syntax-vb/samples/sample57.vb)]

<span data-ttu-id="98190-436">Bu yöntem, belirtilen sanal yolu sunucuda karşılık gelen fiziksel yolu döndürür.</span><span class="sxs-lookup"><span data-stu-id="98190-436">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="98190-437">Yöntem için üç parametreleri `virtualPath`, `baseVirtualDir`, ve `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="98190-437">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="98190-438">(Kabul verileri veri türleriyle parametreleri bildiriminde listelenen dikkat edin.) Bu yöntemi çağırdığınızda, tüm üç parametreleri için değerler girmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="98190-438">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="98190-439">Visual Basic Razor sözdizimi ile kullanırken, bir yöntem parametreleri geçirme için iki seçeneğiniz vardır: *konumsal parametreler* veya *adlandırılmış parametreleri*.</span><span class="sxs-lookup"><span data-stu-id="98190-439">When you're using Visual Basic with the Razor syntax, you have two options for passing parameters to a method: *positional parameters* or *named parameters*.</span></span> <span data-ttu-id="98190-440">Konumsal parametreler kullanarak bir yöntemi çağırmak için yöntem bildiriminde belirtilen katı bir sırada parametreleri geçirin.</span><span class="sxs-lookup"><span data-stu-id="98190-440">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="98190-441">(Genellikle bu sırada yöntemi belgelerine okuyarak bilmeniz.) Sırayı izler ve parametrelerinden herhangi biri &#8212;atlayamazsınız; Gerekirse, boş bir dize geçirdiğiniz (`""`) için bir değer yoksa konumsal bir parametre için ya da null.</span><span class="sxs-lookup"><span data-stu-id="98190-441">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or null for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="98190-442">Aşağıdaki örnek adlı bir klasör olduğunu varsayar *betikleri* Web sitenizde.</span><span class="sxs-lookup"><span data-stu-id="98190-442">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="98190-443">Kod çağrıları `Request.MapPath` üç parametre doğru sırada yöntemi ve geçişleri değerlerini.</span><span class="sxs-lookup"><span data-stu-id="98190-443">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="98190-444">Ardından, elde edilen eşlenen yolun görüntüler.</span><span class="sxs-lookup"><span data-stu-id="98190-444">It then displays the resulting mapped path.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample58.vbhtml)]

<span data-ttu-id="98190-445">Bir yöntem için birçok parametrelerini olduğunda adlandırılmış parametreler kullanılarak temizleyici ve daha okunabilir kodunuzu tutabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-445">When there are many parameters for a method, you can keep your code cleaner and more readable by using named parameters.</span></span> <span data-ttu-id="98190-446">Adlandırılmış parametreler kullanılarak bir yöntemi çağırmak için belirtin ve ardından parametre adı `:=` değeri girin.</span><span class="sxs-lookup"><span data-stu-id="98190-446">To call a method using named parameters, specify the parameter name followed by `:=` and then provide the value.</span></span> <span data-ttu-id="98190-447">Adlandırılmış parametreler avantajı, bunları istediğiniz herhangi bir sırada ekleyebilirsiniz ' dir.</span><span class="sxs-lookup"><span data-stu-id="98190-447">An advantage of named parameters is that you can add them in any order you want.</span></span> <span data-ttu-id="98190-448">(Bir dezavantajı yöntem çağrısının olarak compact olmamasıdır.)</span><span class="sxs-lookup"><span data-stu-id="98190-448">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="98190-449">Aşağıdaki örnek yukarıdaki gibi aynı yöntemini çağırır, ancak adlı parametre değerlerini sağlamak için kullanır:</span><span class="sxs-lookup"><span data-stu-id="98190-449">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample59.vbhtml)]

<span data-ttu-id="98190-450">Gördüğünüz gibi parametreleri farklı bir sırayla geçirilir.</span><span class="sxs-lookup"><span data-stu-id="98190-450">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="98190-451">Ancak, önceki örnekte ve bu örnek çalıştırırsanız, aynı değere döneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="98190-451">However, if you run the previous example and this example, they'll return the same value.</span></span>

## <a name="handling-errors"></a><span data-ttu-id="98190-452">Hataları işleme</span><span class="sxs-lookup"><span data-stu-id="98190-452">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="98190-453">Try-Catch deyimleri</span><span class="sxs-lookup"><span data-stu-id="98190-453">Try-Catch statements</span></span>

<span data-ttu-id="98190-454">Genellikle, denetimi dışında kalan nedeniyle başarısız olabilir, kodunuzda deyimleri sahip olacaksınız.</span><span class="sxs-lookup"><span data-stu-id="98190-454">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="98190-455">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="98190-455">For example:</span></span>

- <span data-ttu-id="98190-456">Kodunuzu açın, oluşturmak, okumak veya bir dosya yazmak çalışırsa, her türlü hataları oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-456">If your code tries to open, create, read, or write a file, all sorts of errors might occur.</span></span> <span data-ttu-id="98190-457">İstediğiniz dosya var olmayabilir, kilitli olabilir, kodu değil izinlere sahip ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="98190-457">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="98190-458">Benzer şekilde, kodunuzu veritabanındaki kayıtları güncelleştirme çalışırsa, izin sorunları olabilir, veritabanı bağlantısı bırakılabilir, kaydetmek için verileri geçersiz vb. olabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-458">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="98190-459">Bu durumlarda programlama dilinde denir *özel durumları*.</span><span class="sxs-lookup"><span data-stu-id="98190-459">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="98190-460">Kodunuzu bir özel durum karşılaşırsa (atar) oluşturan bir hata iletisi diğer bir deyişle, en iyi kullanıcılara sinir bozucu.</span><span class="sxs-lookup"><span data-stu-id="98190-460">If your code encounters an exception, it generates (throws) an error message that is, at best, annoying to users.</span></span>

![Razor-Img14](introducing-razor-syntax-vb/_static/image14.jpg)

<span data-ttu-id="98190-462">Burada kodunuzu karşılaşabileceğiniz özel durumlar durumlarda ve hata iletileri bu tür önlemek için kullanabileceğiniz `Try/Catch` deyimleri.</span><span class="sxs-lookup"><span data-stu-id="98190-462">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `Try/Catch` statements.</span></span> <span data-ttu-id="98190-463">İçinde `Try` deyimi, denetimi kod çalıştırma.</span><span class="sxs-lookup"><span data-stu-id="98190-463">In the `Try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="98190-464">Bir veya daha `Catch` deyimleri için özel konum (belirli tür özel durumlar) hatalar oluşmuş olabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-464">In one or more `Catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="98190-465">Kadar içerebilir `Catch` deyimleri yazarken gereken bekleme hataları aramak.</span><span class="sxs-lookup"><span data-stu-id="98190-465">You can include as many `Catch` statements as you need to look for errors that you're anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="98190-466">Kullanmaktan kaçının öneririz `Response.Redirect` yönteminde `Try/Catch` deyimleri, çünkü bu bir özel durum sayfanızda neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="98190-466">We recommend that you avoid using the `Response.Redirect` method in `Try/Catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="98190-467">Aşağıdaki örnek ilk istek üzerinde bir metin dosyası oluşturur ve kullanıcının dosyayı açma olanak sağlayan bir düğme görüntüleyen bir sayfada görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="98190-467">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="98190-468">Örnek kasıtlı olarak hatalı dosya adı kullanır, böylece bir özel durumuna neden olur.</span><span class="sxs-lookup"><span data-stu-id="98190-468">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="98190-469">Kod içeren `Catch` deyimleri iki olası özel durumlar için: `FileNotFoundException`, dosya adı hatalı olması durumunda gerçekleşir ve `DirectoryNotFoundException`, ASP.NET bile klasörünü bulamazsanız oluşur.</span><span class="sxs-lookup"><span data-stu-id="98190-469">The code includes `Catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="98190-470">(, Örnek bir deyimde her şeyin doğru şekilde çalışırken, nasıl çalıştığı görmek için açıklamadan çıkarın.)</span><span class="sxs-lookup"><span data-stu-id="98190-470">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="98190-471">Kodunuzu özel durum işleme alamadık, önceki ekran görüntüsü gibi hata sayfasını görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="98190-471">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="98190-472">Ancak, `Try/Catch` bölüm, kullanıcı bu tür hataların görmemesi yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="98190-472">However, the `Try/Catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample60.vbhtml)]

## <a name="additional-resources"></a><span data-ttu-id="98190-473">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="98190-473">Additional Resources</span></span>

### <a name="reference-documentation"></a><span data-ttu-id="98190-474">Başvuru belgeleri</span><span class="sxs-lookup"><span data-stu-id="98190-474">Reference Documentation</span></span>

- [<span data-ttu-id="98190-475">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="98190-475">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)
- [<span data-ttu-id="98190-476">Visual Basic dili</span><span class="sxs-lookup"><span data-stu-id="98190-476">Visual Basic Language</span></span>](https://msdn.microsoft.com/library/2x7h1hfk.aspx)

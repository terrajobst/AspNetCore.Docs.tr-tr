---
title: ASP.NET Core yönlendirme
author: rick-anderson
description: İstek URI 'Lerini uç nokta seçiclerine eşleyerek ve gelen istekleri uç noktalara gönderen ASP.NET Core yönlendirmenin nasıl sorumlu olduğunu öğrenin.
monikerRange: '>= aspnetcore-2.1'
ms.author: riande
ms.custom: mvc
ms.date: 12/13/2019
uid: fundamentals/routing
ms.openlocfilehash: 462f34664540b92ba6758224a722c7ca8f9c8de0
ms.sourcegitcommit: 7dfe6cc8408ac6a4549c29ca57b0c67ec4baa8de
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 01/09/2020
ms.locfileid: "75829068"
---
# <a name="routing-in-aspnet-core"></a><span data-ttu-id="eea9f-103">ASP.NET Core yönlendirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-103">Routing in ASP.NET Core</span></span>

<span data-ttu-id="eea9f-104">[Ryan şimdi ak](https://github.com/rynowak), [Steve Smith](https://ardalis.com/)ve [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="eea9f-104">By [Ryan Nowak](https://github.com/rynowak), [Steve Smith](https://ardalis.com/), and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

::: moniker range=">= aspnetcore-3.0"

<span data-ttu-id="eea9f-105">Yönlendirme, istek URI 'Lerini uç noktalarla eşleştirmekten ve gelen istekleri bu uç noktalara gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-105">Routing is responsible for mapping request URIs to endpoints and dispatching incoming requests to those endpoints.</span></span> <span data-ttu-id="eea9f-106">Yollar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-106">Routes are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="eea9f-107">Yol, isteğe bağlı olarak istekte bulunan URL 'den değerleri ayıklayabilir ve bu değerler, istek işleme için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-107">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="eea9f-108">Uygulamadan yönlendirme bilgilerini kullanarak, yönlendirme, uç noktalarıyla eşlenen URL 'Ler de oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-108">Using route information from the app, routing is also able to generate URLs that map to endpoints.</span></span> <span data-ttu-id="eea9f-109">Birçok uygulamanın, şablonların sağladıklarının ötesinde yollar eklemesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-109">Many apps don't need to add routes beyond what the templates provide.</span></span> <span data-ttu-id="eea9f-110">Denetleyiciler ve Razor sayfaları için ASP.NET Core şablonları, yol uç noktalarını yapılandırır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-110">The ASP.NET Core templates for controllers and Razor pages configure route endpoints.</span></span> <span data-ttu-id="eea9f-111">Özel yol uç noktaları eklemeniz gerekiyorsa, Özel uç noktalar şablon tarafından oluşturulan yol uç noktaları ile yapılandırılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-111">If you need to add custom route endpoints, the custom endpoints can be configured alongside template generated route endpoints.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="eea9f-112">Bu belge, alt düzey ASP.NET Core yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-112">This document covers low-level ASP.NET Core routing.</span></span> <span data-ttu-id="eea9f-113">ASP.NET Core MVC yönlendirme hakkında daha fazla bilgi için bkz. <xref:mvc/controllers/routing>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-113">For information on ASP.NET Core MVC routing, see <xref:mvc/controllers/routing>.</span></span> <span data-ttu-id="eea9f-114">Razor Pages 'de yönlendirme kuralları hakkında bilgi için bkz. <xref:razor-pages/razor-pages-conventions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-114">For information on routing conventions in Razor Pages, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="eea9f-115">[Görüntüleme veya indirme örnek kodu](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([nasıl indirileceğini](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="eea9f-115">[View or download sample code](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([how to download](xref:index#how-to-download-a-sample))</span></span>

## <a name="routing-basics"></a><span data-ttu-id="eea9f-116">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-116">Routing basics</span></span>

<span data-ttu-id="eea9f-117">Çoğu uygulama, URL 'Lerin okunabilir ve anlamlı olması için temel ve açıklayıcı bir yönlendirme şeması seçmelidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-117">Most apps should choose a basic and descriptive routing scheme so that URLs are readable and meaningful.</span></span> <span data-ttu-id="eea9f-118">Varsayılan geleneksel yol `{controller=Home}/{action=Index}/{id?}`:</span><span class="sxs-lookup"><span data-stu-id="eea9f-118">The default conventional route `{controller=Home}/{action=Index}/{id?}`:</span></span>

* <span data-ttu-id="eea9f-119">Temel ve açıklayıcı bir yönlendirme düzenini destekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-119">Supports a basic and descriptive routing scheme.</span></span>
* <span data-ttu-id="eea9f-120">, UI tabanlı uygulamalar için kullanışlı bir başlangıç noktasıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-120">Is a useful starting point for UI-based apps.</span></span>

<span data-ttu-id="eea9f-121">Geliştiriciler, [öznitelik yönlendirme](xref:mvc/controllers/routing#attribute-routing) veya adanmış geleneksel yollar kullanarak özel durumlarda (örneğin, blog ve e-ticaret uç noktaları) bir uygulamanın yüksek trafik bölümlerine daha fazla terse yolu ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-121">Developers commonly add additional terse routes to high-traffic areas of an app in specialized situations (for example, blog and ecommerce endpoints) using [attribute routing](xref:mvc/controllers/routing#attribute-routing) or dedicated conventional routes.</span></span>

<span data-ttu-id="eea9f-122">Web API 'Leri, uygulamanın işlevselliğini HTTP fiilleri tarafından temsil edilen bir kaynak kümesi olarak modellemek için öznitelik yönlendirmeyi kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-122">Web APIs should use attribute routing to model the app's functionality as a set of resources where operations are represented by HTTP verbs.</span></span> <span data-ttu-id="eea9f-123">Bu, aynı mantıksal kaynaktaki birçok işlemin (örneğin, GET, POST) aynı URL 'YI kullanacağı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-123">This means that many operations (for example, GET, POST) on the same logical resource will use the same URL.</span></span> <span data-ttu-id="eea9f-124">Öznitelik yönlendirme, bir API 'nin Genel uç nokta yerleşimini dikkatle tasarlamak için gereken bir denetim düzeyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-124">Attribute routing provides a level of control that's needed to carefully design an API's public endpoint layout.</span></span>

<span data-ttu-id="eea9f-125">Razor Pages uygulamalar, bir uygulamanın *Sayfalar* klasöründe adlandırılmış kaynaklara hizmeti sağlamak için varsayılan geleneksel yönlendirmeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-125">Razor Pages apps use default conventional routing to serve named resources in the *Pages* folder of an app.</span></span> <span data-ttu-id="eea9f-126">Razor Pages yönlendirme davranışını özelleştirmenizi sağlayan ek kurallar mevcuttur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-126">Additional conventions are available that allow you to customize Razor Pages routing behavior.</span></span> <span data-ttu-id="eea9f-127">Daha fazla bilgi için bkz. <xref:razor-pages/index> ve <xref:razor-pages/razor-pages-conventions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-127">For more information, see <xref:razor-pages/index> and <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="eea9f-128">URL oluşturma desteği, uygulamanın, uygulamayı birbirine bağlamak için sabit kodlama URL 'Leri olmadan geliştirilebilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-128">URL generation support allows the app to be developed without hard-coding URLs to link the app together.</span></span> <span data-ttu-id="eea9f-129">Bu destek, temel bir yönlendirme yapılandırmasıyla başlayıp uygulamanın kaynak düzeni belirlendikten sonra yolların değiştirilmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-129">This support allows for starting with a basic routing configuration and modifying the routes after the app's resource layout is determined.</span></span>

<span data-ttu-id="eea9f-130">Yönlendirme, bir uygulamadaki mantıksal uç noktaları temsil etmek için *uç noktaları* (`Endpoint`) kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-130">Routing uses *endpoints* (`Endpoint`) to represent logical endpoints in an app.</span></span>

<span data-ttu-id="eea9f-131">Bir uç nokta, istekleri işlemek için bir temsilci ve rastgele meta veri koleksiyonunu tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-131">An endpoint defines a delegate to process requests and a collection of arbitrary metadata.</span></span> <span data-ttu-id="eea9f-132">Meta veriler, her uç noktaya eklenen ilkelere ve yapılandırmaya göre çapraz kesme sorunları uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-132">The metadata is used to implement cross-cutting concerns based on policies and configuration attached to each endpoint.</span></span>

<span data-ttu-id="eea9f-133">Yönlendirme sistemi aşağıdaki özelliklere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-133">The routing system has the following characteristics:</span></span>

* <span data-ttu-id="eea9f-134">Yol şablonu sözdizimi, simgeleştirilmiş yol parametrelerine sahip yolları tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-134">Route template syntax is used to define routes with tokenized route parameters.</span></span>
* <span data-ttu-id="eea9f-135">Geleneksel stil ve öznitelik stili uç nokta yapılandırmasına izin verilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-135">Conventional-style and attribute-style endpoint configuration is permitted.</span></span>
* <span data-ttu-id="eea9f-136"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, bir URL parametresinin belirli bir uç nokta kısıtlaması için geçerli bir değer içerip içermediğini belirlemekte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-136"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> is used to determine whether a URL parameter contains a valid value for a given endpoint constraint.</span></span>
* <span data-ttu-id="eea9f-137">MVC/Razor Pages gibi uygulama modelleri, yönlendirme senaryolarının öngörülebilir bir uygulaması olan tüm uç noktalarını kaydeder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-137">App models, such as MVC/Razor Pages, register all of their endpoints, which have a predictable implementation of routing scenarios.</span></span>
* <span data-ttu-id="eea9f-138">Yönlendirme gerçekleştirme, yönlendirme kararlarını, ara yazılım ardışık düzeninde istediğiniz yere getirir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-138">The routing implementation makes routing decisions wherever desired in the middleware pipeline.</span></span>
* <span data-ttu-id="eea9f-139">Bir yönlendirme ara yazılımı, belirli bir istek URI 'SI için yönlendirme ara yazılımı uç noktası kararının sonucunu inceleyebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-139">Middleware that appears after a Routing Middleware can inspect the result of the Routing Middleware's endpoint decision for a given request URI.</span></span>
* <span data-ttu-id="eea9f-140">Uygulamadaki tüm uç noktaları, ara yazılım ardışık düzeninde herhangi bir yerde listelemek mümkündür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-140">It's possible to enumerate all of the endpoints in the app anywhere in the middleware pipeline.</span></span>
* <span data-ttu-id="eea9f-141">Bir uygulama, uç nokta bilgilerine göre URL 'Ler oluşturmak için (örneğin, yeniden yönlendirme veya bağlantılar için) yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-141">An app can use routing to generate URLs (for example, for redirection or links) based on endpoint information and thus avoid hard-coded URLs, which helps maintainability.</span></span>
* <span data-ttu-id="eea9f-142">URL oluşturma, rastgele genişletilebilirliği destekleyen adreslere dayalıdır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-142">URL generation is based on addresses, which support arbitrary extensibility:</span></span>

  * <span data-ttu-id="eea9f-143">Bağlantı Oluşturucu API 'SI (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>), [bağımlılık ekleme (dı)](xref:fundamentals/dependency-injection) kullanılarak URL 'ler oluşturmak için her yerde çözülebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-143">The Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>) can be resolved anywhere using [dependency injection (DI)](xref:fundamentals/dependency-injection) to generate URLs.</span></span>
  * <span data-ttu-id="eea9f-144">Bağlantı Oluşturucu API 'sinin DI aracılığıyla kullanılamadığı durumlarda <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> URL 'Leri derlemek için yöntemler sunar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-144">Where the Link Generator API isn't available via DI, <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> offers methods to build URLs.</span></span>

> [!NOTE]
> <span data-ttu-id="eea9f-145">Uç nokta bağlama, MVC/Razor Pages eylemleri ve sayfalarıyla sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-145">Endpoint linking is limited to MVC/Razor Pages actions and pages.</span></span> <span data-ttu-id="eea9f-146">Uç nokta bağlama yeteneklerinin genişletmeleri, gelecek sürümlerde planlanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-146">The expansions of endpoint-linking capabilities is planned for future releases.</span></span>

<span data-ttu-id="eea9f-147">Yönlendirme, <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> sınıfı tarafından, [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-147">Routing is connected to the [middleware](xref:fundamentals/middleware/index) pipeline by the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> class.</span></span> <span data-ttu-id="eea9f-148">[ASP.NET Core MVC](xref:mvc/overview) , yapılandırma kapsamında bir ara yazılım ardışık düzenine yönlendirme ekler ve MVC ve Razor Pages uygulamalarında yönlendirmeyi işler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-148">[ASP.NET Core MVC](xref:mvc/overview) adds routing to the middleware pipeline as part of its configuration and handles routing in MVC and Razor Pages apps.</span></span> <span data-ttu-id="eea9f-149">Tek başına bileşen olarak yönlendirmeyi nasıl kullanacağınızı öğrenmek için [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-149">To learn how to use routing as a standalone component, see the [Use Routing Middleware](#use-routing-middleware) section.</span></span>

### <a name="url-matching"></a><span data-ttu-id="eea9f-150">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-150">URL matching</span></span>

<span data-ttu-id="eea9f-151">URL eşleştirme, yönlendirmenin bir *uç noktaya*gelen isteği gönderdiği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-151">URL matching is the process by which routing dispatches an incoming request to an *endpoint*.</span></span> <span data-ttu-id="eea9f-152">Bu işlem, URL yolundaki verileri temel alır, ancak istekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-152">This process is based on data in the URL path but can be extended to consider any data in the request.</span></span> <span data-ttu-id="eea9f-153">Ayrı işleyicilere istek gönderme özelliği, bir uygulamanın boyutunu ve karmaşıklığını ölçeklendirmeye yönelik anahtardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-153">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an app.</span></span>

<span data-ttu-id="eea9f-154">Bir yönlendirme ara yazılımı yürütüldüğünde, bir uç nokta (`Endpoint`) ayarlar ve değerleri <xref:Microsoft.AspNetCore.Http.HttpContext>bir özelliğe yönlendirir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-154">When a Routing Middleware executes, it sets an endpoint (`Endpoint`) and route values to a feature on the <xref:Microsoft.AspNetCore.Http.HttpContext>.</span></span> <span data-ttu-id="eea9f-155">Geçerli istek için:</span><span class="sxs-lookup"><span data-stu-id="eea9f-155">For the current request:</span></span>

* <span data-ttu-id="eea9f-156">Çağırma `HttpContext.GetEndpoint` bitiş noktasını alır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-156">Calling `HttpContext.GetEndpoint` gets the endpoint.</span></span>
* <span data-ttu-id="eea9f-157">`HttpRequest.RouteValues` yol değerlerinin koleksiyonunu alır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-157">`HttpRequest.RouteValues` gets the collection of route values.</span></span>

<span data-ttu-id="eea9f-158">Yönlendirme ara yazılımı ile çalışan ara yazılım, uç noktayı görebilir ve işlem yapabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-158">Middleware running after the Routing Middleware can see the endpoint and take action.</span></span> <span data-ttu-id="eea9f-159">Örneğin, bir yetkilendirme ara yazılımı, bir yetkilendirme ilkesi için bitiş noktasının meta veri koleksiyonunu sorgulanamıyor.</span><span class="sxs-lookup"><span data-stu-id="eea9f-159">For example, an Authorization Middleware can interrogate the endpoint's metadata collection for an authorization policy.</span></span> <span data-ttu-id="eea9f-160">İstek işleme ardışık düzeninde bulunan tüm ara yazılım yürütüldükten sonra, seçilen uç noktanın temsilcisi çağrılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-160">After all of the middleware in the request processing pipeline is executed, the selected endpoint's delegate is invoked.</span></span>

<span data-ttu-id="eea9f-161">Uç nokta yönlendirmesinde yönlendirme sistemi tüm gönderme kararlarından sorumludur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-161">The routing system in endpoint routing is responsible for all dispatching decisions.</span></span> <span data-ttu-id="eea9f-162">Ara yazılım, seçili uç noktaya göre ilkeler uyguladığı için, yönlendirme sisteminde güvenlik ilkelerinin dağıtımını etkileyebilecek veya uygulamanın uygulanmasını etkileyebilecek herhangi bir kararın olması önemlidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-162">Since the middleware applies policies based on the selected endpoint, it's important that any decision that can affect dispatching or the application of security policies is made inside the routing system.</span></span>

<span data-ttu-id="eea9f-163">Uç nokta temsilcisi yürütüldüğünde, [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özellikleri, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-163">When the endpoint delegate is executed, the properties of [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) are set to appropriate values based on the request processing performed thus far.</span></span>

<span data-ttu-id="eea9f-164">[RouteData. Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) , rotada oluşturulan *yol değerlerinin* bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-164">[RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="eea9f-165">Bu değerler genellikle URL 'YI simgeleştirerek belirlenir ve Kullanıcı girişini kabul etmek ya da uygulama içinde daha fazla kararlar almak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-165">These values are usually determined by tokenizing the URL and can be used to accept user input or to make further dispatching decisions inside the app.</span></span>

<span data-ttu-id="eea9f-166">[RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) , eşleşen rotayla ilgili ek verilerin bir özellik çantadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-166">[RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="eea9f-167"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*>, uygulamanın hangi yolun eşleştiğini temel alarak kararlar verebilmesi için durum verilerinin her bir rota ile ilişkilendirilmesini desteklemek amacıyla sağlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-167"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> are provided to support associating state data with each route so that the app can make decisions based on which route matched.</span></span> <span data-ttu-id="eea9f-168">Bu değerler, geliştirici tarafından tanımlanır ve yönlendirme davranışını herhangi bir **şekilde etkilemez.**</span><span class="sxs-lookup"><span data-stu-id="eea9f-168">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="eea9f-169">Ayrıca, [RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) içinde bulunan değerler her türlü türden olabilir. Bu, [veri](xref:Microsoft.AspNetCore.Routing.RouteData.Values)dizeleri arasında dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-169">Additionally, values stashed in [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) can be of any type, in contrast to [RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values), which must be convertible to and from strings.</span></span>

<span data-ttu-id="eea9f-170">[RouteData. yönlendiriciler](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) , isteği başarıyla eşleştirirken geçen yolların bir listesidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-170">[RouteData.Routers](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="eea9f-171">Yollar bir diğerinin içinde iç içe olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-171">Routes can be nested inside of one another.</span></span> <span data-ttu-id="eea9f-172"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers> özelliği, bir eşleşme ile sonuçlanan yolların mantıksal ağacı aracılığıyla yolu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-172">The <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="eea9f-173">Genellikle, <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> ilk öğe yol koleksiyonudur ve URL oluşturma için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-173">Generally, the first item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route collection and should be used for URL generation.</span></span> <span data-ttu-id="eea9f-174"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers> son öğe, eşleşen yol işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-174">The last item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route handler that matched.</span></span>

<a name="lg"></a>

### <a name="url-generation-with-linkgenerator"></a><span data-ttu-id="eea9f-175">LinkGenerator ile URL oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-175">URL generation with LinkGenerator</span></span>

<span data-ttu-id="eea9f-176">URL oluşturma, yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturabileceği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-176">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="eea9f-177">Bu, uç noktalarınız ve bunlara erişen URL 'Ler arasında mantıksal bir ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-177">This allows for a logical separation between your endpoints and the URLs that access them.</span></span>

<span data-ttu-id="eea9f-178">Uç nokta yönlendirme bağlantı Oluşturucu API 'sini (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>) içerir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-178">Endpoint routing includes the Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>).</span></span> <span data-ttu-id="eea9f-179"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>, DI 'dan alınabilecek bir tek hizmettir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-179"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is a singleton service that can be retrieved from DI.</span></span> <span data-ttu-id="eea9f-180">API, yürütülen bir istek bağlamı dışında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-180">The API can be used outside of the context of an executing request.</span></span> <span data-ttu-id="eea9f-181">MVC 'nin [Etiket Yardımcıları](xref:mvc/views/tag-helpers/intro), HTML Yardımcıları ve [eylem sonuçları](xref:mvc/controllers/actions)gibi <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>kullanan <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> ve senaryoları, bağlantı oluşturma yetenekleri sağlamak için bağlantı oluşturucuyu kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-181">MVC's <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> and scenarios that rely on <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>, such as [Tag Helpers](xref:mvc/views/tag-helpers/intro), HTML Helpers, and [Action Results](xref:mvc/controllers/actions), use the link generator to provide link generating capabilities.</span></span>

<span data-ttu-id="eea9f-182">Bağlantı Oluşturucu, bir *Adres* ve *Adres şemaları*kavramıyla desteklenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-182">The link generator is backed by the concept of an *address* and *address schemes*.</span></span> <span data-ttu-id="eea9f-183">Adres şeması, bağlantı oluşturma için göz önünde bulundurmanız gereken uç noktaları belirlemenin bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-183">An address scheme is a way of determining the endpoints that should be considered for link generation.</span></span> <span data-ttu-id="eea9f-184">Örneğin, çok sayıda kullanıcının yol adı ve yol değerleri senaryoları, MVC/Razor Pages tarafından tanıdık bir adres düzeni olarak uygulanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-184">For example, the route name and route values scenarios many users are familiar with from MVC/Razor Pages are implemented as an address scheme.</span></span>

<span data-ttu-id="eea9f-185">Bağlantı Oluşturucu, aşağıdaki genişletme yöntemleri aracılığıyla MVC/Razor Pages eylemlerine ve sayfalarına bağlanabilir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-185">The link generator can link to MVC/Razor Pages actions and pages via the following extension methods:</span></span>

* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetPathByAction*>
* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetUriByAction*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetPathByPage*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetUriByPage*>

<span data-ttu-id="eea9f-186">Bu yöntemlerin aşırı yüklemesi `HttpContext`içeren bağımsız değişkenleri kabul eder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-186">An overload of these methods accepts arguments that include the `HttpContext`.</span></span> <span data-ttu-id="eea9f-187">Bu yöntemler `Url.Action` ve `Url.Page` işlevsel olarak eşdeğerdir ancak ek esneklik ve seçenekler sunar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-187">These methods are functionally equivalent to `Url.Action` and `Url.Page` but offer additional flexibility and options.</span></span>

<span data-ttu-id="eea9f-188">`GetPath*` yöntemleri, `Url.Action` ve `Url.Page`, mutlak bir yol içeren bir URI oluşturmak için en çok benzerdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-188">The `GetPath*` methods are most similar to `Url.Action` and `Url.Page` in that they generate a URI containing an absolute path.</span></span> <span data-ttu-id="eea9f-189">`GetUri*` Yöntemler her zaman bir düzen ve konak içeren mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-189">The `GetUri*` methods always generate an absolute URI containing a scheme and host.</span></span> <span data-ttu-id="eea9f-190">Bir `HttpContext` kabul eden yöntemler, yürütülmekte olan istek bağlamında bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-190">The methods that accept an `HttpContext` generate a URI in the context of the executing request.</span></span> <span data-ttu-id="eea9f-191">Ortam yolu değerleri, URL taban yolu, şeması ve yürütülen istekten ana bilgisayar, geçersiz kılınmadıkça kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-191">The ambient route values, URL base path, scheme, and host from the executing request are used unless overridden.</span></span>

<span data-ttu-id="eea9f-192"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> bir adresle çağrılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-192"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is called with an address.</span></span> <span data-ttu-id="eea9f-193">URI oluşturma iki adımda gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-193">Generating a URI occurs in two steps:</span></span>

1. <span data-ttu-id="eea9f-194">Adres, adresle eşleşen bir uç nokta listesine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-194">An address is bound to a list of endpoints that match the address.</span></span>
1. <span data-ttu-id="eea9f-195">Her uç noktanın `RoutePattern`, sağlanan değerlerle eşleşen bir rota deseninin bulunana kadar değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-195">Each endpoint's `RoutePattern` is evaluated until a route pattern that matches the supplied values is found.</span></span> <span data-ttu-id="eea9f-196">Elde edilen çıktı, bağlantı oluşturucuya sağlanan diğer URI parçalarıyla birleştirilir ve döndürülür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-196">The resulting output is combined with the other URI parts supplied to the link generator and returned.</span></span>

<span data-ttu-id="eea9f-197"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> tarafından sunulan yöntemler, herhangi bir tür adresin standart bağlantı oluşturma özelliklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-197">The methods provided by <xref:Microsoft.AspNetCore.Routing.LinkGenerator> support standard link generation capabilities for any type of address.</span></span> <span data-ttu-id="eea9f-198">Bağlantı oluşturucuyu kullanmanın en kolay yolu, belirli bir adres türü için işlem gerçekleştiren genişletme yöntemlerine yöneliktir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-198">The most convenient way to use the link generator is through extension methods that perform operations for a specific address type.</span></span>

| <span data-ttu-id="eea9f-199">Genişletme yöntemi</span><span class="sxs-lookup"><span data-stu-id="eea9f-199">Extension Method</span></span> | <span data-ttu-id="eea9f-200">Açıklama</span><span class="sxs-lookup"><span data-stu-id="eea9f-200">Description</span></span> |
| ---------------- | ----------- |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*> | <span data-ttu-id="eea9f-201">Belirtilen değerleri temel alarak mutlak bir yola sahip bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-201">Generates a URI with an absolute path based on the provided values.</span></span> |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetUriByAddress*> | <span data-ttu-id="eea9f-202">Belirtilen değerleri temel alarak mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-202">Generates an absolute URI based on the provided values.</span></span>             |

> [!WARNING]
> <span data-ttu-id="eea9f-203"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> yöntemlerinin çağrılmasının aşağıdaki etkilerine dikkat edin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-203">Pay attention to the following implications of calling <xref:Microsoft.AspNetCore.Routing.LinkGenerator> methods:</span></span>
>
> * <span data-ttu-id="eea9f-204">Gelen isteklerin `Host` üstbilgisini doğrulayan bir uygulama yapılandırmasında `GetUri*` uzantısı yöntemlerini dikkatle kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-204">Use `GetUri*` extension methods with caution in an app configuration that doesn't validate the `Host` header of incoming requests.</span></span> <span data-ttu-id="eea9f-205">Gelen isteklerin `Host` üstbilgisi doğrulandıktan sonra, güvenilir olmayan istek girişi, bir görünüm/sayfada URI 'Ler halinde istemciye geri gönderilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-205">If the `Host` header of incoming requests isn't validated, untrusted request input can be sent back to the client in URIs in a view/page.</span></span> <span data-ttu-id="eea9f-206">Tüm üretim uygulamalarının, `Host` üst bilgisini bilinen geçerli değerlere karşı doğrulamak için kendi sunucusunu yapılandırmasını öneririz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-206">We recommend that all production apps configure their server to validate the `Host` header against known valid values.</span></span>
>
> * <span data-ttu-id="eea9f-207">`Map` veya `MapWhen`birlikte ara yazılım ile birlikte <xref:Microsoft.AspNetCore.Routing.LinkGenerator> kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-207">Use <xref:Microsoft.AspNetCore.Routing.LinkGenerator> with caution in middleware in combination with `Map` or `MapWhen`.</span></span> <span data-ttu-id="eea9f-208">`Map*`, yürütülen isteğin temel yolunu değiştirir ve bu da bağlantı oluşturma çıktısını etkiler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-208">`Map*` changes the base path of the executing request, which affects the output of link generation.</span></span> <span data-ttu-id="eea9f-209">Tüm <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'Leri temel yol belirtmeye izin verir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-209">All of the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> APIs allow specifying a base path.</span></span> <span data-ttu-id="eea9f-210">`Map*`bağlantı oluşturmada etkilerini geri almak için her zaman boş bir temel yol belirtin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-210">Always specify an empty base path to undo `Map*`'s affect on link generation.</span></span>

## <a name="endpoint-routing"></a><span data-ttu-id="eea9f-211">Uç nokta yönlendirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-211">Endpoint routing</span></span>

* <span data-ttu-id="eea9f-212">Bir rota uç noktasında, bir şablon, meta veri ve uç noktanın yanıtına hizmet veren bir istek temsilcisi vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-212">A route endpoint has a template, metadata, and a request delegate that serves the endpoint's response.</span></span> <span data-ttu-id="eea9f-213">Meta veriler, her uç noktaya eklenen ilkelere ve yapılandırmaya göre çapraz kesme sorunları uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-213">The metadata is used to implement cross-cutting concerns based on policies and configuration attached to each endpoint.</span></span> <span data-ttu-id="eea9f-214">Örneğin, bir yetkilendirme ara yazılımı, bir [Yetkilendirme İlkesi](xref:security/authorization/policies#applying-policies-to-mvc-controllers)için bitiş noktasının meta veri koleksiyonunu sorgulanamıyor.</span><span class="sxs-lookup"><span data-stu-id="eea9f-214">For example, an authorization middleware can interrogate the endpoint's metadata collection for an [authorization policy](xref:security/authorization/policies#applying-policies-to-mvc-controllers).</span></span>
* <span data-ttu-id="eea9f-215">Uç nokta yönlendirme iki genişletme yöntemi kullanarak ara yazılım ile tümleşir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-215">Endpoint routing integrates with middleware using two extension methods:</span></span>
  * <span data-ttu-id="eea9f-216">[Userouting](xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*) , ara yazılım ardışık düzenine yol eşleştirmeyi ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-216">[UseRouting](xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*) adds route matching to the middleware pipeline.</span></span> <span data-ttu-id="eea9f-217">Yetkilendirme, uç nokta yürütme vb. gibi rota kullanan herhangi bir ara yazılım öncesinde gelmelidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-217">It must come before any route-aware middleware such as authorization, endpoint execution, etc.</span></span>
  * <span data-ttu-id="eea9f-218">[Useendpoints](xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*) , uç nokta yürütmeyi ara yazılım ardışık düzenine ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-218">[UseEndpoints](xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*) adds endpoint execution to the middleware pipeline.</span></span> <span data-ttu-id="eea9f-219">Uç noktanın yanıtını sunan istek temsilcisini çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-219">It runs the request delegate that serves the endpoint's response.</span></span>
  <span data-ttu-id="eea9f-220">`UseEndpoints` Ayrıca, uygulama tarafından eşleştirilebileceği ve yürütülebileceği yol bitiş noktaları yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-220">`UseEndpoints` is also where route endpoints are configured that can be matched and executed by the app.</span></span> <span data-ttu-id="eea9f-221">Örneğin, <xref:Microsoft.AspNetCore.Builder.RazorPagesEndpointRouteBuilderExtensions.MapRazorPages*>, <xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllers*>, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*>ve <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-221">For example, <xref:Microsoft.AspNetCore.Builder.RazorPagesEndpointRouteBuilderExtensions.MapRazorPages*>, <xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllers*>, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*>, and <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*>.</span></span>
* <span data-ttu-id="eea9f-222">Uygulamalar, yollarını yapılandırmak için ASP.NET Core yardımcı yöntemlerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-222">Apps use ASP.NET Core's helper methods to configure their routes.</span></span> <span data-ttu-id="eea9f-223">ASP.NET Core çerçeveler <xref:Microsoft.AspNetCore.Builder.RazorPagesEndpointRouteBuilderExtensions.MapRazorPages*>,, <xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllers*> ve `MapHub<THub>`gibi yardımcı yöntemler sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-223">ASP.NET Core frameworks provide helper methods like <xref:Microsoft.AspNetCore.Builder.RazorPagesEndpointRouteBuilderExtensions.MapRazorPages*>,, <xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllers*> and `MapHub<THub>`.</span></span> <span data-ttu-id="eea9f-224">Kendi özel yol uç noktalarınızı yapılandırmaya yönelik yardımcı yöntemler de vardır: <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*>, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*>ve [Mapverb](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions).</span><span class="sxs-lookup"><span data-stu-id="eea9f-224">There are also helper methods for configuring your own custom route endpoints: <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*>, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*>, and [MapVerb](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions).</span></span> 
* <span data-ttu-id="eea9f-225">Uç nokta yönlendirme, bir uygulama başladıktan sonra değişen uç noktaları da destekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-225">Endpoint routing also supports endpoints changing after an application has started up.</span></span> <span data-ttu-id="eea9f-226">Bunu uygulamanızda veya ASP.NET Core çerçevesinde desteklemek için, özel bir <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> oluşturulmalı ve kayıtlı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-226">To support this in your app or ASP.NET Core framework, a custom <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> must be created and registered.</span></span> <span data-ttu-id="eea9f-227">Bu gelişmiş bir özelliktir ve genellikle gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-227">This is an advanced feature, and usually not needed.</span></span> <span data-ttu-id="eea9f-228">Uç noktalar genellikle başlangıçta yapılandırılır ve uygulamanın ömrü boyunca statiktir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-228">Endpoints are typically configured at startup and are static for the lifetime of the app.</span></span> <span data-ttu-id="eea9f-229">Başlangıç sırasında bir dosyadan veya veritabanından rota yapılandırması yükleme dinamik değil.</span><span class="sxs-lookup"><span data-stu-id="eea9f-229">Loading route configuration from a file or database at startup is not dynamic.</span></span>

<span data-ttu-id="eea9f-230">Aşağıdaki kod, uç nokta yönlendirmenin temel bir örneğini göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-230">The following code shows a basic example of endpoint routing:</span></span>

[!code-csharp[](routing/samples/3.x/Startup.cs?name=snippet)]

<span data-ttu-id="eea9f-231">Uç nokta yönlendirme hakkında daha fazla bilgi için bu belgede [URL eşleştirmesi](#url-matching) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-231">See [URL matching](#url-matching) in this document for more information on endpoint routing.</span></span>

## <a name="endpoint-routing-differences-from-earlier-versions-of-routing"></a><span data-ttu-id="eea9f-232">Önceki yönlendirme sürümlerinden uç nokta yönlendirme farkları</span><span class="sxs-lookup"><span data-stu-id="eea9f-232">Endpoint routing differences from earlier versions of routing</span></span>

<span data-ttu-id="eea9f-233">ASP.NET Core 2,2 ' den önceki yönlendirmenin uç nokta yönlendirme ve sürümleri arasında birkaç fark vardır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-233">A few differences exist between endpoint routing and versions of routing earlier than in ASP.NET Core 2.2:</span></span>

* <span data-ttu-id="eea9f-234">Uç nokta yönlendirme sistemi, <xref:Microsoft.AspNetCore.Routing.Route>devralma dahil <xref:Microsoft.AspNetCore.Routing.IRouter>tabanlı genişletilebilirliği desteklemez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-234">The endpoint routing system doesn't support <xref:Microsoft.AspNetCore.Routing.IRouter>-based extensibility, including inheriting from <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>

* <span data-ttu-id="eea9f-235">Uç nokta yönlendirme [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim)desteklemez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-235">Endpoint routing doesn't support [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim).</span></span> <span data-ttu-id="eea9f-236">Uyumluluk Shim 'yi kullanmaya devam etmek için 2,1 [Uyumluluk sürümünü](xref:mvc/compatibility-version) (`.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)`) kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-236">Use the 2.1 [compatibility version](xref:mvc/compatibility-version) (`.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)`) to continue using the compatibility shim.</span></span>

* <span data-ttu-id="eea9f-237">Uç nokta yönlendirmesinde, geleneksel yollar kullanılırken oluşturulan URI 'lerin büyük küçük harfleri için farklı davranış vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-237">Endpoint Routing has different behavior for the casing of generated URIs when using conventional routes.</span></span>

  <span data-ttu-id="eea9f-238">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="eea9f-238">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="eea9f-239">Aşağıdaki rotayı kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="eea9f-239">Suppose you generate a link to an action using the following route:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "blog", new { id = 17, });
  ```

  <span data-ttu-id="eea9f-240"><xref:Microsoft.AspNetCore.Routing.IRouter>tabanlı yönlendirme ile bu kod, `/blog/ReadPost/17`URI 'sini oluşturur ve bu da, belirtilen yol değerinin büyük küçük harflerini üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-240">With <xref:Microsoft.AspNetCore.Routing.IRouter>-based routing, this code generates a URI of `/blog/ReadPost/17`, which respects the casing of the provided route value.</span></span> <span data-ttu-id="eea9f-241">ASP.NET Core 2,2 veya sonraki bir sürümde uç nokta yönlendirme `/Blog/ReadPost/17` ("blog" büyük harfli) oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-241">Endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` ("Blog" is capitalized).</span></span> <span data-ttu-id="eea9f-242">Endpoint Routing, bu davranışı küresel olarak özelleştirmek veya URL eşleme için farklı kurallar uygulamak üzere kullanılabilecek `IOutboundParameterTransformer` arabirimini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-242">Endpoint routing provides the `IOutboundParameterTransformer` interface that can be used to customize this behavior globally or to apply different conventions for mapping URLs.</span></span>

  <span data-ttu-id="eea9f-243">Daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="eea9f-243">For more information, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

* <span data-ttu-id="eea9f-244">Standart yollarla MVC/Razor Pages tarafından kullanılan bağlantı oluşturma, mevcut olmayan bir denetleyiciye/eyleme veya sayfaya bağlantı kurmaya çalışırken farklı şekilde davranır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-244">Link Generation used by MVC/Razor Pages with conventional routes behaves differently when attempting to link to an controller/action or page that doesn't exist.</span></span>

  <span data-ttu-id="eea9f-245">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="eea9f-245">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="eea9f-246">Aşağıdaki ile varsayılan şablonu kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="eea9f-246">Suppose you generate a link to an action using the default template with the following:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "Blog", new { id = 17, });
  ```

  <span data-ttu-id="eea9f-247">`IRouter`tabanlı yönlendirme sayesinde, `BlogController` var olmasa veya bir `ReadPost` Action yöntemine sahip olmasa bile sonuç daima `/Blog/ReadPost/17`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-247">With `IRouter`-based routing, the result is always `/Blog/ReadPost/17`, even if the `BlogController` doesn't exist or doesn't have a `ReadPost` action method.</span></span> <span data-ttu-id="eea9f-248">Beklenen şekilde, işlem yöntemi varsa ASP.NET Core 2,2 veya sonraki bir sürümde uç nokta yönlendirmesi `/Blog/ReadPost/17` üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-248">As expected, endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` if the action method exists.</span></span> <span data-ttu-id="eea9f-249">*Ancak, uç nokta yönlendirme, eylem yoksa boş bir dize oluşturur.*</span><span class="sxs-lookup"><span data-stu-id="eea9f-249">*However, endpoint routing produces an empty string if the action doesn't exist.*</span></span> <span data-ttu-id="eea9f-250">Kavramsal olarak, uç nokta yönlendirme, eylem mevcut değilse uç noktanın var olduğunu varsaymaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-250">Conceptually, endpoint routing doesn't assume that the endpoint exists if the action doesn't exist.</span></span>

* <span data-ttu-id="eea9f-251">Bağlantı oluşturma *çevresel değeri ınvalidation algoritması* , uç nokta yönlendirme ile kullanıldığında farklı davranır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-251">The link generation *ambient value invalidation algorithm* behaves differently when used with endpoint routing.</span></span>

  <span data-ttu-id="eea9f-252">*Çevresel değer ınvalidation* , şu anda yürütülmekte olan isteğin (çevresel değerler) hangi rota değerlerinin bağlantı oluşturma işlemlerinde kullanılabileceğini belirleyen algoritmadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-252">*Ambient value invalidation* is the algorithm that decides which route values from the currently executing request (the ambient values) can be used in link generation operations.</span></span> <span data-ttu-id="eea9f-253">Geleneksel yönlendirme, farklı bir eyleme bağlanılırken ek yol değerlerini her zaman geçersiz kıldı.</span><span class="sxs-lookup"><span data-stu-id="eea9f-253">Conventional routing always invalidated extra route values when linking to a different action.</span></span> <span data-ttu-id="eea9f-254">ASP.NET Core 2,2 sürümünden önce öznitelik yönlendirmesinde bu davranış yoktu.</span><span class="sxs-lookup"><span data-stu-id="eea9f-254">Attribute routing didn't have this behavior prior to the release of ASP.NET Core 2.2.</span></span> <span data-ttu-id="eea9f-255">ASP.NET Core 'nin önceki sürümlerinde, aynı yol parametresi adlarını kullanan başka bir eyleme bağlantılar, bağlantı oluşturma hatalarıyla sonuçlandı.</span><span class="sxs-lookup"><span data-stu-id="eea9f-255">In earlier versions of ASP.NET Core, links to another action that use the same route parameter names resulted in link generation errors.</span></span> <span data-ttu-id="eea9f-256">ASP.NET Core 2,2 veya sonraki sürümlerde, yönlendirme biçimlerinin her ikisi de başka bir eyleme bağlanırken değerleri geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-256">In ASP.NET Core 2.2 or later, both forms of routing invalidate values when linking to another action.</span></span>

  <span data-ttu-id="eea9f-257">ASP.NET Core 2,1 veya önceki sürümlerde aşağıdaki örneği göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-257">Consider the following example in ASP.NET Core 2.1 or earlier.</span></span> <span data-ttu-id="eea9f-258">Başka bir eyleme (veya başka bir sayfaya) bağlanırken, yol değerleri istenmeyen yollarla yeniden kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-258">When linking to another action (or another page), route values can be reused in undesirable ways.</span></span>

  <span data-ttu-id="eea9f-259">*/Pages/Store/Product.exe*:</span><span class="sxs-lookup"><span data-stu-id="eea9f-259">In */Pages/Store/Product.cshtml*:</span></span>

  ```cshtml
  @page "{id}"
  @Url.Page("/Login")
  ```

  <span data-ttu-id="eea9f-260">*/Pages/Login.exe*içinde:</span><span class="sxs-lookup"><span data-stu-id="eea9f-260">In */Pages/Login.cshtml*:</span></span>

  ```cshtml
  @page "{id?}"
  ```

  <span data-ttu-id="eea9f-261">URI, ASP.NET Core 2,1 veya önceki sürümlerde `/Store/Product/18`, `@Url.Page("/Login")` tarafından Store/Info sayfasında oluşturulan bağlantı `/Login/18`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-261">If the URI is `/Store/Product/18` in ASP.NET Core 2.1 or earlier, the link generated on the Store/Info page by `@Url.Page("/Login")` is `/Login/18`.</span></span> <span data-ttu-id="eea9f-262">18 ' in `id` değeri, bağlantı hedefi uygulamanın tamamen farklı bir parçası olsa bile yeniden kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-262">The `id` value of 18 is reused, even though the link destination is different part of the app entirely.</span></span> <span data-ttu-id="eea9f-263">`/Login` sayfasının bağlamındaki `id` yol değeri büyük olasılıkla bir depolama ürün KIMLIĞI değeri değil, bir kullanıcı KIMLIĞI değeridir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-263">The `id` route value in the context of the `/Login` page is probably a user ID value, not a store product ID value.</span></span>

  <span data-ttu-id="eea9f-264">ASP.NET Core 2,2 veya sonraki bir sürümü ile Endpoint Routing, sonuç `/Login`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-264">In endpoint routing with ASP.NET Core 2.2 or later, the result is `/Login`.</span></span> <span data-ttu-id="eea9f-265">Bağlantılı hedef farklı bir eylem veya sayfa olduğunda çevresel değerler yeniden kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-265">Ambient values aren't reused when the linked destination is a different action or page.</span></span>

* <span data-ttu-id="eea9f-266">Gidiş dönüşü yol parametresi sözdizimi: çift yıldız (`**`) catch-all parametre sözdizimi kullanılırken eğik çizgiler kodlanmaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-266">Round-tripping route parameter syntax: Forward slashes aren't encoded when using a double-asterisk (`**`) catch-all parameter syntax.</span></span>

  <span data-ttu-id="eea9f-267">Bağlantı oluşturma sırasında, yönlendirme sistemi, eğik çizgiler dışında bir çift yıldız (`**`) catch-all parametresinde (örneğin, `{**myparametername}`) yakalanan değeri kodlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-267">During link generation, the routing system encodes the value captured in a double-asterisk (`**`) catch-all parameter (for example, `{**myparametername}`) except the forward slashes.</span></span> <span data-ttu-id="eea9f-268">Çift yıldız yakalama, ASP.NET Core 2,2 veya sonraki sürümlerde `IRouter`tabanlı yönlendirme ile desteklenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-268">The double-asterisk catch-all is supported with `IRouter`-based routing in ASP.NET Core 2.2 or later.</span></span>

  <span data-ttu-id="eea9f-269">ASP.NET Core (`{*myparametername}`) önceki sürümlerindeki tek yıldız catch-all parametre sözdizimi desteklenmeye devam eder ve eğik çizgi kodlandı.</span><span class="sxs-lookup"><span data-stu-id="eea9f-269">The single asterisk catch-all parameter syntax in prior versions of ASP.NET Core (`{*myparametername}`) remains supported, and forward slashes are encoded.</span></span>

  | <span data-ttu-id="eea9f-270">Yol</span><span class="sxs-lookup"><span data-stu-id="eea9f-270">Route</span></span>              | <span data-ttu-id="eea9f-271">İle oluşturulan bağlantı</span><span class="sxs-lookup"><span data-stu-id="eea9f-271">Link generated with</span></span><br>`Url.Action(new { category = "admin/products" })`&hellip; |
  | ------------------ | --------------------------------------------------------------------- |
  | `/search/{*page}`  | <span data-ttu-id="eea9f-272">`/search/admin%2Fproducts` (eğik çizgi kodlanmış)</span><span class="sxs-lookup"><span data-stu-id="eea9f-272">`/search/admin%2Fproducts` (the forward slash is encoded)</span></span>             |
  | `/search/{**page}` | `/search/admin/products`                                              |

### <a name="middleware-example"></a><span data-ttu-id="eea9f-273">Ara yazılım örneği</span><span class="sxs-lookup"><span data-stu-id="eea9f-273">Middleware example</span></span>

<span data-ttu-id="eea9f-274">Aşağıdaki örnekte, bir ara yazılım, mağaza ürünlerini listeleyen bir eylem yöntemine bağlantı oluşturmak için <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'sini kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-274">In the following example, a middleware uses the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API to create link to an action method that lists store products.</span></span> <span data-ttu-id="eea9f-275">Ekleme tarafından bir sınıfa olan ve `GenerateLink` çağıran bağlantı Oluşturucuyu kullanmak, bir uygulamadaki herhangi bir sınıfta kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-275">Using the link generator by injecting it into a class and calling `GenerateLink` is available to any class in an app.</span></span>

```csharp
using Microsoft.AspNetCore.Routing;

public class ProductsLinkMiddleware
{
    private readonly LinkGenerator _linkGenerator;

    public ProductsLinkMiddleware(RequestDelegate next, LinkGenerator linkGenerator)
    {
        _linkGenerator = linkGenerator;
    }

    public async Task InvokeAsync(HttpContext httpContext)
    {
        var url = _linkGenerator.GetPathByAction("ListProducts", "Store");

        httpContext.Response.ContentType = "text/plain";

        await httpContext.Response.WriteAsync($"Go to {url} to see our products.");
    }
}
```

### <a name="create-routes"></a><span data-ttu-id="eea9f-276">Yolları oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-276">Create routes</span></span>

<span data-ttu-id="eea9f-277">Çoğu uygulama, <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>tanımlanan benzer uzantı yöntemlerinden birini veya <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> çağırarak yollar oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-277">Most apps create routes by calling <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> or one of the similar extension methods defined on <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>.</span></span> <span data-ttu-id="eea9f-278"><xref:Microsoft.AspNetCore.Routing.IRouteBuilder> uzantısı yöntemlerinden herhangi biri bir <xref:Microsoft.AspNetCore.Routing.Route> örneği oluşturur ve bunu yol koleksiyonuna ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-278">Any of the <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> extension methods create an instance of <xref:Microsoft.AspNetCore.Routing.Route> and add it to the route collection.</span></span>

<span data-ttu-id="eea9f-279"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> yol işleyicisi parametresini kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-279"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> doesn't accept a route handler parameter.</span></span> <span data-ttu-id="eea9f-280"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>, yalnızca <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>tarafından işlenen rotaları ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-280"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> only adds routes that are handled by the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span> <span data-ttu-id="eea9f-281">MVC 'de yönlendirme hakkında daha fazla bilgi için bkz. <xref:mvc/controllers/routing>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-281">To learn more about routing in MVC, see <xref:mvc/controllers/routing>.</span></span>

<span data-ttu-id="eea9f-282">Aşağıdaki kod örneği, tipik bir ASP.NET Core MVC yol tanımı tarafından kullanılan <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> çağrısının bir örneğidir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-282">The following code example is an example of a <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> call used by a typical ASP.NET Core MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="eea9f-283">Bu şablon bir URL yoluyla eşleşir ve yol değerlerini ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-283">This template matches a URL path and extracts the route values.</span></span> <span data-ttu-id="eea9f-284">Örneğin, yol `/Products/Details/17` şu yol değerlerini üretir: `{ controller = Products, action = Details, id = 17 }`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-284">For example, the path `/Products/Details/17` generates the following route values: `{ controller = Products, action = Details, id = 17 }`.</span></span>

<span data-ttu-id="eea9f-285">Rota değerleri, URL yolunun kesimlere bölünüyor ve rota şablonundaki *yol parametresi* adı ile her bir segmenti eşleştirerek belirlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-285">Route values are determined by splitting the URL path into segments and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="eea9f-286">Yol parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-286">Route parameters are named.</span></span> <span data-ttu-id="eea9f-287">Küme ayraçları içinde parametre adı eklenerek tanımlanan parametreler `{ ... }`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-287">The parameters defined by enclosing the parameter name in braces `{ ... }`.</span></span>

<span data-ttu-id="eea9f-288">Yukarıdaki şablon, URL yolu `/` de eşleştirebilir ve `{ controller = Home, action = Index }`değerler üretecektir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-288">The preceding template could also match the URL path `/` and produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="eea9f-289">Bu, `{controller}` ve `{action}` yol parametrelerinin varsayılan değerleri olduğu ve `id` Route parametresinin isteğe bağlı olduğu için oluşur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-289">This occurs because the `{controller}` and `{action}` route parameters have default values and the `id` route parameter is optional.</span></span> <span data-ttu-id="eea9f-290">Eşittir işareti (`=`), yol parametresi adı parametre için varsayılan bir değer tanımladıktan sonra bir değer izler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-290">An equals sign (`=`) followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="eea9f-291">Yol parametre adından sonra bir soru işareti (`?`) isteğe bağlı bir parametre tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-291">A question mark (`?`) after the route parameter name defines an optional parameter.</span></span>

<span data-ttu-id="eea9f-292">Yol parametreleri varsayılan değer ile *her zaman* rota değeri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-292">Route parameters with a default value *always* produce a route value when the route matches.</span></span> <span data-ttu-id="eea9f-293">Karşılık gelen bir URL yol kesimi yoksa isteğe bağlı parametreler yol değeri oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-293">Optional parameters don't produce a route value if there was no corresponding URL path segment.</span></span> <span data-ttu-id="eea9f-294">Yol şablonu senaryolarının ve sözdiziminin kapsamlı bir açıklaması için [yol şablonu başvurusu](#route-template-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-294">See the [Route template reference](#route-template-reference) section for a thorough description of route template scenarios and syntax.</span></span>

<span data-ttu-id="eea9f-295">Aşağıdaki örnekte, yol parametresi tanımı `{id:int}` `id` Route parametresi için bir [yol kısıtlaması](#route-constraint-reference) tanımlıyor:</span><span class="sxs-lookup"><span data-stu-id="eea9f-295">In the following example, the route parameter definition `{id:int}` defines a [route constraint](#route-constraint-reference) for the `id` route parameter:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="eea9f-296">Bu şablon `/Products/Details/17` gibi bir URL yoluyla eşleşir ancak `/Products/Details/Apples`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-296">This template matches a URL path like `/Products/Details/17` but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="eea9f-297">Yol kısıtlamaları <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> uygular ve yol değerlerini inceleyerek onları doğrular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-297">Route constraints implement <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> and inspect route values to verify them.</span></span> <span data-ttu-id="eea9f-298">Bu örnekte, yol değeri `id` bir tamsayıya dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-298">In this example, the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="eea9f-299">Framework tarafından sunulan yol kısıtlamalarının açıklaması için bkz. [route-Constraint-Reference](#route-constraint-reference) .</span><span class="sxs-lookup"><span data-stu-id="eea9f-299">See [route-constraint-reference](#route-constraint-reference) for an explanation of route constraints provided by the framework.</span></span>

<span data-ttu-id="eea9f-300"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> ek aşırı yüklemeleri `constraints`, `dataTokens`ve `defaults`için değer kabul eder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-300">Additional overloads of <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="eea9f-301">Bu parametrelerin tipik kullanımı, anonim türdeki özellik adlarının yol parametre adlarıyla eşleşen anonim olarak yazılmış bir nesneyi geçirmektir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-301">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="eea9f-302">Aşağıdaki <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> örnekleri eşdeğer yollar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="eea9f-302">The following <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

> [!TIP]
> <span data-ttu-id="eea9f-303">Kısıtlamaları ve Varsayılanları tanımlamaya yönelik satır içi sözdizimi basit yollar için kullanışlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-303">The inline syntax for defining constraints and defaults can be convenient for simple routes.</span></span> <span data-ttu-id="eea9f-304">Ancak, satır içi sözdizimi tarafından desteklenmeyen veri belirteçleri gibi senaryolar vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-304">However, there are scenarios, such as data tokens, that aren't supported by inline syntax.</span></span>

<span data-ttu-id="eea9f-305">Aşağıdaki örnek birkaç ek senaryoyu göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-305">The following example demonstrates a few additional scenarios:</span></span>

```csharp
routes.MapRoute(
    name: "blog",
    template: "Blog/{**article}",
    defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="eea9f-306">Önceki şablon `/Blog/All-About-Routing/Introduction` gibi bir URL yoluyla eşleşir ve `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`değerleri ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-306">The preceding template matches a URL path like `/Blog/All-About-Routing/Introduction` and extracts the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="eea9f-307">`controller` ve `action` için varsayılan yol değerleri, şablonda karşılık gelen hiçbir yol parametresi olmasa bile yol tarafından üretilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-307">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="eea9f-308">Varsayılan değerler yol şablonunda belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-308">Default values can be specified in the route template.</span></span> <span data-ttu-id="eea9f-309">`article` Route parametresi, yol parametre adından önce bir çift yıldız işareti (`**`) görünümüne göre *catch-all* olarak tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-309">The `article` route parameter is defined as a *catch-all* by the appearance of an double asterisk (`**`) before the route parameter name.</span></span> <span data-ttu-id="eea9f-310">Catch-all yol parametreleri, URL yolunun kalanını yakalar ve boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-310">Catch-all route parameters capture the remainder of the URL path and can also match the empty string.</span></span>

<span data-ttu-id="eea9f-311">Aşağıdaki örnek yol kısıtlamalarını ve veri belirteçlerini ekler:</span><span class="sxs-lookup"><span data-stu-id="eea9f-311">The following example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="eea9f-312">Önceki şablon `/en-US/Products/5` gibi bir URL yoluyla eşleşir ve `{ controller = Products, action = Details, id = 5 }` ve veri belirteçleri `{ locale = en-US }`değerleri ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-312">The preceding template matches a URL path like `/en-US/Products/5` and extracts the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Yereller Windows belirteçleri](routing/_static/tokens.png)

### <a name="route-class-url-generation"></a><span data-ttu-id="eea9f-314">Yol sınıfı URL 'SI oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-314">Route class URL generation</span></span>

<span data-ttu-id="eea9f-315"><xref:Microsoft.AspNetCore.Routing.Route> sınıfı, yol değerlerini bir kümesini rota şablonuyla birleştirerek URL oluşturmayı da gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-315">The <xref:Microsoft.AspNetCore.Routing.Route> class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="eea9f-316">Bu, URL yolunu eşleştirmenin mantıksal bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-316">This is logically the reverse process of matching the URL path.</span></span>

> [!TIP]
> <span data-ttu-id="eea9f-317">URL oluşturmayı daha iyi anlamak için, oluşturmak istediğiniz URL 'YI düşünün ve sonra bir yönlendirme şablonunun bu URL ile nasıl eşleşeceğini düşünün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-317">To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="eea9f-318">Hangi değerler üretilemidir?</span><span class="sxs-lookup"><span data-stu-id="eea9f-318">What values would be produced?</span></span> <span data-ttu-id="eea9f-319">Bu, URL oluşturma 'nın <xref:Microsoft.AspNetCore.Routing.Route> sınıfında nasıl çalıştığına ilişkin kaba bir eştir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-319">This is the rough equivalent of how URL generation works in the <xref:Microsoft.AspNetCore.Routing.Route> class.</span></span>

<span data-ttu-id="eea9f-320">Aşağıdaki örnek genel ASP.NET Core MVC varsayılan yolunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-320">The following example uses a general ASP.NET Core MVC default route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="eea9f-321">Yol değerleri `{ controller = Products, action = List }`, `/Products/List` URL 'SI oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-321">With the route values `{ controller = Products, action = List }`, the URL `/Products/List` is generated.</span></span> <span data-ttu-id="eea9f-322">Yol değerleri, URL yolunu biçimlendirmek için karşılık gelen yol parametrelerinin yerine kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-322">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="eea9f-323">`id`, isteğe bağlı bir yol parametresi olduğundan, `id`için bir değer olmadan URL başarıyla oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-323">Since `id` is an optional route parameter, the URL is successfully generated without a value for `id`.</span></span>

<span data-ttu-id="eea9f-324">Yol değerleri `{ controller = Home, action = Index }`, `/` URL 'SI oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-324">With the route values `{ controller = Home, action = Index }`, the URL `/` is generated.</span></span> <span data-ttu-id="eea9f-325">Belirtilen yol değerleri varsayılan değerlerle eşleşir ve varsayılan değerlere karşılık gelen segmentler güvenle atlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-325">The provided route values match the default values, and the segments corresponding to the default values are safely omitted.</span></span>

<span data-ttu-id="eea9f-326">Her iki URL de aşağıdaki yol tanımıyla (`/Home/Index` ve `/`) bir gidiş dönüş, URL oluşturmak için kullanılan rota değerlerini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-326">Both URLs generated round-trip with the following route definition (`/Home/Index` and `/`) produce the same route values that were used to generate the URL.</span></span>

> [!NOTE]
> <span data-ttu-id="eea9f-327">ASP.NET Core MVC kullanan bir uygulama doğrudan yönlendirmeye çağırmak yerine URL oluşturmak için <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-327">An app using ASP.NET Core MVC should use <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="eea9f-328">URL oluşturma hakkında daha fazla bilgi için [URL oluşturma başvurusu](#url-generation-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-328">For more information on URL generation, see the [Url generation reference](#url-generation-reference) section.</span></span>

## <a name="use-routing-middleware"></a><span data-ttu-id="eea9f-329">Yönlendirme ara yazılımı kullanma</span><span class="sxs-lookup"><span data-stu-id="eea9f-329">Use Routing Middleware</span></span>

<span data-ttu-id="eea9f-330">Uygulamanın proje dosyasındaki [Microsoft. AspNetCore. app metapackage](xref:fundamentals/metapackage-app) öğesine başvurun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-330">Reference the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in the app's project file.</span></span>

<span data-ttu-id="eea9f-331">`Startup.ConfigureServices`' de hizmet kapsayıcısına yönlendirme ekleyin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-331">Add routing to the service container in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Startup.cs?name=snippet_ConfigureServices&highlight=3)]

<span data-ttu-id="eea9f-332">Yolların `Startup.Configure` yönteminde yapılandırılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-332">Routes must be configured in the `Startup.Configure` method.</span></span> <span data-ttu-id="eea9f-333">Örnek uygulama aşağıdaki API 'Leri kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-333">The sample app uses the following APIs:</span></span>

* <xref:Microsoft.AspNetCore.Routing.RouteBuilder>
* <span data-ttu-id="eea9f-334"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> &ndash; yalnızca HTTP GET istekleriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-334"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> &ndash; Matches only HTTP GET requests.</span></span>
* <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*>

[!code-csharp[](routing/samples/3.x/RoutingSample/Startup.cs?name=snippet_RouteHandler)]

<span data-ttu-id="eea9f-335">Aşağıdaki tabloda verilen URI 'Ler ile ilgili yanıtlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-335">The following table shows the responses with the given URIs.</span></span>

| <span data-ttu-id="eea9f-336">{1&gt;URI&lt;1}</span><span class="sxs-lookup"><span data-stu-id="eea9f-336">URI</span></span>                    | <span data-ttu-id="eea9f-337">Yanıt</span><span class="sxs-lookup"><span data-stu-id="eea9f-337">Response</span></span>                                          |
| ---------------------- | ------------------------------------------------- |
| `/package/create/3`    | <span data-ttu-id="eea9f-338">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-338">Hello!</span></span> <span data-ttu-id="eea9f-339">Rota değerleri: [işlem, oluşturma], [kimlik, 3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-339">Route values: [operation, create], [id, 3]</span></span> |
| `/package/track/-3`    | <span data-ttu-id="eea9f-340">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-340">Hello!</span></span> <span data-ttu-id="eea9f-341">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-341">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/-3/`   | <span data-ttu-id="eea9f-342">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-342">Hello!</span></span> <span data-ttu-id="eea9f-343">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-343">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/`      | <span data-ttu-id="eea9f-344">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-344">The request falls through, no match.</span></span>              |
| `GET /hello/Joe`       | <span data-ttu-id="eea9f-345">Merhaba, ali!</span><span class="sxs-lookup"><span data-stu-id="eea9f-345">Hi, Joe!</span></span>                                          |
| `POST /hello/Joe`      | <span data-ttu-id="eea9f-346">İstek üzerinden geçer, yalnızca HTTP GET ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-346">The request falls through, matches HTTP GET only.</span></span> |
| `GET /hello/Joe/Smith` | <span data-ttu-id="eea9f-347">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-347">The request falls through, no match.</span></span>              |

<span data-ttu-id="eea9f-348">Çerçeve, yollar oluşturmak için bir genişletme yöntemleri kümesi sağlar (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span><span class="sxs-lookup"><span data-stu-id="eea9f-348">The framework provides a set of extension methods for creating routes (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span></span>

* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareVerb*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>

<span data-ttu-id="eea9f-349">`Map[Verb]` yöntemleri, yöntem adındaki HTTP fiili ile rotayı sınırlandırmak için kısıtlamaları kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-349">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="eea9f-350">Örneğin, bkz. <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> ve <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-350">For example, see <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> and <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="eea9f-351">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-351">Route template reference</span></span>

<span data-ttu-id="eea9f-352">Küme ayraçları içindeki belirteçler (`{ ... }`), yol eşleştirildiği takdirde bağlanan *Rota parametrelerini* tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-352">Tokens within curly braces (`{ ... }`) define *route parameters* that are bound if the route is matched.</span></span> <span data-ttu-id="eea9f-353">Bir yol segmentinde birden fazla yol parametresi tanımlayabilirsiniz, ancak bunların bir değişmez değer ile ayrılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-353">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="eea9f-354">Örneğin, `{controller}` ve `{action}`arasında değişmez değer olmadığından `{controller=Home}{action=Index}` geçerli bir yol değil.</span><span class="sxs-lookup"><span data-stu-id="eea9f-354">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="eea9f-355">Bu rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-355">These route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="eea9f-356">Yol parametreleri dışında (örneğin, `{id}`) ve yol ayırıcı `/` URL içindeki metinle eşleşmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-356">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="eea9f-357">Metin eşleştirme, büyük/küçük harfe duyarsızdır ve URL yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-357">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="eea9f-358">Değişmez değer yol parametresi sınırlayıcısından (`{` veya `}`) eşleştirmek için, karakteri (`{{` veya `}}`) tekrarlayarak sınırlayıcıdan kaçış.</span><span class="sxs-lookup"><span data-stu-id="eea9f-358">To match a literal route parameter delimiter (`{` or `}`), escape the delimiter by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="eea9f-359">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-359">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="eea9f-360">Örneğin, `files/{filename}.{ext?}`şablonu düşünün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-360">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="eea9f-361">`filename` ve `ext` değerlerinin her ikisi de varsa, her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-361">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="eea9f-362">URL 'de yalnızca `filename` için bir değer varsa, bitiş dönemi (`.`) isteğe bağlı olduğundan yol eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-362">If only a value for `filename` exists in the URL, the route matches because the trailing period (`.`) is  optional.</span></span> <span data-ttu-id="eea9f-363">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="eea9f-363">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="eea9f-364">URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak bir yıldız işareti (`*`) veya çift yıldız işareti (`**`) kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-364">You can use an asterisk (`*`) or double asterisk (`**`) as a prefix to a route parameter to bind to the rest of the URI.</span></span> <span data-ttu-id="eea9f-365">Bunlara *catch-all* parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-365">These are called a *catch-all* parameters.</span></span> <span data-ttu-id="eea9f-366">Örneğin, `blog/{**slug}` `/blog` ile başlayan ve ondan sonraki bir değere sahip olan `slug` yol değerine atanan tüm URI ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-366">For example, `blog/{**slug}` matches any URI that starts with `/blog` and has any value following it, which is assigned to the `slug` route value.</span></span> <span data-ttu-id="eea9f-367">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-367">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="eea9f-368">Yol ayırıcısı (`/`) karakterleri de dahil olmak üzere bir URL oluşturmak için bir yol kullanıldığında, catch-all parametresi uygun karakterleri çıkar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-368">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator (`/`) characters.</span></span> <span data-ttu-id="eea9f-369">Örneğin yol `foo/{*path}` rota değerleriyle `{ path = "my/path" }` `foo/my%2Fpath`üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-369">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="eea9f-370">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-370">Note the escaped forward slash.</span></span> <span data-ttu-id="eea9f-371">Yol ayırıcı karakterlerini geri döndürmek için `**` Route parametresi önekini kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-371">To round-trip path separator characters, use the `**` route parameter prefix.</span></span> <span data-ttu-id="eea9f-372">`{ path = "my/path" }` ile yol `foo/{**path}` `foo/my/path`üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-372">The route `foo/{**path}` with `{ path = "my/path" }` generates `foo/my/path`.</span></span>

<span data-ttu-id="eea9f-373">Yol parametrelerinin, parametre adından sonra varsayılan değer belirtilerek bir eşittir işareti (`=`) ile ayrıldıktan sonra belirlenen *varsayılan değerleri* olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-373">Route parameters may have *default values* designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="eea9f-374">Örneğin, `Home` `controller`için varsayılan değer olarak `{controller=Home}` tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-374">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="eea9f-375">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-375">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="eea9f-376">Yol parametreleri, `id?`gibi parametre adının sonuna bir soru işareti (`?`) eklenerek isteğe bağlı olarak yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-376">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name, as in `id?`.</span></span> <span data-ttu-id="eea9f-377">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark, varsayılan değere sahip bir yol parametresinin her zaman bir değer ürettiğinden&mdash;isteğe bağlı bir parametre yalnızca istek URL 'SI tarafından bir değer sağlandığında bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-377">The difference between optional values and default route parameters is that a route parameter with a default value always produces a value&mdash;an optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="eea9f-378">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-378">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="eea9f-379">Yol parametre adından sonra bir iki nokta üst üste (`:`) ve kısıtlama adının eklenmesi, bir yol parametresi üzerinde bir *satır içi kısıtlamayı* belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-379">Adding a colon (`:`) and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="eea9f-380">Kısıtlama bağımsız değişkenler gerektiriyorsa, kısıtlama adından sonra parantez (`(...)`) içine alınır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-380">If the constraint requires arguments, they're enclosed in parentheses (`(...)`) after the constraint name.</span></span> <span data-ttu-id="eea9f-381">Birden çok satır içi kısıtlama, başka bir iki nokta (`:`) ve kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-381">Multiple inline constraints can be specified by appending another colon (`:`) and constraint name.</span></span>

<span data-ttu-id="eea9f-382">Kısıtlama adı ve bağımsız değişkenler, URL işlemede kullanılacak bir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> örneği oluşturmak için <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-382">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="eea9f-383">Örneğin, yol şablonu `blog/{article:minlength(10)}`, `10`bağımsız değişkeniyle bir `minlength` kısıtlaması belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-383">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="eea9f-384">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-384">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="eea9f-385">Yol parametrelerinde Ayrıca, bağlantı oluştururken ve URL 'Ler ile eşleşen eylemler ve sayfalar için bir parametrenin değerini dönüştüren parametre dönüştürücüler bulunabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-385">Route parameters may also have parameter transformers, which transform a parameter's value when generating links and matching actions and pages to URLs.</span></span> <span data-ttu-id="eea9f-386">LIKE kısıtlamaları, yol parametre adından sonra iki nokta üst üste (`:`) ve transformatör adı eklenerek, parametre dönüştürücüleri bir rota parametresine eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-386">Like constraints, parameter transformers can be added inline to a route parameter by adding a colon (`:`) and transformer name after the route parameter name.</span></span> <span data-ttu-id="eea9f-387">Örneğin, yol şablonu `blog/{article:slugify}` bir `slugify` transformatörü belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-387">For example, the route template `blog/{article:slugify}` specifies a `slugify` transformer.</span></span> <span data-ttu-id="eea9f-388">Parametre dönüştürücüler hakkında daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="eea9f-388">For more information on parameter transformers, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

<span data-ttu-id="eea9f-389">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-389">The following table demonstrates example route templates and their behavior.</span></span>

| <span data-ttu-id="eea9f-390">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="eea9f-390">Route Template</span></span>                           | <span data-ttu-id="eea9f-391">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="eea9f-391">Example Matching URI</span></span>    | <span data-ttu-id="eea9f-392">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="eea9f-392">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="eea9f-393">Yalnızca `/hello`tek yol ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-393">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="eea9f-394">`Home``Page` eşleşir ve ayarlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-394">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="eea9f-395">`Contact``Page` eşleşir ve ayarlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-395">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="eea9f-396">`Products` denetleyicisi ve `List` eylemiyle eşlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-396">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="eea9f-397">`Products` denetleyicisi ve `Details` eylemine eşlenir (`id` 123 olarak ayarlanır).</span><span class="sxs-lookup"><span data-stu-id="eea9f-397">Maps to the `Products` controller and  `Details` action (`id` set to 123).</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="eea9f-398">`Home` denetleyicisi ve `Index` yöntemiyle eşlenir (`id` yok sayılır).</span><span class="sxs-lookup"><span data-stu-id="eea9f-398">Maps to the `Home` controller and `Index` method (`id` is ignored).</span></span>        |

<span data-ttu-id="eea9f-399">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-399">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="eea9f-400">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-400">Constraints and defaults can also be specified outside the route template.</span></span>

> [!TIP]
> <span data-ttu-id="eea9f-401"><xref:Microsoft.AspNetCore.Routing.Route>, istekleri eşleştirme gibi yerleşik yönlendirme uygulamalarının nasıl yapıldığını görmek için [günlük kaydını](xref:fundamentals/logging/index) etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-401">Enable [Logging](xref:fundamentals/logging/index) to see how the built-in routing implementations, such as <xref:Microsoft.AspNetCore.Routing.Route>, match requests.</span></span>

## <a name="reserved-routing-names"></a><span data-ttu-id="eea9f-402">Ayrılmış yönlendirme adları</span><span class="sxs-lookup"><span data-stu-id="eea9f-402">Reserved routing names</span></span>

<span data-ttu-id="eea9f-403">Aşağıdaki anahtar sözcükler ayrılmış isimlerdir ve yol adları veya parametreler olarak kullanılamaz:</span><span class="sxs-lookup"><span data-stu-id="eea9f-403">The following keywords are reserved names and can't be used as route names or parameters:</span></span>

* `action`
* `area`
* `controller`
* `handler`
* `page`

## <a name="route-constraint-reference"></a><span data-ttu-id="eea9f-404">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-404">Route constraint reference</span></span>

<span data-ttu-id="eea9f-405">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-405">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="eea9f-406">Rota kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda bir Evet/Hayır kararı getirir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-406">Route constraints generally inspect the route value associated via the route template and make a yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="eea9f-407">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-407">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="eea9f-408">Örneğin <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint>, HTTP fiiline bağlı olarak bir isteği kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-408">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="eea9f-409">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-409">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="eea9f-410">**Giriş doğrulaması**için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-410">Don't use constraints for **input validation**.</span></span> <span data-ttu-id="eea9f-411">**Giriş doğrulaması**için kısıtlamalar kullanılıyorsa, doğru bir hata iletisine sahip *400-Bad isteği* yerine *404-* olmayan bir Yanıt ile geçersiz giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-411">If constraints are used for **input validation**, invalid input results in a *404 - Not Found* response instead of a *400 - Bad Request* with an appropriate error message.</span></span> <span data-ttu-id="eea9f-412">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların **belirsizliğini ortadan** kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-412">Route constraints are used to **disambiguate** similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="eea9f-413">Aşağıdaki tabloda örnek yol kısıtlamaları ve bunların beklenen davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-413">The following table demonstrates example route constraints and their expected behavior.</span></span>

| <span data-ttu-id="eea9f-414">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="eea9f-414">constraint</span></span> | <span data-ttu-id="eea9f-415">Örnek</span><span class="sxs-lookup"><span data-stu-id="eea9f-415">Example</span></span> | <span data-ttu-id="eea9f-416">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="eea9f-416">Example Matches</span></span> | <span data-ttu-id="eea9f-417">Notlar</span><span class="sxs-lookup"><span data-stu-id="eea9f-417">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="eea9f-418">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="eea9f-418">`123456789`, `-123456789`</span></span> | <span data-ttu-id="eea9f-419">Herhangi bir tamsayıyla eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-419">Matches any integer</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="eea9f-420">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="eea9f-420">`true`, `FALSE`</span></span> | <span data-ttu-id="eea9f-421">`true` veya `false` eşleşir (büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="eea9f-421">Matches `true` or `false` (case-insensitive)</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="eea9f-422">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="eea9f-422">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="eea9f-423">Geçerli bir `DateTime` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-423">Matches a valid `DateTime` value (in the invariant culture - see warning)</span></span> |
| `decimal` | `{price:decimal}` | <span data-ttu-id="eea9f-424">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="eea9f-424">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="eea9f-425">Geçerli bir `decimal` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-425">Matches a valid `decimal` value (in the invariant culture - see warning)</span></span> |
| `double` | `{weight:double}` | <span data-ttu-id="eea9f-426">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="eea9f-426">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="eea9f-427">Geçerli bir `double` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-427">Matches a valid `double` value (in the invariant culture - see warning)</span></span> |
| `float` | `{weight:float}` | <span data-ttu-id="eea9f-428">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="eea9f-428">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="eea9f-429">Geçerli bir `float` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-429">Matches a valid `float` value (in the invariant culture - see warning)</span></span> |
| `guid` | `{id:guid}` | <span data-ttu-id="eea9f-430">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="eea9f-430">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="eea9f-431">Geçerli bir `Guid` değeriyle eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-431">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="eea9f-432">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="eea9f-432">`123456789`, `-123456789`</span></span> | <span data-ttu-id="eea9f-433">Geçerli bir `long` değeriyle eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-433">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="eea9f-434">Dize en az 4 karakter olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-434">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `Richard` | <span data-ttu-id="eea9f-435">Dize 8 karakterden uzun olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-435">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="eea9f-436">Dize tam olarak 12 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-436">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="eea9f-437">Dize en az 8 ve en fazla 16 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-437">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="eea9f-438">Tamsayı değeri en az 18 olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-438">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="eea9f-439">Tamsayı değeri 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-439">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="eea9f-440">Tamsayı değeri en az 18 olmalı ancak 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-440">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="eea9f-441">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır (`a`-`z`, büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="eea9f-441">String must consist of one or more alphabetical characters (`a`-`z`, case-insensitive)</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="eea9f-442">Dize, normal ifadeyle eşleşmelidir (normal ifade tanımlama hakkında ipuçlarına bakın)</span><span class="sxs-lookup"><span data-stu-id="eea9f-442">String must match the regular expression (see tips about defining a regular expression)</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="eea9f-443">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır</span><span class="sxs-lookup"><span data-stu-id="eea9f-443">Used to enforce that a non-parameter value is present during URL generation</span></span> |

<span data-ttu-id="eea9f-444">Birden çok, iki nokta üst üste sınırlı kısıtlama tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-444">Multiple, colon-delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="eea9f-445">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="eea9f-445">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="eea9f-446">URL 'YI doğrulayan ve bir CLR türüne (örneğin `int` veya `DateTime`) dönüştürülen yol kısıtlamaları her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-446">Route constraints that verify the URL and are converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture.</span></span> <span data-ttu-id="eea9f-447">Bu kısıtlamalar, URL 'nin yerelleştirilemeyen olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-447">These constraints assume that the URL is non-localizable.</span></span> <span data-ttu-id="eea9f-448">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-448">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="eea9f-449">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-449">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="eea9f-450">Örneğin, `float` kısıtlaması yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-450">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="eea9f-451">Normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="eea9f-451">Regular expressions</span></span>

<span data-ttu-id="eea9f-452">ASP.NET Core Framework, normal ifade oluşturucusuna `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-452">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="eea9f-453">Bu üyelerin açıklaması için bkz. <xref:System.Text.RegularExpressions.RegexOptions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-453">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="eea9f-454">Normal ifadeler, C# yönlendirme ve dil tarafından kullanılanlarla aynı sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-454">Regular expressions use delimiters and tokens similar to those used by Routing and the C# language.</span></span> <span data-ttu-id="eea9f-455">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-455">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="eea9f-456">`^\d{3}-\d{2}-\d{4}$` normal ifade ' i yönlendirmede kullanmak için, ifadede, C# kaynak `\` dosyadaki `\\` (çift ters eğik çizgi) karakter olarak dize içinde belirtilen `\` (tek ters eğik çizgi) karakterleri olmalıdır (tam [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string)kullanmadıkça).</span><span class="sxs-lookup"><span data-stu-id="eea9f-456">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in routing, the expression must have the `\` (single backslash) characters provided in the string as `\\` (double backslash) characters in the C# source file in order to escape the `\` string escape character (unless using [verbatim string literals](/dotnet/csharp/language-reference/keywords/string)).</span></span> <span data-ttu-id="eea9f-457">Yönlendirme parametresi sınırlayıcı karakterlerini (`{`, `}`, `[`, `]`) atlamak için, ifadedeki karakterleri çift (`{{`, `}`, `[[`, `]]`).</span><span class="sxs-lookup"><span data-stu-id="eea9f-457">To escape routing parameter delimiter characters (`{`, `}`, `[`, `]`), double the characters in the expression (`{{`, `}`, `[[`, `]]`).</span></span> <span data-ttu-id="eea9f-458">Aşağıdaki tabloda, bir normal ifade ve kaçan sürümü gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-458">The following table shows a regular expression and the escaped version.</span></span>

| <span data-ttu-id="eea9f-459">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="eea9f-459">Regular Expression</span></span>    | <span data-ttu-id="eea9f-460">Kaçan normal Ifade</span><span class="sxs-lookup"><span data-stu-id="eea9f-460">Escaped Regular Expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="eea9f-461">Yönlendirmelerde kullanılan normal ifadeler, genellikle şapka işareti (`^`) karakteriyle başlar ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-461">Regular expressions used in routing often start with the caret (`^`) character and match starting position of the string.</span></span> <span data-ttu-id="eea9f-462">İfadeler genellikle dolar işareti (`$`) karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-462">The expressions often end with the dollar sign (`$`) character and match end of the string.</span></span> <span data-ttu-id="eea9f-463">`^` ve `$` karakterler, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-463">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="eea9f-464">`^` ve `$` karakterleri olmadan normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-464">Without the `^` and `$` characters, the regular expression match any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="eea9f-465">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-465">The following table provides examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="eea9f-466">İfade</span><span class="sxs-lookup"><span data-stu-id="eea9f-466">Expression</span></span>   | <span data-ttu-id="eea9f-467">Dize</span><span class="sxs-lookup"><span data-stu-id="eea9f-467">String</span></span>    | <span data-ttu-id="eea9f-468">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-468">Match</span></span> | <span data-ttu-id="eea9f-469">Yorum</span><span class="sxs-lookup"><span data-stu-id="eea9f-469">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-470">merhaba</span><span class="sxs-lookup"><span data-stu-id="eea9f-470">hello</span></span>     | <span data-ttu-id="eea9f-471">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-471">Yes</span></span>   | <span data-ttu-id="eea9f-472">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-472">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-473">123abc456</span><span class="sxs-lookup"><span data-stu-id="eea9f-473">123abc456</span></span> | <span data-ttu-id="eea9f-474">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-474">Yes</span></span>   | <span data-ttu-id="eea9f-475">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-475">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-476">mz</span><span class="sxs-lookup"><span data-stu-id="eea9f-476">mz</span></span>        | <span data-ttu-id="eea9f-477">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-477">Yes</span></span>   | <span data-ttu-id="eea9f-478">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="eea9f-478">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-479">MZ</span><span class="sxs-lookup"><span data-stu-id="eea9f-479">MZ</span></span>        | <span data-ttu-id="eea9f-480">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-480">Yes</span></span>   | <span data-ttu-id="eea9f-481">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="eea9f-481">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="eea9f-482">merhaba</span><span class="sxs-lookup"><span data-stu-id="eea9f-482">hello</span></span>     | <span data-ttu-id="eea9f-483">Hayır</span><span class="sxs-lookup"><span data-stu-id="eea9f-483">No</span></span>    | <span data-ttu-id="eea9f-484">Yukarıdaki `^` ve `$` bakın</span><span class="sxs-lookup"><span data-stu-id="eea9f-484">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="eea9f-485">123abc456</span><span class="sxs-lookup"><span data-stu-id="eea9f-485">123abc456</span></span> | <span data-ttu-id="eea9f-486">Hayır</span><span class="sxs-lookup"><span data-stu-id="eea9f-486">No</span></span>    | <span data-ttu-id="eea9f-487">Yukarıdaki `^` ve `$` bakın</span><span class="sxs-lookup"><span data-stu-id="eea9f-487">See `^` and `$` above</span></span> |

<span data-ttu-id="eea9f-488">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="eea9f-488">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="eea9f-489">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-489">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="eea9f-490">Örneğin `{action:regex(^(list|get|create)$)}`, yalnızca `action` Route değeri `list`, `get`veya `create`ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-490">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="eea9f-491">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-491">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="eea9f-492">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğüne (bir şablon içinde satır içi değil) geçirilen kısıtlamalar da normal ifadeler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-492">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="custom-route-constraints"></a><span data-ttu-id="eea9f-493">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="eea9f-493">Custom Route Constraints</span></span>

<span data-ttu-id="eea9f-494">Yerleşik yol kısıtlamalarına ek olarak, <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> arabirimi uygulayarak özel yol kısıtlamaları oluşturulabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-494">In addition to the built-in route constraints, custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="eea9f-495"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> arabirimi, kısıtlama karşılandıysanız `true` döndüren `Match`tek bir yöntem içerir ve aksi takdirde `false`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-495">The <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface contains a single method, `Match`, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="eea9f-496">Özel bir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>kullanmak için, yol kısıtlama türü uygulamanın hizmet kapsayıcısında uygulamanın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> kayıtlı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-496">To use a custom <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the app's service container.</span></span> <span data-ttu-id="eea9f-497"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, yol kısıtlama anahtarlarını bu kısıtlamaları doğrulayan <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-497">A <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> is a dictionary that maps route constraint keys to <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> implementations that validate those constraints.</span></span> <span data-ttu-id="eea9f-498">Bir uygulamanın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, bir hizmetin parçası olarak `Startup.ConfigureServices` güncelleştirilebilen olabilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan `services.Configure<RouteOptions>`ile yapılandırma.</span><span class="sxs-lookup"><span data-stu-id="eea9f-498">An app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="eea9f-499">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-499">For example:</span></span>

```csharp
services.AddRouting(options =>
{
    options.ConstraintMap.Add("customName", typeof(MyCustomConstraint));
});
```

<span data-ttu-id="eea9f-500">Kısıtlama daha sonra, kısıtlama türü kaydedilirken belirtilen ad kullanılarak yollara her zamanki şekilde uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-500">The constraint can then be applied to routes in the usual manner, using the name specified when registering the constraint type.</span></span> <span data-ttu-id="eea9f-501">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-501">For example:</span></span>

```csharp
[HttpGet("{id:customName}")]
public ActionResult<string> Get(string id)
```

## <a name="parameter-transformer-reference"></a><span data-ttu-id="eea9f-502">Parametre transformatörü başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-502">Parameter transformer reference</span></span>

<span data-ttu-id="eea9f-503">Parametre dönüştürücüler:</span><span class="sxs-lookup"><span data-stu-id="eea9f-503">Parameter transformers:</span></span>

* <span data-ttu-id="eea9f-504"><xref:Microsoft.AspNetCore.Routing.Route>için bağlantı oluştururken yürütün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-504">Execute when generating a link for a <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>
* <span data-ttu-id="eea9f-505">`Microsoft.AspNetCore.Routing.IOutboundParameterTransformer`uygulayın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-505">Implement `Microsoft.AspNetCore.Routing.IOutboundParameterTransformer`.</span></span>
* <span data-ttu-id="eea9f-506"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>kullanılarak yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-506">Are configured using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>.</span></span>
* <span data-ttu-id="eea9f-507">Parametrenin yol değerini alın ve yeni bir dize değerine dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-507">Take the parameter's route value and transform it to a new string value.</span></span>
* <span data-ttu-id="eea9f-508">Oluşturulan bağlantıda Dönüştürülmüş değerin kullanılmasına neden olacak.</span><span class="sxs-lookup"><span data-stu-id="eea9f-508">Result in using the transformed value in the generated link.</span></span>

<span data-ttu-id="eea9f-509">Örneğin, `Url.Action(new { article = "MyTestArticle" })` ile `blog\{article:slugify}` yol düzeninde özel `slugify` parametresi transformatörü `blog\my-test-article`üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-509">For example, a custom `slugify` parameter transformer in route pattern `blog\{article:slugify}` with `Url.Action(new { article = "MyTestArticle" })` generates `blog\my-test-article`.</span></span>

<span data-ttu-id="eea9f-510">Bir parametre transformatörü bir yol düzeninde kullanmak için, önce `Startup.ConfigureServices`<xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> kullanarak yapılandırın:</span><span class="sxs-lookup"><span data-stu-id="eea9f-510">To use a parameter transformer in a route pattern, configure it first using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddRouting(options =>
{
    // Replace the type and the name used to refer to it with your own
    // IOutboundParameterTransformer implementation
    options.ConstraintMap["slugify"] = typeof(SlugifyParameterTransformer);
});
```

<span data-ttu-id="eea9f-511">Parametre dönüştürücüler, bir uç noktanın çözümlediği URI 'yi dönüştürmek için çerçevesi tarafından kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-511">Parameter transformers are used by the framework to transform the URI where an endpoint resolves.</span></span> <span data-ttu-id="eea9f-512">Örneğin, ASP.NET Core MVC, `area`, `controller`, `action`ve `page`eşleşecek şekilde kullanılan rota değerini dönüştürmek için parametre dönüştürücüler kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-512">For example, ASP.NET Core MVC uses parameter transformers to transform the route value used to match an `area`, `controller`, `action`, and `page`.</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller:slugify=Home}/{action:slugify=Index}/{id?}");
```

<span data-ttu-id="eea9f-513">Önceki yol ile eylem `SubscriptionManagementController.GetAll()`, URI `/subscription-management/get-all`ile eşleştirilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-513">With the preceding route, the action `SubscriptionManagementController.GetAll()` is matched with the URI `/subscription-management/get-all`.</span></span> <span data-ttu-id="eea9f-514">Bir parametre transformatörü bir bağlantı oluşturmak için kullanılan rota değerlerini değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-514">A parameter transformer doesn't change the route values used to generate a link.</span></span> <span data-ttu-id="eea9f-515">Örneğin, `Url.Action("GetAll", "SubscriptionManagement")` çıkışları `/subscription-management/get-all`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-515">For example, `Url.Action("GetAll", "SubscriptionManagement")` outputs `/subscription-management/get-all`.</span></span>

<span data-ttu-id="eea9f-516">ASP.NET Core, oluşturulan yollarla bir parametre dönüştürücüler kullanmak için API kuralları sağlar:</span><span class="sxs-lookup"><span data-stu-id="eea9f-516">ASP.NET Core provides API conventions for using a parameter transformers with generated routes:</span></span>

* <span data-ttu-id="eea9f-517">ASP.NET Core MVC 'nin `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API kuralı vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-517">ASP.NET Core MVC has the `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API convention.</span></span> <span data-ttu-id="eea9f-518">Bu kural, uygulamadaki tüm öznitelik yollarına belirtilen bir parametre transformatörü uygular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-518">This convention applies a specified parameter transformer to all attribute routes in the app.</span></span> <span data-ttu-id="eea9f-519">Parametre transformatörü, öznitelik yol belirteçlerini değiştirildiklerinde dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-519">The parameter transformer transforms attribute route tokens as they are replaced.</span></span> <span data-ttu-id="eea9f-520">Daha fazla bilgi için bkz. [belirteç değişimini özelleştirmek için bir parametre transformatörü kullanma](/aspnet/core/mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span><span class="sxs-lookup"><span data-stu-id="eea9f-520">For more information, see [Use a parameter transformer to customize token replacement](/aspnet/core/mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span></span>
* <span data-ttu-id="eea9f-521">Razor Pages `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API kuralına sahiptir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-521">Razor Pages has the `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API convention.</span></span> <span data-ttu-id="eea9f-522">Bu kural, belirtilen bir parametre transformatörü otomatik olarak keşfedilen Razor Pages uygular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-522">This convention applies a specified parameter transformer to all automatically discovered Razor Pages.</span></span> <span data-ttu-id="eea9f-523">Parametre transformatörü Razor Pages yolların klasör ve dosya adı segmentlerini dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-523">The parameter transformer transforms the folder and file name segments of Razor Pages routes.</span></span> <span data-ttu-id="eea9f-524">Daha fazla bilgi için bkz. [sayfa yollarını özelleştirmek için bir parametre transformatörü kullanma](/aspnet/core/razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span><span class="sxs-lookup"><span data-stu-id="eea9f-524">For more information, see [Use a parameter transformer to customize page routes](/aspnet/core/razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span></span>

## <a name="url-generation-reference"></a><span data-ttu-id="eea9f-525">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-525">URL generation reference</span></span>

<span data-ttu-id="eea9f-526">Aşağıdaki örnek, yol değerlerinin bir sözlüğü ve bir <xref:Microsoft.AspNetCore.Routing.RouteCollection>için bir yol bağlantısının nasıl oluşturulacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-526">The following example shows how to generate a link to a route given a dictionary of route values and a <xref:Microsoft.AspNetCore.Routing.RouteCollection>.</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Startup.cs?name=snippet_Dictionary)]

<span data-ttu-id="eea9f-527">Yukarıdaki örnek sonunda oluşturulan <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> `/package/create/123`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-527">The <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> generated at the end of the preceding sample is `/package/create/123`.</span></span> <span data-ttu-id="eea9f-528">Sözlük, "paket yolunu Izle" şablonunun `package/{operation}/{id}``operation` ve `id` yol değerlerini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-528">The dictionary supplies the `operation` and `id` route values of the "Track Package Route" template, `package/{operation}/{id}`.</span></span> <span data-ttu-id="eea9f-529">Ayrıntılar için, [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümünde veya [örnek uygulamada](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples)örnek koda bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-529">For details, see the sample code in the [Use Routing Middleware](#use-routing-middleware) section or the [sample app](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples).</span></span>

<span data-ttu-id="eea9f-530"><xref:Microsoft.AspNetCore.Routing.VirtualPathContext> oluşturucusunun ikinci parametresi bir *ortam değerleri*koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-530">The second parameter to the <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="eea9f-531">Ortam değerleri, bir geliştiricinin bir istek bağlamı içinde belirtmesi gereken değer sayısını sınırlandırdığından kullanım için uygundur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-531">Ambient values are convenient to use because they limit the number of values a developer must specify within a request context.</span></span> <span data-ttu-id="eea9f-532">Geçerli isteğin geçerli yol değerleri, bağlantı oluşturma için çevresel değerler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-532">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="eea9f-533">ASP.NET Core MVC uygulamasının `HomeController``About` eyleminde,&mdash;ortam değeri `Home` `Index` eyleme bağlamak için denetleyici yolu değerini belirtmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-533">In an ASP.NET Core MVC app's `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action&mdash;the ambient value of `Home` is used.</span></span>

<span data-ttu-id="eea9f-534">Bir parametreyle eşleşmeyen çevresel değerler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-534">Ambient values that don't match a parameter are ignored.</span></span> <span data-ttu-id="eea9f-535">Ayrıca, açıkça sağlanmış bir değer çevresel değeri geçersiz kıldığında çevresel değerler de yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-535">Ambient values are also ignored when an explicitly provided value overrides the ambient value.</span></span> <span data-ttu-id="eea9f-536">Eşleştirme, URL 'de soldan sağa doğru gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-536">Matching occurs from left to right in the URL.</span></span>

<span data-ttu-id="eea9f-537">Açık olarak sağlanmış ancak yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-537">Values explicitly provided but that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="eea9f-538">Aşağıdaki tabloda `{controller}/{action}/{id?}`yol şablonu kullanılırken sonuç gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-538">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="eea9f-539">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="eea9f-539">Ambient Values</span></span>                     | <span data-ttu-id="eea9f-540">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="eea9f-540">Explicit Values</span></span>                        | <span data-ttu-id="eea9f-541">Sonuç</span><span class="sxs-lookup"><span data-stu-id="eea9f-541">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="eea9f-542">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-542">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-543">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-543">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="eea9f-544">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-544">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-545">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-545">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="eea9f-546">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="eea9f-546">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="eea9f-547">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-547">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="eea9f-548">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-548">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-549">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="eea9f-549">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

<span data-ttu-id="eea9f-550">Bir rotada bir parametreye karşılık gelen bir varsayılan değer varsa ve bu değer açıkça sağlanmışsa, varsayılan değerle eşleşmelidir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-550">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
    defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="eea9f-551">Bağlantı oluşturma yalnızca `controller` ve `action` için eşleşen değerler sağlandığında bu yol için bir bağlantı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-551">Link generation only generates a link for this route when the matching values for `controller` and `action` are provided.</span></span>

## <a name="complex-segments"></a><span data-ttu-id="eea9f-552">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="eea9f-552">Complex segments</span></span>

<span data-ttu-id="eea9f-553">Karmaşık segmentler (örneğin `[Route("/x{token}y")]`), sabit değerli olmayan değişmez değerler ile sağdan sola eşleştirilirken işlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-553">Complex segments (for example `[Route("/x{token}y")]`) are processed by matching up literals from right to left in a non-greedy way.</span></span> <span data-ttu-id="eea9f-554">Karmaşık segmentlerin nasıl eşleştirileceği hakkında ayrıntılı bir açıklama için [bu koda](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-554">See [this code](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) for a detailed explanation of how complex segments are matched.</span></span> <span data-ttu-id="eea9f-555">[Kod örneği](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) ASP.NET Core tarafından kullanılmaz, ancak karmaşık segmentler hakkında iyi bir açıklama sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-555">The [code sample](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) is not used by ASP.NET Core, but it provides a good explanation of complex segments.</span></span>
<!-- While that code is no longer used by ASP.NET Core for complex segment matching, it provides a good match to the current algorithm. The [current code](https://github.com/dotnet/AspNetCore/blob/91514c9af7e0f4c44029b51f05a01c6fe4c96e4c/src/Http/Routing/src/Matching/DfaMatcherBuilder.cs#L227-L244) is too abstracted from matching to be useful for understanding complex segment matching.
-->

## <a name="configuring-endpoint-metadata"></a><span data-ttu-id="eea9f-556">Uç nokta meta verilerini yapılandırma</span><span class="sxs-lookup"><span data-stu-id="eea9f-556">Configuring endpoint metadata</span></span>

<span data-ttu-id="eea9f-557">Aşağıdaki bağlantılar, uç nokta meta verilerini yapılandırma hakkında bilgi sağlar:</span><span class="sxs-lookup"><span data-stu-id="eea9f-557">The following links provide information on configuring endpoint metadata:</span></span>

* [<span data-ttu-id="eea9f-558">Uç nokta yönlendirme ile CORS 'yi etkinleştirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-558">Enable Cors with endpoint routing</span></span>](xref:security/cors#enable-cors-with-endpoint-routing)
* <span data-ttu-id="eea9f-559">Özel bir `[MinimumAgeAuthorize]` özniteliği kullanan [ıauthorizationpolicyprovider örneği](https://github.com/dotnet/AspNetCore/tree/release/3.0/src/Security/samples/CustomPolicyProvider)</span><span class="sxs-lookup"><span data-stu-id="eea9f-559">[IAuthorizationPolicyProvider sample](https://github.com/dotnet/AspNetCore/tree/release/3.0/src/Security/samples/CustomPolicyProvider) using a custom `[MinimumAgeAuthorize]` attribute</span></span>
* <span data-ttu-id="eea9f-560">[[Yetkilendir] özniteliğiyle test kimlik doğrulaması](xref:security/authentication/identity#test-identity)</span><span class="sxs-lookup"><span data-stu-id="eea9f-560">[Test authentication with the [Authorize] attribute](xref:security/authentication/identity#test-identity)</span></span>
* <xref:Microsoft.AspNetCore.Builder.AuthorizationEndpointConventionBuilderExtensions.RequireAuthorization*>
* <span data-ttu-id="eea9f-561">[[Yetkilendir] özniteliğiyle düzeni seçme](xref:security/authorization/limitingidentitybyscheme#selecting-the-scheme-with-the-authorize-attribute)</span><span class="sxs-lookup"><span data-stu-id="eea9f-561">[Selecting the scheme with the [Authorize] attribute](xref:security/authorization/limitingidentitybyscheme#selecting-the-scheme-with-the-authorize-attribute)</span></span>
* <span data-ttu-id="eea9f-562">[[Yetkilendir] özniteliği kullanılarak ilkeler uygulanıyor](xref:security/authorization/policies#applying-policies-to-mvc-controllers)</span><span class="sxs-lookup"><span data-stu-id="eea9f-562">[Applying policies using the [Authorize] attribute](xref:security/authorization/policies#applying-policies-to-mvc-controllers)</span></span>
* <xref:security/authorization/roles>

<a name="hostmatch"></a>

## <a name="host-matching-in-routes-with-requirehost"></a><span data-ttu-id="eea9f-563">RequireHost ile yollarla eşleşen ana bilgisayar</span><span class="sxs-lookup"><span data-stu-id="eea9f-563">Host matching in routes with RequireHost</span></span>

<span data-ttu-id="eea9f-564">`RequireHost`, belirtilen Konağı gerektiren rotaya bir kısıtlama uygular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-564">`RequireHost` applies a constraint to the route which requires the specified host.</span></span> <span data-ttu-id="eea9f-565">`RequireHost` veya `[Host]` parametresi şu olabilir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-565">The `RequireHost` or `[Host]` parameter can be:</span></span>

* <span data-ttu-id="eea9f-566">Ana bilgisayar: `www.domain.com` (herhangi bir bağlantı noktasıyla `www.domain.com` eşleşir)</span><span class="sxs-lookup"><span data-stu-id="eea9f-566">Host: `www.domain.com` (matches `www.domain.com` with any port)</span></span>
* <span data-ttu-id="eea9f-567">Joker karakterle ana bilgisayar: `*.domain.com` (herhangi bir bağlantı noktasında `www.domain.com`, `subdomain.domain.com`veya `www.subdomain.domain.com` eşleşir)</span><span class="sxs-lookup"><span data-stu-id="eea9f-567">Host with wildcard: `*.domain.com` (matches `www.domain.com`, `subdomain.domain.com`, or `www.subdomain.domain.com` on any port)</span></span>
* <span data-ttu-id="eea9f-568">Bağlantı noktası: `*:5000` (herhangi bir konakla bağlantı noktası 5000 ile eşleşir)</span><span class="sxs-lookup"><span data-stu-id="eea9f-568">Port: `*:5000` (matches port 5000 with any host)</span></span>
* <span data-ttu-id="eea9f-569">Konak ve bağlantı noktası: `www.domain.com:5000`, `*.domain.com:5000` (ana bilgisayar ve bağlantı noktasıyla eşleşir)</span><span class="sxs-lookup"><span data-stu-id="eea9f-569">Host and port: `www.domain.com:5000`, `*.domain.com:5000` (matches host and port)</span></span>

<span data-ttu-id="eea9f-570">`RequireHost` veya `[Host]`kullanılarak birden çok parametre belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-570">Multiple parameters can be specified using `RequireHost` or `[Host]`.</span></span> <span data-ttu-id="eea9f-571">Kısıtlama, parametrelerden herhangi biri için geçerli olan konaklara göre eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-571">The constraint will match hosts valid for any of the parameters.</span></span> <span data-ttu-id="eea9f-572">Örneğin, `[Host("domain.com", "*.domain.com")]` `domain.com`, `www.domain.com`veya `subdomain.domain.com`eşleştirecektir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-572">For example, `[Host("domain.com", "*.domain.com")]` will match `domain.com`, `www.domain.com`, or `subdomain.domain.com`.</span></span>

<span data-ttu-id="eea9f-573">Aşağıdaki kod, rotada belirtilen ana bilgisayarı gerektirmek için `RequireHost` kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-573">The following code uses `RequireHost` to require the specified host on the route:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapGet("/", context => context.Response.WriteAsync("Hi Contoso!"))
            .RequireHost("contoso.com");
        endpoints.MapGet("/", context => context.Response.WriteAsync("Hi AdventureWorks!"))
            .RequireHost("adventure-works.com");
        endpoints.MapHealthChecks("/healthz").RequireHost("*:8080");
    });
}
```

<span data-ttu-id="eea9f-574">Aşağıdaki kod, denetleyicide belirtilen Konağı gerektirmek için `[Host]` özniteliğini kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-574">The following code uses the `[Host]` attribute to require the specified host on the controller:</span></span>

```csharp
[Host("contoso.com", "adventure-works.com")]
public class HomeController : Controller
{
    private readonly ILogger<HomeController> _logger;

    public HomeController(ILogger<HomeController> logger)
    {
        _logger = logger;
    }

    public IActionResult Index()
    {
        return View();
    }

    [Host("example.com:8080")]
    public IActionResult Privacy()
    {
        return View();
    }

}
```

<span data-ttu-id="eea9f-575">`[Host]` özniteliği hem denetleyici hem de eylem yöntemine uygulandığında:</span><span class="sxs-lookup"><span data-stu-id="eea9f-575">When the `[Host]` attribute is applied to both the controller and action method:</span></span>

* <span data-ttu-id="eea9f-576">Eylem üzerindeki özniteliği kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-576">The attribute on the action is used.</span></span>
* <span data-ttu-id="eea9f-577">Denetleyici özniteliği yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-577">The controller attribute is ignored.</span></span>

::: moniker-end

::: moniker range="= aspnetcore-2.2"

<span data-ttu-id="eea9f-578">Yönlendirme, istek URI 'Lerini uç noktalarla eşleştirmekten ve gelen istekleri bu uç noktalara gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-578">Routing is responsible for mapping request URIs to endpoints and dispatching incoming requests to those endpoints.</span></span> <span data-ttu-id="eea9f-579">Yollar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-579">Routes are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="eea9f-580">Yol, isteğe bağlı olarak istekte bulunan URL 'den değerleri ayıklayabilir ve bu değerler, istek işleme için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-580">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="eea9f-581">Uygulamadan yönlendirme bilgilerini kullanarak, yönlendirme, uç noktalarıyla eşlenen URL 'Ler de oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-581">Using route information from the app, routing is also able to generate URLs that map to endpoints.</span></span>

<span data-ttu-id="eea9f-582">ASP.NET Core 2,2 ' de en son yönlendirme senaryolarını kullanmak için `Startup.ConfigureServices`MVC Hizmetleri kaydının [Uyumluluk sürümünü](xref:mvc/compatibility-version) belirtin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-582">To use the latest routing scenarios in ASP.NET Core 2.2, specify the [compatibility version](xref:mvc/compatibility-version) to the MVC services registration in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc()
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

<span data-ttu-id="eea9f-583"><xref:Microsoft.AspNetCore.Mvc.MvcOptions.EnableEndpointRouting> seçeneği, yönlendirmenin, ASP.NET Core 2,1 veya önceki bir sürümünün uç nokta tabanlı mantık veya <xref:Microsoft.AspNetCore.Routing.IRouter>tabanlı mantığını dahili olarak kullanması gerektiğini belirler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-583">The <xref:Microsoft.AspNetCore.Mvc.MvcOptions.EnableEndpointRouting> option determines if routing should internally use endpoint-based logic or the <xref:Microsoft.AspNetCore.Routing.IRouter>-based logic of ASP.NET Core 2.1 or earlier.</span></span> <span data-ttu-id="eea9f-584">Uyumluluk sürümü 2,2 veya üzeri bir değere ayarlandığında, varsayılan değer `true`olur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-584">When the compatibility version is set to 2.2 or later, the default value is `true`.</span></span> <span data-ttu-id="eea9f-585">Önceki yönlendirme mantığını kullanmak için değeri `false` olarak ayarlayın:</span><span class="sxs-lookup"><span data-stu-id="eea9f-585">Set the value to `false` to use the prior routing logic:</span></span>

```csharp
// Use the routing logic of ASP.NET Core 2.1 or earlier:
services.AddMvc(options => options.EnableEndpointRouting = false)
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

<span data-ttu-id="eea9f-586"><xref:Microsoft.AspNetCore.Routing.IRouter>tabanlı yönlendirme hakkında daha fazla bilgi için [Bu konunun ASP.NET Core 2,1 sürümüne](/aspnet/core/fundamentals/routing?view=aspnetcore-2.1)bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-586">For more information on <xref:Microsoft.AspNetCore.Routing.IRouter>-based routing, see the [ASP.NET Core 2.1 version of this topic](/aspnet/core/fundamentals/routing?view=aspnetcore-2.1).</span></span>

> [!IMPORTANT]
> <span data-ttu-id="eea9f-587">Bu belge, alt düzey ASP.NET Core yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-587">This document covers low-level ASP.NET Core routing.</span></span> <span data-ttu-id="eea9f-588">ASP.NET Core MVC yönlendirme hakkında daha fazla bilgi için bkz. <xref:mvc/controllers/routing>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-588">For information on ASP.NET Core MVC routing, see <xref:mvc/controllers/routing>.</span></span> <span data-ttu-id="eea9f-589">Razor Pages 'de yönlendirme kuralları hakkında bilgi için bkz. <xref:razor-pages/razor-pages-conventions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-589">For information on routing conventions in Razor Pages, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="eea9f-590">[Görüntüleme veya indirme örnek kodu](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([nasıl indirileceğini](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="eea9f-590">[View or download sample code](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([how to download](xref:index#how-to-download-a-sample))</span></span>

## <a name="routing-basics"></a><span data-ttu-id="eea9f-591">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-591">Routing basics</span></span>

<span data-ttu-id="eea9f-592">Çoğu uygulama, URL 'Lerin okunabilir ve anlamlı olması için temel ve açıklayıcı bir yönlendirme şeması seçmelidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-592">Most apps should choose a basic and descriptive routing scheme so that URLs are readable and meaningful.</span></span> <span data-ttu-id="eea9f-593">Varsayılan geleneksel yol `{controller=Home}/{action=Index}/{id?}`:</span><span class="sxs-lookup"><span data-stu-id="eea9f-593">The default conventional route `{controller=Home}/{action=Index}/{id?}`:</span></span>

* <span data-ttu-id="eea9f-594">Temel ve açıklayıcı bir yönlendirme düzenini destekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-594">Supports a basic and descriptive routing scheme.</span></span>
* <span data-ttu-id="eea9f-595">, UI tabanlı uygulamalar için kullanışlı bir başlangıç noktasıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-595">Is a useful starting point for UI-based apps.</span></span>

<span data-ttu-id="eea9f-596">Geliştiriciler, [öznitelik yönlendirme](xref:mvc/controllers/routing#attribute-routing) veya adanmış geleneksel yollar kullanarak özel durumlarda (örneğin, blog ve e-ticaret uç noktaları) bir uygulamanın yüksek trafik bölümlerine daha fazla terse yolu ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-596">Developers commonly add additional terse routes to high-traffic areas of an app in specialized situations (for example, blog and ecommerce endpoints) using [attribute routing](xref:mvc/controllers/routing#attribute-routing) or dedicated conventional routes.</span></span>

<span data-ttu-id="eea9f-597">Web API 'Leri, uygulamanın işlevselliğini HTTP fiilleri tarafından temsil edilen bir kaynak kümesi olarak modellemek için öznitelik yönlendirmeyi kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-597">Web APIs should use attribute routing to model the app's functionality as a set of resources where operations are represented by HTTP verbs.</span></span> <span data-ttu-id="eea9f-598">Bu, aynı mantıksal kaynaktaki birçok işlemin (örneğin, GET, POST) aynı URL 'YI kullanacağı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-598">This means that many operations (for example, GET, POST) on the same logical resource will use the same URL.</span></span> <span data-ttu-id="eea9f-599">Öznitelik yönlendirme, bir API 'nin Genel uç nokta yerleşimini dikkatle tasarlamak için gereken bir denetim düzeyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-599">Attribute routing provides a level of control that's needed to carefully design an API's public endpoint layout.</span></span>

<span data-ttu-id="eea9f-600">Razor Pages uygulamalar, bir uygulamanın *Sayfalar* klasöründe adlandırılmış kaynaklara hizmeti sağlamak için varsayılan geleneksel yönlendirmeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-600">Razor Pages apps use default conventional routing to serve named resources in the *Pages* folder of an app.</span></span> <span data-ttu-id="eea9f-601">Razor Pages yönlendirme davranışını özelleştirmenizi sağlayan ek kurallar mevcuttur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-601">Additional conventions are available that allow you to customize Razor Pages routing behavior.</span></span> <span data-ttu-id="eea9f-602">Daha fazla bilgi için bkz. <xref:razor-pages/index> ve <xref:razor-pages/razor-pages-conventions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-602">For more information, see <xref:razor-pages/index> and <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="eea9f-603">URL oluşturma desteği, uygulamanın, uygulamayı birbirine bağlamak için sabit kodlama URL 'Leri olmadan geliştirilebilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-603">URL generation support allows the app to be developed without hard-coding URLs to link the app together.</span></span> <span data-ttu-id="eea9f-604">Bu destek, temel bir yönlendirme yapılandırmasıyla başlayıp uygulamanın kaynak düzeni belirlendikten sonra yolların değiştirilmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-604">This support allows for starting with a basic routing configuration and modifying the routes after the app's resource layout is determined.</span></span>

<span data-ttu-id="eea9f-605">Yönlendirme, bir uygulamadaki mantıksal uç noktaları temsil etmek için *uç noktaları* (`Endpoint`) kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-605">Routing uses *endpoints* (`Endpoint`) to represent logical endpoints in an app.</span></span>

<span data-ttu-id="eea9f-606">Bir uç nokta, istekleri işlemek için bir temsilci ve rastgele meta veri koleksiyonunu tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-606">An endpoint defines a delegate to process requests and a collection of arbitrary metadata.</span></span> <span data-ttu-id="eea9f-607">Meta veriler, her bir uç noktaya eklenen ilkelere ve yapılandırmaya bağlı olarak çapraz kesme sorunları uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-607">The metadata is used implement cross-cutting concerns based on policies and configuration attached to each endpoint.</span></span>

<span data-ttu-id="eea9f-608">Yönlendirme sistemi aşağıdaki özelliklere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-608">The routing system has the following characteristics:</span></span>

* <span data-ttu-id="eea9f-609">Yol şablonu sözdizimi, simgeleştirilmiş yol parametrelerine sahip yolları tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-609">Route template syntax is used to define routes with tokenized route parameters.</span></span>
* <span data-ttu-id="eea9f-610">Geleneksel stil ve öznitelik stili uç nokta yapılandırmasına izin verilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-610">Conventional-style and attribute-style endpoint configuration is permitted.</span></span>
* <span data-ttu-id="eea9f-611"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, bir URL parametresinin belirli bir uç nokta kısıtlaması için geçerli bir değer içerip içermediğini belirlemekte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-611"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> is used to determine whether a URL parameter contains a valid value for a given endpoint constraint.</span></span>
* <span data-ttu-id="eea9f-612">MVC/Razor Pages gibi uygulama modelleri, yönlendirme senaryolarının öngörülebilir bir uygulaması olan tüm uç noktalarını kaydeder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-612">App models, such as MVC/Razor Pages, register all of their endpoints, which have a predictable implementation of routing scenarios.</span></span>
* <span data-ttu-id="eea9f-613">Yönlendirme gerçekleştirme, yönlendirme kararlarını, ara yazılım ardışık düzeninde istediğiniz yere getirir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-613">The routing implementation makes routing decisions wherever desired in the middleware pipeline.</span></span>
* <span data-ttu-id="eea9f-614">Bir yönlendirme ara yazılımı, belirli bir istek URI 'SI için yönlendirme ara yazılımı uç noktası kararının sonucunu inceleyebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-614">Middleware that appears after a Routing Middleware can inspect the result of the Routing Middleware's endpoint decision for a given request URI.</span></span>
* <span data-ttu-id="eea9f-615">Uygulamadaki tüm uç noktaları, ara yazılım ardışık düzeninde herhangi bir yerde listelemek mümkündür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-615">It's possible to enumerate all of the endpoints in the app anywhere in the middleware pipeline.</span></span>
* <span data-ttu-id="eea9f-616">Bir uygulama, uç nokta bilgilerine göre URL 'Ler oluşturmak için (örneğin, yeniden yönlendirme veya bağlantılar için) yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-616">An app can use routing to generate URLs (for example, for redirection or links) based on endpoint information and thus avoid hard-coded URLs, which helps maintainability.</span></span>
* <span data-ttu-id="eea9f-617">URL oluşturma, rastgele genişletilebilirliği destekleyen adreslere dayalıdır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-617">URL generation is based on addresses, which support arbitrary extensibility:</span></span>

  * <span data-ttu-id="eea9f-618">Bağlantı Oluşturucu API 'SI (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>), [bağımlılık ekleme (dı)](xref:fundamentals/dependency-injection) kullanılarak URL 'ler oluşturmak için her yerde çözülebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-618">The Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>) can be resolved anywhere using [dependency injection (DI)](xref:fundamentals/dependency-injection) to generate URLs.</span></span>
  * <span data-ttu-id="eea9f-619">Bağlantı Oluşturucu API 'sinin DI aracılığıyla kullanılamadığı durumlarda <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> URL 'Leri derlemek için yöntemler sunar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-619">Where the Link Generator API isn't available via DI, <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> offers methods to build URLs.</span></span>

> [!NOTE]
> <span data-ttu-id="eea9f-620">ASP.NET Core 2,2 ' de uç nokta yönlendirme yayını ile, uç nokta bağlama MVC/Razor Pages eylemleri ve sayfaları ile sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-620">With the release of endpoint routing in ASP.NET Core 2.2, endpoint linking is limited to MVC/Razor Pages actions and pages.</span></span> <span data-ttu-id="eea9f-621">Uç nokta bağlama yeteneklerinin genişletmeleri, gelecek sürümlerde planlanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-621">The expansions of endpoint-linking capabilities is planned for future releases.</span></span>

<span data-ttu-id="eea9f-622">Yönlendirme, <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> sınıfı tarafından, [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-622">Routing is connected to the [middleware](xref:fundamentals/middleware/index) pipeline by the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> class.</span></span> <span data-ttu-id="eea9f-623">[ASP.NET Core MVC](xref:mvc/overview) , yapılandırma kapsamında bir ara yazılım ardışık düzenine yönlendirme ekler ve MVC ve Razor Pages uygulamalarında yönlendirmeyi işler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-623">[ASP.NET Core MVC](xref:mvc/overview) adds routing to the middleware pipeline as part of its configuration and handles routing in MVC and Razor Pages apps.</span></span> <span data-ttu-id="eea9f-624">Tek başına bileşen olarak yönlendirmeyi nasıl kullanacağınızı öğrenmek için [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-624">To learn how to use routing as a standalone component, see the [Use Routing Middleware](#use-routing-middleware) section.</span></span>

### <a name="url-matching"></a><span data-ttu-id="eea9f-625">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-625">URL matching</span></span>

<span data-ttu-id="eea9f-626">URL eşleştirme, yönlendirmenin bir *uç noktaya*gelen isteği gönderdiği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-626">URL matching is the process by which routing dispatches an incoming request to an *endpoint*.</span></span> <span data-ttu-id="eea9f-627">Bu işlem, URL yolundaki verileri temel alır, ancak istekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-627">This process is based on data in the URL path but can be extended to consider any data in the request.</span></span> <span data-ttu-id="eea9f-628">Ayrı işleyicilere istek gönderme özelliği, bir uygulamanın boyutunu ve karmaşıklığını ölçeklendirmeye yönelik anahtardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-628">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an app.</span></span>

<span data-ttu-id="eea9f-629">Uç nokta yönlendirmesinde yönlendirme sistemi tüm gönderme kararlarından sorumludur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-629">The routing system in endpoint routing is responsible for all dispatching decisions.</span></span> <span data-ttu-id="eea9f-630">Ara yazılım, seçili uç noktaya göre ilkeler uyguladığı için, yönlendirme sisteminde güvenlik ilkelerinin dağıtımını etkileyebilecek veya uygulamanın uygulanmasını etkileyebilecek herhangi bir kararın olması önemlidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-630">Since the middleware applies policies based on the selected endpoint, it's important that any decision that can affect dispatching or the application of security policies is made inside the routing system.</span></span>

<span data-ttu-id="eea9f-631">Uç nokta temsilcisi yürütüldüğünde, [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özellikleri, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-631">When the endpoint delegate is executed, the properties of [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) are set to appropriate values based on the request processing performed thus far.</span></span>

<span data-ttu-id="eea9f-632">[RouteData. Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) , rotada oluşturulan *yol değerlerinin* bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-632">[RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="eea9f-633">Bu değerler genellikle URL 'YI simgeleştirerek belirlenir ve Kullanıcı girişini kabul etmek ya da uygulama içinde daha fazla kararlar almak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-633">These values are usually determined by tokenizing the URL and can be used to accept user input or to make further dispatching decisions inside the app.</span></span>

<span data-ttu-id="eea9f-634">[RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) , eşleşen rotayla ilgili ek verilerin bir özellik çantadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-634">[RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="eea9f-635"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*>, uygulamanın hangi yolun eşleştiğini temel alarak kararlar verebilmesi için durum verilerinin her bir rota ile ilişkilendirilmesini desteklemek amacıyla sağlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-635"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> are provided to support associating state data with each route so that the app can make decisions based on which route matched.</span></span> <span data-ttu-id="eea9f-636">Bu değerler, geliştirici tarafından tanımlanır ve yönlendirme davranışını herhangi bir **şekilde etkilemez.**</span><span class="sxs-lookup"><span data-stu-id="eea9f-636">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="eea9f-637">Ayrıca, [RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) içinde bulunan değerler her türlü türden olabilir. Bu, [veri](xref:Microsoft.AspNetCore.Routing.RouteData.Values)dizeleri arasında dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-637">Additionally, values stashed in [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) can be of any type, in contrast to [RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values), which must be convertible to and from strings.</span></span>

<span data-ttu-id="eea9f-638">[RouteData. yönlendiriciler](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) , isteği başarıyla eşleştirirken geçen yolların bir listesidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-638">[RouteData.Routers](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="eea9f-639">Yollar bir diğerinin içinde iç içe olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-639">Routes can be nested inside of one another.</span></span> <span data-ttu-id="eea9f-640"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers> özelliği, bir eşleşme ile sonuçlanan yolların mantıksal ağacı aracılığıyla yolu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-640">The <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="eea9f-641">Genellikle, <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> ilk öğe yol koleksiyonudur ve URL oluşturma için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-641">Generally, the first item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route collection and should be used for URL generation.</span></span> <span data-ttu-id="eea9f-642"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers> son öğe, eşleşen yol işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-642">The last item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route handler that matched.</span></span>

<a name="lg"></a>

### <a name="url-generation-with-linkgenerator"></a><span data-ttu-id="eea9f-643">LinkGenerator ile URL oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-643">URL generation with LinkGenerator</span></span>

<span data-ttu-id="eea9f-644">URL oluşturma, yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturabileceği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-644">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="eea9f-645">Bu, uç noktalarınız ve bunlara erişen URL 'Ler arasında mantıksal bir ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-645">This allows for a logical separation between your endpoints and the URLs that access them.</span></span>

<span data-ttu-id="eea9f-646">Uç nokta yönlendirme bağlantı Oluşturucu API 'sini (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>) içerir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-646">Endpoint routing includes the Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>).</span></span> <span data-ttu-id="eea9f-647"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>, DI 'dan alınabilecek bir tek hizmettir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-647"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is a singleton service that can be retrieved from DI.</span></span> <span data-ttu-id="eea9f-648">API, yürütülen bir istek bağlamı dışında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-648">The API can be used outside of the context of an executing request.</span></span> <span data-ttu-id="eea9f-649">MVC 'nin [Etiket Yardımcıları](xref:mvc/views/tag-helpers/intro), HTML Yardımcıları ve [eylem sonuçları](xref:mvc/controllers/actions)gibi <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>kullanan <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> ve senaryoları, bağlantı oluşturma yetenekleri sağlamak için bağlantı oluşturucuyu kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-649">MVC's <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> and scenarios that rely on <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>, such as [Tag Helpers](xref:mvc/views/tag-helpers/intro), HTML Helpers, and [Action Results](xref:mvc/controllers/actions), use the link generator to provide link generating capabilities.</span></span>

<span data-ttu-id="eea9f-650">Bağlantı Oluşturucu, bir *Adres* ve *Adres şemaları*kavramıyla desteklenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-650">The link generator is backed by the concept of an *address* and *address schemes*.</span></span> <span data-ttu-id="eea9f-651">Adres şeması, bağlantı oluşturma için göz önünde bulundurmanız gereken uç noktaları belirlemenin bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-651">An address scheme is a way of determining the endpoints that should be considered for link generation.</span></span> <span data-ttu-id="eea9f-652">Örneğin, çok sayıda kullanıcının yol adı ve yol değerleri senaryoları, MVC/Razor Pages tarafından tanıdık bir adres düzeni olarak uygulanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-652">For example, the route name and route values scenarios many users are familiar with from MVC/Razor Pages are implemented as an address scheme.</span></span>

<span data-ttu-id="eea9f-653">Bağlantı Oluşturucu, aşağıdaki genişletme yöntemleri aracılığıyla MVC/Razor Pages eylemlerine ve sayfalarına bağlanabilir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-653">The link generator can link to MVC/Razor Pages actions and pages via the following extension methods:</span></span>

* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetPathByAction*>
* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetUriByAction*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetPathByPage*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetUriByPage*>

<span data-ttu-id="eea9f-654">Bu yöntemlerin aşırı yüklemesi `HttpContext`içeren bağımsız değişkenleri kabul eder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-654">An overload of these methods accepts arguments that include the `HttpContext`.</span></span> <span data-ttu-id="eea9f-655">Bu yöntemler `Url.Action` ve `Url.Page` işlevsel olarak eşdeğerdir ancak ek esneklik ve seçenekler sunar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-655">These methods are functionally equivalent to `Url.Action` and `Url.Page` but offer additional flexibility and options.</span></span>

<span data-ttu-id="eea9f-656">`GetPath*` yöntemleri, `Url.Action` ve `Url.Page`, mutlak bir yol içeren bir URI oluşturmak için en çok benzerdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-656">The `GetPath*` methods are most similar to `Url.Action` and `Url.Page` in that they generate a URI containing an absolute path.</span></span> <span data-ttu-id="eea9f-657">`GetUri*` Yöntemler her zaman bir düzen ve konak içeren mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-657">The `GetUri*` methods always generate an absolute URI containing a scheme and host.</span></span> <span data-ttu-id="eea9f-658">Bir `HttpContext` kabul eden yöntemler, yürütülmekte olan istek bağlamında bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-658">The methods that accept an `HttpContext` generate a URI in the context of the executing request.</span></span> <span data-ttu-id="eea9f-659">Ortam yolu değerleri, URL taban yolu, şeması ve yürütülen istekten ana bilgisayar, geçersiz kılınmadıkça kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-659">The ambient route values, URL base path, scheme, and host from the executing request are used unless overridden.</span></span>

<span data-ttu-id="eea9f-660"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> bir adresle çağrılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-660"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is called with an address.</span></span> <span data-ttu-id="eea9f-661">URI oluşturma iki adımda gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-661">Generating a URI occurs in two steps:</span></span>

1. <span data-ttu-id="eea9f-662">Adres, adresle eşleşen bir uç nokta listesine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-662">An address is bound to a list of endpoints that match the address.</span></span>
1. <span data-ttu-id="eea9f-663">Her uç noktanın `RoutePattern`, sağlanan değerlerle eşleşen bir rota deseninin bulunana kadar değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-663">Each endpoint's `RoutePattern` is evaluated until a route pattern that matches the supplied values is found.</span></span> <span data-ttu-id="eea9f-664">Elde edilen çıktı, bağlantı oluşturucuya sağlanan diğer URI parçalarıyla birleştirilir ve döndürülür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-664">The resulting output is combined with the other URI parts supplied to the link generator and returned.</span></span>

<span data-ttu-id="eea9f-665"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> tarafından sunulan yöntemler, herhangi bir tür adresin standart bağlantı oluşturma özelliklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-665">The methods provided by <xref:Microsoft.AspNetCore.Routing.LinkGenerator> support standard link generation capabilities for any type of address.</span></span> <span data-ttu-id="eea9f-666">Bağlantı oluşturucuyu kullanmanın en kolay yolu, belirli bir adres türü için işlem gerçekleştiren genişletme yöntemlerine yöneliktir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-666">The most convenient way to use the link generator is through extension methods that perform operations for a specific address type.</span></span>

| <span data-ttu-id="eea9f-667">Genişletme yöntemi</span><span class="sxs-lookup"><span data-stu-id="eea9f-667">Extension Method</span></span>   | <span data-ttu-id="eea9f-668">Açıklama</span><span class="sxs-lookup"><span data-stu-id="eea9f-668">Description</span></span>                                                         |
| ------------------ | ------------------------------------------------------------------- |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*> | <span data-ttu-id="eea9f-669">Belirtilen değerleri temel alarak mutlak bir yola sahip bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-669">Generates a URI with an absolute path based on the provided values.</span></span> |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetUriByAddress*> | <span data-ttu-id="eea9f-670">Belirtilen değerleri temel alarak mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-670">Generates an absolute URI based on the provided values.</span></span>             |

> [!WARNING]
> <span data-ttu-id="eea9f-671"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> yöntemlerinin çağrılmasının aşağıdaki etkilerine dikkat edin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-671">Pay attention to the following implications of calling <xref:Microsoft.AspNetCore.Routing.LinkGenerator> methods:</span></span>
>
> * <span data-ttu-id="eea9f-672">Gelen isteklerin `Host` üstbilgisini doğrulayan bir uygulama yapılandırmasında `GetUri*` uzantısı yöntemlerini dikkatle kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-672">Use `GetUri*` extension methods with caution in an app configuration that doesn't validate the `Host` header of incoming requests.</span></span> <span data-ttu-id="eea9f-673">Gelen isteklerin `Host` üstbilgisi doğrulandıktan sonra, güvenilir olmayan istek girişi, bir görünüm/sayfada URI 'Ler halinde istemciye geri gönderilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-673">If the `Host` header of incoming requests isn't validated, untrusted request input can be sent back to the client in URIs in a view/page.</span></span> <span data-ttu-id="eea9f-674">Tüm üretim uygulamalarının, `Host` üst bilgisini bilinen geçerli değerlere karşı doğrulamak için kendi sunucusunu yapılandırmasını öneririz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-674">We recommend that all production apps configure their server to validate the `Host` header against known valid values.</span></span>
>
> * <span data-ttu-id="eea9f-675">`Map` veya `MapWhen`birlikte ara yazılım ile birlikte <xref:Microsoft.AspNetCore.Routing.LinkGenerator> kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-675">Use <xref:Microsoft.AspNetCore.Routing.LinkGenerator> with caution in middleware in combination with `Map` or `MapWhen`.</span></span> <span data-ttu-id="eea9f-676">`Map*`, yürütülen isteğin temel yolunu değiştirir ve bu da bağlantı oluşturma çıktısını etkiler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-676">`Map*` changes the base path of the executing request, which affects the output of link generation.</span></span> <span data-ttu-id="eea9f-677">Tüm <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'Leri temel yol belirtmeye izin verir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-677">All of the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> APIs allow specifying a base path.</span></span> <span data-ttu-id="eea9f-678">`Map*`bağlantı oluşturmada etkilerini geri almak için her zaman boş bir temel yol belirtin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-678">Always specify an empty base path to undo `Map*`'s affect on link generation.</span></span>

## <a name="differences-from-earlier-versions-of-routing"></a><span data-ttu-id="eea9f-679">Yönlendirmenin önceki sürümlerinden farklılıklar</span><span class="sxs-lookup"><span data-stu-id="eea9f-679">Differences from earlier versions of routing</span></span>

<span data-ttu-id="eea9f-680">ASP.NET Core 2,2 veya üzeri ve daha önceki yönlendirme sürümlerindeki ASP.NET Core uç nokta yönlendirmesi arasında birkaç fark vardır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-680">A few differences exist between endpoint routing in ASP.NET Core 2.2 or later and earlier versions of routing in ASP.NET Core:</span></span>

* <span data-ttu-id="eea9f-681">Uç nokta yönlendirme sistemi, <xref:Microsoft.AspNetCore.Routing.Route>devralma dahil <xref:Microsoft.AspNetCore.Routing.IRouter>tabanlı genişletilebilirliği desteklemez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-681">The endpoint routing system doesn't support <xref:Microsoft.AspNetCore.Routing.IRouter>-based extensibility, including inheriting from <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>

* <span data-ttu-id="eea9f-682">Uç nokta yönlendirme [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim)desteklemez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-682">Endpoint routing doesn't support [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim).</span></span> <span data-ttu-id="eea9f-683">Uyumluluk Shim 'yi kullanmaya devam etmek için 2,1 [Uyumluluk sürümünü](xref:mvc/compatibility-version) (`.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)`) kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-683">Use the 2.1 [compatibility version](xref:mvc/compatibility-version) (`.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)`) to continue using the compatibility shim.</span></span>

* <span data-ttu-id="eea9f-684">Uç nokta yönlendirmesinde, geleneksel yollar kullanılırken oluşturulan URI 'lerin büyük küçük harfleri için farklı davranış vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-684">Endpoint Routing has different behavior for the casing of generated URIs when using conventional routes.</span></span>

  <span data-ttu-id="eea9f-685">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="eea9f-685">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="eea9f-686">Aşağıdaki rotayı kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="eea9f-686">Suppose you generate a link to an action using the following route:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "blog", new { id = 17, });
  ```

  <span data-ttu-id="eea9f-687"><xref:Microsoft.AspNetCore.Routing.IRouter>tabanlı yönlendirme ile bu kod, `/blog/ReadPost/17`URI 'sini oluşturur ve bu da, belirtilen yol değerinin büyük küçük harflerini üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-687">With <xref:Microsoft.AspNetCore.Routing.IRouter>-based routing, this code generates a URI of `/blog/ReadPost/17`, which respects the casing of the provided route value.</span></span> <span data-ttu-id="eea9f-688">ASP.NET Core 2,2 veya sonraki bir sürümde uç nokta yönlendirme `/Blog/ReadPost/17` ("blog" büyük harfli) oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-688">Endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` ("Blog" is capitalized).</span></span> <span data-ttu-id="eea9f-689">Endpoint Routing, bu davranışı küresel olarak özelleştirmek veya URL eşleme için farklı kurallar uygulamak üzere kullanılabilecek `IOutboundParameterTransformer` arabirimini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-689">Endpoint routing provides the `IOutboundParameterTransformer` interface that can be used to customize this behavior globally or to apply different conventions for mapping URLs.</span></span>

  <span data-ttu-id="eea9f-690">Daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="eea9f-690">For more information, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

* <span data-ttu-id="eea9f-691">Standart yollarla MVC/Razor Pages tarafından kullanılan bağlantı oluşturma, mevcut olmayan bir denetleyiciye/eyleme veya sayfaya bağlantı kurmaya çalışırken farklı şekilde davranır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-691">Link Generation used by MVC/Razor Pages with conventional routes behaves differently when attempting to link to an controller/action or page that doesn't exist.</span></span>

  <span data-ttu-id="eea9f-692">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="eea9f-692">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="eea9f-693">Aşağıdaki ile varsayılan şablonu kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="eea9f-693">Suppose you generate a link to an action using the default template with the following:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "Blog", new { id = 17, });
  ```

  <span data-ttu-id="eea9f-694">`IRouter`tabanlı yönlendirme sayesinde, `BlogController` var olmasa veya bir `ReadPost` Action yöntemine sahip olmasa bile sonuç daima `/Blog/ReadPost/17`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-694">With `IRouter`-based routing, the result is always `/Blog/ReadPost/17`, even if the `BlogController` doesn't exist or doesn't have a `ReadPost` action method.</span></span> <span data-ttu-id="eea9f-695">Beklenen şekilde, işlem yöntemi varsa ASP.NET Core 2,2 veya sonraki bir sürümde uç nokta yönlendirmesi `/Blog/ReadPost/17` üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-695">As expected, endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` if the action method exists.</span></span> <span data-ttu-id="eea9f-696">*Ancak, uç nokta yönlendirme, eylem yoksa boş bir dize oluşturur.*</span><span class="sxs-lookup"><span data-stu-id="eea9f-696">*However, endpoint routing produces an empty string if the action doesn't exist.*</span></span> <span data-ttu-id="eea9f-697">Kavramsal olarak, uç nokta yönlendirme, eylem mevcut değilse uç noktanın var olduğunu varsaymaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-697">Conceptually, endpoint routing doesn't assume that the endpoint exists if the action doesn't exist.</span></span>

* <span data-ttu-id="eea9f-698">Bağlantı oluşturma *çevresel değeri ınvalidation algoritması* , uç nokta yönlendirme ile kullanıldığında farklı davranır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-698">The link generation *ambient value invalidation algorithm* behaves differently when used with endpoint routing.</span></span>

  <span data-ttu-id="eea9f-699">*Çevresel değer ınvalidation* , şu anda yürütülmekte olan isteğin (çevresel değerler) hangi rota değerlerinin bağlantı oluşturma işlemlerinde kullanılabileceğini belirleyen algoritmadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-699">*Ambient value invalidation* is the algorithm that decides which route values from the currently executing request (the ambient values) can be used in link generation operations.</span></span> <span data-ttu-id="eea9f-700">Geleneksel yönlendirme, farklı bir eyleme bağlanılırken ek yol değerlerini her zaman geçersiz kıldı.</span><span class="sxs-lookup"><span data-stu-id="eea9f-700">Conventional routing always invalidated extra route values when linking to a different action.</span></span> <span data-ttu-id="eea9f-701">ASP.NET Core 2,2 sürümünden önce öznitelik yönlendirmesinde bu davranış yoktu.</span><span class="sxs-lookup"><span data-stu-id="eea9f-701">Attribute routing didn't have this behavior prior to the release of ASP.NET Core 2.2.</span></span> <span data-ttu-id="eea9f-702">ASP.NET Core 'nin önceki sürümlerinde, aynı yol parametresi adlarını kullanan başka bir eyleme bağlantılar, bağlantı oluşturma hatalarıyla sonuçlandı.</span><span class="sxs-lookup"><span data-stu-id="eea9f-702">In earlier versions of ASP.NET Core, links to another action that use the same route parameter names resulted in link generation errors.</span></span> <span data-ttu-id="eea9f-703">ASP.NET Core 2,2 veya sonraki sürümlerde, yönlendirme biçimlerinin her ikisi de başka bir eyleme bağlanırken değerleri geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-703">In ASP.NET Core 2.2 or later, both forms of routing invalidate values when linking to another action.</span></span>

  <span data-ttu-id="eea9f-704">ASP.NET Core 2,1 veya önceki sürümlerde aşağıdaki örneği göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-704">Consider the following example in ASP.NET Core 2.1 or earlier.</span></span> <span data-ttu-id="eea9f-705">Başka bir eyleme (veya başka bir sayfaya) bağlanırken, yol değerleri istenmeyen yollarla yeniden kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-705">When linking to another action (or another page), route values can be reused in undesirable ways.</span></span>

  <span data-ttu-id="eea9f-706">*/Pages/Store/Product.exe*:</span><span class="sxs-lookup"><span data-stu-id="eea9f-706">In */Pages/Store/Product.cshtml*:</span></span>

  ```cshtml
  @page "{id}"
  @Url.Page("/Login")
  ```

  <span data-ttu-id="eea9f-707">*/Pages/Login.exe*içinde:</span><span class="sxs-lookup"><span data-stu-id="eea9f-707">In */Pages/Login.cshtml*:</span></span>

  ```cshtml
  @page "{id?}"
  ```

  <span data-ttu-id="eea9f-708">URI, ASP.NET Core 2,1 veya önceki sürümlerde `/Store/Product/18`, `@Url.Page("/Login")` tarafından Store/Info sayfasında oluşturulan bağlantı `/Login/18`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-708">If the URI is `/Store/Product/18` in ASP.NET Core 2.1 or earlier, the link generated on the Store/Info page by `@Url.Page("/Login")` is `/Login/18`.</span></span> <span data-ttu-id="eea9f-709">18 ' in `id` değeri, bağlantı hedefi uygulamanın tamamen farklı bir parçası olsa bile yeniden kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-709">The `id` value of 18 is reused, even though the link destination is different part of the app entirely.</span></span> <span data-ttu-id="eea9f-710">`/Login` sayfasının bağlamındaki `id` yol değeri büyük olasılıkla bir depolama ürün KIMLIĞI değeri değil, bir kullanıcı KIMLIĞI değeridir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-710">The `id` route value in the context of the `/Login` page is probably a user ID value, not a store product ID value.</span></span>

  <span data-ttu-id="eea9f-711">ASP.NET Core 2,2 veya sonraki bir sürümü ile Endpoint Routing, sonuç `/Login`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-711">In endpoint routing with ASP.NET Core 2.2 or later, the result is `/Login`.</span></span> <span data-ttu-id="eea9f-712">Bağlantılı hedef farklı bir eylem veya sayfa olduğunda çevresel değerler yeniden kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-712">Ambient values aren't reused when the linked destination is a different action or page.</span></span>

* <span data-ttu-id="eea9f-713">Gidiş dönüşü yol parametresi sözdizimi: çift yıldız (`**`) catch-all parametre sözdizimi kullanılırken eğik çizgiler kodlanmaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-713">Round-tripping route parameter syntax: Forward slashes aren't encoded when using a double-asterisk (`**`) catch-all parameter syntax.</span></span>

  <span data-ttu-id="eea9f-714">Bağlantı oluşturma sırasında, yönlendirme sistemi, eğik çizgiler dışında bir çift yıldız (`**`) catch-all parametresinde (örneğin, `{**myparametername}`) yakalanan değeri kodlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-714">During link generation, the routing system encodes the value captured in a double-asterisk (`**`) catch-all parameter (for example, `{**myparametername}`) except the forward slashes.</span></span> <span data-ttu-id="eea9f-715">Çift yıldız yakalama, ASP.NET Core 2,2 veya sonraki sürümlerde `IRouter`tabanlı yönlendirme ile desteklenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-715">The double-asterisk catch-all is supported with `IRouter`-based routing in ASP.NET Core 2.2 or later.</span></span>

  <span data-ttu-id="eea9f-716">ASP.NET Core (`{*myparametername}`) önceki sürümlerindeki tek yıldız catch-all parametre sözdizimi desteklenmeye devam eder ve eğik çizgi kodlandı.</span><span class="sxs-lookup"><span data-stu-id="eea9f-716">The single asterisk catch-all parameter syntax in prior versions of ASP.NET Core (`{*myparametername}`) remains supported, and forward slashes are encoded.</span></span>

  | <span data-ttu-id="eea9f-717">Yol</span><span class="sxs-lookup"><span data-stu-id="eea9f-717">Route</span></span>              | <span data-ttu-id="eea9f-718">İle oluşturulan bağlantı</span><span class="sxs-lookup"><span data-stu-id="eea9f-718">Link generated with</span></span><br>`Url.Action(new { category = "admin/products" })`&hellip; |
  | ------------------ | --------------------------------------------------------------------- |
  | `/search/{*page}`  | <span data-ttu-id="eea9f-719">`/search/admin%2Fproducts` (eğik çizgi kodlanmış)</span><span class="sxs-lookup"><span data-stu-id="eea9f-719">`/search/admin%2Fproducts` (the forward slash is encoded)</span></span>             |
  | `/search/{**page}` | `/search/admin/products`                                              |

### <a name="middleware-example"></a><span data-ttu-id="eea9f-720">Ara yazılım örneği</span><span class="sxs-lookup"><span data-stu-id="eea9f-720">Middleware example</span></span>

<span data-ttu-id="eea9f-721">Aşağıdaki örnekte, bir ara yazılım, mağaza ürünlerini listeleyen bir eylem yöntemine bağlantı oluşturmak için <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'sini kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-721">In the following example, a middleware uses the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API to create link to an action method that lists store products.</span></span> <span data-ttu-id="eea9f-722">Ekleme tarafından bir sınıfa olan ve `GenerateLink` çağıran bağlantı Oluşturucuyu kullanmak, bir uygulamadaki herhangi bir sınıfta kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-722">Using the link generator by injecting it into a class and calling `GenerateLink` is available to any class in an app.</span></span>

```csharp
using Microsoft.AspNetCore.Routing;

public class ProductsLinkMiddleware
{
    private readonly LinkGenerator _linkGenerator;

    public ProductsLinkMiddleware(RequestDelegate next, LinkGenerator linkGenerator)
    {
        _linkGenerator = linkGenerator;
    }

    public async Task InvokeAsync(HttpContext httpContext)
    {
        var url = _linkGenerator.GetPathByAction("ListProducts", "Store");

        httpContext.Response.ContentType = "text/plain";

        await httpContext.Response.WriteAsync($"Go to {url} to see our products.");
    }
}
```

### <a name="create-routes"></a><span data-ttu-id="eea9f-723">Yolları oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-723">Create routes</span></span>

<span data-ttu-id="eea9f-724">Çoğu uygulama, <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>tanımlanan benzer uzantı yöntemlerinden birini veya <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> çağırarak yollar oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-724">Most apps create routes by calling <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> or one of the similar extension methods defined on <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>.</span></span> <span data-ttu-id="eea9f-725"><xref:Microsoft.AspNetCore.Routing.IRouteBuilder> uzantısı yöntemlerinden herhangi biri bir <xref:Microsoft.AspNetCore.Routing.Route> örneği oluşturur ve bunu yol koleksiyonuna ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-725">Any of the <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> extension methods create an instance of <xref:Microsoft.AspNetCore.Routing.Route> and add it to the route collection.</span></span>

<span data-ttu-id="eea9f-726"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> yol işleyicisi parametresini kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-726"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> doesn't accept a route handler parameter.</span></span> <span data-ttu-id="eea9f-727"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>, yalnızca <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>tarafından işlenen rotaları ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-727"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> only adds routes that are handled by the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span> <span data-ttu-id="eea9f-728">MVC 'de yönlendirme hakkında daha fazla bilgi için bkz. <xref:mvc/controllers/routing>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-728">To learn more about routing in MVC, see <xref:mvc/controllers/routing>.</span></span>

<span data-ttu-id="eea9f-729">Aşağıdaki kod örneği, tipik bir ASP.NET Core MVC yol tanımı tarafından kullanılan <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> çağrısının bir örneğidir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-729">The following code example is an example of a <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> call used by a typical ASP.NET Core MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="eea9f-730">Bu şablon bir URL yoluyla eşleşir ve yol değerlerini ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-730">This template matches a URL path and extracts the route values.</span></span> <span data-ttu-id="eea9f-731">Örneğin, yol `/Products/Details/17` şu yol değerlerini üretir: `{ controller = Products, action = Details, id = 17 }`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-731">For example, the path `/Products/Details/17` generates the following route values: `{ controller = Products, action = Details, id = 17 }`.</span></span>

<span data-ttu-id="eea9f-732">Rota değerleri, URL yolunun kesimlere bölünüyor ve rota şablonundaki *yol parametresi* adı ile her bir segmenti eşleştirerek belirlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-732">Route values are determined by splitting the URL path into segments and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="eea9f-733">Yol parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-733">Route parameters are named.</span></span> <span data-ttu-id="eea9f-734">Küme ayraçları içinde parametre adı eklenerek tanımlanan parametreler `{ ... }`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-734">The parameters defined by enclosing the parameter name in braces `{ ... }`.</span></span>

<span data-ttu-id="eea9f-735">Yukarıdaki şablon, URL yolu `/` de eşleştirebilir ve `{ controller = Home, action = Index }`değerler üretecektir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-735">The preceding template could also match the URL path `/` and produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="eea9f-736">Bu, `{controller}` ve `{action}` yol parametrelerinin varsayılan değerleri olduğu ve `id` Route parametresinin isteğe bağlı olduğu için oluşur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-736">This occurs because the `{controller}` and `{action}` route parameters have default values and the `id` route parameter is optional.</span></span> <span data-ttu-id="eea9f-737">Eşittir işareti (`=`), yol parametresi adı parametre için varsayılan bir değer tanımladıktan sonra bir değer izler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-737">An equals sign (`=`) followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="eea9f-738">Yol parametre adından sonra bir soru işareti (`?`) isteğe bağlı bir parametre tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-738">A question mark (`?`) after the route parameter name defines an optional parameter.</span></span>

<span data-ttu-id="eea9f-739">Yol parametreleri varsayılan değer ile *her zaman* rota değeri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-739">Route parameters with a default value *always* produce a route value when the route matches.</span></span> <span data-ttu-id="eea9f-740">Karşılık gelen bir URL yol kesimi yoksa isteğe bağlı parametreler yol değeri oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-740">Optional parameters don't produce a route value if there was no corresponding URL path segment.</span></span> <span data-ttu-id="eea9f-741">Yol şablonu senaryolarının ve sözdiziminin kapsamlı bir açıklaması için [yol şablonu başvurusu](#route-template-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-741">See the [Route template reference](#route-template-reference) section for a thorough description of route template scenarios and syntax.</span></span>

<span data-ttu-id="eea9f-742">Aşağıdaki örnekte, yol parametresi tanımı `{id:int}` `id` Route parametresi için bir [yol kısıtlaması](#route-constraint-reference) tanımlıyor:</span><span class="sxs-lookup"><span data-stu-id="eea9f-742">In the following example, the route parameter definition `{id:int}` defines a [route constraint](#route-constraint-reference) for the `id` route parameter:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="eea9f-743">Bu şablon `/Products/Details/17` gibi bir URL yoluyla eşleşir ancak `/Products/Details/Apples`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-743">This template matches a URL path like `/Products/Details/17` but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="eea9f-744">Yol kısıtlamaları <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> uygular ve yol değerlerini inceleyerek onları doğrular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-744">Route constraints implement <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> and inspect route values to verify them.</span></span> <span data-ttu-id="eea9f-745">Bu örnekte, yol değeri `id` bir tamsayıya dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-745">In this example, the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="eea9f-746">Framework tarafından sunulan yol kısıtlamalarının açıklaması için bkz. [route-Constraint-Reference](#route-constraint-reference) .</span><span class="sxs-lookup"><span data-stu-id="eea9f-746">See [route-constraint-reference](#route-constraint-reference) for an explanation of route constraints provided by the framework.</span></span>

<span data-ttu-id="eea9f-747"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> ek aşırı yüklemeleri `constraints`, `dataTokens`ve `defaults`için değer kabul eder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-747">Additional overloads of <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="eea9f-748">Bu parametrelerin tipik kullanımı, anonim türdeki özellik adlarının yol parametre adlarıyla eşleşen anonim olarak yazılmış bir nesneyi geçirmektir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-748">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="eea9f-749">Aşağıdaki <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> örnekleri eşdeğer yollar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="eea9f-749">The following <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

> [!TIP]
> <span data-ttu-id="eea9f-750">Kısıtlamaları ve Varsayılanları tanımlamaya yönelik satır içi sözdizimi basit yollar için kullanışlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-750">The inline syntax for defining constraints and defaults can be convenient for simple routes.</span></span> <span data-ttu-id="eea9f-751">Ancak, satır içi sözdizimi tarafından desteklenmeyen veri belirteçleri gibi senaryolar vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-751">However, there are scenarios, such as data tokens, that aren't supported by inline syntax.</span></span>

<span data-ttu-id="eea9f-752">Aşağıdaki örnek birkaç ek senaryoyu göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-752">The following example demonstrates a few additional scenarios:</span></span>

```csharp
routes.MapRoute(
    name: "blog",
    template: "Blog/{**article}",
    defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="eea9f-753">Önceki şablon `/Blog/All-About-Routing/Introduction` gibi bir URL yoluyla eşleşir ve `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`değerleri ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-753">The preceding template matches a URL path like `/Blog/All-About-Routing/Introduction` and extracts the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="eea9f-754">`controller` ve `action` için varsayılan yol değerleri, şablonda karşılık gelen hiçbir yol parametresi olmasa bile yol tarafından üretilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-754">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="eea9f-755">Varsayılan değerler yol şablonunda belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-755">Default values can be specified in the route template.</span></span> <span data-ttu-id="eea9f-756">`article` Route parametresi, yol parametre adından önce bir çift yıldız işareti (`**`) görünümüne göre *catch-all* olarak tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-756">The `article` route parameter is defined as a *catch-all* by the appearance of an double asterisk (`**`) before the route parameter name.</span></span> <span data-ttu-id="eea9f-757">Catch-all yol parametreleri, URL yolunun kalanını yakalar ve boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-757">Catch-all route parameters capture the remainder of the URL path and can also match the empty string.</span></span>

<span data-ttu-id="eea9f-758">Aşağıdaki örnek yol kısıtlamalarını ve veri belirteçlerini ekler:</span><span class="sxs-lookup"><span data-stu-id="eea9f-758">The following example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="eea9f-759">Önceki şablon `/en-US/Products/5` gibi bir URL yoluyla eşleşir ve `{ controller = Products, action = Details, id = 5 }` ve veri belirteçleri `{ locale = en-US }`değerleri ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-759">The preceding template matches a URL path like `/en-US/Products/5` and extracts the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Yereller Windows belirteçleri](routing/_static/tokens.png)

### <a name="route-class-url-generation"></a><span data-ttu-id="eea9f-761">Yol sınıfı URL 'SI oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-761">Route class URL generation</span></span>

<span data-ttu-id="eea9f-762"><xref:Microsoft.AspNetCore.Routing.Route> sınıfı, yol değerlerini bir kümesini rota şablonuyla birleştirerek URL oluşturmayı da gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-762">The <xref:Microsoft.AspNetCore.Routing.Route> class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="eea9f-763">Bu, URL yolunu eşleştirmenin mantıksal bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-763">This is logically the reverse process of matching the URL path.</span></span>

> [!TIP]
> <span data-ttu-id="eea9f-764">URL oluşturmayı daha iyi anlamak için, oluşturmak istediğiniz URL 'YI düşünün ve sonra bir yönlendirme şablonunun bu URL ile nasıl eşleşeceğini düşünün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-764">To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="eea9f-765">Hangi değerler üretilemidir?</span><span class="sxs-lookup"><span data-stu-id="eea9f-765">What values would be produced?</span></span> <span data-ttu-id="eea9f-766">Bu, URL oluşturma 'nın <xref:Microsoft.AspNetCore.Routing.Route> sınıfında nasıl çalıştığına ilişkin kaba bir eştir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-766">This is the rough equivalent of how URL generation works in the <xref:Microsoft.AspNetCore.Routing.Route> class.</span></span>

<span data-ttu-id="eea9f-767">Aşağıdaki örnek genel ASP.NET Core MVC varsayılan yolunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-767">The following example uses a general ASP.NET Core MVC default route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="eea9f-768">Yol değerleri `{ controller = Products, action = List }`, `/Products/List` URL 'SI oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-768">With the route values `{ controller = Products, action = List }`, the URL `/Products/List` is generated.</span></span> <span data-ttu-id="eea9f-769">Yol değerleri, URL yolunu biçimlendirmek için karşılık gelen yol parametrelerinin yerine kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-769">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="eea9f-770">`id`, isteğe bağlı bir yol parametresi olduğundan, `id`için bir değer olmadan URL başarıyla oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-770">Since `id` is an optional route parameter, the URL is successfully generated without a value for `id`.</span></span>

<span data-ttu-id="eea9f-771">Yol değerleri `{ controller = Home, action = Index }`, `/` URL 'SI oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-771">With the route values `{ controller = Home, action = Index }`, the URL `/` is generated.</span></span> <span data-ttu-id="eea9f-772">Belirtilen yol değerleri varsayılan değerlerle eşleşir ve varsayılan değerlere karşılık gelen segmentler güvenle atlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-772">The provided route values match the default values, and the segments corresponding to the default values are safely omitted.</span></span>

<span data-ttu-id="eea9f-773">Her iki URL de aşağıdaki yol tanımıyla (`/Home/Index` ve `/`) bir gidiş dönüş, URL oluşturmak için kullanılan rota değerlerini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-773">Both URLs generated round-trip with the following route definition (`/Home/Index` and `/`) produce the same route values that were used to generate the URL.</span></span>

> [!NOTE]
> <span data-ttu-id="eea9f-774">ASP.NET Core MVC kullanan bir uygulama doğrudan yönlendirmeye çağırmak yerine URL oluşturmak için <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-774">An app using ASP.NET Core MVC should use <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="eea9f-775">URL oluşturma hakkında daha fazla bilgi için [URL oluşturma başvurusu](#url-generation-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-775">For more information on URL generation, see the [Url generation reference](#url-generation-reference) section.</span></span>

## <a name="use-routing-middleware"></a><span data-ttu-id="eea9f-776">Yönlendirme ara yazılımı kullanma</span><span class="sxs-lookup"><span data-stu-id="eea9f-776">Use Routing Middleware</span></span>

<span data-ttu-id="eea9f-777">Uygulamanın proje dosyasındaki [Microsoft. AspNetCore. app metapackage](xref:fundamentals/metapackage-app) öğesine başvurun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-777">Reference the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in the app's project file.</span></span>

<span data-ttu-id="eea9f-778">`Startup.ConfigureServices`' de hizmet kapsayıcısına yönlendirme ekleyin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-778">Add routing to the service container in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_ConfigureServices&highlight=3)]

<span data-ttu-id="eea9f-779">Yolların `Startup.Configure` yönteminde yapılandırılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-779">Routes must be configured in the `Startup.Configure` method.</span></span> <span data-ttu-id="eea9f-780">Örnek uygulama aşağıdaki API 'Leri kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-780">The sample app uses the following APIs:</span></span>

* <xref:Microsoft.AspNetCore.Routing.RouteBuilder>
* <span data-ttu-id="eea9f-781"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> &ndash; yalnızca HTTP GET istekleriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-781"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> &ndash; Matches only HTTP GET requests.</span></span>
* <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_RouteHandler)]

<span data-ttu-id="eea9f-782">Aşağıdaki tabloda verilen URI 'Ler ile ilgili yanıtlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-782">The following table shows the responses with the given URIs.</span></span>

| <span data-ttu-id="eea9f-783">{1&gt;URI&lt;1}</span><span class="sxs-lookup"><span data-stu-id="eea9f-783">URI</span></span>                    | <span data-ttu-id="eea9f-784">Yanıt</span><span class="sxs-lookup"><span data-stu-id="eea9f-784">Response</span></span>                                          |
| ---------------------- | ------------------------------------------------- |
| `/package/create/3`    | <span data-ttu-id="eea9f-785">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-785">Hello!</span></span> <span data-ttu-id="eea9f-786">Rota değerleri: [işlem, oluşturma], [kimlik, 3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-786">Route values: [operation, create], [id, 3]</span></span> |
| `/package/track/-3`    | <span data-ttu-id="eea9f-787">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-787">Hello!</span></span> <span data-ttu-id="eea9f-788">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-788">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/-3/`   | <span data-ttu-id="eea9f-789">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-789">Hello!</span></span> <span data-ttu-id="eea9f-790">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-790">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/`      | <span data-ttu-id="eea9f-791">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-791">The request falls through, no match.</span></span>              |
| `GET /hello/Joe`       | <span data-ttu-id="eea9f-792">Merhaba, ali!</span><span class="sxs-lookup"><span data-stu-id="eea9f-792">Hi, Joe!</span></span>                                          |
| `POST /hello/Joe`      | <span data-ttu-id="eea9f-793">İstek üzerinden geçer, yalnızca HTTP GET ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-793">The request falls through, matches HTTP GET only.</span></span> |
| `GET /hello/Joe/Smith` | <span data-ttu-id="eea9f-794">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-794">The request falls through, no match.</span></span>              |

<span data-ttu-id="eea9f-795">Çerçeve, yollar oluşturmak için bir genişletme yöntemleri kümesi sağlar (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span><span class="sxs-lookup"><span data-stu-id="eea9f-795">The framework provides a set of extension methods for creating routes (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span></span>

* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareVerb*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>

<span data-ttu-id="eea9f-796">`Map[Verb]` yöntemleri, yöntem adındaki HTTP fiili ile rotayı sınırlandırmak için kısıtlamaları kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-796">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="eea9f-797">Örneğin, bkz. <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> ve <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-797">For example, see <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> and <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="eea9f-798">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-798">Route template reference</span></span>

<span data-ttu-id="eea9f-799">Küme ayraçları içindeki belirteçler (`{ ... }`), yol eşleştirildiği takdirde bağlanan *Rota parametrelerini* tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-799">Tokens within curly braces (`{ ... }`) define *route parameters* that are bound if the route is matched.</span></span> <span data-ttu-id="eea9f-800">Bir yol segmentinde birden fazla yol parametresi tanımlayabilirsiniz, ancak bunların bir değişmez değer ile ayrılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-800">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="eea9f-801">Örneğin, `{controller}` ve `{action}`arasında değişmez değer olmadığından `{controller=Home}{action=Index}` geçerli bir yol değil.</span><span class="sxs-lookup"><span data-stu-id="eea9f-801">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="eea9f-802">Bu rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-802">These route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="eea9f-803">Yol parametreleri dışında (örneğin, `{id}`) ve yol ayırıcı `/` URL içindeki metinle eşleşmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-803">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="eea9f-804">Metin eşleştirme, büyük/küçük harfe duyarsızdır ve URL yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-804">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="eea9f-805">Değişmez değer yol parametresi sınırlayıcısından (`{` veya `}`) eşleştirmek için, karakteri (`{{` veya `}}`) tekrarlayarak sınırlayıcıdan kaçış.</span><span class="sxs-lookup"><span data-stu-id="eea9f-805">To match a literal route parameter delimiter (`{` or `}`), escape the delimiter by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="eea9f-806">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-806">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="eea9f-807">Örneğin, `files/{filename}.{ext?}`şablonu düşünün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-807">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="eea9f-808">`filename` ve `ext` değerlerinin her ikisi de varsa, her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-808">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="eea9f-809">URL 'de yalnızca `filename` için bir değer varsa, bitiş dönemi (`.`) isteğe bağlı olduğundan yol eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-809">If only a value for `filename` exists in the URL, the route matches because the trailing period (`.`) is  optional.</span></span> <span data-ttu-id="eea9f-810">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="eea9f-810">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="eea9f-811">URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak bir yıldız işareti (`*`) veya çift yıldız işareti (`**`) kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-811">You can use an asterisk (`*`) or double asterisk (`**`) as a prefix to a route parameter to bind to the rest of the URI.</span></span> <span data-ttu-id="eea9f-812">Bunlara *catch-all* parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-812">These are called a *catch-all* parameters.</span></span> <span data-ttu-id="eea9f-813">Örneğin, `blog/{**slug}` `/blog` ile başlayan ve ondan sonraki bir değere sahip olan `slug` yol değerine atanan tüm URI ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-813">For example, `blog/{**slug}` matches any URI that starts with `/blog` and has any value following it, which is assigned to the `slug` route value.</span></span> <span data-ttu-id="eea9f-814">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-814">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="eea9f-815">Yol ayırıcısı (`/`) karakterleri de dahil olmak üzere bir URL oluşturmak için bir yol kullanıldığında, catch-all parametresi uygun karakterleri çıkar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-815">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator (`/`) characters.</span></span> <span data-ttu-id="eea9f-816">Örneğin yol `foo/{*path}` rota değerleriyle `{ path = "my/path" }` `foo/my%2Fpath`üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-816">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="eea9f-817">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-817">Note the escaped forward slash.</span></span> <span data-ttu-id="eea9f-818">Yol ayırıcı karakterlerini geri döndürmek için `**` Route parametresi önekini kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-818">To round-trip path separator characters, use the `**` route parameter prefix.</span></span> <span data-ttu-id="eea9f-819">`{ path = "my/path" }` ile yol `foo/{**path}` `foo/my/path`üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-819">The route `foo/{**path}` with `{ path = "my/path" }` generates `foo/my/path`.</span></span>

<span data-ttu-id="eea9f-820">Yol parametrelerinin, parametre adından sonra varsayılan değer belirtilerek bir eşittir işareti (`=`) ile ayrıldıktan sonra belirlenen *varsayılan değerleri* olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-820">Route parameters may have *default values* designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="eea9f-821">Örneğin, `Home` `controller`için varsayılan değer olarak `{controller=Home}` tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-821">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="eea9f-822">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-822">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="eea9f-823">Yol parametreleri, `id?`gibi parametre adının sonuna bir soru işareti (`?`) eklenerek isteğe bağlı olarak yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-823">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name, as in `id?`.</span></span> <span data-ttu-id="eea9f-824">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark, varsayılan değere sahip bir yol parametresinin her zaman bir değer ürettiğinden&mdash;isteğe bağlı bir parametre yalnızca istek URL 'SI tarafından bir değer sağlandığında bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-824">The difference between optional values and default route parameters is that a route parameter with a default value always produces a value&mdash;an optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="eea9f-825">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-825">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="eea9f-826">Yol parametre adından sonra bir iki nokta üst üste (`:`) ve kısıtlama adının eklenmesi, bir yol parametresi üzerinde bir *satır içi kısıtlamayı* belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-826">Adding a colon (`:`) and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="eea9f-827">Kısıtlama bağımsız değişkenler gerektiriyorsa, kısıtlama adından sonra parantez (`(...)`) içine alınır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-827">If the constraint requires arguments, they're enclosed in parentheses (`(...)`) after the constraint name.</span></span> <span data-ttu-id="eea9f-828">Birden çok satır içi kısıtlama, başka bir iki nokta (`:`) ve kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-828">Multiple inline constraints can be specified by appending another colon (`:`) and constraint name.</span></span>

<span data-ttu-id="eea9f-829">Kısıtlama adı ve bağımsız değişkenler, URL işlemede kullanılacak bir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> örneği oluşturmak için <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-829">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="eea9f-830">Örneğin, yol şablonu `blog/{article:minlength(10)}`, `10`bağımsız değişkeniyle bir `minlength` kısıtlaması belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-830">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="eea9f-831">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-831">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="eea9f-832">Yol parametrelerinde Ayrıca, bağlantı oluştururken ve URL 'Ler ile eşleşen eylemler ve sayfalar için bir parametrenin değerini dönüştüren parametre dönüştürücüler bulunabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-832">Route parameters may also have parameter transformers, which transform a parameter's value when generating links and matching actions and pages to URLs.</span></span> <span data-ttu-id="eea9f-833">LIKE kısıtlamaları, yol parametre adından sonra iki nokta üst üste (`:`) ve transformatör adı eklenerek, parametre dönüştürücüleri bir rota parametresine eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-833">Like constraints, parameter transformers can be added inline to a route parameter by adding a colon (`:`) and transformer name after the route parameter name.</span></span> <span data-ttu-id="eea9f-834">Örneğin, yol şablonu `blog/{article:slugify}` bir `slugify` transformatörü belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-834">For example, the route template `blog/{article:slugify}` specifies a `slugify` transformer.</span></span> <span data-ttu-id="eea9f-835">Parametre dönüştürücüler hakkında daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="eea9f-835">For more information on parameter transformers, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

<span data-ttu-id="eea9f-836">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-836">The following table demonstrates example route templates and their behavior.</span></span>

| <span data-ttu-id="eea9f-837">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="eea9f-837">Route Template</span></span>                           | <span data-ttu-id="eea9f-838">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="eea9f-838">Example Matching URI</span></span>    | <span data-ttu-id="eea9f-839">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="eea9f-839">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="eea9f-840">Yalnızca `/hello`tek yol ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-840">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="eea9f-841">`Home``Page` eşleşir ve ayarlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-841">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="eea9f-842">`Contact``Page` eşleşir ve ayarlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-842">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="eea9f-843">`Products` denetleyicisi ve `List` eylemiyle eşlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-843">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="eea9f-844">`Products` denetleyicisi ve `Details` eylemine eşlenir (`id` 123 olarak ayarlanır).</span><span class="sxs-lookup"><span data-stu-id="eea9f-844">Maps to the `Products` controller and  `Details` action (`id` set to 123).</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="eea9f-845">`Home` denetleyicisi ve `Index` yöntemiyle eşlenir (`id` yok sayılır).</span><span class="sxs-lookup"><span data-stu-id="eea9f-845">Maps to the `Home` controller and `Index` method (`id` is ignored).</span></span>        |

<span data-ttu-id="eea9f-846">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-846">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="eea9f-847">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-847">Constraints and defaults can also be specified outside the route template.</span></span>

> [!TIP]
> <span data-ttu-id="eea9f-848"><xref:Microsoft.AspNetCore.Routing.Route>, istekleri eşleştirme gibi yerleşik yönlendirme uygulamalarının nasıl yapıldığını görmek için [günlük kaydını](xref:fundamentals/logging/index) etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-848">Enable [Logging](xref:fundamentals/logging/index) to see how the built-in routing implementations, such as <xref:Microsoft.AspNetCore.Routing.Route>, match requests.</span></span>

## <a name="reserved-routing-names"></a><span data-ttu-id="eea9f-849">Ayrılmış yönlendirme adları</span><span class="sxs-lookup"><span data-stu-id="eea9f-849">Reserved routing names</span></span>

<span data-ttu-id="eea9f-850">Aşağıdaki anahtar sözcükler ayrılmış isimlerdir ve yol adları veya parametreler olarak kullanılamaz:</span><span class="sxs-lookup"><span data-stu-id="eea9f-850">The following keywords are reserved names and can't be used as route names or parameters:</span></span>

* `action`
* `area`
* `controller`
* `handler`
* `page`

## <a name="route-constraint-reference"></a><span data-ttu-id="eea9f-851">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-851">Route constraint reference</span></span>

<span data-ttu-id="eea9f-852">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-852">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="eea9f-853">Rota kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda bir Evet/Hayır kararı getirir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-853">Route constraints generally inspect the route value associated via the route template and make a yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="eea9f-854">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-854">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="eea9f-855">Örneğin <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint>, HTTP fiiline bağlı olarak bir isteği kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-855">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="eea9f-856">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-856">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="eea9f-857">**Giriş doğrulaması**için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-857">Don't use constraints for **input validation**.</span></span> <span data-ttu-id="eea9f-858">**Giriş doğrulaması**için kısıtlamalar kullanılıyorsa, doğru bir hata iletisine sahip *400-Bad isteği* yerine *404-* olmayan bir Yanıt ile geçersiz giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-858">If constraints are used for **input validation**, invalid input results in a *404 - Not Found* response instead of a *400 - Bad Request* with an appropriate error message.</span></span> <span data-ttu-id="eea9f-859">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların **belirsizliğini ortadan** kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-859">Route constraints are used to **disambiguate** similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="eea9f-860">Aşağıdaki tabloda örnek yol kısıtlamaları ve bunların beklenen davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-860">The following table demonstrates example route constraints and their expected behavior.</span></span>

| <span data-ttu-id="eea9f-861">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="eea9f-861">constraint</span></span> | <span data-ttu-id="eea9f-862">Örnek</span><span class="sxs-lookup"><span data-stu-id="eea9f-862">Example</span></span> | <span data-ttu-id="eea9f-863">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="eea9f-863">Example Matches</span></span> | <span data-ttu-id="eea9f-864">Notlar</span><span class="sxs-lookup"><span data-stu-id="eea9f-864">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="eea9f-865">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="eea9f-865">`123456789`, `-123456789`</span></span> | <span data-ttu-id="eea9f-866">Herhangi bir tamsayıyla eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-866">Matches any integer</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="eea9f-867">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="eea9f-867">`true`, `FALSE`</span></span> | <span data-ttu-id="eea9f-868">`true` veya `false` eşleşir (büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="eea9f-868">Matches `true` or `false` (case-insensitive)</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="eea9f-869">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="eea9f-869">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="eea9f-870">Geçerli bir `DateTime` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-870">Matches a valid `DateTime` value (in the invariant culture - see warning)</span></span> |
| `decimal` | `{price:decimal}` | <span data-ttu-id="eea9f-871">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="eea9f-871">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="eea9f-872">Geçerli bir `decimal` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-872">Matches a valid `decimal` value (in the invariant culture - see warning)</span></span> |
| `double` | `{weight:double}` | <span data-ttu-id="eea9f-873">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="eea9f-873">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="eea9f-874">Geçerli bir `double` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-874">Matches a valid `double` value (in the invariant culture - see warning)</span></span> |
| `float` | `{weight:float}` | <span data-ttu-id="eea9f-875">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="eea9f-875">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="eea9f-876">Geçerli bir `float` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-876">Matches a valid `float` value (in the invariant culture - see warning)</span></span> |
| `guid` | `{id:guid}` | <span data-ttu-id="eea9f-877">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="eea9f-877">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="eea9f-878">Geçerli bir `Guid` değeriyle eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-878">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="eea9f-879">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="eea9f-879">`123456789`, `-123456789`</span></span> | <span data-ttu-id="eea9f-880">Geçerli bir `long` değeriyle eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-880">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="eea9f-881">Dize en az 4 karakter olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-881">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `Richard` | <span data-ttu-id="eea9f-882">Dize 8 karakterden uzun olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-882">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="eea9f-883">Dize tam olarak 12 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-883">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="eea9f-884">Dize en az 8 ve en fazla 16 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-884">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="eea9f-885">Tamsayı değeri en az 18 olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-885">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="eea9f-886">Tamsayı değeri 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-886">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="eea9f-887">Tamsayı değeri en az 18 olmalı ancak 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-887">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="eea9f-888">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır (`a`-`z`, büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="eea9f-888">String must consist of one or more alphabetical characters (`a`-`z`, case-insensitive)</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="eea9f-889">Dize, normal ifadeyle eşleşmelidir (normal ifade tanımlama hakkında ipuçlarına bakın)</span><span class="sxs-lookup"><span data-stu-id="eea9f-889">String must match the regular expression (see tips about defining a regular expression)</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="eea9f-890">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır</span><span class="sxs-lookup"><span data-stu-id="eea9f-890">Used to enforce that a non-parameter value is present during URL generation</span></span> |

<span data-ttu-id="eea9f-891">Birden çok, iki nokta üst üste sınırlı kısıtlama tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-891">Multiple, colon-delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="eea9f-892">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="eea9f-892">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="eea9f-893">URL 'YI doğrulayan ve bir CLR türüne (örneğin `int` veya `DateTime`) dönüştürülen yol kısıtlamaları her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-893">Route constraints that verify the URL and are converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture.</span></span> <span data-ttu-id="eea9f-894">Bu kısıtlamalar, URL 'nin yerelleştirilemeyen olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-894">These constraints assume that the URL is non-localizable.</span></span> <span data-ttu-id="eea9f-895">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-895">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="eea9f-896">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-896">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="eea9f-897">Örneğin, `float` kısıtlaması yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-897">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="eea9f-898">Normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="eea9f-898">Regular expressions</span></span>

<span data-ttu-id="eea9f-899">ASP.NET Core Framework, normal ifade oluşturucusuna `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-899">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="eea9f-900">Bu üyelerin açıklaması için bkz. <xref:System.Text.RegularExpressions.RegexOptions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-900">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="eea9f-901">Normal ifadeler, C# yönlendirme ve dil tarafından kullanılanlarla aynı sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-901">Regular expressions use delimiters and tokens similar to those used by Routing and the C# language.</span></span> <span data-ttu-id="eea9f-902">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-902">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="eea9f-903">`^\d{3}-\d{2}-\d{4}$` normal ifade ' i yönlendirmede kullanmak için, ifadede, C# kaynak `\` dosyadaki `\\` (çift ters eğik çizgi) karakter olarak dize içinde belirtilen `\` (tek ters eğik çizgi) karakterleri olmalıdır (tam [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string)kullanmadıkça).</span><span class="sxs-lookup"><span data-stu-id="eea9f-903">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in routing, the expression must have the `\` (single backslash) characters provided in the string as `\\` (double backslash) characters in the C# source file in order to escape the `\` string escape character (unless using [verbatim string literals](/dotnet/csharp/language-reference/keywords/string)).</span></span> <span data-ttu-id="eea9f-904">Yönlendirme parametresi sınırlayıcı karakterlerini (`{`, `}`, `[`, `]`) atlamak için, ifadedeki karakterleri çift (`{{`, `}`, `[[`, `]]`).</span><span class="sxs-lookup"><span data-stu-id="eea9f-904">To escape routing parameter delimiter characters (`{`, `}`, `[`, `]`), double the characters in the expression (`{{`, `}`, `[[`, `]]`).</span></span> <span data-ttu-id="eea9f-905">Aşağıdaki tabloda, bir normal ifade ve kaçan sürümü gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-905">The following table shows a regular expression and the escaped version.</span></span>

| <span data-ttu-id="eea9f-906">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="eea9f-906">Regular Expression</span></span>    | <span data-ttu-id="eea9f-907">Kaçan normal Ifade</span><span class="sxs-lookup"><span data-stu-id="eea9f-907">Escaped Regular Expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="eea9f-908">Yönlendirmelerde kullanılan normal ifadeler, genellikle şapka işareti (`^`) karakteriyle başlar ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-908">Regular expressions used in routing often start with the caret (`^`) character and match starting position of the string.</span></span> <span data-ttu-id="eea9f-909">İfadeler genellikle dolar işareti (`$`) karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-909">The expressions often end with the dollar sign (`$`) character and match end of the string.</span></span> <span data-ttu-id="eea9f-910">`^` ve `$` karakterler, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-910">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="eea9f-911">`^` ve `$` karakterleri olmadan normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-911">Without the `^` and `$` characters, the regular expression match any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="eea9f-912">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-912">The following table provides examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="eea9f-913">İfade</span><span class="sxs-lookup"><span data-stu-id="eea9f-913">Expression</span></span>   | <span data-ttu-id="eea9f-914">Dize</span><span class="sxs-lookup"><span data-stu-id="eea9f-914">String</span></span>    | <span data-ttu-id="eea9f-915">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-915">Match</span></span> | <span data-ttu-id="eea9f-916">Yorum</span><span class="sxs-lookup"><span data-stu-id="eea9f-916">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-917">merhaba</span><span class="sxs-lookup"><span data-stu-id="eea9f-917">hello</span></span>     | <span data-ttu-id="eea9f-918">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-918">Yes</span></span>   | <span data-ttu-id="eea9f-919">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-919">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-920">123abc456</span><span class="sxs-lookup"><span data-stu-id="eea9f-920">123abc456</span></span> | <span data-ttu-id="eea9f-921">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-921">Yes</span></span>   | <span data-ttu-id="eea9f-922">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-922">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-923">mz</span><span class="sxs-lookup"><span data-stu-id="eea9f-923">mz</span></span>        | <span data-ttu-id="eea9f-924">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-924">Yes</span></span>   | <span data-ttu-id="eea9f-925">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="eea9f-925">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-926">MZ</span><span class="sxs-lookup"><span data-stu-id="eea9f-926">MZ</span></span>        | <span data-ttu-id="eea9f-927">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-927">Yes</span></span>   | <span data-ttu-id="eea9f-928">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="eea9f-928">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="eea9f-929">merhaba</span><span class="sxs-lookup"><span data-stu-id="eea9f-929">hello</span></span>     | <span data-ttu-id="eea9f-930">Hayır</span><span class="sxs-lookup"><span data-stu-id="eea9f-930">No</span></span>    | <span data-ttu-id="eea9f-931">Yukarıdaki `^` ve `$` bakın</span><span class="sxs-lookup"><span data-stu-id="eea9f-931">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="eea9f-932">123abc456</span><span class="sxs-lookup"><span data-stu-id="eea9f-932">123abc456</span></span> | <span data-ttu-id="eea9f-933">Hayır</span><span class="sxs-lookup"><span data-stu-id="eea9f-933">No</span></span>    | <span data-ttu-id="eea9f-934">Yukarıdaki `^` ve `$` bakın</span><span class="sxs-lookup"><span data-stu-id="eea9f-934">See `^` and `$` above</span></span> |

<span data-ttu-id="eea9f-935">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="eea9f-935">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="eea9f-936">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-936">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="eea9f-937">Örneğin `{action:regex(^(list|get|create)$)}`, yalnızca `action` Route değeri `list`, `get`veya `create`ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-937">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="eea9f-938">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-938">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="eea9f-939">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğüne (bir şablon içinde satır içi değil) geçirilen kısıtlamalar da normal ifadeler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-939">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="custom-route-constraints"></a><span data-ttu-id="eea9f-940">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="eea9f-940">Custom Route Constraints</span></span>

<span data-ttu-id="eea9f-941">Yerleşik yol kısıtlamalarına ek olarak, <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> arabirimi uygulayarak özel yol kısıtlamaları oluşturulabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-941">In addition to the built-in route constraints, custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="eea9f-942"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> arabirimi, kısıtlama karşılandıysanız `true` döndüren `Match`tek bir yöntem içerir ve aksi takdirde `false`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-942">The <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface contains a single method, `Match`, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="eea9f-943">Özel bir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>kullanmak için, yol kısıtlama türü uygulamanın hizmet kapsayıcısında uygulamanın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> kayıtlı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-943">To use a custom <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the app's service container.</span></span> <span data-ttu-id="eea9f-944"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, yol kısıtlama anahtarlarını bu kısıtlamaları doğrulayan <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-944">A <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> is a dictionary that maps route constraint keys to <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> implementations that validate those constraints.</span></span> <span data-ttu-id="eea9f-945">Bir uygulamanın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, bir hizmetin parçası olarak `Startup.ConfigureServices` güncelleştirilebilen olabilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan `services.Configure<RouteOptions>`ile yapılandırma.</span><span class="sxs-lookup"><span data-stu-id="eea9f-945">An app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="eea9f-946">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-946">For example:</span></span>

```csharp
services.AddRouting(options =>
{
    options.ConstraintMap.Add("customName", typeof(MyCustomConstraint));
});
```

<span data-ttu-id="eea9f-947">Kısıtlama daha sonra, kısıtlama türü kaydedilirken belirtilen ad kullanılarak yollara her zamanki şekilde uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-947">The constraint can then be applied to routes in the usual manner, using the name specified when registering the constraint type.</span></span> <span data-ttu-id="eea9f-948">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-948">For example:</span></span>

```csharp
[HttpGet("{id:customName}")]
public ActionResult<string> Get(string id)
```

## <a name="parameter-transformer-reference"></a><span data-ttu-id="eea9f-949">Parametre transformatörü başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-949">Parameter transformer reference</span></span>

<span data-ttu-id="eea9f-950">Parametre dönüştürücüler:</span><span class="sxs-lookup"><span data-stu-id="eea9f-950">Parameter transformers:</span></span>

* <span data-ttu-id="eea9f-951"><xref:Microsoft.AspNetCore.Routing.Route>için bağlantı oluştururken yürütün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-951">Execute when generating a link for a <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>
* <span data-ttu-id="eea9f-952">`Microsoft.AspNetCore.Routing.IOutboundParameterTransformer`uygulayın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-952">Implement `Microsoft.AspNetCore.Routing.IOutboundParameterTransformer`.</span></span>
* <span data-ttu-id="eea9f-953"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>kullanılarak yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-953">Are configured using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>.</span></span>
* <span data-ttu-id="eea9f-954">Parametrenin yol değerini alın ve yeni bir dize değerine dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-954">Take the parameter's route value and transform it to a new string value.</span></span>
* <span data-ttu-id="eea9f-955">Oluşturulan bağlantıda Dönüştürülmüş değerin kullanılmasına neden olacak.</span><span class="sxs-lookup"><span data-stu-id="eea9f-955">Result in using the transformed value in the generated link.</span></span>

<span data-ttu-id="eea9f-956">Örneğin, `Url.Action(new { article = "MyTestArticle" })` ile `blog\{article:slugify}` yol düzeninde özel `slugify` parametresi transformatörü `blog\my-test-article`üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-956">For example, a custom `slugify` parameter transformer in route pattern `blog\{article:slugify}` with `Url.Action(new { article = "MyTestArticle" })` generates `blog\my-test-article`.</span></span>

<span data-ttu-id="eea9f-957">Bir parametre transformatörü bir yol düzeninde kullanmak için, önce `Startup.ConfigureServices`<xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> kullanarak yapılandırın:</span><span class="sxs-lookup"><span data-stu-id="eea9f-957">To use a parameter transformer in a route pattern, configure it first using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddRouting(options =>
{
    // Replace the type and the name used to refer to it with your own
    // IOutboundParameterTransformer implementation
    options.ConstraintMap["slugify"] = typeof(SlugifyParameterTransformer);
});
```

<span data-ttu-id="eea9f-958">Parametre dönüştürücüler, bir uç noktanın çözümlediği URI 'yi dönüştürmek için çerçevesi tarafından kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-958">Parameter transformers are used by the framework to transform the URI where an endpoint resolves.</span></span> <span data-ttu-id="eea9f-959">Örneğin, ASP.NET Core MVC, `area`, `controller`, `action`ve `page`eşleşecek şekilde kullanılan rota değerini dönüştürmek için parametre dönüştürücüler kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-959">For example, ASP.NET Core MVC uses parameter transformers to transform the route value used to match an `area`, `controller`, `action`, and `page`.</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller:slugify=Home}/{action:slugify=Index}/{id?}");
```

<span data-ttu-id="eea9f-960">Önceki yol ile eylem `SubscriptionManagementController.GetAll()`, URI `/subscription-management/get-all`ile eşleştirilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-960">With the preceding route, the action `SubscriptionManagementController.GetAll()` is matched with the URI `/subscription-management/get-all`.</span></span> <span data-ttu-id="eea9f-961">Bir parametre transformatörü bir bağlantı oluşturmak için kullanılan rota değerlerini değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-961">A parameter transformer doesn't change the route values used to generate a link.</span></span> <span data-ttu-id="eea9f-962">Örneğin, `Url.Action("GetAll", "SubscriptionManagement")` çıkışları `/subscription-management/get-all`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-962">For example, `Url.Action("GetAll", "SubscriptionManagement")` outputs `/subscription-management/get-all`.</span></span>

<span data-ttu-id="eea9f-963">ASP.NET Core, oluşturulan yollarla bir parametre dönüştürücüler kullanmak için API kuralları sağlar:</span><span class="sxs-lookup"><span data-stu-id="eea9f-963">ASP.NET Core provides API conventions for using a parameter transformers with generated routes:</span></span>

* <span data-ttu-id="eea9f-964">ASP.NET Core MVC 'nin `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API kuralı vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-964">ASP.NET Core MVC has the `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API convention.</span></span> <span data-ttu-id="eea9f-965">Bu kural, uygulamadaki tüm öznitelik yollarına belirtilen bir parametre transformatörü uygular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-965">This convention applies a specified parameter transformer to all attribute routes in the app.</span></span> <span data-ttu-id="eea9f-966">Parametre transformatörü, öznitelik yol belirteçlerini değiştirildiklerinde dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-966">The parameter transformer transforms attribute route tokens as they are replaced.</span></span> <span data-ttu-id="eea9f-967">Daha fazla bilgi için bkz. [belirteç değişimini özelleştirmek için bir parametre transformatörü kullanma](/aspnet/core/mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span><span class="sxs-lookup"><span data-stu-id="eea9f-967">For more information, see [Use a parameter transformer to customize token replacement](/aspnet/core/mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span></span>
* <span data-ttu-id="eea9f-968">Razor Pages `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API kuralına sahiptir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-968">Razor Pages has the `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API convention.</span></span> <span data-ttu-id="eea9f-969">Bu kural, belirtilen bir parametre transformatörü otomatik olarak keşfedilen Razor Pages uygular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-969">This convention applies a specified parameter transformer to all automatically discovered Razor Pages.</span></span> <span data-ttu-id="eea9f-970">Parametre transformatörü Razor Pages yolların klasör ve dosya adı segmentlerini dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-970">The parameter transformer transforms the folder and file name segments of Razor Pages routes.</span></span> <span data-ttu-id="eea9f-971">Daha fazla bilgi için bkz. [sayfa yollarını özelleştirmek için bir parametre transformatörü kullanma](/aspnet/core/razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span><span class="sxs-lookup"><span data-stu-id="eea9f-971">For more information, see [Use a parameter transformer to customize page routes](/aspnet/core/razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span></span>

## <a name="url-generation-reference"></a><span data-ttu-id="eea9f-972">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-972">URL generation reference</span></span>

<span data-ttu-id="eea9f-973">Aşağıdaki örnek, yol değerlerinin bir sözlüğü ve bir <xref:Microsoft.AspNetCore.Routing.RouteCollection>için bir yol bağlantısının nasıl oluşturulacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-973">The following example shows how to generate a link to a route given a dictionary of route values and a <xref:Microsoft.AspNetCore.Routing.RouteCollection>.</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_Dictionary)]

<span data-ttu-id="eea9f-974">Yukarıdaki örnek sonunda oluşturulan <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> `/package/create/123`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-974">The <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> generated at the end of the preceding sample is `/package/create/123`.</span></span> <span data-ttu-id="eea9f-975">Sözlük, "paket yolunu Izle" şablonunun `package/{operation}/{id}``operation` ve `id` yol değerlerini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-975">The dictionary supplies the `operation` and `id` route values of the "Track Package Route" template, `package/{operation}/{id}`.</span></span> <span data-ttu-id="eea9f-976">Ayrıntılar için, [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümünde veya [örnek uygulamada](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples)örnek koda bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-976">For details, see the sample code in the [Use Routing Middleware](#use-routing-middleware) section or the [sample app](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples).</span></span>

<span data-ttu-id="eea9f-977"><xref:Microsoft.AspNetCore.Routing.VirtualPathContext> oluşturucusunun ikinci parametresi bir *ortam değerleri*koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-977">The second parameter to the <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="eea9f-978">Ortam değerleri, bir geliştiricinin bir istek bağlamı içinde belirtmesi gereken değer sayısını sınırlandırdığından kullanım için uygundur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-978">Ambient values are convenient to use because they limit the number of values a developer must specify within a request context.</span></span> <span data-ttu-id="eea9f-979">Geçerli isteğin geçerli yol değerleri, bağlantı oluşturma için çevresel değerler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-979">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="eea9f-980">ASP.NET Core MVC uygulamasının `HomeController``About` eyleminde,&mdash;ortam değeri `Home` `Index` eyleme bağlamak için denetleyici yolu değerini belirtmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-980">In an ASP.NET Core MVC app's `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action&mdash;the ambient value of `Home` is used.</span></span>

<span data-ttu-id="eea9f-981">Bir parametreyle eşleşmeyen çevresel değerler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-981">Ambient values that don't match a parameter are ignored.</span></span> <span data-ttu-id="eea9f-982">Ayrıca, açıkça sağlanmış bir değer çevresel değeri geçersiz kıldığında çevresel değerler de yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-982">Ambient values are also ignored when an explicitly provided value overrides the ambient value.</span></span> <span data-ttu-id="eea9f-983">Eşleştirme, URL 'de soldan sağa doğru gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-983">Matching occurs from left to right in the URL.</span></span>

<span data-ttu-id="eea9f-984">Açık olarak sağlanmış ancak yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-984">Values explicitly provided but that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="eea9f-985">Aşağıdaki tabloda `{controller}/{action}/{id?}`yol şablonu kullanılırken sonuç gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-985">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="eea9f-986">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="eea9f-986">Ambient Values</span></span>                     | <span data-ttu-id="eea9f-987">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="eea9f-987">Explicit Values</span></span>                        | <span data-ttu-id="eea9f-988">Sonuç</span><span class="sxs-lookup"><span data-stu-id="eea9f-988">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="eea9f-989">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-989">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-990">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-990">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="eea9f-991">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-991">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-992">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-992">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="eea9f-993">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="eea9f-993">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="eea9f-994">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-994">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="eea9f-995">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-995">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-996">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="eea9f-996">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

<span data-ttu-id="eea9f-997">Bir rotada bir parametreye karşılık gelen bir varsayılan değer varsa ve bu değer açıkça sağlanmışsa, varsayılan değerle eşleşmelidir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-997">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
    defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="eea9f-998">Bağlantı oluşturma yalnızca `controller` ve `action` için eşleşen değerler sağlandığında bu yol için bir bağlantı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-998">Link generation only generates a link for this route when the matching values for `controller` and `action` are provided.</span></span>

## <a name="complex-segments"></a><span data-ttu-id="eea9f-999">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="eea9f-999">Complex segments</span></span>

<span data-ttu-id="eea9f-1000">Karmaşık segmentler (örneğin `[Route("/x{token}y")]`), sabit değerli olmayan değişmez değerler ile sağdan sola eşleştirilirken işlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1000">Complex segments (for example `[Route("/x{token}y")]`) are processed by matching up literals from right to left in a non-greedy way.</span></span> <span data-ttu-id="eea9f-1001">Karmaşık segmentlerin nasıl eşleştirileceği hakkında ayrıntılı bir açıklama için [bu koda](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1001">See [this code](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) for a detailed explanation of how complex segments are matched.</span></span> <span data-ttu-id="eea9f-1002">[Kod örneği](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) ASP.NET Core tarafından kullanılmaz, ancak karmaşık segmentler hakkında iyi bir açıklama sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1002">The [code sample](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) is not used by ASP.NET Core, but it provides a good explanation of complex segments.</span></span>
<!-- While that code is no longer used by ASP.NET Core for complex segment matching, it provides a good match to the current algorithm. The [current code](https://github.com/dotnet/AspNetCore/blob/91514c9af7e0f4c44029b51f05a01c6fe4c96e4c/src/Http/Routing/src/Matching/DfaMatcherBuilder.cs#L227-L244) is too abstracted from matching to be useful for understanding complex segment matching.
-->

::: moniker-end

::: moniker range="< aspnetcore-2.2"

<span data-ttu-id="eea9f-1003">Yönlendirme, istek URI 'Lerini rota işleyicileriyle eşleştirmekten ve gelen istekleri gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1003">Routing is responsible for mapping request URIs to route handlers and dispatching an incoming requests.</span></span> <span data-ttu-id="eea9f-1004">Yollar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1004">Routes are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="eea9f-1005">Yol, isteğe bağlı olarak istekte bulunan URL 'den değerleri ayıklayabilir ve bu değerler, istek işleme için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1005">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="eea9f-1006">Uygulamadan yapılandırılan yollar kullanıldığında, yönlendirme, yol işleyicileriyle eşlenen URL 'Ler oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1006">Using configured routes from the app, routing is able to generate URLs that map to route handlers.</span></span>

<span data-ttu-id="eea9f-1007">ASP.NET Core 2,1 ' de en son yönlendirme senaryolarını kullanmak için `Startup.ConfigureServices`MVC Hizmetleri kaydının [Uyumluluk sürümünü](xref:mvc/compatibility-version) belirtin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1007">To use the latest routing scenarios in ASP.NET Core 2.1, specify the [compatibility version](xref:mvc/compatibility-version) to the MVC services registration in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc()
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_1);
```

> [!IMPORTANT]
> <span data-ttu-id="eea9f-1008">Bu belge, alt düzey ASP.NET Core yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1008">This document covers low-level ASP.NET Core routing.</span></span> <span data-ttu-id="eea9f-1009">ASP.NET Core MVC yönlendirme hakkında daha fazla bilgi için bkz. <xref:mvc/controllers/routing>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1009">For information on ASP.NET Core MVC routing, see <xref:mvc/controllers/routing>.</span></span> <span data-ttu-id="eea9f-1010">Razor Pages 'de yönlendirme kuralları hakkında bilgi için bkz. <xref:razor-pages/razor-pages-conventions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1010">For information on routing conventions in Razor Pages, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="eea9f-1011">[Görüntüleme veya indirme örnek kodu](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([nasıl indirileceğini](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="eea9f-1011">[View or download sample code](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([how to download](xref:index#how-to-download-a-sample))</span></span>

## <a name="routing-basics"></a><span data-ttu-id="eea9f-1012">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-1012">Routing basics</span></span>

<span data-ttu-id="eea9f-1013">Çoğu uygulama, URL 'Lerin okunabilir ve anlamlı olması için temel ve açıklayıcı bir yönlendirme şeması seçmelidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1013">Most apps should choose a basic and descriptive routing scheme so that URLs are readable and meaningful.</span></span> <span data-ttu-id="eea9f-1014">Varsayılan geleneksel yol `{controller=Home}/{action=Index}/{id?}`:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1014">The default conventional route `{controller=Home}/{action=Index}/{id?}`:</span></span>

* <span data-ttu-id="eea9f-1015">Temel ve açıklayıcı bir yönlendirme düzenini destekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1015">Supports a basic and descriptive routing scheme.</span></span>
* <span data-ttu-id="eea9f-1016">, UI tabanlı uygulamalar için kullanışlı bir başlangıç noktasıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1016">Is a useful starting point for UI-based apps.</span></span>

<span data-ttu-id="eea9f-1017">Geliştiriciler, [öznitelik yönlendirme](xref:mvc/controllers/routing#attribute-routing) veya adanmış geleneksel yollar kullanarak özel durumlarda (örneğin, blog ve e-ticaret uç noktaları) bir uygulamanın yüksek trafik bölümlerine daha fazla terse yolu ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1017">Developers commonly add additional terse routes to high-traffic areas of an app in specialized situations (for example, blog and ecommerce endpoints) using [attribute routing](xref:mvc/controllers/routing#attribute-routing) or dedicated conventional routes.</span></span>

<span data-ttu-id="eea9f-1018">Web API 'Leri, uygulamanın işlevselliğini HTTP fiilleri tarafından temsil edilen bir kaynak kümesi olarak modellemek için öznitelik yönlendirmeyi kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1018">Web APIs should use attribute routing to model the app's functionality as a set of resources where operations are represented by HTTP verbs.</span></span> <span data-ttu-id="eea9f-1019">Bu, aynı mantıksal kaynaktaki birçok işlemin (örneğin, GET, POST) aynı URL 'YI kullanacağı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1019">This means that many operations (for example, GET, POST) on the same logical resource will use the same URL.</span></span> <span data-ttu-id="eea9f-1020">Öznitelik yönlendirme, bir API 'nin Genel uç nokta yerleşimini dikkatle tasarlamak için gereken bir denetim düzeyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1020">Attribute routing provides a level of control that's needed to carefully design an API's public endpoint layout.</span></span>

<span data-ttu-id="eea9f-1021">Razor Pages uygulamalar, bir uygulamanın *Sayfalar* klasöründe adlandırılmış kaynaklara hizmeti sağlamak için varsayılan geleneksel yönlendirmeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1021">Razor Pages apps use default conventional routing to serve named resources in the *Pages* folder of an app.</span></span> <span data-ttu-id="eea9f-1022">Razor Pages yönlendirme davranışını özelleştirmenizi sağlayan ek kurallar mevcuttur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1022">Additional conventions are available that allow you to customize Razor Pages routing behavior.</span></span> <span data-ttu-id="eea9f-1023">Daha fazla bilgi için bkz. <xref:razor-pages/index> ve <xref:razor-pages/razor-pages-conventions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1023">For more information, see <xref:razor-pages/index> and <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="eea9f-1024">URL oluşturma desteği, uygulamanın, uygulamayı birbirine bağlamak için sabit kodlama URL 'Leri olmadan geliştirilebilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1024">URL generation support allows the app to be developed without hard-coding URLs to link the app together.</span></span> <span data-ttu-id="eea9f-1025">Bu destek, temel bir yönlendirme yapılandırmasıyla başlayıp uygulamanın kaynak düzeni belirlendikten sonra yolların değiştirilmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1025">This support allows for starting with a basic routing configuration and modifying the routes after the app's resource layout is determined.</span></span>

<span data-ttu-id="eea9f-1026">Yönlendirme, *yolları* kullanır (<xref:Microsoft.AspNetCore.Routing.IRouter>uygulamalar):</span><span class="sxs-lookup"><span data-stu-id="eea9f-1026">Routing uses *routes* (implementations of <xref:Microsoft.AspNetCore.Routing.IRouter>) to:</span></span>

* <span data-ttu-id="eea9f-1027">Gelen istekleri *Rota işleyicilerine*eşleyin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1027">Map incoming requests to *route handlers*.</span></span>
* <span data-ttu-id="eea9f-1028">Yanıtlarda kullanılan URL 'Leri oluşturun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1028">Generate the URLs used in responses.</span></span>

<span data-ttu-id="eea9f-1029">Varsayılan olarak, bir uygulama tek bir yollar koleksiyonuna sahiptir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1029">By default, an app has a single collection of routes.</span></span> <span data-ttu-id="eea9f-1030">Bir istek geldiğinde koleksiyondaki yollar, koleksiyonda var olan sırada işlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1030">When a request arrives, the routes in the collection are processed in the order that they exist in the collection.</span></span> <span data-ttu-id="eea9f-1031">Çerçeve, koleksiyondaki her bir rotada <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> yöntemini çağırarak, gelen istek URL 'sini koleksiyondaki bir yola eşleştirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1031">The framework attempts to match an incoming request URL to a route in the collection by calling the <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> method on each route in the collection.</span></span> <span data-ttu-id="eea9f-1032">Yanıt, yönlendirme bilgilerine göre URL (örneğin, yeniden yönlendirme veya bağlantılar için) oluşturmak için yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1032">A response can use routing to generate URLs (for example, for redirection or links) based on route information and thus avoid hard-coded URLs, which helps maintainability.</span></span>

<span data-ttu-id="eea9f-1033">Yönlendirme sistemi aşağıdaki özelliklere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1033">The routing system has the following characteristics:</span></span>

* <span data-ttu-id="eea9f-1034">Yol şablonu sözdizimi, simgeleştirilmiş yol parametrelerine sahip yolları tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1034">Route template syntax is used to define routes with tokenized route parameters.</span></span>
* <span data-ttu-id="eea9f-1035">Geleneksel stil ve öznitelik stili uç nokta yapılandırmasına izin verilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1035">Conventional-style and attribute-style endpoint configuration is permitted.</span></span>
* <span data-ttu-id="eea9f-1036"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, bir URL parametresinin belirli bir uç nokta kısıtlaması için geçerli bir değer içerip içermediğini belirlemekte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1036"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> is used to determine whether a URL parameter contains a valid value for a given endpoint constraint.</span></span>
* <span data-ttu-id="eea9f-1037">MVC/Razor Pages gibi uygulama modelleri, yönlendirme senaryolarının öngörülebilir bir uygulaması olan tüm yollarını kaydeder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1037">App models, such as MVC/Razor Pages, register all of their routes, which have a predictable implementation of routing scenarios.</span></span>
* <span data-ttu-id="eea9f-1038">Yanıt, yönlendirme bilgilerine göre URL (örneğin, yeniden yönlendirme veya bağlantılar için) oluşturmak için yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1038">A response can use routing to generate URLs (for example, for redirection or links) based on route information and thus avoid hard-coded URLs, which helps maintainability.</span></span>
* <span data-ttu-id="eea9f-1039">URL oluşturma, rastgele genişletilebilirliği destekleyen yollara dayalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1039">URL generation is based on routes, which support arbitrary extensibility.</span></span> <span data-ttu-id="eea9f-1040"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper>, URL 'Leri derlemek için yöntemler sunar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1040"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper> offers methods to build URLs.</span></span>

<span data-ttu-id="eea9f-1041">Yönlendirme, <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> sınıfı tarafından, [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1041">Routing is connected to the [middleware](xref:fundamentals/middleware/index) pipeline by the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> class.</span></span> <span data-ttu-id="eea9f-1042">[ASP.NET Core MVC](xref:mvc/overview) , yapılandırma kapsamında bir ara yazılım ardışık düzenine yönlendirme ekler ve MVC ve Razor Pages uygulamalarında yönlendirmeyi işler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1042">[ASP.NET Core MVC](xref:mvc/overview) adds routing to the middleware pipeline as part of its configuration and handles routing in MVC and Razor Pages apps.</span></span> <span data-ttu-id="eea9f-1043">Tek başına bileşen olarak yönlendirmeyi nasıl kullanacağınızı öğrenmek için [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1043">To learn how to use routing as a standalone component, see the [Use Routing Middleware](#use-routing-middleware) section.</span></span>

### <a name="url-matching"></a><span data-ttu-id="eea9f-1044">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-1044">URL matching</span></span>

<span data-ttu-id="eea9f-1045">URL eşleştirme, yönlendirmenin bir *işleyiciye*gelen istek gönderdiğine yönelik işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1045">URL matching is the process by which routing dispatches an incoming request to a *handler*.</span></span> <span data-ttu-id="eea9f-1046">Bu işlem, URL yolundaki verileri temel alır, ancak istekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1046">This process is based on data in the URL path but can be extended to consider any data in the request.</span></span> <span data-ttu-id="eea9f-1047">Ayrı işleyicilere istek gönderme özelliği, bir uygulamanın boyutunu ve karmaşıklığını ölçeklendirmeye yönelik anahtardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1047">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an app.</span></span>

<span data-ttu-id="eea9f-1048">Gelen istekler, sırayla her bir rotada <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> yöntemini çağıran <xref:Microsoft.AspNetCore.Builder.RouterMiddleware>girer.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1048">Incoming requests enter the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware>, which calls the <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> method on each route in sequence.</span></span> <span data-ttu-id="eea9f-1049"><xref:Microsoft.AspNetCore.Routing.IRouter> örneği, [Routecontext. Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler*) değerini null olmayan bir <xref:Microsoft.AspNetCore.Http.RequestDelegate>ayarlayarak isteğin *işleneceğini* belirler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1049">The <xref:Microsoft.AspNetCore.Routing.IRouter> instance chooses whether to *handle* the request by setting the [RouteContext.Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler*) to a non-null <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span> <span data-ttu-id="eea9f-1050">Bir yol istek için bir işleyici ayarlarsa, yol işleme duraklar ve işleyici isteği işlemek için çağrılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1050">If a route sets a handler for the request, route processing stops, and the handler is invoked to process the request.</span></span> <span data-ttu-id="eea9f-1051">İsteği işlemek için yol işleyicisi bulunmazsa, ara yazılım istek ardışık düzeninde sonraki bir ara yazılım için isteği kapatır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1051">If no route handler is found to process the request, the middleware hands the request off to the next middleware in the request pipeline.</span></span>

<span data-ttu-id="eea9f-1052"><xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> birincil giriş, geçerli istekle ilişkili [Routecontext. HttpContext](xref:Microsoft.AspNetCore.Routing.RouteContext.HttpContext*) ' dir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1052">The primary input to <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> is the [RouteContext.HttpContext](xref:Microsoft.AspNetCore.Routing.RouteContext.HttpContext*) associated with the current request.</span></span> <span data-ttu-id="eea9f-1053">[Routecontext. Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler) ve [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData*) , bir yol eşleştirdikten sonra çıkış olarak ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1053">The [RouteContext.Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler) and [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData*) are outputs set after a route is matched.</span></span>

<span data-ttu-id="eea9f-1054"><xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> çağıran bir eşleşme de [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özelliklerinin özelliklerini, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1054">A match that calls <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> also sets the properties of the [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) to appropriate values based on the request processing performed thus far.</span></span>

<span data-ttu-id="eea9f-1055">[RouteData. Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) , rotada oluşturulan *yol değerlerinin* bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1055">[RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="eea9f-1056">Bu değerler genellikle URL 'YI simgeleştirerek belirlenir ve Kullanıcı girişini kabul etmek ya da uygulama içinde daha fazla kararlar almak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1056">These values are usually determined by tokenizing the URL and can be used to accept user input or to make further dispatching decisions inside the app.</span></span>

<span data-ttu-id="eea9f-1057">[RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) , eşleşen rotayla ilgili ek verilerin bir özellik çantadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1057">[RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="eea9f-1058"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*>, uygulamanın hangi yolun eşleştiğini temel alarak kararlar verebilmesi için durum verilerinin her bir rota ile ilişkilendirilmesini desteklemek amacıyla sağlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1058"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> are provided to support associating state data with each route so that the app can make decisions based on which route matched.</span></span> <span data-ttu-id="eea9f-1059">Bu değerler, geliştirici tarafından tanımlanır ve yönlendirme davranışını herhangi bir **şekilde etkilemez.**</span><span class="sxs-lookup"><span data-stu-id="eea9f-1059">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="eea9f-1060">Ayrıca, [RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) içinde bulunan değerler her türlü türden olabilir. Bu, [veri](xref:Microsoft.AspNetCore.Routing.RouteData.Values)dizeleri arasında dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1060">Additionally, values stashed in [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) can be of any type, in contrast to [RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values), which must be convertible to and from strings.</span></span>

<span data-ttu-id="eea9f-1061">[RouteData. yönlendiriciler](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) , isteği başarıyla eşleştirirken geçen yolların bir listesidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1061">[RouteData.Routers](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="eea9f-1062">Yollar bir diğerinin içinde iç içe olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1062">Routes can be nested inside of one another.</span></span> <span data-ttu-id="eea9f-1063"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers> özelliği, bir eşleşme ile sonuçlanan yolların mantıksal ağacı aracılığıyla yolu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1063">The <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="eea9f-1064">Genellikle, <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> ilk öğe yol koleksiyonudur ve URL oluşturma için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1064">Generally, the first item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route collection and should be used for URL generation.</span></span> <span data-ttu-id="eea9f-1065"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers> son öğe, eşleşen yol işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1065">The last item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route handler that matched.</span></span>

<a name="lg"></a>

### <a name="url-generation"></a><span data-ttu-id="eea9f-1066">URL oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-1066">URL generation</span></span>

<span data-ttu-id="eea9f-1067">URL oluşturma, yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturabileceği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1067">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="eea9f-1068">Bu, yönlendirme işleyicileri ve bunlara erişen URL 'Ler arasındaki mantıksal bir ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1068">This allows for a logical separation between route handlers and the URLs that access them.</span></span>

<span data-ttu-id="eea9f-1069">URL oluşturma benzer bir yinelemeli işlem izler, ancak yol koleksiyonunun <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> metoduna çağıran kullanıcı veya çerçeve kodu ile başlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1069">URL generation follows a similar iterative process, but it starts with user or framework code calling into the <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> method of the route collection.</span></span> <span data-ttu-id="eea9f-1070">Her *rotada* null olmayan bir <xref:Microsoft.AspNetCore.Routing.VirtualPathData> döndürülene kadar <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> yöntemi sırayla çağırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1070">Each *route* has its <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> method called in sequence until a non-null <xref:Microsoft.AspNetCore.Routing.VirtualPathData> is returned.</span></span>

<span data-ttu-id="eea9f-1071"><xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> birincil girişleri şunlardır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1071">The primary inputs to <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> are:</span></span>

* [<span data-ttu-id="eea9f-1072">VirtualPathContext. HttpContext</span><span class="sxs-lookup"><span data-stu-id="eea9f-1072">VirtualPathContext.HttpContext</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext)
* [<span data-ttu-id="eea9f-1073">VirtualPathContext. Values</span><span class="sxs-lookup"><span data-stu-id="eea9f-1073">VirtualPathContext.Values</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values)
* [<span data-ttu-id="eea9f-1074">VirtualPathContext. AmbientValues</span><span class="sxs-lookup"><span data-stu-id="eea9f-1074">VirtualPathContext.AmbientValues</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues)

<span data-ttu-id="eea9f-1075">Rotalar öncelikle <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> ve <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> tarafından sunulan yol değerlerini kullanarak bir URL oluşturma ve hangi değerlerin dahil edileceğini belirleyin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1075">Routes primarily use the route values provided by <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> and <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> to decide whether it's possible to generate a URL and what values to include.</span></span> <span data-ttu-id="eea9f-1076"><xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues>, geçerli istekle eşleşmeden üretilmiş olan rota değerleri kümesidir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1076">The <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> are the set of route values that were produced from matching the current request.</span></span> <span data-ttu-id="eea9f-1077">Buna karşılık <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values>, geçerli işlem için istenen URL 'nin nasıl oluşturulacağını belirten rota değerlerdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1077">In contrast, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> are the route values that specify how to generate the desired URL for the current operation.</span></span> <span data-ttu-id="eea9f-1078"><xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext>, bir yolun geçerli bağlamla ilişkili hizmetleri veya ek verileri alması durumunda sağlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1078">The <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext> is provided in case a route should obtain services or additional data associated with the current context.</span></span>

> [!TIP]
> <span data-ttu-id="eea9f-1079">Virtualpathcontext [. Values](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values*) öğesini [Virtualpathcontext. AmbientValues](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues*)için bir geçersiz kılma kümesi olarak düşünün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1079">Think of [VirtualPathContext.Values](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values*) as a set of overrides for the [VirtualPathContext.AmbientValues](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues*).</span></span> <span data-ttu-id="eea9f-1080">URL oluşturma, aynı rota veya yol değerlerini kullanan bağlantılar için URL 'Ler oluşturmak üzere geçerli istekten yol değerlerini yeniden kullanmayı dener.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1080">URL generation attempts to reuse route values from the current request to generate URLs for links using the same route or route values.</span></span>

<span data-ttu-id="eea9f-1081"><xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> çıkışı bir <xref:Microsoft.AspNetCore.Routing.VirtualPathData>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1081">The output of <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> is a <xref:Microsoft.AspNetCore.Routing.VirtualPathData>.</span></span> <span data-ttu-id="eea9f-1082"><xref:Microsoft.AspNetCore.Routing.VirtualPathData>, <xref:Microsoft.AspNetCore.Routing.RouteData>paraleldir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1082"><xref:Microsoft.AspNetCore.Routing.VirtualPathData> is a parallel of <xref:Microsoft.AspNetCore.Routing.RouteData>.</span></span> <span data-ttu-id="eea9f-1083"><xref:Microsoft.AspNetCore.Routing.VirtualPathData>, çıkış URL 'SI için <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> ve yol tarafından ayarlanması gereken bazı ek özellikleri içerir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1083"><xref:Microsoft.AspNetCore.Routing.VirtualPathData> contains the <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> for the output URL and some additional properties that should be set by the route.</span></span>

<span data-ttu-id="eea9f-1084">[VirtualPathData. VirtualPath](xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath*) özelliği, yol tarafından üretilen *sanal yolu* içerir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1084">The [VirtualPathData.VirtualPath](xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath*) property contains the *virtual path* produced by the route.</span></span> <span data-ttu-id="eea9f-1085">Gereksinimlerinize bağlı olarak, yolu daha fazla işlem yapmanız gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1085">Depending on your needs, you may need to process the path further.</span></span> <span data-ttu-id="eea9f-1086">Oluşturulan URL 'YI HTML 'de işlemek istiyorsanız, uygulamanın temel yolunu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1086">If you want to render the generated URL in HTML, prepend the base path of the app.</span></span>

<span data-ttu-id="eea9f-1087">[VirtualPathData. Router](xref:Microsoft.AspNetCore.Routing.VirtualPathData.Router*) , URL 'yi başarıyla oluşturmuş olan yola bir başvurudur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1087">The [VirtualPathData.Router](xref:Microsoft.AspNetCore.Routing.VirtualPathData.Router*) is a reference to the route that successfully generated the URL.</span></span>

<span data-ttu-id="eea9f-1088">[VirtualPathData. DataTokens](xref:Microsoft.AspNetCore.Routing.VirtualPathData.DataTokens*) özellikleri, URL 'yi oluşturan rotayla ilgili ek verilerin bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1088">The [VirtualPathData.DataTokens](xref:Microsoft.AspNetCore.Routing.VirtualPathData.DataTokens*) properties is a dictionary of additional data related to the route that generated the URL.</span></span> <span data-ttu-id="eea9f-1089">Bu, [RouteData. Datatoken](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*)'ların paraleldir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1089">This is the parallel of [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*).</span></span>

### <a name="create-routes"></a><span data-ttu-id="eea9f-1090">Yolları oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-1090">Create routes</span></span>

<span data-ttu-id="eea9f-1091">Yönlendirme, <xref:Microsoft.AspNetCore.Routing.IRouter>standart uygulama olarak <xref:Microsoft.AspNetCore.Routing.Route> sınıfını sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1091">Routing provides the <xref:Microsoft.AspNetCore.Routing.Route> class as the standard implementation of <xref:Microsoft.AspNetCore.Routing.IRouter>.</span></span> <span data-ttu-id="eea9f-1092"><xref:Microsoft.AspNetCore.Routing.Route>, <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> çağrıldığında URL yoluyla eşleştirilecek desenleri tanımlamak için *yol şablonu* sözdizimini kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1092"><xref:Microsoft.AspNetCore.Routing.Route> uses the *route template* syntax to define patterns to match against the URL path when <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> is called.</span></span> <span data-ttu-id="eea9f-1093"><xref:Microsoft.AspNetCore.Routing.Route>, <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> çağrıldığında bir URL oluşturmak için aynı yol şablonunu kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1093"><xref:Microsoft.AspNetCore.Routing.Route> uses the same route template to generate a URL when <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> is called.</span></span>

<span data-ttu-id="eea9f-1094">Çoğu uygulama, <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>tanımlanan benzer uzantı yöntemlerinden birini veya <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> çağırarak yollar oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1094">Most apps create routes by calling <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> or one of the similar extension methods defined on <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>.</span></span> <span data-ttu-id="eea9f-1095"><xref:Microsoft.AspNetCore.Routing.IRouteBuilder> uzantısı yöntemlerinden herhangi biri bir <xref:Microsoft.AspNetCore.Routing.Route> örneği oluşturur ve bunu yol koleksiyonuna ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1095">Any of the <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> extension methods create an instance of <xref:Microsoft.AspNetCore.Routing.Route> and add it to the route collection.</span></span>

<span data-ttu-id="eea9f-1096"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> yol işleyicisi parametresini kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1096"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> doesn't accept a route handler parameter.</span></span> <span data-ttu-id="eea9f-1097"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>, yalnızca <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>tarafından işlenen rotaları ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1097"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> only adds routes that are handled by the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span> <span data-ttu-id="eea9f-1098">Varsayılan işleyici bir `IRouter`ve işleyici isteği işleyemeyebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1098">The default handler is an `IRouter`, and the handler might not handle the request.</span></span> <span data-ttu-id="eea9f-1099">Örneğin, ASP.NET Core MVC genellikle yalnızca kullanılabilir bir denetleyici ve eylemle eşleşen istekleri işleyen varsayılan bir işleyici olarak yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1099">For example, ASP.NET Core MVC is typically configured as a default handler that only handles requests that match an available controller and action.</span></span> <span data-ttu-id="eea9f-1100">MVC 'de yönlendirme hakkında daha fazla bilgi için bkz. <xref:mvc/controllers/routing>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1100">To learn more about routing in MVC, see <xref:mvc/controllers/routing>.</span></span>

<span data-ttu-id="eea9f-1101">Aşağıdaki kod örneği, tipik bir ASP.NET Core MVC yol tanımı tarafından kullanılan <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> çağrısının bir örneğidir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1101">The following code example is an example of a <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> call used by a typical ASP.NET Core MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="eea9f-1102">Bu şablon bir URL yoluyla eşleşir ve yol değerlerini ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1102">This template matches a URL path and extracts the route values.</span></span> <span data-ttu-id="eea9f-1103">Örneğin, yol `/Products/Details/17` şu yol değerlerini üretir: `{ controller = Products, action = Details, id = 17 }`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1103">For example, the path `/Products/Details/17` generates the following route values: `{ controller = Products, action = Details, id = 17 }`.</span></span>

<span data-ttu-id="eea9f-1104">Rota değerleri, URL yolunun kesimlere bölünüyor ve rota şablonundaki *yol parametresi* adı ile her bir segmenti eşleştirerek belirlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1104">Route values are determined by splitting the URL path into segments and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="eea9f-1105">Yol parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1105">Route parameters are named.</span></span> <span data-ttu-id="eea9f-1106">Küme ayraçları içinde parametre adı eklenerek tanımlanan parametreler `{ ... }`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1106">The parameters defined by enclosing the parameter name in braces `{ ... }`.</span></span>

<span data-ttu-id="eea9f-1107">Yukarıdaki şablon, URL yolu `/` de eşleştirebilir ve `{ controller = Home, action = Index }`değerler üretecektir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1107">The preceding template could also match the URL path `/` and produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="eea9f-1108">Bu, `{controller}` ve `{action}` yol parametrelerinin varsayılan değerleri olduğu ve `id` Route parametresinin isteğe bağlı olduğu için oluşur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1108">This occurs because the `{controller}` and `{action}` route parameters have default values and the `id` route parameter is optional.</span></span> <span data-ttu-id="eea9f-1109">Eşittir işareti (`=`), yol parametresi adı parametre için varsayılan bir değer tanımladıktan sonra bir değer izler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1109">An equals sign (`=`) followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="eea9f-1110">Yol parametre adından sonra bir soru işareti (`?`) isteğe bağlı bir parametre tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1110">A question mark (`?`) after the route parameter name defines an optional parameter.</span></span>

<span data-ttu-id="eea9f-1111">Yol parametreleri varsayılan değer ile *her zaman* rota değeri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1111">Route parameters with a default value *always* produce a route value when the route matches.</span></span> <span data-ttu-id="eea9f-1112">Karşılık gelen bir URL yol kesimi yoksa isteğe bağlı parametreler yol değeri oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1112">Optional parameters don't produce a route value if there was no corresponding URL path segment.</span></span> <span data-ttu-id="eea9f-1113">Yol şablonu senaryolarının ve sözdiziminin kapsamlı bir açıklaması için [yol şablonu başvurusu](#route-template-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1113">See the [Route template reference](#route-template-reference) section for a thorough description of route template scenarios and syntax.</span></span>

<span data-ttu-id="eea9f-1114">Aşağıdaki örnekte, yol parametresi tanımı `{id:int}` `id` Route parametresi için bir [yol kısıtlaması](#route-constraint-reference) tanımlıyor:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1114">In the following example, the route parameter definition `{id:int}` defines a [route constraint](#route-constraint-reference) for the `id` route parameter:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="eea9f-1115">Bu şablon `/Products/Details/17` gibi bir URL yoluyla eşleşir ancak `/Products/Details/Apples`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1115">This template matches a URL path like `/Products/Details/17` but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="eea9f-1116">Yol kısıtlamaları <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> uygular ve yol değerlerini inceleyerek onları doğrular.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1116">Route constraints implement <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> and inspect route values to verify them.</span></span> <span data-ttu-id="eea9f-1117">Bu örnekte, yol değeri `id` bir tamsayıya dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1117">In this example, the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="eea9f-1118">Framework tarafından sunulan yol kısıtlamalarının açıklaması için bkz. [route-Constraint-Reference](#route-constraint-reference) .</span><span class="sxs-lookup"><span data-stu-id="eea9f-1118">See [route-constraint-reference](#route-constraint-reference) for an explanation of route constraints provided by the framework.</span></span>

<span data-ttu-id="eea9f-1119"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> ek aşırı yüklemeleri `constraints`, `dataTokens`ve `defaults`için değer kabul eder.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1119">Additional overloads of <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="eea9f-1120">Bu parametrelerin tipik kullanımı, anonim türdeki özellik adlarının yol parametre adlarıyla eşleşen anonim olarak yazılmış bir nesneyi geçirmektir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1120">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="eea9f-1121">Aşağıdaki <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> örnekleri eşdeğer yollar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1121">The following <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

> [!TIP]
> <span data-ttu-id="eea9f-1122">Kısıtlamaları ve Varsayılanları tanımlamaya yönelik satır içi sözdizimi basit yollar için kullanışlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1122">The inline syntax for defining constraints and defaults can be convenient for simple routes.</span></span> <span data-ttu-id="eea9f-1123">Ancak, satır içi sözdizimi tarafından desteklenmeyen veri belirteçleri gibi senaryolar vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1123">However, there are scenarios, such as data tokens, that aren't supported by inline syntax.</span></span>

<span data-ttu-id="eea9f-1124">Aşağıdaki örnek birkaç ek senaryoyu göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1124">The following example demonstrates a few additional scenarios:</span></span>

```csharp
routes.MapRoute(
    name: "blog",
    template: "Blog/{*article}",
    defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="eea9f-1125">Önceki şablon `/Blog/All-About-Routing/Introduction` gibi bir URL yoluyla eşleşir ve `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`değerleri ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1125">The preceding template matches a URL path like `/Blog/All-About-Routing/Introduction` and extracts the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="eea9f-1126">`controller` ve `action` için varsayılan yol değerleri, şablonda karşılık gelen hiçbir yol parametresi olmasa bile yol tarafından üretilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1126">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="eea9f-1127">Varsayılan değerler yol şablonunda belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1127">Default values can be specified in the route template.</span></span> <span data-ttu-id="eea9f-1128">`article` Route parametresi, yol parametre adından önce bir yıldız işareti (`*`) görünümüne göre *catch-all* olarak tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1128">The `article` route parameter is defined as a *catch-all* by the appearance of an asterisk (`*`) before the route parameter name.</span></span> <span data-ttu-id="eea9f-1129">Catch-all yol parametreleri, URL yolunun kalanını yakalar ve boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1129">Catch-all route parameters capture the remainder of the URL path and can also match the empty string.</span></span>

<span data-ttu-id="eea9f-1130">Aşağıdaki örnek yol kısıtlamalarını ve veri belirteçlerini ekler:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1130">The following example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="eea9f-1131">Önceki şablon `/en-US/Products/5` gibi bir URL yoluyla eşleşir ve `{ controller = Products, action = Details, id = 5 }` ve veri belirteçleri `{ locale = en-US }`değerleri ayıklar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1131">The preceding template matches a URL path like `/en-US/Products/5` and extracts the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Yereller Windows belirteçleri](routing/_static/tokens.png)

### <a name="route-class-url-generation"></a><span data-ttu-id="eea9f-1133">Yol sınıfı URL 'SI oluşturma</span><span class="sxs-lookup"><span data-stu-id="eea9f-1133">Route class URL generation</span></span>

<span data-ttu-id="eea9f-1134"><xref:Microsoft.AspNetCore.Routing.Route> sınıfı, yol değerlerini bir kümesini rota şablonuyla birleştirerek URL oluşturmayı da gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1134">The <xref:Microsoft.AspNetCore.Routing.Route> class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="eea9f-1135">Bu, URL yolunu eşleştirmenin mantıksal bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1135">This is logically the reverse process of matching the URL path.</span></span>

> [!TIP]
> <span data-ttu-id="eea9f-1136">URL oluşturmayı daha iyi anlamak için, oluşturmak istediğiniz URL 'YI düşünün ve sonra bir yönlendirme şablonunun bu URL ile nasıl eşleşeceğini düşünün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1136">To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="eea9f-1137">Hangi değerler üretilemidir?</span><span class="sxs-lookup"><span data-stu-id="eea9f-1137">What values would be produced?</span></span> <span data-ttu-id="eea9f-1138">Bu, URL oluşturma 'nın <xref:Microsoft.AspNetCore.Routing.Route> sınıfında nasıl çalıştığına ilişkin kaba bir eştir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1138">This is the rough equivalent of how URL generation works in the <xref:Microsoft.AspNetCore.Routing.Route> class.</span></span>

<span data-ttu-id="eea9f-1139">Aşağıdaki örnek genel ASP.NET Core MVC varsayılan yolunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1139">The following example uses a general ASP.NET Core MVC default route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="eea9f-1140">Yol değerleri `{ controller = Products, action = List }`, `/Products/List` URL 'SI oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1140">With the route values `{ controller = Products, action = List }`, the URL `/Products/List` is generated.</span></span> <span data-ttu-id="eea9f-1141">Yol değerleri, URL yolunu biçimlendirmek için karşılık gelen yol parametrelerinin yerine kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1141">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="eea9f-1142">`id`, isteğe bağlı bir yol parametresi olduğundan, `id`için bir değer olmadan URL başarıyla oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1142">Since `id` is an optional route parameter, the URL is successfully generated without a value for `id`.</span></span>

<span data-ttu-id="eea9f-1143">Yol değerleri `{ controller = Home, action = Index }`, `/` URL 'SI oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1143">With the route values `{ controller = Home, action = Index }`, the URL `/` is generated.</span></span> <span data-ttu-id="eea9f-1144">Belirtilen yol değerleri varsayılan değerlerle eşleşir ve varsayılan değerlere karşılık gelen segmentler güvenle atlanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1144">The provided route values match the default values, and the segments corresponding to the default values are safely omitted.</span></span>

<span data-ttu-id="eea9f-1145">Her iki URL de aşağıdaki yol tanımıyla (`/Home/Index` ve `/`) bir gidiş dönüş, URL oluşturmak için kullanılan rota değerlerini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1145">Both URLs generated round-trip with the following route definition (`/Home/Index` and `/`) produce the same route values that were used to generate the URL.</span></span>

> [!NOTE]
> <span data-ttu-id="eea9f-1146">ASP.NET Core MVC kullanan bir uygulama doğrudan yönlendirmeye çağırmak yerine URL oluşturmak için <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1146">An app using ASP.NET Core MVC should use <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="eea9f-1147">URL oluşturma hakkında daha fazla bilgi için [URL oluşturma başvurusu](#url-generation-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1147">For more information on URL generation, see the [Url generation reference](#url-generation-reference) section.</span></span>

## <a name="use-routing-middleware"></a><span data-ttu-id="eea9f-1148">Yönlendirme ara yazılımı kullanma</span><span class="sxs-lookup"><span data-stu-id="eea9f-1148">Use Routing Middleware</span></span>

<span data-ttu-id="eea9f-1149">Uygulamanın proje dosyasındaki [Microsoft. AspNetCore. app metapackage](xref:fundamentals/metapackage-app) öğesine başvurun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1149">Reference the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in the app's project file.</span></span>

<span data-ttu-id="eea9f-1150">`Startup.ConfigureServices`' de hizmet kapsayıcısına yönlendirme ekleyin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1150">Add routing to the service container in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_ConfigureServices&highlight=3)]

<span data-ttu-id="eea9f-1151">Yolların `Startup.Configure` yönteminde yapılandırılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1151">Routes must be configured in the `Startup.Configure` method.</span></span> <span data-ttu-id="eea9f-1152">Örnek uygulama aşağıdaki API 'Leri kullanır:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1152">The sample app uses the following APIs:</span></span>

* <xref:Microsoft.AspNetCore.Routing.RouteBuilder>
* <span data-ttu-id="eea9f-1153"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> &ndash; yalnızca HTTP GET istekleriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1153"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> &ndash; Matches only HTTP GET requests.</span></span>
* <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_RouteHandler)]

<span data-ttu-id="eea9f-1154">Aşağıdaki tabloda verilen URI 'Ler ile ilgili yanıtlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1154">The following table shows the responses with the given URIs.</span></span>

| <span data-ttu-id="eea9f-1155">{1&gt;URI&lt;1}</span><span class="sxs-lookup"><span data-stu-id="eea9f-1155">URI</span></span>                    | <span data-ttu-id="eea9f-1156">Yanıt</span><span class="sxs-lookup"><span data-stu-id="eea9f-1156">Response</span></span>                                          |
| ---------------------- | ------------------------------------------------- |
| `/package/create/3`    | <span data-ttu-id="eea9f-1157">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-1157">Hello!</span></span> <span data-ttu-id="eea9f-1158">Rota değerleri: [işlem, oluşturma], [kimlik, 3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-1158">Route values: [operation, create], [id, 3]</span></span> |
| `/package/track/-3`    | <span data-ttu-id="eea9f-1159">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-1159">Hello!</span></span> <span data-ttu-id="eea9f-1160">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-1160">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/-3/`   | <span data-ttu-id="eea9f-1161">Merhaba!</span><span class="sxs-lookup"><span data-stu-id="eea9f-1161">Hello!</span></span> <span data-ttu-id="eea9f-1162">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="eea9f-1162">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/`      | <span data-ttu-id="eea9f-1163">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1163">The request falls through, no match.</span></span>              |
| `GET /hello/Joe`       | <span data-ttu-id="eea9f-1164">Merhaba, ali!</span><span class="sxs-lookup"><span data-stu-id="eea9f-1164">Hi, Joe!</span></span>                                          |
| `POST /hello/Joe`      | <span data-ttu-id="eea9f-1165">İstek üzerinden geçer, yalnızca HTTP GET ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1165">The request falls through, matches HTTP GET only.</span></span> |
| `GET /hello/Joe/Smith` | <span data-ttu-id="eea9f-1166">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1166">The request falls through, no match.</span></span>              |

<span data-ttu-id="eea9f-1167">Tek bir yol yapılandırıyorsanız, bir `IRouter` örneğinde geçen <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*> çağırın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1167">If you're configuring a single route, call <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*> passing in an `IRouter` instance.</span></span> <span data-ttu-id="eea9f-1168"><xref:Microsoft.AspNetCore.Routing.RouteBuilder>kullanmanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1168">You won't need to use <xref:Microsoft.AspNetCore.Routing.RouteBuilder>.</span></span>

<span data-ttu-id="eea9f-1169">Çerçeve, yollar oluşturmak için bir genişletme yöntemleri kümesi sağlar (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span><span class="sxs-lookup"><span data-stu-id="eea9f-1169">The framework provides a set of extension methods for creating routes (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span></span>

* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareVerb*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>

<span data-ttu-id="eea9f-1170"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>gibi listelenen yöntemlerin bazıları <xref:Microsoft.AspNetCore.Http.RequestDelegate>gerektirir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1170">Some of listed methods, such as <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>, require a <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span> <span data-ttu-id="eea9f-1171">Yol eşleştiğinde *yol işleyicisi* olarak <xref:Microsoft.AspNetCore.Http.RequestDelegate> kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1171">The <xref:Microsoft.AspNetCore.Http.RequestDelegate> is used as the *route handler* when the route matches.</span></span> <span data-ttu-id="eea9f-1172">Bu ailedeki diğer yöntemler, yönlendirme işleyicisi olarak kullanılmak üzere bir ara yazılım ardışık düzeni yapılandırmaya olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1172">Other methods in this family allow configuring a middleware pipeline for use as the route handler.</span></span> <span data-ttu-id="eea9f-1173">`Map*` yöntemi <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>gibi bir işleyiciyi kabul etmez <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1173">If the `Map*` method doesn't accept a handler, such as <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>, it uses the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span>

<span data-ttu-id="eea9f-1174">`Map[Verb]` yöntemleri, yöntem adındaki HTTP fiili ile rotayı sınırlandırmak için kısıtlamaları kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1174">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="eea9f-1175">Örneğin, bkz. <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> ve <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1175">For example, see <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> and <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="eea9f-1176">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-1176">Route template reference</span></span>

<span data-ttu-id="eea9f-1177">Küme ayraçları içindeki belirteçler (`{ ... }`), yol eşleştirildiği takdirde bağlanan *Rota parametrelerini* tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1177">Tokens within curly braces (`{ ... }`) define *route parameters* that are bound if the route is matched.</span></span> <span data-ttu-id="eea9f-1178">Bir yol segmentinde birden fazla yol parametresi tanımlayabilirsiniz, ancak bunların bir değişmez değer ile ayrılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1178">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="eea9f-1179">Örneğin, `{controller}` ve `{action}`arasında değişmez değer olmadığından `{controller=Home}{action=Index}` geçerli bir yol değil.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1179">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="eea9f-1180">Bu rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1180">These route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="eea9f-1181">Yol parametreleri dışında (örneğin, `{id}`) ve yol ayırıcı `/` URL içindeki metinle eşleşmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1181">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="eea9f-1182">Metin eşleştirme, büyük/küçük harfe duyarsızdır ve URL yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1182">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="eea9f-1183">Değişmez değer yol parametresi sınırlayıcısından (`{` veya `}`) eşleştirmek için, karakteri (`{{` veya `}}`) tekrarlayarak sınırlayıcıdan kaçış.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1183">To match a literal route parameter delimiter (`{` or `}`), escape the delimiter by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="eea9f-1184">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1184">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="eea9f-1185">Örneğin, `files/{filename}.{ext?}`şablonu düşünün.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1185">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="eea9f-1186">`filename` ve `ext` değerlerinin her ikisi de varsa, her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1186">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="eea9f-1187">URL 'de yalnızca `filename` için bir değer varsa, bitiş dönemi (`.`) isteğe bağlı olduğundan yol eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1187">If only a value for `filename` exists in the URL, the route matches because the trailing period (`.`) is  optional.</span></span> <span data-ttu-id="eea9f-1188">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1188">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="eea9f-1189">Yıldız işaretini (`*`) URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1189">You can use the asterisk (`*`) as a prefix to a route parameter to bind to the rest of the URI.</span></span> <span data-ttu-id="eea9f-1190">Buna *catch-all* parametresi denir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1190">This is called a *catch-all* parameter.</span></span> <span data-ttu-id="eea9f-1191">Örneğin, `blog/{*slug}` `/blog` ile başlayan ve ondan sonraki bir değere sahip olan `slug` yol değerine atanan tüm URI ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1191">For example, `blog/{*slug}` matches any URI that starts with `/blog` and has any value following it, which is assigned to the `slug` route value.</span></span> <span data-ttu-id="eea9f-1192">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1192">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="eea9f-1193">Yol ayırıcısı (`/`) karakterleri de dahil olmak üzere bir URL oluşturmak için bir yol kullanıldığında, catch-all parametresi uygun karakterleri çıkar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1193">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator (`/`) characters.</span></span> <span data-ttu-id="eea9f-1194">Örneğin yol `foo/{*path}` rota değerleriyle `{ path = "my/path" }` `foo/my%2Fpath`üretir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1194">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="eea9f-1195">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1195">Note the escaped forward slash.</span></span>

<span data-ttu-id="eea9f-1196">Yol parametrelerinin, parametre adından sonra varsayılan değer belirtilerek bir eşittir işareti (`=`) ile ayrıldıktan sonra belirlenen *varsayılan değerleri* olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1196">Route parameters may have *default values* designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="eea9f-1197">Örneğin, `Home` `controller`için varsayılan değer olarak `{controller=Home}` tanımlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1197">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="eea9f-1198">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1198">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="eea9f-1199">Yol parametreleri, `id?`gibi parametre adının sonuna bir soru işareti (`?`) eklenerek isteğe bağlı olarak yapılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1199">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name, as in `id?`.</span></span> <span data-ttu-id="eea9f-1200">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark, varsayılan değere sahip bir yol parametresinin her zaman bir değer ürettiğinden&mdash;isteğe bağlı bir parametre yalnızca istek URL 'SI tarafından bir değer sağlandığında bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1200">The difference between optional values and default route parameters is that a route parameter with a default value always produces a value&mdash;an optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="eea9f-1201">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1201">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="eea9f-1202">Yol parametre adından sonra bir iki nokta üst üste (`:`) ve kısıtlama adının eklenmesi, bir yol parametresi üzerinde bir *satır içi kısıtlamayı* belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1202">Adding a colon (`:`) and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="eea9f-1203">Kısıtlama bağımsız değişkenler gerektiriyorsa, kısıtlama adından sonra parantez (`(...)`) içine alınır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1203">If the constraint requires arguments, they're enclosed in parentheses (`(...)`) after the constraint name.</span></span> <span data-ttu-id="eea9f-1204">Birden çok satır içi kısıtlama, başka bir iki nokta (`:`) ve kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1204">Multiple inline constraints can be specified by appending another colon (`:`) and constraint name.</span></span>

<span data-ttu-id="eea9f-1205">Kısıtlama adı ve bağımsız değişkenler, URL işlemede kullanılacak bir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> örneği oluşturmak için <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1205">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="eea9f-1206">Örneğin, yol şablonu `blog/{article:minlength(10)}`, `10`bağımsız değişkeniyle bir `minlength` kısıtlaması belirtir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1206">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="eea9f-1207">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1207">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="eea9f-1208">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1208">The following table demonstrates example route templates and their behavior.</span></span>

| <span data-ttu-id="eea9f-1209">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="eea9f-1209">Route Template</span></span>                           | <span data-ttu-id="eea9f-1210">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="eea9f-1210">Example Matching URI</span></span>    | <span data-ttu-id="eea9f-1211">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="eea9f-1211">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="eea9f-1212">Yalnızca `/hello`tek yol ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1212">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="eea9f-1213">`Home``Page` eşleşir ve ayarlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1213">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="eea9f-1214">`Contact``Page` eşleşir ve ayarlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1214">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="eea9f-1215">`Products` denetleyicisi ve `List` eylemiyle eşlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1215">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="eea9f-1216">`Products` denetleyicisi ve `Details` eylemine eşlenir (`id` 123 olarak ayarlanır).</span><span class="sxs-lookup"><span data-stu-id="eea9f-1216">Maps to the `Products` controller and  `Details` action (`id` set to 123).</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="eea9f-1217">`Home` denetleyicisi ve `Index` yöntemiyle eşlenir (`id` yok sayılır).</span><span class="sxs-lookup"><span data-stu-id="eea9f-1217">Maps to the `Home` controller and `Index` method (`id` is ignored).</span></span>        |

<span data-ttu-id="eea9f-1218">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1218">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="eea9f-1219">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1219">Constraints and defaults can also be specified outside the route template.</span></span>

> [!TIP]
> <span data-ttu-id="eea9f-1220"><xref:Microsoft.AspNetCore.Routing.Route>, istekleri eşleştirme gibi yerleşik yönlendirme uygulamalarının nasıl yapıldığını görmek için [günlük kaydını](xref:fundamentals/logging/index) etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1220">Enable [Logging](xref:fundamentals/logging/index) to see how the built-in routing implementations, such as <xref:Microsoft.AspNetCore.Routing.Route>, match requests.</span></span>

## <a name="reserved-routing-names"></a><span data-ttu-id="eea9f-1221">Ayrılmış yönlendirme adları</span><span class="sxs-lookup"><span data-stu-id="eea9f-1221">Reserved routing names</span></span>

<span data-ttu-id="eea9f-1222">Aşağıdaki anahtar sözcükler ayrılmış isimlerdir ve yol adları veya parametreler olarak kullanılamaz:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1222">The following keywords are reserved names and can't be used as route names or parameters:</span></span>

* `action`
* `area`
* `controller`
* `handler`
* `page`

## <a name="route-constraint-reference"></a><span data-ttu-id="eea9f-1223">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-1223">Route constraint reference</span></span>

<span data-ttu-id="eea9f-1224">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1224">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="eea9f-1225">Rota kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda bir Evet/Hayır kararı getirir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1225">Route constraints generally inspect the route value associated via the route template and make a yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="eea9f-1226">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1226">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="eea9f-1227">Örneğin <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint>, HTTP fiiline bağlı olarak bir isteği kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1227">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="eea9f-1228">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1228">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="eea9f-1229">**Giriş doğrulaması**için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1229">Don't use constraints for **input validation**.</span></span> <span data-ttu-id="eea9f-1230">**Giriş doğrulaması**için kısıtlamalar kullanılıyorsa, doğru bir hata iletisine sahip *400-Bad isteği* yerine *404-* olmayan bir Yanıt ile geçersiz giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1230">If constraints are used for **input validation**, invalid input results in a *404 - Not Found* response instead of a *400 - Bad Request* with an appropriate error message.</span></span> <span data-ttu-id="eea9f-1231">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların **belirsizliğini ortadan** kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1231">Route constraints are used to **disambiguate** similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="eea9f-1232">Aşağıdaki tabloda örnek yol kısıtlamaları ve bunların beklenen davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1232">The following table demonstrates example route constraints and their expected behavior.</span></span>

| <span data-ttu-id="eea9f-1233">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="eea9f-1233">constraint</span></span> | <span data-ttu-id="eea9f-1234">Örnek</span><span class="sxs-lookup"><span data-stu-id="eea9f-1234">Example</span></span> | <span data-ttu-id="eea9f-1235">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="eea9f-1235">Example Matches</span></span> | <span data-ttu-id="eea9f-1236">Notlar</span><span class="sxs-lookup"><span data-stu-id="eea9f-1236">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="eea9f-1237">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1237">`123456789`, `-123456789`</span></span> | <span data-ttu-id="eea9f-1238">Herhangi bir tamsayıyla eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-1238">Matches any integer</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="eea9f-1239">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1239">`true`, `FALSE`</span></span> | <span data-ttu-id="eea9f-1240">`true` veya `false` eşleşir (büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="eea9f-1240">Matches `true` or `false` (case-insensitive)</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="eea9f-1241">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1241">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="eea9f-1242">Geçerli bir `DateTime` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-1242">Matches a valid `DateTime` value (in the invariant culture - see warning)</span></span> |
| `decimal` | `{price:decimal}` | <span data-ttu-id="eea9f-1243">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1243">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="eea9f-1244">Geçerli bir `decimal` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-1244">Matches a valid `decimal` value (in the invariant culture - see warning)</span></span> |
| `double` | `{weight:double}` | <span data-ttu-id="eea9f-1245">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1245">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="eea9f-1246">Geçerli bir `double` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-1246">Matches a valid `double` value (in the invariant culture - see warning)</span></span> |
| `float` | `{weight:float}` | <span data-ttu-id="eea9f-1247">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1247">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="eea9f-1248">Geçerli bir `float` değeriyle eşleşir (Sabit kültür içinde, bkz. uyarı)</span><span class="sxs-lookup"><span data-stu-id="eea9f-1248">Matches a valid `float` value (in the invariant culture - see warning)</span></span> |
| `guid` | `{id:guid}` | <span data-ttu-id="eea9f-1249">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1249">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="eea9f-1250">Geçerli bir `Guid` değeriyle eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-1250">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="eea9f-1251">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="eea9f-1251">`123456789`, `-123456789`</span></span> | <span data-ttu-id="eea9f-1252">Geçerli bir `long` değeriyle eşleşir</span><span class="sxs-lookup"><span data-stu-id="eea9f-1252">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="eea9f-1253">Dize en az 4 karakter olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1253">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `Richard` | <span data-ttu-id="eea9f-1254">Dize 8 karakterden uzun olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1254">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="eea9f-1255">Dize tam olarak 12 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1255">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="eea9f-1256">Dize en az 8 ve en fazla 16 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1256">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="eea9f-1257">Tamsayı değeri en az 18 olmalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1257">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="eea9f-1258">Tamsayı değeri 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1258">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="eea9f-1259">Tamsayı değeri en az 18 olmalı ancak 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1259">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="eea9f-1260">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır (`a`-`z`, büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="eea9f-1260">String must consist of one or more alphabetical characters (`a`-`z`, case-insensitive)</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="eea9f-1261">Dize, normal ifadeyle eşleşmelidir (normal ifade tanımlama hakkında ipuçlarına bakın)</span><span class="sxs-lookup"><span data-stu-id="eea9f-1261">String must match the regular expression (see tips about defining a regular expression)</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="eea9f-1262">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1262">Used to enforce that a non-parameter value is present during URL generation</span></span> |

<span data-ttu-id="eea9f-1263">Birden çok, iki nokta üst üste sınırlı kısıtlama tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1263">Multiple, colon-delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="eea9f-1264">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1264">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="eea9f-1265">URL 'YI doğrulayan ve bir CLR türüne (örneğin `int` veya `DateTime`) dönüştürülen yol kısıtlamaları her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1265">Route constraints that verify the URL and are converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture.</span></span> <span data-ttu-id="eea9f-1266">Bu kısıtlamalar, URL 'nin yerelleştirilemeyen olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1266">These constraints assume that the URL is non-localizable.</span></span> <span data-ttu-id="eea9f-1267">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1267">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="eea9f-1268">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1268">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="eea9f-1269">Örneğin, `float` kısıtlaması yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1269">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="eea9f-1270">Normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="eea9f-1270">Regular expressions</span></span>

<span data-ttu-id="eea9f-1271">ASP.NET Core Framework, normal ifade oluşturucusuna `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` ekler.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1271">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="eea9f-1272">Bu üyelerin açıklaması için bkz. <xref:System.Text.RegularExpressions.RegexOptions>.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1272">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="eea9f-1273">Normal ifadeler, C# yönlendirme ve dil tarafından kullanılanlarla aynı sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1273">Regular expressions use delimiters and tokens similar to those used by Routing and the C# language.</span></span> <span data-ttu-id="eea9f-1274">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1274">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="eea9f-1275">`^\d{3}-\d{2}-\d{4}$` normal ifade ' i yönlendirmede kullanmak için, ifadede, C# kaynak `\` dosyadaki `\\` (çift ters eğik çizgi) karakter olarak dize içinde belirtilen `\` (tek ters eğik çizgi) karakterleri olmalıdır (tam [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string)kullanmadıkça).</span><span class="sxs-lookup"><span data-stu-id="eea9f-1275">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in routing, the expression must have the `\` (single backslash) characters provided in the string as `\\` (double backslash) characters in the C# source file in order to escape the `\` string escape character (unless using [verbatim string literals](/dotnet/csharp/language-reference/keywords/string)).</span></span> <span data-ttu-id="eea9f-1276">Yönlendirme parametresi sınırlayıcı karakterlerini (`{`, `}`, `[`, `]`) atlamak için, ifadedeki karakterleri çift (`{{`, `}`, `[[`, `]]`).</span><span class="sxs-lookup"><span data-stu-id="eea9f-1276">To escape routing parameter delimiter characters (`{`, `}`, `[`, `]`), double the characters in the expression (`{{`, `}`, `[[`, `]]`).</span></span> <span data-ttu-id="eea9f-1277">Aşağıdaki tabloda, bir normal ifade ve kaçan sürümü gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1277">The following table shows a regular expression and the escaped version.</span></span>

| <span data-ttu-id="eea9f-1278">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="eea9f-1278">Regular Expression</span></span>    | <span data-ttu-id="eea9f-1279">Kaçan normal Ifade</span><span class="sxs-lookup"><span data-stu-id="eea9f-1279">Escaped Regular Expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="eea9f-1280">Yönlendirmelerde kullanılan normal ifadeler, genellikle şapka işareti (`^`) karakteriyle başlar ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1280">Regular expressions used in routing often start with the caret (`^`) character and match starting position of the string.</span></span> <span data-ttu-id="eea9f-1281">İfadeler genellikle dolar işareti (`$`) karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1281">The expressions often end with the dollar sign (`$`) character and match end of the string.</span></span> <span data-ttu-id="eea9f-1282">`^` ve `$` karakterler, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1282">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="eea9f-1283">`^` ve `$` karakterleri olmadan normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1283">Without the `^` and `$` characters, the regular expression match any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="eea9f-1284">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1284">The following table provides examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="eea9f-1285">İfade</span><span class="sxs-lookup"><span data-stu-id="eea9f-1285">Expression</span></span>   | <span data-ttu-id="eea9f-1286">Dize</span><span class="sxs-lookup"><span data-stu-id="eea9f-1286">String</span></span>    | <span data-ttu-id="eea9f-1287">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="eea9f-1287">Match</span></span> | <span data-ttu-id="eea9f-1288">Yorum</span><span class="sxs-lookup"><span data-stu-id="eea9f-1288">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-1289">merhaba</span><span class="sxs-lookup"><span data-stu-id="eea9f-1289">hello</span></span>     | <span data-ttu-id="eea9f-1290">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-1290">Yes</span></span>   | <span data-ttu-id="eea9f-1291">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-1291">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-1292">123abc456</span><span class="sxs-lookup"><span data-stu-id="eea9f-1292">123abc456</span></span> | <span data-ttu-id="eea9f-1293">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-1293">Yes</span></span>   | <span data-ttu-id="eea9f-1294">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="eea9f-1294">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-1295">mz</span><span class="sxs-lookup"><span data-stu-id="eea9f-1295">mz</span></span>        | <span data-ttu-id="eea9f-1296">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-1296">Yes</span></span>   | <span data-ttu-id="eea9f-1297">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="eea9f-1297">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="eea9f-1298">MZ</span><span class="sxs-lookup"><span data-stu-id="eea9f-1298">MZ</span></span>        | <span data-ttu-id="eea9f-1299">Evet</span><span class="sxs-lookup"><span data-stu-id="eea9f-1299">Yes</span></span>   | <span data-ttu-id="eea9f-1300">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="eea9f-1300">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="eea9f-1301">merhaba</span><span class="sxs-lookup"><span data-stu-id="eea9f-1301">hello</span></span>     | <span data-ttu-id="eea9f-1302">Hayır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1302">No</span></span>    | <span data-ttu-id="eea9f-1303">Yukarıdaki `^` ve `$` bakın</span><span class="sxs-lookup"><span data-stu-id="eea9f-1303">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="eea9f-1304">123abc456</span><span class="sxs-lookup"><span data-stu-id="eea9f-1304">123abc456</span></span> | <span data-ttu-id="eea9f-1305">Hayır</span><span class="sxs-lookup"><span data-stu-id="eea9f-1305">No</span></span>    | <span data-ttu-id="eea9f-1306">Yukarıdaki `^` ve `$` bakın</span><span class="sxs-lookup"><span data-stu-id="eea9f-1306">See `^` and `$` above</span></span> |

<span data-ttu-id="eea9f-1307">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="eea9f-1307">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="eea9f-1308">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1308">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="eea9f-1309">Örneğin `{action:regex(^(list|get|create)$)}`, yalnızca `action` Route değeri `list`, `get`veya `create`ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1309">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="eea9f-1310">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1310">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="eea9f-1311">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğüne (bir şablon içinde satır içi değil) geçirilen kısıtlamalar da normal ifadeler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1311">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="custom-route-constraints"></a><span data-ttu-id="eea9f-1312">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="eea9f-1312">Custom Route Constraints</span></span>

<span data-ttu-id="eea9f-1313">Yerleşik yol kısıtlamalarına ek olarak, <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> arabirimi uygulayarak özel yol kısıtlamaları oluşturulabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1313">In addition to the built-in route constraints, custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="eea9f-1314"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> arabirimi, kısıtlama karşılandıysanız `true` döndüren `Match`tek bir yöntem içerir ve aksi takdirde `false`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1314">The <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface contains a single method, `Match`, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="eea9f-1315">Özel bir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>kullanmak için, yol kısıtlama türü uygulamanın hizmet kapsayıcısında uygulamanın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> kayıtlı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1315">To use a custom <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the app's service container.</span></span> <span data-ttu-id="eea9f-1316"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, yol kısıtlama anahtarlarını bu kısıtlamaları doğrulayan <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1316">A <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> is a dictionary that maps route constraint keys to <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> implementations that validate those constraints.</span></span> <span data-ttu-id="eea9f-1317">Bir uygulamanın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, bir hizmetin parçası olarak `Startup.ConfigureServices` güncelleştirilebilen olabilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan `services.Configure<RouteOptions>`ile yapılandırma.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1317">An app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="eea9f-1318">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1318">For example:</span></span>

```csharp
services.AddRouting(options =>
{
    options.ConstraintMap.Add("customName", typeof(MyCustomConstraint));
});
```

<span data-ttu-id="eea9f-1319">Kısıtlama daha sonra, kısıtlama türü kaydedilirken belirtilen ad kullanılarak yollara her zamanki şekilde uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1319">The constraint can then be applied to routes in the usual manner, using the name specified when registering the constraint type.</span></span> <span data-ttu-id="eea9f-1320">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1320">For example:</span></span>

```csharp
[HttpGet("{id:customName}")]
public ActionResult<string> Get(string id)
```

## <a name="url-generation-reference"></a><span data-ttu-id="eea9f-1321">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="eea9f-1321">URL generation reference</span></span>

<span data-ttu-id="eea9f-1322">Aşağıdaki örnek, yol değerlerinin bir sözlüğü ve bir <xref:Microsoft.AspNetCore.Routing.RouteCollection>için bir yol bağlantısının nasıl oluşturulacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1322">The following example shows how to generate a link to a route given a dictionary of route values and a <xref:Microsoft.AspNetCore.Routing.RouteCollection>.</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_Dictionary)]

<span data-ttu-id="eea9f-1323">Yukarıdaki örnek sonunda oluşturulan <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> `/package/create/123`.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1323">The <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> generated at the end of the preceding sample is `/package/create/123`.</span></span> <span data-ttu-id="eea9f-1324">Sözlük, "paket yolunu Izle" şablonunun `package/{operation}/{id}``operation` ve `id` yol değerlerini sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1324">The dictionary supplies the `operation` and `id` route values of the "Track Package Route" template, `package/{operation}/{id}`.</span></span> <span data-ttu-id="eea9f-1325">Ayrıntılar için, [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümünde veya [örnek uygulamada](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples)örnek koda bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1325">For details, see the sample code in the [Use Routing Middleware](#use-routing-middleware) section or the [sample app](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples).</span></span>

<span data-ttu-id="eea9f-1326"><xref:Microsoft.AspNetCore.Routing.VirtualPathContext> oluşturucusunun ikinci parametresi bir *ortam değerleri*koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1326">The second parameter to the <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="eea9f-1327">Ortam değerleri, bir geliştiricinin bir istek bağlamı içinde belirtmesi gereken değer sayısını sınırlandırdığından kullanım için uygundur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1327">Ambient values are convenient to use because they limit the number of values a developer must specify within a request context.</span></span> <span data-ttu-id="eea9f-1328">Geçerli isteğin geçerli yol değerleri, bağlantı oluşturma için çevresel değerler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1328">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="eea9f-1329">ASP.NET Core MVC uygulamasının `HomeController``About` eyleminde,&mdash;ortam değeri `Home` `Index` eyleme bağlamak için denetleyici yolu değerini belirtmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1329">In an ASP.NET Core MVC app's `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action&mdash;the ambient value of `Home` is used.</span></span>

<span data-ttu-id="eea9f-1330">Bir parametreyle eşleşmeyen çevresel değerler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1330">Ambient values that don't match a parameter are ignored.</span></span> <span data-ttu-id="eea9f-1331">Ayrıca, açıkça sağlanmış bir değer çevresel değeri geçersiz kıldığında çevresel değerler de yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1331">Ambient values are also ignored when an explicitly provided value overrides the ambient value.</span></span> <span data-ttu-id="eea9f-1332">Eşleştirme, URL 'de soldan sağa doğru gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1332">Matching occurs from left to right in the URL.</span></span>

<span data-ttu-id="eea9f-1333">Açık olarak sağlanmış ancak yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1333">Values explicitly provided but that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="eea9f-1334">Aşağıdaki tabloda `{controller}/{action}/{id?}`yol şablonu kullanılırken sonuç gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1334">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="eea9f-1335">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="eea9f-1335">Ambient Values</span></span>                     | <span data-ttu-id="eea9f-1336">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="eea9f-1336">Explicit Values</span></span>                        | <span data-ttu-id="eea9f-1337">Sonuç</span><span class="sxs-lookup"><span data-stu-id="eea9f-1337">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="eea9f-1338">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1338">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-1339">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1339">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="eea9f-1340">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1340">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-1341">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1341">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="eea9f-1342">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1342">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="eea9f-1343">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1343">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="eea9f-1344">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1344">controller = "Home"</span></span>                | <span data-ttu-id="eea9f-1345">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="eea9f-1345">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

<span data-ttu-id="eea9f-1346">Bir rotada bir parametreye karşılık gelen bir varsayılan değer varsa ve bu değer açıkça sağlanmışsa, varsayılan değerle eşleşmelidir:</span><span class="sxs-lookup"><span data-stu-id="eea9f-1346">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
    defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="eea9f-1347">Bağlantı oluşturma yalnızca `controller` ve `action` için eşleşen değerler sağlandığında bu yol için bir bağlantı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1347">Link generation only generates a link for this route when the matching values for `controller` and `action` are provided.</span></span>

## <a name="complex-segments"></a><span data-ttu-id="eea9f-1348">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="eea9f-1348">Complex segments</span></span>

<span data-ttu-id="eea9f-1349">Karmaşık segmentler (örneğin `[Route("/x{token}y")]`), sabit değerli olmayan değişmez değerler ile sağdan sola eşleştirilirken işlenir.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1349">Complex segments (for example `[Route("/x{token}y")]`) are processed by matching up literals from right to left in a non-greedy way.</span></span> <span data-ttu-id="eea9f-1350">Karmaşık segmentlerin nasıl eşleştirileceği hakkında ayrıntılı bir açıklama için [bu koda](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) bakın.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1350">See [this code](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) for a detailed explanation of how complex segments are matched.</span></span> <span data-ttu-id="eea9f-1351">[Kod örneği](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) ASP.NET Core tarafından kullanılmaz, ancak karmaşık segmentler hakkında iyi bir açıklama sağlar.</span><span class="sxs-lookup"><span data-stu-id="eea9f-1351">The [code sample](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) is not used by ASP.NET Core, but it provides a good explanation of complex segments.</span></span>
<!-- While that code is no longer used by ASP.NET Core for complex segment matching, it provides a good match to the current algorithm. The [current code](https://github.com/dotnet/AspNetCore/blob/91514c9af7e0f4c44029b51f05a01c6fe4c96e4c/src/Http/Routing/src/Matching/DfaMatcherBuilder.cs#L227-L244) is too abstracted from matching to be useful for understanding complex segment matching.
-->

::: moniker-end

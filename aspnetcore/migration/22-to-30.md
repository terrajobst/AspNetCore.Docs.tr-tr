---
title: 2\.2-3.0 Önizleme ASP.NET çekirdek geçirme
author: tdykstra
description: ASP.NET Core 3. 0'için bir ASP.NET Core 2.2 proje geçirmeyi öğrenin.
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 7f78daeae1147adaa4711b60da71aa83ad939e63
ms.sourcegitcommit: d6e51c60439f03a8992bda70cc982ddb15d3f100
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 07/03/2019
ms.locfileid: "67555900"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="e883f-103">ASP.NET Core-2.2 3.0 geçiş</span><span class="sxs-lookup"><span data-stu-id="e883f-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="e883f-104">Tarafından [Scott Addie](https://github.com/scottaddie) ve [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="e883f-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="e883f-105">Bu makalede, ASP.NET Core 3.0 için mevcut bir ASP.NET Core 2.2 projeyi güncelleştirin açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="e883f-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="e883f-106">Önkoşullar</span><span class="sxs-lookup"><span data-stu-id="e883f-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="e883f-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="e883f-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="e883f-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="e883f-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="e883f-109">Mac için Visual Studio</span><span class="sxs-lookup"><span data-stu-id="e883f-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="e883f-110">Proje dosyasını güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="e883f-110">Update the project file</span></span>

* <span data-ttu-id="e883f-111">Ayarlama [hedef çerçeve adı (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) için `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="e883f-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="e883f-112">Kaldırmak `<PackageReference>` için [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) veya [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span><span class="sxs-lookup"><span data-stu-id="e883f-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="e883f-113">Güncelleştirme `Version` kalan üzerinde öznitelik `<PackageReference>` için öğeleri `Microsoft.AspNetCore.*` geçerli Önizleme paketleri (örneğin, `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="e883f-113">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="e883f-114">Bir paketin hiçbir 3.0 sürümü varsa, paket 3. 0'kullanım dışı bırakılmış.</span><span class="sxs-lookup"><span data-stu-id="e883f-114">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="e883f-115">Bu paketler birçoğu parçası olan `Microsoft.AspNetCore.App` ve ayrı ayrı başvurulan olmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="e883f-115">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="e883f-116">3\. 0'artık üretilen paketlerin ön bir listesi için bkz [3.0 paylaşılan çerçeve derlemeleri için paketleri oluşturmayı durdur (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="e883f-116">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="e883f-117">*Paylaşılan çerçeve* derlemeleri kümesidir ( *.dll* dosyaları) bu makinede yüklü ve tarafından başvurulan `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="e883f-117">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="e883f-118">Daha fazla bilgi için [paylaşılan çerçeve](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="e883f-118">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="e883f-119">Birkaç önemli bileşeni için derlemeleri sıradan kaldırıldığını `Microsoft.AspNetCore.App` 3.0.</span><span class="sxs-lookup"><span data-stu-id="e883f-119">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="e883f-120">Ekleme `<PackageReference>` listelenen paketlerinden API kullanıyorsanız, öğeleri [Microsoft.AspNetCore.App 3.0 uygulamasından kaldırılmakta olan derlemeleri (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="e883f-120">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="e883f-121">Kaldırılan bileşenlerinin örnekleri şunlardır:</span><span class="sxs-lookup"><span data-stu-id="e883f-121">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="e883f-122">İçinde sevkiyat derlemelerin listesini `Microsoft.AspNetCore.App` kesin olmayan ve önce 3.0 RTM değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="e883f-122">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="e883f-123">Aşağıdaki kodu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="e883f-123">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="e883f-124">`ReadAsAsync` Önceki kodda adlı bir yöntem dahil `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="e883f-124">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="e883f-125">Yükleme [System.NET.http.Formatting](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) 3.0 derleme sorunu gidermek için NuGet paketi.</span><span class="sxs-lookup"><span data-stu-id="e883f-125">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="e883f-126">Ekleme [Json.NET Destek](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="e883f-126">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="e883f-127">Projeleri için varsayılan değer [işlem içi barındırma modeli](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) ASP.NET Core 3.0 veya üzeri.</span><span class="sxs-lookup"><span data-stu-id="e883f-127">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="e883f-128">İsteğe bağlı olarak kaldırabilir `<AspNetCoreHostingModel>` özellik değeri ise proje dosyasında `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="e883f-128">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="e883f-129">Json.NET desteği</span><span class="sxs-lookup"><span data-stu-id="e883f-129">Json.NET support</span></span>

<span data-ttu-id="e883f-130">İşin bir parçası olarak [ASP.NET Core paylaşılan çerçeve geliştirmek](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) ASP.NET Core paylaşılan Framework'ten kaldırıldı.</span><span class="sxs-lookup"><span data-stu-id="e883f-130">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="e883f-131">ASP.NET Core 3.0 projesinde Json.NET kullanmak için:</span><span class="sxs-lookup"><span data-stu-id="e883f-131">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="e883f-132">Paket başvurusu ekleme [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="e883f-132">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="e883f-133">Güncelleştirme `Startup.ConfigureServices` çağrılacak `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="e883f-133">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="e883f-134">Json.NET ayarları çağrısında ayarlanabilir `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="e883f-134">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="e883f-135">MVC hizmeti kaydı</span><span class="sxs-lookup"><span data-stu-id="e883f-135">MVC service registration</span></span>

<span data-ttu-id="e883f-136">3\.0 ASP.NET Core MVC senaryoları içine kaydetmek için yeni seçenekler ekler `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="e883f-136">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="e883f-137">Üç yeni üst düzey genişletme yöntemleri üzerinde MVC senaryoları için ilgili `IServiceCollection` kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="e883f-137">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="e883f-138">Şablonları kullanmak yerine bu yeni yöntemler `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="e883f-138">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="e883f-139">Ancak, `AddMvc` önceki sürümlerde olduğu gibi davranmaya devam eder.</span><span class="sxs-lookup"><span data-stu-id="e883f-139">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="e883f-140">Aşağıdaki örnek denetleyicileri ve API güvenlikle ilgili özellikler, ancak görünümleri veya sayfaları için destek ekler.</span><span class="sxs-lookup"><span data-stu-id="e883f-140">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="e883f-141">Bu kodu API şablonu kullanır:</span><span class="sxs-lookup"><span data-stu-id="e883f-141">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="e883f-142">Aşağıdaki örnek denetleyicileri, API ile ilgili özellikler ve görünümleri, ancak değil sayfaları için destek ekler.</span><span class="sxs-lookup"><span data-stu-id="e883f-142">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="e883f-143">Web uygulaması (MVC) şablonu, bu kod kullanır:</span><span class="sxs-lookup"><span data-stu-id="e883f-143">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="e883f-144">Aşağıdaki örnek, Razor sayfaları ve en az bir denetleyici desteği ekler.</span><span class="sxs-lookup"><span data-stu-id="e883f-144">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="e883f-145">Web uygulaması şablonu, bu kod kullanır:</span><span class="sxs-lookup"><span data-stu-id="e883f-145">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="e883f-146">Yeni yöntemler de birleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="e883f-146">The new methods can also be combined.</span></span> <span data-ttu-id="e883f-147">Aşağıdaki örnek çağırmakla eşdeğerdir `AddMvc` ASP.NET Core 2.2 içinde:</span><span class="sxs-lookup"><span data-stu-id="e883f-147">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="e883f-148">Yönlendirme başlatma kodunu güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="e883f-148">Update routing startup code</span></span>

<span data-ttu-id="e883f-149">Bir uygulama çağırırsa `UseMvc` veya `UseSignalR`, uygulamaya geçmeleri [uç noktası yönlendirme](xref:fundamentals/routing) mümkünse.</span><span class="sxs-lookup"><span data-stu-id="e883f-149">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="e883f-150">MVC önceki sürümleriyle uyumluluk uç noktası yönlendirme geliştirmek için biz bazı değişiklikler ASP.NET Core 2.2 içinde sunulan URL üretimi içinde geri.</span><span class="sxs-lookup"><span data-stu-id="e883f-150">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="e883f-151">2\.2 içinde uç noktası yönlendirme kullanarak sorunları yaşadı, geliştirmeleri aşağıdaki istisnalar içinde ASP.NET Core 3.0 beklediğiniz:</span><span class="sxs-lookup"><span data-stu-id="e883f-151">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="e883f-152">Uygulama uyguluyorsa `IRouter` veya devralınan `Route`, şu anda geçiş önlemek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="e883f-152">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="e883f-153">Geribildirim, [IRouter tabanlı uygulamaları uç noktası yönlendirme üzerine geçirmeyi planladığınız](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="e883f-153">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="e883f-154">Uygulamayı doğrudan erişiyorsa `RouteData.Routers` MVC içinde şu anda geçiş önlemek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="e883f-154">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="e883f-155">Geribildirim, [RouteData.Routers kullanarak geçiş Kılavuzu](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="e883f-155">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="e883f-156">Uç noktası yönlendirme destekleyen aynı yol deseni sözdizimi ve rota düzeni özellikleri yazma `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="e883f-156">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="e883f-157">Uç noktası yönlendirme destekler `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="e883f-157">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="e883f-158">Uç noktası yönlendirme destekler `[Route]`, `[HttpGet]`ve diğer MVC yönlendirme öznitelikleri.</span><span class="sxs-lookup"><span data-stu-id="e883f-158">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="e883f-159">Çoğu uygulama, yalnızca için `Startup` değişiklikler gerektirir.</span><span class="sxs-lookup"><span data-stu-id="e883f-159">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="e883f-160">Startup.Configure geçirme</span><span class="sxs-lookup"><span data-stu-id="e883f-160">Migrate Startup.Configure</span></span>

<span data-ttu-id="e883f-161">Genel öneriler:</span><span class="sxs-lookup"><span data-stu-id="e883f-161">General advice:</span></span>

* <span data-ttu-id="e883f-162">Ekleme `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="e883f-162">Add `UseRouting`.</span></span> 
* <span data-ttu-id="e883f-163">Uygulama çağırırsa `UseStaticFiles`, yerleştirin `UseStaticFiles` **önce** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="e883f-163">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="e883f-164">Uygulama kimlik doğrulama/yetkilendirme özellikleri gibi kullanıyorsa `AuthorizePage` veya `[Authorize]`, çağrı yapmak `UseAuthentication` ve `UseAuthorization` **sonra** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="e883f-164">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="e883f-165">Uygulama kullanıyorsa [CORS](xref:security/cors) gibi özellikleri `[EnableCors]`, yerleştirin `UseCors` sonraki.</span><span class="sxs-lookup"><span data-stu-id="e883f-165">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="e883f-166">Değiştirin `UseMvc` veya `UseSignalR` ile `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="e883f-166">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="e883f-167">Aşağıdaki örneğidir `Startup.Configure` tipik bir ASP.NET Core 2.2 uygulamasında:</span><span class="sxs-lookup"><span data-stu-id="e883f-167">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="e883f-168">Önceki güncelleştirdikten sonra `Startup.Configure` kod:</span><span class="sxs-lookup"><span data-stu-id="e883f-168">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="e883f-169">Güvenlik ara yazılım Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="e883f-169">Security middleware guidance</span></span>

<span data-ttu-id="e883f-170">Yetkilendirme ve CORS desteği birleşik etrafında [ara yazılım](xref:fundamentals/middleware/index) yaklaşım.</span><span class="sxs-lookup"><span data-stu-id="e883f-170">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="e883f-171">Bu, bu senaryolar arasında aynı ara yazılım ve işlevsellik kullanımına izin verir.</span><span class="sxs-lookup"><span data-stu-id="e883f-171">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="e883f-172">Bu sürümde bir güncelleştirilmiş yetkilendirme ara yazılımı sağlanır ve MVC denetleyicileri tarafından kullanılan öznitelikler anlayabilmeniz CORS ara yazılımı geliştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="e883f-172">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="e883f-173">CORS</span><span class="sxs-lookup"><span data-stu-id="e883f-173">CORS</span></span>

<span data-ttu-id="e883f-174">Daha önce CORS yapılandırma zor olabilir.</span><span class="sxs-lookup"><span data-stu-id="e883f-174">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="e883f-175">Ara yazılım belirtildi kullanın, bazı durumlarda, ancak MVC filtreleri hedeflenen kullanılacak **olmadan** ara yazılım diğer kullanım örnekleri.</span><span class="sxs-lookup"><span data-stu-id="e883f-175">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="e883f-176">ASP.NET Core 3.0 ile CORS gerektiren tüm uygulamaları dağıtımınızla birlikte uç noktası yönlendirme CORS ara yazılımı kullanmanızı öneririz.</span><span class="sxs-lookup"><span data-stu-id="e883f-176">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="e883f-177">`UseCors` bir varsayılan ilke ile sağlanabilir ve `[EnableCors]` ve `[DisableCors]` öznitelikleri, varsayılan ilkesini geçersiz kılmak için kullanılabilir gerektiğinde.</span><span class="sxs-lookup"><span data-stu-id="e883f-177">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="e883f-178">Aşağıdaki örnekte:</span><span class="sxs-lookup"><span data-stu-id="e883f-178">In the following example:</span></span>

* <span data-ttu-id="e883f-179">CORS ile tüm uç noktalar için etkin `default` ilke adı.</span><span class="sxs-lookup"><span data-stu-id="e883f-179">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="e883f-180">`MyController` Sınıf, CORS ile devre dışı bırakır `[DisableCors]` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="e883f-180">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="e883f-181">Yetkilendirme</span><span class="sxs-lookup"><span data-stu-id="e883f-181">Authorization</span></span>

<span data-ttu-id="e883f-182">ASP.NET Core önceki sürümlerinde, yetkilendirme Destek aracılığıyla sağlanan `[Authorize]` özniteliği.</span><span class="sxs-lookup"><span data-stu-id="e883f-182">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="e883f-183">Yetkilendirme ara yazılımı kullanılabilir değildi.</span><span class="sxs-lookup"><span data-stu-id="e883f-183">Authorization middleware wasn't available.</span></span> <span data-ttu-id="e883f-184">ASP.NET Core 3. 0'yetkilendirme ara yazılım gereklidir.</span><span class="sxs-lookup"><span data-stu-id="e883f-184">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="e883f-185">ASP.NET Core yetkilendirme ara yazılımı yerleştirme öneririz (`UseAuthorization`) hemen sonra `UseAuthentication`.</span><span class="sxs-lookup"><span data-stu-id="e883f-185">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="e883f-186">Yetkilendirme ara yazılımı de geçersiz kılınabilir bir varsayılan ilkesi ile yapılandırılabilir.</span><span class="sxs-lookup"><span data-stu-id="e883f-186">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="e883f-187">ASP.NET Core 3.0 veya sonraki sürümlerde, `UseAuthorization` çağrılma yeri `Startup.Configure`, şunları `HomeController` kullanıcı olarak oturum açtıysanız gerektirir:</span><span class="sxs-lookup"><span data-stu-id="e883f-187">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="e883f-188">Uygulama kullanıyorsa bir `AuthorizeFilter` mvc'de genel filtre olarak, bir ilke sağlamak üzere kodu yeniden düzenleme olan öneririz `UseAuthorization` ara yazılım.</span><span class="sxs-lookup"><span data-stu-id="e883f-188">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="e883f-189">Aşağıdaki örnekte, tüm uygulanması için özel bir ilke isteklerinin ne zaman `UseAuthorization` denir ve `HomeController` uygulamaya oturum açılması kullanıcı olmadan erişim sağlar:</span><span class="sxs-lookup"><span data-stu-id="e883f-189">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="e883f-190">Yetkilendirme ara yazılımı tarafından yetkilendirme belirli bilgisine sahip framework çalışır.</span><span class="sxs-lookup"><span data-stu-id="e883f-190">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="e883f-191">Örneğin, [sistem durumu denetimleri](xref:host-and-deploy/health-checks) özel yetkilendirme bilgisi var, ancak sistem durumu denetimleri, ara yazılım tarafından uygulanan yapılandırılabilir yetkilendirme ilkesi olabilir.</span><span class="sxs-lookup"><span data-stu-id="e883f-191">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="e883f-192">Aşağıdaki örnekte, `UseAuthorization` yetkilendirme bir varsayılan ilke olmadan işler ancak `/healthz` sistem durumu onay uç noktası gerektirir, kullanıcı `admin` rolü:</span><span class="sxs-lookup"><span data-stu-id="e883f-192">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="e883f-193">Koruma için bazı senaryolar uygulanır.</span><span class="sxs-lookup"><span data-stu-id="e883f-193">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="e883f-194">`UseEndpoint` Ara yazılım bir yetkilendirme veya CORS ilkesi eksik ara yazılım nedeniyle atlandı bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="e883f-194">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="e883f-195">Devam eden hatalı yapılandırma hakkında ek geri bildirim sağlamak için Çözümleyicisi desteği.</span><span class="sxs-lookup"><span data-stu-id="e883f-195">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="e883f-196">SignalR geçirme</span><span class="sxs-lookup"><span data-stu-id="e883f-196">Migrate SignalR</span></span>

<span data-ttu-id="e883f-197">SignalR hub'ları şimdi eşlemesi içinde yer alan `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="e883f-197">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="e883f-198">Her hub'la harita `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="e883f-198">Map each hub with `MapHub`.</span></span> <span data-ttu-id="e883f-199">Önceki sürümlerinde olduğu gibi her hub açıkça listelenir.</span><span class="sxs-lookup"><span data-stu-id="e883f-199">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="e883f-200">Aşağıdaki örnekte, destek `ChatHub` SignalR hub'ı eklenir:</span><span class="sxs-lookup"><span data-stu-id="e883f-200">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="e883f-201">MVC denetleyicileri geçirme</span><span class="sxs-lookup"><span data-stu-id="e883f-201">Migrate MVC controllers</span></span>

<span data-ttu-id="e883f-202">Denetleyicileri artık eşlemesi içinde yer alan `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="e883f-202">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="e883f-203">Ekleme `MapControllers` uygulama öznitelik yönlendirme kullanıyorsa.</span><span class="sxs-lookup"><span data-stu-id="e883f-203">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="e883f-204">ASP.NET Core 3.0 veya daha sonra birçok çerçeveler için destek yönlendirme içerdiğinden, öznitelik yönlendirilen denetleyicileri ekleme kabul etme.</span><span class="sxs-lookup"><span data-stu-id="e883f-204">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="e883f-205">Aşağıdakileri değiştirin:</span><span class="sxs-lookup"><span data-stu-id="e883f-205">Replace the following:</span></span>

* <span data-ttu-id="e883f-206">`MapRoute` ile `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="e883f-206">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="e883f-207">`MapAreaRoute` ile `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="e883f-207">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="e883f-208">Daha fazlasını MVC desteği artık yönlendirme içerdiğinden, bu yöntemlerin yapabileceklerini açıkça durum yapmak için terminoloji değişti.</span><span class="sxs-lookup"><span data-stu-id="e883f-208">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="e883f-209">Geleneksel yolları gibi `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` eklenen sırayla uygulanır.</span><span class="sxs-lookup"><span data-stu-id="e883f-209">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="e883f-210">Daha özel yollar (örneğin, bir alan için rotalar) ilk yerleştirin.</span><span class="sxs-lookup"><span data-stu-id="e883f-210">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="e883f-211">Aşağıdaki örnekte:</span><span class="sxs-lookup"><span data-stu-id="e883f-211">In the following example:</span></span>

* <span data-ttu-id="e883f-212">`MapControllers` öznitelik yönlendirilen denetleyicileri için destek ekler.</span><span class="sxs-lookup"><span data-stu-id="e883f-212">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="e883f-213">`MapAreaControllerRoute` bir alanı denetleyicileri için geleneksel bir rota ekler.</span><span class="sxs-lookup"><span data-stu-id="e883f-213">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="e883f-214">`MapControllerRoute` denetleyicileri için geleneksel bir rota ekler.</span><span class="sxs-lookup"><span data-stu-id="e883f-214">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="e883f-215">Razor sayfaları geçirme</span><span class="sxs-lookup"><span data-stu-id="e883f-215">Migrate Razor Pages</span></span>

<span data-ttu-id="e883f-216">Razor sayfaları artık eşlemesi içinde yer alan `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="e883f-216">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="e883f-217">Ekleme `MapRazorPages` uygulama Razor sayfaları kullanıyorsa.</span><span class="sxs-lookup"><span data-stu-id="e883f-217">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="e883f-218">Uç noktası yönlendirme birçok çerçeveler için destek içerdiğinden, Razor sayfaları ekleme katılımı sunulmuştur.</span><span class="sxs-lookup"><span data-stu-id="e883f-218">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="e883f-219">Aşağıdaki örnekte, `MapRazorPages` Razor sayfaları için destek ekler:</span><span class="sxs-lookup"><span data-stu-id="e883f-219">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="e883f-220">MVC uç noktası yönlendirme olmadan kullanın</span><span class="sxs-lookup"><span data-stu-id="e883f-220">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="e883f-221">MVC ile kullanma `UseMvc` veya `UseMvcWithDefaultRoute` açık bir ASP.NET Core 3. 0'gerektirir katılımı içinde `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="e883f-221">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="e883f-222">Bu yetkilendirme ve CORS ara yazılımı başlatma sırasında güvenebilirsiniz olmadığını MVC bilmesi gerektiğinden bu gereklidir.</span><span class="sxs-lookup"><span data-stu-id="e883f-222">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="e883f-223">Bir çözümleyici, desteklenmeyen bir yapılandırma kullanmayı deneyip denemeyeceğini uygulama konusunda uyarır sağlanır.</span><span class="sxs-lookup"><span data-stu-id="e883f-223">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="e883f-224">Uygulama eski gerektiriyorsa `IRouter` desteği, devre dışı `EnableEndpointRouting` aşağıdakilerden herhangi birini kullanarak yaklaşıyor içinde `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="e883f-224">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="e883f-225">Sistem durumu denetimleri geçirme</span><span class="sxs-lookup"><span data-stu-id="e883f-225">Migrate health checks</span></span>

<span data-ttu-id="e883f-226">Sistem durumu denetimleri olarak kullanılabilir bir *Yönlendirici donanımlar* uç noktası yönlendirme.</span><span class="sxs-lookup"><span data-stu-id="e883f-226">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="e883f-227">Ekleme `MapHealthChecks` sistem durumu uç nokta yönlendirme ile denetler.</span><span class="sxs-lookup"><span data-stu-id="e883f-227">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="e883f-228">`MapHealthChecks` Yöntemi benzer bağımsız değişkenleri kabul `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="e883f-228">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="e883f-229">Kullanmanın avantajı `MapHealthChecks` üzerinden `UseHealthChecks` yetkilendirme uygulamak ve eşleşen ilke üzerinde daha ayrıntılı denetime sahip olmasını özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="e883f-229">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="e883f-230">Aşağıdaki örnekte, `MapHealthChecks` için sistem durumu denetimi uç noktada adlı `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="e883f-230">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="e883f-231">HostBuilder WebHostBuilder yerini alır.</span><span class="sxs-lookup"><span data-stu-id="e883f-231">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="e883f-232">ASP.NET Core 3.0 şablonlarını kullanma [genel ana bilgisayar](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="e883f-232">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="e883f-233">Önceki sürümlerinde kullanılan [Web ana bilgisayarı](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="e883f-233">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="e883f-234">Aşağıdaki kod oluşturulan ASP.NET Core 3.0 şablonunu gösterir `Program` sınıfı:</span><span class="sxs-lookup"><span data-stu-id="e883f-234">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="e883f-235">Aşağıdaki kod, ASP.NET Core Şablonu oluşturulan 2.2 göstermektedir `Program` sınıfı:</span><span class="sxs-lookup"><span data-stu-id="e883f-235">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="e883f-236"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> 3. 0'kalır ve türü `webBuilder` Yukarıdaki kod örneğinde görülen.</span><span class="sxs-lookup"><span data-stu-id="e883f-236"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="e883f-237"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> gelecekteki bir sürümde kullanımdan ve yerine `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="e883f-237"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="e883f-238">En önemli değişiklik `WebHostBuilder` için `HostBuilder` bulunduğu [bağımlılık ekleme (dı)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="e883f-238">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="e883f-239">Kullanırken `HostBuilder`, yalnızca ekleyebilir <xref:Microsoft.Extensions.Configuration.IConfiguration> ve <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> içine `Startup`'s Oluşturucusu.</span><span class="sxs-lookup"><span data-stu-id="e883f-239">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="e883f-240">`HostBuilder` DI kısıtlamaları:</span><span class="sxs-lookup"><span data-stu-id="e883f-240">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="e883f-241">Yalnızca bir kez oluşturulacak DI kapsayıcı etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="e883f-241">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="e883f-242">Birden çok örneğini teklileri çözümleme gibi elde edilen nesne kullanım ömrü sorunlarını önler.</span><span class="sxs-lookup"><span data-stu-id="e883f-242">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="e883f-243">SignalR kodunu güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="e883f-243">Update SignalR code</span></span>

<span data-ttu-id="e883f-244">`System.Text.Json` Varsayılan Hub protokol istemci ve sunucu tarafından kullanılan artık olur.</span><span class="sxs-lookup"><span data-stu-id="e883f-244">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="e883f-245">İçinde `Startup.ConfigureServices`, çağrı `AddJsonProtocol` seri hale getirici seçeneklerini ayarlamak için.</span><span class="sxs-lookup"><span data-stu-id="e883f-245">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="e883f-246">**Sunucu:**</span><span class="sxs-lookup"><span data-stu-id="e883f-246">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="e883f-247">**İstemci:**</span><span class="sxs-lookup"><span data-stu-id="e883f-247">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="e883f-248">Newtonsoft.Json için geçiş</span><span class="sxs-lookup"><span data-stu-id="e883f-248">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="e883f-249">Özelliklerini kullanıyorsanız, `Newtonsoft.Json` içinde desteklenmeyen `System.Text.Json`, geri geçiş yapabilirsiniz `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="e883f-249">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="e883f-250">Yükleme [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet paketi.</span><span class="sxs-lookup"><span data-stu-id="e883f-250">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="e883f-251">İstemcide, zincir bir `AddNewtonsoftJsonProtocol` yöntem çağrısını `HubConnectionBuilder` örneği:</span><span class="sxs-lookup"><span data-stu-id="e883f-251">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="e883f-252">Sunucuda zincirleme bir `AddNewtonsoftJsonProtocol` yöntem çağrısını `AddSignalR` yöntem çağrısı `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="e883f-252">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="e883f-253">Çalışma zamanı derlemesi kabul et</span><span class="sxs-lookup"><span data-stu-id="e883f-253">Opt in to runtime compilation</span></span>

<span data-ttu-id="e883f-254">3\. 0'çalışma zamanı derlemesi Tercihli bir senaryodur.</span><span class="sxs-lookup"><span data-stu-id="e883f-254">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="e883f-255">Çalışma zamanı derlemesi etkinleştirmek için bkz: <xref:mvc/views/view-compilation#runtime-compilation>.</span><span class="sxs-lookup"><span data-stu-id="e883f-255">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>

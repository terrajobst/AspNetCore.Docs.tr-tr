---
title: ASP.NET Core 2,2 ' den 3,0 önizlemeye geçiş yapın
author: tdykstra
description: ASP.NET Core 2,2 projesini ASP.NET Core 3,0 ' e geçirmeyi öğrenin.
ms.author: tdykstra
ms.custom: mvc
ms.date: 08/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 3d93526d609da4f216d0ec943f24c542341c49fb
ms.sourcegitcommit: 38cac2552029fc19428722bb204ff9e16eb94225
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/18/2019
ms.locfileid: "69573130"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="9bad4-103">ASP.NET Core 2,2 ' den 3,0 ' e geçiş yapın</span><span class="sxs-lookup"><span data-stu-id="9bad4-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="9bad4-104">[Scott Ade](https://github.com/scottaddie) ve [Rick Anderson](https://twitter.com/RickAndMSFT) tarafından</span><span class="sxs-lookup"><span data-stu-id="9bad4-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="9bad4-105">Bu makalede, mevcut bir ASP.NET Core 2,2 projesinin ASP.NET Core 3,0 ' e nasıl güncelleştirilmesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="9bad4-106">Önkoşullar</span><span class="sxs-lookup"><span data-stu-id="9bad4-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="9bad4-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="9bad4-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="9bad4-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="9bad4-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="9bad4-109">Mac için Visual Studio</span><span class="sxs-lookup"><span data-stu-id="9bad4-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="9bad4-110">Proje dosyasını güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="9bad4-110">Update the project file</span></span>

* <span data-ttu-id="9bad4-111">[Hedef Framework bilinen adını (tfd)](/dotnet/standard/frameworks) şu şekilde `netcoreapp3.0`ayarlayın:</span><span class="sxs-lookup"><span data-stu-id="9bad4-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="9bad4-112">Her türlü `<PackageReference>` [Microsoft. aspnetcore. All](xref:fundamentals/metapackage) veya [Microsoft. aspnetcore. app](xref:fundamentals/metapackage-app) metapackage ' i kaldırın.</span><span class="sxs-lookup"><span data-stu-id="9bad4-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="9bad4-113">[Microsoft. aspnetcore. Razor. Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) paketini kaldırın. `<PackageReference>`</span><span class="sxs-lookup"><span data-stu-id="9bad4-113">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package.</span></span>

* <span data-ttu-id="9bad4-114">`<PackageReference>` `3.0.0-preview5-19227-01`Paketler için `Version` kalanöğelerinözniteliğinigeçerliönizlemeyegüncelleştirin(`Microsoft.AspNetCore.*` Örneğin,).</span><span class="sxs-lookup"><span data-stu-id="9bad4-114">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="9bad4-115">Bir paketin 3,0 sürümü yoksa, paket 3,0 ' de kullanım dışı bırakılmış olabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-115">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="9bad4-116">Bu paketlerin birçoğu bir `Microsoft.AspNetCore.App` parçasıdır ve tek tek başvurulmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-116">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="9bad4-117">3,0 ' de artık üretilmeyen paketlerin ön listesi için bkz. [3,0 (ASPNET/AspNetCore #3756) içindeki paylaşılan çerçeve derlemeleri için paketleri üretme](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="9bad4-117">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="9bad4-118">*Paylaşılan çerçeve* , makinede yüklü olan ve tarafından `Microsoft.AspNetCore.App`başvurulan derleme ( *. dll* dosyaları) kümesidir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-118">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="9bad4-119">Daha fazla bilgi için bkz. [paylaşılan çerçeve](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="9bad4-119">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="9bad4-120">Birçok önemli bileşeni için derlemeler 3,0 ' den `Microsoft.AspNetCore.App` kaldırılmıştır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-120">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="9bad4-121">`<PackageReference>` [Microsoft. aspnetcore. app 3,0 (ASPNET/aspnetcore #3755) ' den kaldırılmakta olan derlemelerde](https://github.com/aspnet/AspNetCore/issues/3755)listelenen paketlerden API 'leri kullanıyorsanız, öğeleri ekleyin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-121">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="9bad4-122">Kaldırılan bileşenlerin örnekleri şunlardır:</span><span class="sxs-lookup"><span data-stu-id="9bad4-122">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="9bad4-123">' Deki `Microsoft.AspNetCore.App` derleme gönderimi listesi sonlandırılmamıştır ve 3,0 RTM 'den önce değişecektir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-123">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="9bad4-124">Aşağıdaki kodu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="9bad4-124">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="9bad4-125">Yukarıdaki kodda çağrılan `Microsoft.AspNet.WebApi.Client`yöntemi'ye eklenmiştir. `ReadAsAsync`</span><span class="sxs-lookup"><span data-stu-id="9bad4-125">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="9bad4-126">3,0 'deki derleme sorununu çözmek için [Microsoft. Aspnet. WebApi. Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet paketini yükler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-126">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="9bad4-127">[JSON.NET desteği](#jsonnet-support)ekleyin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-127">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="9bad4-128">Projeler varsayılan olarak ASP.NET Core 3,0 veya sonraki sürümlerde [işlem içi barındırma modeline](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-128">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="9bad4-129">İsteğe bağlı olarak, `<AspNetCoreHostingModel>` `InProcess`değeri proje dosyasındaki özelliği olarak kaldırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9bad4-129">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="9bad4-130">Json.NET desteği</span><span class="sxs-lookup"><span data-stu-id="9bad4-130">Json.NET support</span></span>

<span data-ttu-id="9bad4-131">[ASP.NET Core paylaşılan Framework 'ü geliştirmek](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)için çalışmanın bir parçası olarak, [JSON.net](https://www.newtonsoft.com/json/help/html/Introduction.htm) ASP.NET Core paylaşılan çerçevesinden kaldırılmıştır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-131">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="9bad4-132">ASP.NET Core 3,0 projesinde Json.NET kullanmak için:</span><span class="sxs-lookup"><span data-stu-id="9bad4-132">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="9bad4-133">[Microsoft. AspNetCore. Mvc. NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)öğesine bir paket başvurusu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-133">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="9bad4-134">`Startup.ConfigureServices` Çağrılacak`AddNewtonsoftJson`güncelleştirme.</span><span class="sxs-lookup"><span data-stu-id="9bad4-134">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="9bad4-135">`AddNewtonsoftJson`, yeni MVC hizmeti kayıt yöntemleriyle uyumludur:</span><span class="sxs-lookup"><span data-stu-id="9bad4-135">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="9bad4-136">Json.NET ayarları şu çağrıda `AddNewtonsoftJson`ayarlanabilir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-136">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="9bad4-137">MVC hizmeti kaydı</span><span class="sxs-lookup"><span data-stu-id="9bad4-137">MVC service registration</span></span>

<span data-ttu-id="9bad4-138">ASP.NET Core 3,0, içinde `Startup.ConfigureServices`MVC senaryolarını kaydetmek için yeni seçenekler ekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-138">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="9bad4-139">Üzerinde `IServiceCollection` MVC senaryolarıyla ilgili üç yeni en üst düzey uzantı yöntemi kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-139">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="9bad4-140">Şablonlar yerine `UseMvc`bu yeni yöntemleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-140">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="9bad4-141">Ancak, `AddMvc` önceki sürümlerde olduğu gibi davranmaya devam eder.</span><span class="sxs-lookup"><span data-stu-id="9bad4-141">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="9bad4-142">Aşağıdaki örnek, denetleyiciler ve API ile ilgili özellikler için destek ekler, ancak görünümler veya sayfalar değildir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-142">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="9bad4-143">API şablonu şu kodu kullanır:</span><span class="sxs-lookup"><span data-stu-id="9bad4-143">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="9bad4-144">Aşağıdaki örnek, denetleyiciler, API ile ilgili özellikler ve görünümler için destek ekler, ancak sayfalar için değildir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-144">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="9bad4-145">Web uygulaması (MVC) şablonu şu kodu kullanır:</span><span class="sxs-lookup"><span data-stu-id="9bad4-145">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="9bad4-146">Aşağıdaki örnek, Razor Pages ve en düşük denetleyici desteği için destek ekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-146">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="9bad4-147">Web uygulaması şablonu şu kodu kullanır:</span><span class="sxs-lookup"><span data-stu-id="9bad4-147">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="9bad4-148">Yeni yöntemler de birleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-148">The new methods can also be combined.</span></span> <span data-ttu-id="9bad4-149">Aşağıdaki örnek, ASP.NET Core 2,2 ' de `AddMvc` çağırma ile eşdeğerdir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-149">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="9bad4-150">Yönlendirme başlangıç kodunu Güncelleştir</span><span class="sxs-lookup"><span data-stu-id="9bad4-150">Update routing startup code</span></span>

<span data-ttu-id="9bad4-151">Bir uygulama veya `UseSignalR`olarak `UseMvc` çağırırsa, mümkünse uygulamayı [Endpoint Routing](xref:fundamentals/routing) 'e geçirin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-151">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="9bad4-152">MVC 'nin önceki sürümleriyle Endpoint Routing uyumluluğu geliştirmek için ASP.NET Core 2,2 ' de sunulan URL oluşturmayla ilgili bazı değişiklikleri geri çevirdik.</span><span class="sxs-lookup"><span data-stu-id="9bad4-152">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="9bad4-153">2,2 ' de Endpoint Routing kullanarak sorunlarla karşılaşırsanız, aşağıdaki özel durumlarla ASP.NET Core 3,0 ' de iyileştirmeler beklenir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-153">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="9bad4-154">Uygulama `IRouter` veya`Route`devralırsa, şu anda geçiş yapmaktan kaçınmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9bad4-154">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="9bad4-155">[IRouter tabanlı uygulamaları Endpoint Routing 'e geçirmeyi planlıyoruz](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="9bad4-155">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="9bad4-156">Uygulama doğrudan MVC 'nin içine `RouteData.Routers` erişirse, şu anda geçiş yapmaktan kaçınmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9bad4-156">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="9bad4-157">[RouteData. yönlendiricilerini kullanmaya yönelik geçiş kılavuzlarından](https://github.com/aspnet/AspNetCore/issues/9148)geri bildirim sağlayın.</span><span class="sxs-lookup"><span data-stu-id="9bad4-157">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="9bad4-158">Uç nokta yönlendirme, aynı yol deseninin sözdizimini ve yönlendirme deseninin yazma özelliklerini `IRouter`destekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-158">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="9bad4-159">Uç nokta yönlendirme `IRouteContraint`destekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-159">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="9bad4-160">Endpoint Routing, `[Route]` `[HttpGet]`, ve diğer mvc yönlendirme özniteliklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-160">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="9bad4-161">Çoğu uygulama için yalnızca `Startup` değişiklik yapılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-161">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="9bad4-162">Geçişi Başlat. yapılandırma</span><span class="sxs-lookup"><span data-stu-id="9bad4-162">Migrate Startup.Configure</span></span>

<span data-ttu-id="9bad4-163">Genel öneri:</span><span class="sxs-lookup"><span data-stu-id="9bad4-163">General advice:</span></span>

* <span data-ttu-id="9bad4-164">Ekleyin `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="9bad4-164">Add `UseRouting`.</span></span> 
* <span data-ttu-id="9bad4-165">Uygulama çağrııyorsa `UseStaticFiles`, daha **önce** `UseRouting`yerleştirin. `UseStaticFiles`</span><span class="sxs-lookup"><span data-stu-id="9bad4-165">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="9bad4-166">`UseCors` Uygulama, veya `[Authorize]`gibi `AuthorizePage` kimlik doğrulama/yetkilendirme özelliklerini kullanıyorsa `UseAuthentication` , çağrıyı (CORS ara yazılımı kullanılıyorsa `UseAuthorization` ve sonrasında) ve **sonra** `UseRouting` da girin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-166">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting` (and **after** `UseCors` if CORS Middleware is used).</span></span>
* <span data-ttu-id="9bad4-167">Veya `UseMvc` ile`UseSignalR` değiştirin`UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="9bad4-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>
* <span data-ttu-id="9bad4-168">Uygulama gibi [CORS](xref:security/cors) senaryolarını `[EnableCors]`kullanıyorsa, CORS 'yi kullanan diğer tüm middlewares `UseAuthorization`(örneğin `UseCors` ,, ve `UseMvc`' dan önce `UseAuthentication`yer `UseCors` alan) önce çağrısı yapın.</span><span class="sxs-lookup"><span data-stu-id="9bad4-168">If the app uses [CORS](xref:security/cors) scenarios, such as `[EnableCors]`, place the call to `UseCors` before any other middlewares that use CORS (for example, place `UseCors` before `UseAuthentication`, `UseAuthorization`, and `UseMvc`).</span></span>
* <span data-ttu-id="9bad4-169">İle `IHostingEnvironment` `Microsoft.Extensions.Hosting` değiştirin ve ad alanı `using` için bir ifade ekleyin. `IWebHostEnvironment`</span><span class="sxs-lookup"><span data-stu-id="9bad4-169">Replace `IHostingEnvironment` with `IWebHostEnvironment` and add a `using` statement for the `Microsoft.Extensions.Hosting` namespace.</span></span>

<span data-ttu-id="9bad4-170">Aşağıda, tipik bir ASP.NET Core 2,2 `Startup.Configure` uygulamasında bir örneği verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-170">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="9bad4-171">Önceki `Startup.Configure` kodu güncelleştirdikten sonra:</span><span class="sxs-lookup"><span data-stu-id="9bad4-171">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseRouting();

    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="9bad4-172">Güvenlik ara yazılımı Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="9bad4-172">Security middleware guidance</span></span>

<span data-ttu-id="9bad4-173">Yetkilendirme ve CORS desteği, [Ara yazılım](xref:fundamentals/middleware/index) yaklaşımına göre birleştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-173">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="9bad4-174">Bu, bu senaryolarda aynı ara yazılım ve işlevselliğin kullanılmasına izin verir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-174">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="9bad4-175">Bu sürümde güncelleştirilmiş bir yetkilendirme ara yazılımı sunulmaktadır ve CORS ara yazılımı, MVC denetleyicileri tarafından kullanılan öznitelikleri anlayabilmesi için geliştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-175">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="9bad4-176">CORS</span><span class="sxs-lookup"><span data-stu-id="9bad4-176">CORS</span></span>

<span data-ttu-id="9bad4-177">Daha önce CORS yapılandırması zor olabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-177">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="9bad4-178">Ara yazılım bazı kullanım durumlarında kullanılmak üzere sağlandı, ancak MVC filtrelerinin diğer kullanım durumlarında ara yazılım **olmadan** kullanılması amaçlandı.</span><span class="sxs-lookup"><span data-stu-id="9bad4-178">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="9bad4-179">ASP.NET Core 3,0 ile, CORS gerektiren tüm uygulamaların, uç nokta yönlendirme ile birlikte CORS ara yazılımını kullanması önerilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-179">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="9bad4-180">`UseCors`, varsayılan bir ilkeyle ve `[EnableCors]` `[DisableCors]` gerekli olduğunda varsayılan ilkeyi geçersiz kılmak için öznitelikler kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-180">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="9bad4-181">Aşağıdaki örnekte:</span><span class="sxs-lookup"><span data-stu-id="9bad4-181">In the following example:</span></span>

* <span data-ttu-id="9bad4-182">CORS, `default` adlandırılmış ilkeye sahip tüm uç noktalar için etkinleştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-182">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="9bad4-183">Sınıfı, CORS `[DisableCors]` 'yi özniteliğiyle devre dışı bırakır. `MyController`</span><span class="sxs-lookup"><span data-stu-id="9bad4-183">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="9bad4-184">Yetkilendirme</span><span class="sxs-lookup"><span data-stu-id="9bad4-184">Authorization</span></span>

<span data-ttu-id="9bad4-185">ASP.NET Core önceki sürümlerinde, yetkilendirme desteği `[Authorize]` özniteliği aracılığıyla sağlandı.</span><span class="sxs-lookup"><span data-stu-id="9bad4-185">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="9bad4-186">Yetkilendirme ara yazılımı kullanılamıyor.</span><span class="sxs-lookup"><span data-stu-id="9bad4-186">Authorization middleware wasn't available.</span></span> <span data-ttu-id="9bad4-187">ASP.NET Core 3,0 ' de, yetkilendirme ara yazılımı gereklidir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-187">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="9bad4-188">ASP.NET Core yetkilendirmesi ara yazılımını (`UseAuthorization`) hemen sonra `UseAuthentication`yerleştirmenizi öneririz.</span><span class="sxs-lookup"><span data-stu-id="9bad4-188">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="9bad4-189">Yetkilendirme ara yazılımı, geçersiz kılınabilen varsayılan ilkeyle de yapılandırılabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-189">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="9bad4-190">ASP.NET Core 3,0 veya üzeri `UseAuthorization` sürümlerde ' de `Startup.Configure`çağrılır ve aşağıdakiler `HomeController` oturum açmış bir kullanıcı gerektirir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-190">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="9bad4-191">Uygulama, MVC 'de Genel `AuthorizeFilter` bir filtre olarak kullanıyorsa, bir ilke `UseAuthorization` sağlamak için kodu yeniden düzenlemenizi öneririz.</span><span class="sxs-lookup"><span data-stu-id="9bad4-191">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="9bad4-192">Aşağıdaki örnekte, çağrıldığında tüm isteklere `UseAuthorization` uygulanacak özel bir ilke `HomeController` ve Kullanıcı, uygulamada oturum açmadan erişime izin verir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-192">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="9bad4-193">Belirli bir yetkilendirme bilgisine sahip olmayan, ara yazılım tarafından yetkilendirme çalışmaktadır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-193">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="9bad4-194">Örneğin, [sistem durumu denetimlerinde](xref:host-and-deploy/health-checks) belirli bir yetkilendirme bilgisi yoktur, ancak sistem durumu denetimleri, ara yazılım tarafından uygulanan yapılandırılabilir bir yetkilendirme ilkesine sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-194">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="9bad4-195">Aşağıdaki örnekte, `UseAuthorization` yetkilendirmeyi varsayılan bir ilke olmadan işler, `/healthz` ancak sistem durumu denetimi uç noktası `admin` kullanıcının rolünde olmasını gerektirir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-195">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="9bad4-196">Koruma bazı senaryolar için uygulanır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-196">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="9bad4-197">`UseEndpoint`eksik bir ara yazılım nedeniyle yetkilendirme veya CORS ilkesi atlandıysa, ara yazılım bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="9bad4-197">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="9bad4-198">Yanlış yapılandırma hakkında ek geribildirim sağlamak için çözümleyici desteği devam ediyor.</span><span class="sxs-lookup"><span data-stu-id="9bad4-198">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="9bad4-199">SignalR 'yi geçir</span><span class="sxs-lookup"><span data-stu-id="9bad4-199">Migrate SignalR</span></span>

<span data-ttu-id="9bad4-200">SignalR hub 'larının eşlemesi artık içinde `UseEndpoints`yer alır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-200">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="9bad4-201">Her hub 'ı ile `MapHub`eşleyin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-201">Map each hub with `MapHub`.</span></span> <span data-ttu-id="9bad4-202">Önceki sürümlerde olduğu gibi, her hub açık olarak listelenir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-202">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="9bad4-203">Aşağıdaki örnekte, `ChatHub` SignalR hub 'ı için destek eklenmiştir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-203">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="9bad4-204">MVC denetleyicilerini geçirme</span><span class="sxs-lookup"><span data-stu-id="9bad4-204">Migrate MVC controllers</span></span>

<span data-ttu-id="9bad4-205">Denetleyicilerin eşlemesi artık içinde `UseEndpoints`yer alır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-205">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="9bad4-206">Uygulama `MapControllers` öznitelik yönlendirme kullanıyorsa ekleyin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-206">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="9bad4-207">Yönlendirme ASP.NET Core 3,0 veya sonraki sürümlerde birçok çerçeve için destek içerdiğinden, öznitelik yönlendirmeli denetleyiciler eklemek kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-207">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="9bad4-208">Şunları değiştirin:</span><span class="sxs-lookup"><span data-stu-id="9bad4-208">Replace the following:</span></span>

* <span data-ttu-id="9bad4-209">`MapRoute`kullanılarak`MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="9bad4-209">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="9bad4-210">`MapAreaRoute`kullanılarak`MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="9bad4-210">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="9bad4-211">Yönlendirme şimdi yalnızca MVC 'den daha fazlasına destek içerdiğinden, bu yöntemlerin ne yapacaklarına açık bir şekilde sahip olması için terminoloji değişmiştir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-211">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="9bad4-212">Gibi `MapControllerRoute` geleneksel yollar `MapAreaControllerRoute` eklendikleri /sıraylauygulanır /. `MapDefaultControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="9bad4-212">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="9bad4-213">Önce daha belirli yollar (örneğin, bir alana yönelik yollar) yerleştirin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-213">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="9bad4-214">Aşağıdaki örnekte:</span><span class="sxs-lookup"><span data-stu-id="9bad4-214">In the following example:</span></span>

* <span data-ttu-id="9bad4-215">`MapControllers`öznitelik yönlendirmeli denetleyiciler için destek ekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-215">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="9bad4-216">`MapAreaControllerRoute`bir alanındaki denetleyiciler için geleneksel bir yol ekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-216">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="9bad4-217">`MapControllerRoute`denetleyiciler için geleneksel bir yol ekler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-217">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="9bad4-218">Razor Pages geçir</span><span class="sxs-lookup"><span data-stu-id="9bad4-218">Migrate Razor Pages</span></span>

<span data-ttu-id="9bad4-219">Eşleme Razor Pages artık içinde `UseEndpoints`yer alır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-219">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="9bad4-220">Uygulama `MapRazorPages` Razor Pages kullanıyorsa ekleyin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-220">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="9bad4-221">Endpoint Routing birçok çerçeve için destek içerdiğinden Razor Pages eklemek artık tercih edilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-221">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="9bad4-222">Aşağıdaki örnekte `MapRazorPages` Razor Pages için destek ekler:</span><span class="sxs-lookup"><span data-stu-id="9bad4-222">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="9bad4-223">Endpoint Routing olmadan MVC kullanma</span><span class="sxs-lookup"><span data-stu-id="9bad4-223">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="9bad4-224">ASP.NET Core 3,0 aracılığıyla `UseMvc` veya `UseMvcWithDefaultRoute` içinde MVC kullanmanın içinde `Startup.ConfigureServices`açık bir kabul olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-224">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="9bad4-225">Bu, MVC 'nin başlatma sırasında yetkilendirme ve CORS ara yazılımını kullanıp kullanamayacağını bilmeleri gerektiğinden gereklidir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-225">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="9bad4-226">Uygulama desteklenmeyen bir yapılandırmayı kullanmayı denerse sizi uyaran bir çözümleyici sağlanır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-226">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="9bad4-227">Uygulama eski `IRouter` destek gerektiriyorsa, içinde `Startup.ConfigureServices`aşağıdaki yaklaşımlardan herhangi birini kullanarak devre dışı bırakın `EnableEndpointRouting` :</span><span class="sxs-lookup"><span data-stu-id="9bad4-227">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="9bad4-228">Durum denetimlerini geçir</span><span class="sxs-lookup"><span data-stu-id="9bad4-228">Migrate health checks</span></span>

<span data-ttu-id="9bad4-229">Sistem durumu denetimleri, uç nokta yönlendirme özellikli bir *yönlendirici-yazılım* olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-229">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="9bad4-230">Uç `MapHealthChecks` nokta yönlendirme ile durum denetimlerini kullanmak için ekleyin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-230">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="9bad4-231">Yöntemi şuna benzer bağımsız değişkenleri kabul eder. `UseHealthChecks` `MapHealthChecks`</span><span class="sxs-lookup"><span data-stu-id="9bad4-231">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="9bad4-232">`MapHealthChecks` Kullanmanın`UseHealthChecks` avantajı, yetkilendirme uygulayabilir ve eşleşen ilke üzerinde daha ayrıntılı denetime sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-232">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="9bad4-233">Aşağıdaki örnekte, `MapHealthChecks` şu konumda `/healthz`bir sistem durumu denetim uç noktası için çağrılır:</span><span class="sxs-lookup"><span data-stu-id="9bad4-233">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="9bad4-234">HostBuilder WebHostBuilder 'ın yerini alır</span><span class="sxs-lookup"><span data-stu-id="9bad4-234">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="9bad4-235">ASP.NET Core 3,0 şablonları [genel konak](xref:fundamentals/host/generic-host)kullanır.</span><span class="sxs-lookup"><span data-stu-id="9bad4-235">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="9bad4-236">Önceki sürümler [Web ana bilgisayarı](xref:fundamentals/host/web-host)kullandı.</span><span class="sxs-lookup"><span data-stu-id="9bad4-236">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="9bad4-237">Aşağıdaki kod ASP.NET Core 3,0 şablonu oluşturulan `Program` sınıfını gösterir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-237">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="9bad4-238">Aşağıdaki kod, ASP.NET Core 2,2 şablonu tarafından oluşturulan `Program` sınıfı gösterir:</span><span class="sxs-lookup"><span data-stu-id="9bad4-238">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="9bad4-239"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder>3,0 ' de kalır ve önceki kod örneğinde `webBuilder` görülen öğesinin türüdür.</span><span class="sxs-lookup"><span data-stu-id="9bad4-239"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="9bad4-240"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder>gelecekteki bir sürümde kullanımdan kaldırılacak ve tarafından `HostBuilder`değiştirilmeyecektir.</span><span class="sxs-lookup"><span data-stu-id="9bad4-240"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="9bad4-241">' Den `WebHostBuilder` ' ye `HostBuilder` en önemli değişiklik, [bağımlılık ekleme (dı)](xref:fundamentals/dependency-injection)içinde.</span><span class="sxs-lookup"><span data-stu-id="9bad4-241">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="9bad4-242">Kullanırken `HostBuilder`, <xref:Microsoft.Extensions.Configuration.IConfiguration> yalnızca <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> oluşturucusunu ekleyebilir. `Startup`</span><span class="sxs-lookup"><span data-stu-id="9bad4-242">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="9bad4-243">`HostBuilder` Dı kısıtlamaları:</span><span class="sxs-lookup"><span data-stu-id="9bad4-243">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="9bad4-244">Dı kapsayıcısının yalnızca bir kez derlenme özelliğini etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="9bad4-244">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="9bad4-245">Birden çok tekton örneğini çözümlemek gibi ortaya çıkan nesne ömrü sorunlarını önler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-245">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="9bad4-246">SignalR kodunu Güncelleştir</span><span class="sxs-lookup"><span data-stu-id="9bad4-246">Update SignalR code</span></span>

<span data-ttu-id="9bad4-247">`System.Text.Json`, artık istemci ve sunucu tarafından kullanılan varsayılan Hub protokolüdür.</span><span class="sxs-lookup"><span data-stu-id="9bad4-247">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="9bad4-248">İçinde `Startup.ConfigureServices`, seri `AddJsonProtocol` hale getirici seçeneklerini ayarlamak için çağırın.</span><span class="sxs-lookup"><span data-stu-id="9bad4-248">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="9bad4-249">**Server**</span><span class="sxs-lookup"><span data-stu-id="9bad4-249">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="9bad4-250">**İstemci:**</span><span class="sxs-lookup"><span data-stu-id="9bad4-250">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="9bad4-251">Newtonsoft. JSON öğesine geç</span><span class="sxs-lookup"><span data-stu-id="9bad4-251">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="9bad4-252">' De `Newtonsoft.Json` `System.Text.Json`desteklenmeyen özellikleri kullanıyorsanız, ' ye `Newtonsoft.Json`geri dönebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="9bad4-252">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="9bad4-253">[Microsoft. AspNetCore. SignalR. Protocols. NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet paketini yükler.</span><span class="sxs-lookup"><span data-stu-id="9bad4-253">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="9bad4-254">İstemcisinde, `AddNewtonsoftJsonProtocol` `HubConnectionBuilder` örneğine bir yöntem çağrısı zincirleyebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="9bad4-254">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="9bad4-255">Sunucusunda, içinde `AddNewtonsoftJsonProtocol` `AddSignalR` yöntemçağrısınabiryöntemçağrısızincirleyebilirsiniz`Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="9bad4-255">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="9bad4-256">Çalışma zamanı derlemesini kabul et</span><span class="sxs-lookup"><span data-stu-id="9bad4-256">Opt in to runtime compilation</span></span>

<span data-ttu-id="9bad4-257">3,0 ' de, çalışma zamanı derlemesi bir katılım senaryosudur.</span><span class="sxs-lookup"><span data-stu-id="9bad4-257">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="9bad4-258">Çalışma zamanı derlemesini etkinleştirmek için bkz https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span><span class="sxs-lookup"><span data-stu-id="9bad4-258">To enable runtime compilation, see https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span></span>

---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: "ASP.NET Web sayfaları sunarak - veritabanı verileri güncelleştirme | Microsoft Docs"
author: tfitzmac
description: "Bu öğreticide, ASP.NET Web sayfaları (Razor) kullandığınızda (değiştirin) var olan bir veritabanını girişini güncelleştirmek nasıl gösterir. Seri tamamladığınızdan varsayar th..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 01/02/2018
ms.topic: article
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: aabf572e254de9861719fdc502340353482919b4
ms.sourcegitcommit: 281f0c614543a6c3db565ea4655b70fe49b61d84
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 01/03/2018
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="f7fa2-104">ASP.NET Web sayfaları sunarak - veritabanı verileri güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>
====================
<span data-ttu-id="f7fa2-105">tarafından [zel FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="f7fa2-106">Bu öğreticide, ASP.NET Web sayfaları (Razor) kullandığınızda (değiştirin) var olan bir veritabanını girişini güncelleştirmek nasıl gösterir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="f7fa2-107">Seri aracılığıyla tamamladığınızdan varsayar [veri girişi tarafından Forms kullanarak ASP.NET Web sayfalarını kullanma](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="f7fa2-108">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="f7fa2-109">Tek bir kayıtta seçmek nasıl `WebGrid` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="f7fa2-110">Tek kayıtlı bir veritabanından okunmaya yapma.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="f7fa2-111">Veritabanı kaydı değerlerinden bir formla dağıtılacak yapma.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="f7fa2-112">Bir veritabanında varolan bir kaydı güncelleştirmeye nasıl.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="f7fa2-113">Görüntülemeden sayfasındaki bilgileri depolamak nasıl.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="f7fa2-114">Gizli bir alan bilgilerini depolamak için nasıl kullanılacağını.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="f7fa2-115">Özellikler/teknolojilerini ele alınan:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="f7fa2-116">`WebGrid` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="f7fa2-117">SQL `Update` komutu.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="f7fa2-118">`Database.Execute` Yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="f7fa2-119">Gizli alanlar (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="f7fa2-120">Ne oluşturacağınız</span><span class="sxs-lookup"><span data-stu-id="f7fa2-120">What You'll Build</span></span>

<span data-ttu-id="f7fa2-121">Önceki öğreticide, bir veritabanına bir kayıt eklemek öğrendiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="f7fa2-122">Burada, düzenlemek için bir kayıt görüntülemek nasıl öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="f7fa2-123">İçinde *filmler* update sayfasında, `WebGrid` onun görüntüler için yardımcı bir **Düzenle** her film yanındaki bağlantı:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![WebGrid görüntülemek her film için 'Düzenle' bağlantı](updating-data/_static/image1.png)

<span data-ttu-id="f7fa2-125">Tıkladığınızda **Düzenle** bağlantı sürdüğünü, farklı bir sayfaya film bilgileri zaten bir formda olduğu:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Düzenlenecek film gösteren film sayfasını Düzenle](updating-data/_static/image2.png)

<span data-ttu-id="f7fa2-127">Herhangi bir değeri değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-127">You can change any of the values.</span></span> <span data-ttu-id="f7fa2-128">Değişiklikleri gönderdiğinizde, sayfa kodunda veritabanını güncelleştirir ve film listenin geri alır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="f7fa2-129">Bu işlemin parçası çalışır neredeyse tam olarak gibi *AddMovie.cshtml* oluşturduğunuz önceki öğreticide, bu öğreticinin daha tanıdık gelecektir şekilde sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="f7fa2-130">Tek bir filmi düzenlemek için bir yol uygulamak birkaç yolu vardır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="f7fa2-131">Uygulama kolay ve kolay anlaşılır olduğundan gösterilen yaklaşım seçilmiştir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="f7fa2-132">Film listenin bir düzenleme bağlantısı ekleme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="f7fa2-133">Başlamak için güncelleştireceğim *filmler* de listeleme her film içeren sayfasında bir **Düzenle** bağlantı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="f7fa2-134">Açık *Movies.cshtml* dosya.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="f7fa2-135">Sayfasının gövdesinde değiştirmek `WebGrid` bir sütunu ekleyerek biçimlendirme.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="f7fa2-136">Değiştirilen biçimlendirme aşağıdaki gibidir:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="f7fa2-137">Yeni bir sütun bu bilgisayardır:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="f7fa2-138">Bu sütun bir bağlantıyı göstermek için noktasıdır (`<a>` öğesi) "Düzenle" metnini söyler.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="f7fa2-139">Sonra ki olduğundan sayfa çalıştığında, aşağıdakine benzer bir bağlantı oluşturmak için ile `id` her film farklı değeri:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="f7fa2-140">Bu bağlantıyı adlı bir sayfaya çağıracağı *EditMovie*, ve sorgu dizesi geçer `?id=7` bu sayfaya.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="f7fa2-141">Yeni bir sütun için söz dizimi biraz karmaşık görünebilir, ancak yalnızca birkaç öğelerini birlikte koyar olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="f7fa2-142">Her öğesi basittir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-142">Each individual element is straightforward.</span></span> <span data-ttu-id="f7fa2-143">Üzerinde yoğunlaşabilirsiniz varsa yalnızca `<a>` öğesi, bu biçimlendirme bakın:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="f7fa2-144">Kılavuz nasıl çalıştığı hakkında bazı arka plan: kılavuz satırları, her veritabanı kaydı için bir tane görüntüler ve veritabanı kaydında her bir alan sütunlarını görüntüler.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="f7fa2-145">Her kılavuz satır oluşturulmuş olsa da, `item` nesne satır veritabanı kaydını (öğe) içerir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="f7fa2-146">Bu düzenleme kod ilgili satır veri almak için bir yol sağlar.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="f7fa2-147">Burada gördüğünüz olmasıdır: ifade `item.ID` kimliği değeri geçerli veritabanı öğesinin almaktır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="f7fa2-148">Kullanarak veritabanı değerleri (başlık, tarz veya yıl) aynı şekilde alabilir `item.Title`, `item.Genre`, veya `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="f7fa2-149">İfade `"~/EditMovie?id=@item.ID` hedef URL kodlanmış parçası birleştirir (`~/EditMovie?id=`) dinamik olarak türetilen bu kimliğiyle</span><span class="sxs-lookup"><span data-stu-id="f7fa2-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="f7fa2-150">(Gördüğünüz `~` önceki öğreticideki; işleci, geçerli Web sitesi kök temsil eden bir ASP.NET operatördür.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="f7fa2-151">Bu bölümü biçimlendirme sütununda yalnızca aşağıdaki biçimlendirme gibi çalışma zamanında oluşturduğundan emin sonucudur:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="f7fa2-152">Doğal olarak, gerçek değerini `id` her satır için farklı olacaktır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="f7fa2-153">Kılavuz sütunu için özel bir görüntü oluşturma</span><span class="sxs-lookup"><span data-stu-id="f7fa2-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="f7fa2-154">Şimdi kılavuz sütunu yedekleyin.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-154">Now back to the grid column.</span></span> <span data-ttu-id="f7fa2-155">Üç sütun başlangıçta görüntülenen kılavuz yalnızca veri değerlerini (başlık, tarzını ve yıl) içeriyor.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="f7fa2-156">Bu görüntü veritabanı sütununun adını geçirerek belirtilen &mdash; Örneğin, `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="f7fa2-157">Bu yeni **Düzenle** bağlantı sütun farklıdır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="f7fa2-158">Bir sütun adı belirtmek yerine, geçirme bir `format` parametresi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="f7fa2-159">Bu parametre, biçimlendirme tanımlamanıza olanak sağlar, `WebGrid` Yardımcısı ile birlikte sokacak `item` kalın veya yeşil olarak sütun verileri görüntüleyebilir veya ne olursa olsun, biçiminde istediğiniz değer.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="f7fa2-160">Örneğin, kalın görünmesi başlık istediyseniz, bu örnek gibi bir sütun oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="f7fa2-161">(Çeşitli `@` içinde gördüğünüz karakterleri `format` özelliği biçimlendirme ve bir kod değeri arasında geçiş işaretlemek.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="f7fa2-162">İlgili öğrendikten sonra `format` özelliği, onu anlamak daha kolay nasıl yeni **Düzenle** bağlantı sütununu birlikte yerleştir:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="f7fa2-163">Sütun oluşur *yalnızca* bağlantıyı işleyen biçimlerini, bazı bilgiler (ID), ayıklanır satır için veritabanı kaydından.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="f7fa2-164">**Adlandırılmış parametreler ve bir yöntem için konumsal Parametreler**</span><span class="sxs-lookup"><span data-stu-id="f7fa2-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="f7fa2-165">Birçok kez yöntemi çağrılır ve kendisine geçirilen parametreleri, virgülle ayrılmış parametre değerleri yalnızca listelediğiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="f7fa2-166">Aşağıda birkaç örnek verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="f7fa2-167">Biz öncelikle bu kodu gördüğünüz, ancak her durumda, belirli bir sırada yöntemlerine parametreleri geçirme sorunu Bahsediyor kaydetmedi &mdash; yani, bu yöntem parametreleri tanımlanır sırası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="f7fa2-168">İçin `db.Execute` ve `Validation.RequireFields`, geçirdiğiniz değerlerin sırasını karma, sayfa çalıştığında bir hata iletisi veya en azından bazı garip sonuçları elde etmesi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="f7fa2-169">Açıkçası, parametrelerde geçirmek için sipariş bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="f7fa2-170">(Webmatrix'te, IntelliSense, Şekil adı, türü ve parametreler sırasını öğrenmenize yardımcı olabilir.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="f7fa2-171">Geçirme değerleri sırada alternatif olarak, kullandığınız *adlandırılmış parametreleri*.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="f7fa2-172">(Parametreleri sırada geçirme bilinen kullanarak olarak *konumsal parametreler*.) Adlandırılmış parametreler için açıkça parametresinin adı değerini geçirilirken içerir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="f7fa2-173">Bu öğreticiler adlandırılmış parametreleri birkaç kez zaten kullandığınız.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="f7fa2-174">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="f7fa2-175">and</span><span class="sxs-lookup"><span data-stu-id="f7fa2-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="f7fa2-176">Özellikle bir yöntem birçok parametreleri yönlendirdiğinde adlandırılmış parametreleri durumlarda birkaç için kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="f7fa2-177">Yalnızca bir veya iki parametreleri geçirmek istiyorsanız ancak geçirmek istediğiniz değerleri parametre listesindeki ilk konumlar arasında olmayan biridir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="f7fa2-178">Çoğu sizin için anlamlı sırada parametreleri ileterek kodunuzu daha okunabilir hale getirmek istediğinizde başka bir durumdur.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="f7fa2-179">Belli ki, adlandırılmış parametreleri kullanmak için parametreleri adlarını bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="f7fa2-180">WebMatrix IntelliSense yapabilirsiniz *Göster* adları, ancak şu anda bunları sizin için doldurduğunuz olamaz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="f7fa2-181">Düzen sayfası oluşturma</span><span class="sxs-lookup"><span data-stu-id="f7fa2-181">Creating the Edit Page</span></span>

<span data-ttu-id="f7fa2-182">Oluşturabileceğiniz artık *EditMovie* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="f7fa2-183">Kullanıcılar'ı tıklattığınızda **Düzenle** bağlantı, bunlar şunun bu sayfada.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="f7fa2-184">Adlı bir sayfa oluşturma *EditMovie.cshtml* ve dosyasında, aşağıdaki biçimlendirme nedir değiştirin:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="f7fa2-185">Bu biçimlendirme ve kodun, sahip benzer *AddMovie* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="f7fa2-186">Gönder düğmesine metninde küçük bir fark yoktur.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="f7fa2-187">İle *AddMovie* sayfasında, var olan bir `Html.ValidationSummary` varsa, doğrulama hataları görüntüler çağrısı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="f7fa2-188">Biz bırakarak çağrıları çıkışı bu kez `Validation.Message`, bu yana hataları doğrulama özeti görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="f7fa2-189">Önceki öğreticide belirtildiği gibi çeşitli bileşimlerde doğrulama özeti ve tek tek hata iletileri kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="f7fa2-190">Yeniden dikkat `method` özniteliği `<form>` ayarlanır `post`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="f7fa2-191">İle *AddMovie.cshtml* sayfası, bu sayfayı yapar değişiklikleri veritabanına.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="f7fa2-192">Bu nedenle, bu formu gerçekleştirmesi gereken bir `POST` işlemi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="f7fa2-193">(Arasındaki fark hakkında daha fazla bilgi için `GET` ve `POST` işlemleri görmek [GET, POST ve HTTP fiili güvenliği](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) kenar HTML formları öğreticideki.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="f7fa2-194">Bir önceki öğreticide gördüğünüz gibi `value` metin kutuları özniteliklerini ayarlanır Razor kodu ile bunları önceden yüklemek için.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="f7fa2-195">Bu süre, yine de değişkenler gibi kullandığınız `title` ve `genre` yerine bu görev için `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="f7fa2-196">Olarak daha önce bu biçimlendirme metin kutusu değerleri film değerlerle dağıtılacak.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="f7fa2-197">Neden olduğunu kullanmak yerine bu kez değişkenleri kullanmak için kullanışlı bir dakika içinde görürsünüz `Request` nesnesi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="f7fa2-198">Ayrıca bir `<input type="hidden">` bu sayfada öğe.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="f7fa2-199">Bu öğe görünür sayfada yapmadan film Kimliğini depolar.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="f7fa2-200">Kimliği başlangıçta sayfa tarafından bir sorgu dizesi değeri kullanılarak geçirilir (`?id=7` veya URL'deki benzer).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="f7fa2-201">Gizli bir alana kimliği değeri koyarak kullanılabilir olduğundan emin yapabileceğiniz zaman formun gönderildi, sayfa ile çağrıldı özgün URL artık erişiminiz olsa bile.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="f7fa2-202">Farklı *AddMovie* sayfası, kodunu *EditMovie* sayfası, iki farklı işlevler içerir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="f7fa2-203">Sayfa ilk kez görüntülendiğinde olan ilk işlevi (ve *yalnızca* sonra), kod Sorgu dizesinden film Kimliğini alır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="f7fa2-204">Kod sonra kullandığı kimliği veritabanı dışında karşılık gelen film okuyun ve görüntülemek için (Bu metin kutularına dağıtılacak).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="f7fa2-205">Kullanıcı tıklattığında olan ikinci işlev **gönderme değişiklikleri** düğmesini koduna sahip metin kutuları değerini okumak ve onları doğrulamak.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="f7fa2-206">Kod yeni değerlerle veritabanı öğesini güncelleştirmek de vardır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="f7fa2-207">İçinde anlatıldığı gibi bu teknik bir kayıt eklemeye benzer *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="f7fa2-208">Tek bir filmi okumak için kod ekleme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="f7fa2-209">İlk işlevi gerçekleştirmek için bu kodu sayfanın üst kısmına ekleyin:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="f7fa2-210">Bu kod çoğunu başlayan bir blok içinde olduğu `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="f7fa2-211">`!` İşleci anlamına gelir "değil" ifade anlamına gelir şekilde *bu isteği bir posta gönderimi değilse*, bildiren dolaylı bir yolu olduğu *bu isteği bu sayfayı çalıştırmak ilk kez olup olmadığını*.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="f7fa2-212">Daha önce belirtildiği gibi bu kodun çalışması gerektiğini *yalnızca* sayfa ilk çalıştığında.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="f7fa2-213">Kodda içine alamadık, `if(!IsPost)`, sayfa, olup ilk kez çağrılması her zaman çalışması veya yanıt düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="f7fa2-214">Kod içeren bildirim bir `else` engelleme bu süre.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="f7fa2-215">Ne zaman gösterdiğimizi denirse gibi `if` blokları, bazen test koşul doğru değilse alternatif kodu çalıştırmak istediğiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="f7fa2-216">Burada bir durumdur.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-216">That's the case here.</span></span> <span data-ttu-id="f7fa2-217">(Diğer bir deyişle, sayfaya geçirilen kimliği Tamam ise) koşul geçerse, bir satır veritabanından okuyamadı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="f7fa2-218">Ancak, koşul geçmiyor, `else` bloğu çalıştırır ve bu kod hata iletisine ayarlar.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="f7fa2-219">Sayfaya geçirilen bir değer doğrulanıyor</span><span class="sxs-lookup"><span data-stu-id="f7fa2-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="f7fa2-220">Kod kullanan `Request.QueryString["id"]` sayfasına geçirilen kimliği alınamadı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="f7fa2-221">Kod bir değer kimliği için gerçekten geçildi emin olur</span><span class="sxs-lookup"><span data-stu-id="f7fa2-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="f7fa2-222">Hiçbir değer geçirildi, kodu bir doğrulama hatası ayarlar.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="f7fa2-223">Bu kod bilgileri doğrulamak için farklı bir şekilde gösterir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="f7fa2-224">Önceki öğreticide çalıştığınız `Validation` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="f7fa2-225">Doğrulanacak alanları kayıtlı ve ASP.NET otomatik olarak doğrulama vermedi ve hataları kullanarak görüntülenen `Html.ValidationMessage` ve `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="f7fa2-226">Bu durumda, ancak siz değil gerçekten kullanıcı girişini doğrulama.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="f7fa2-227">Bunun yerine, başka bir yerden sayfaya geçirilmedi bir değer doğrulama.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="f7fa2-228">`Validation` Yardımcı değil yapın, sizin için.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="f7fa2-229">Bu nedenle, değer kendiniz ile test ederek denetimi `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="f7fa2-230">Bir sorun varsa, kullanarak hata görüntüleyebilirsiniz `Html.ValidationSummary`ile yaptığınız gibi `Validation` Yardımcısı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="f7fa2-231">Bunu yapmak için arama `Validation.AddFormError` ve görüntülenecek bir ileti geçirin.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="f7fa2-232">`Validation.AddFormError`zaten aşina doğrulama sistemi oturum tie özel iletileri tanımlamanıza izin verir yerleşik bir yöntemdir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="f7fa2-233">(Daha sonra Bu öğreticide Biz bu doğrulama işlemi biraz daha sağlam hale hakkında konuşun.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="f7fa2-234">Film kimliği olduğundan emin olduktan sonra kodu için yalnızca bir tek veritabanı öğesi arayan veritabanı okur.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="f7fa2-235">(Büyük olasılıkla genel düzeni veritabanı işlemleri için fark etmiş: veritabanını açmak, bir SQL deyimi tanımlamak ve deyimini çalıştırın.) Bu süre, SQL `Select` deyimi içeren `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="f7fa2-236">Yalnızca bir kayıt kimliği benzersiz olduğundan döndürülebilir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="f7fa2-237">Sorgu kullanılarak gerçekleştirilen `db.QuerySingle` (değil `db.Query`film listesi için kullanılan gibi), ve sonucu kodu yerleştirir `row` değişkeni.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="f7fa2-238">Adı `row` rastgeledir; değişkenleri herhangi bir şey istediğiniz gibi adlandırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="f7fa2-239">Bu değerleri metin kutularına görüntülenebilir böylece en üstünde başlatılmış değişkenleri sonra film ayrıntıları ile doldurulur.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="f7fa2-240">Düzen Sayfası (kadarki) test etme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="f7fa2-241">Sayfanızın test etmek isterseniz, çalıştırmak *filmler* şimdi sayfasında ve tıklayın bir **Düzenle** herhangi film yanındaki bağlantı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="f7fa2-242">Göreceğiniz *EditMovie* Ayrıntıları sayfası doldurulmuş seçtiğiniz film için:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Düzenlenecek film gösteren film sayfasını Düzenle](updating-data/_static/image3.png)

<span data-ttu-id="f7fa2-244">Sayfanın URL'sini şöyle içerdiğine dikkat edin `?id=10` (veya başka bir sayıyı).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="f7fa2-245">Şu ana kadar test ettiğiniz **Düzenle** içinde bağlantılar *film* çalışma sayfanız kimliği Sorgu dizesinden okuyor ve veritabanı tek film kaydı almak için sorgu çalışma sayfasında.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="f7fa2-246">Film bilgileri değiştirebilirsiniz, ancak tıklattığınızda hiçbir şey olmaz **gönderme değişiklikleri**.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="f7fa2-247">Film kullanıcının değişikliklerle güncelleştirmek için kod ekleme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="f7fa2-248">İçinde *EditMovie.cshtml* dosya, ikinci işlevi (değişiklikleri kaydetme) uygulamak için aşağıdaki kodu kapanış ayracı yalnızca eklemek `@` bloğu.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="f7fa2-249">(Kodu tam olarak nereye yerleştirileceğini emin değilseniz, bakabilir [Düzenle film sayfa için tam kod](#Complete_Page_Listing_for_EditMovie) Bu öğreticinin sonunda görüntülenir.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="f7fa2-250">Yeniden, bu biçimlendirme ve kodun benzer kodunda *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="f7fa2-251">Kod bir `if(IsPost)` yalnızca kullanıcı tıkladığında bu kod çalıştığından bloğunu **gönderme değişiklikleri** düğmesini &mdash; diğer bir deyişle, ne zaman (ve yalnızca) form gönderilen.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="f7fa2-252">Bu durumda, bir test gibi kullanmadığınızı `if(IsPost && Validation.IsValid())`— diğer bir deyişle, her iki testleri kullanarak birleştirme değil ve.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="f7fa2-253">Bu sayfada, öncelikle bir form gönderme olup olmadığını belirlemeniz (`if(IsPost)`) ve yalnızca doğrulama alanları kaydedin.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="f7fa2-254">Doğrulama sonuçlarını sınayabilirsiniz sonra (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="f7fa2-255">Akış biraz daha farklıdır *AddMovie.cshtml* sayfa ancak etkisi aynıdır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="f7fa2-256">Metin kutuları değerlerini kullanarak alma `Request.Form["title"]` ve diğer benzer bir kod `<input>` öğeleri.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="f7fa2-257">Bu süre, film kimliği kodu dışında gizli alan alır dikkat edin (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="f7fa2-258">Sayfa ilk kez çalıştırdığınızda, kod sorgu dizesi dışında kimliği alındı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="f7fa2-259">Sorgu dizesi şekilde o zamandan bu yana değiştirildiği durumunda ilk olarak görüntülenen, film Kimliğini aldığınızdan emin olmak için gizli alanından değeri alır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="f7fa2-260">Arasındaki en önemli fark *AddMovie* kodu ve bu kodu olduğundan bu kodda, SQL kullanmanızı `Update` deyimi yerine `Insert Into` deyimi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="f7fa2-261">Aşağıdaki örnek SQL söz dizimi görülmektedir `Update` deyimi:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="f7fa2-262">Herhangi bir sırada hiçbir sütun belirtebilirsiniz ve her sütun sırasında güncelleştirmek mutlaka yoksa bir `Update` işlemi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="f7fa2-263">(Kimliği kendisini yürürlükte kaydettiğiniz kaydı yeni bir kayıt ve verilmeyen için olduğundan güncelleştirilemiyor bir `Update` işlemi.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="f7fa2-264">**Önemli** `Where` , veritabanı hangi veritabanı nasıl bilir olduğundan yan tümcesi Kimliğine sahip çok önemli güncelleştirmek istediğiniz kaydı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="f7fa2-265">Devre dışı bırakılırsa `Where` yan tümcesi, veritabanı güncelleştirilmesi *her* veritabanındaki kayıt.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="f7fa2-266">Çoğu durumda, bir olağanüstü durumda olacaktır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="f7fa2-267">Kodda yer tutucuları kullanarak SQL deyimine güncelleştirmek için değerleri geçirilir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="f7fa2-268">Ne biz önce belirttiniz yinelenecek: güvenlik nedenleriyle, *yalnızca* yer tutucuları bir SQL deyimi için değerleri geçirmek için kullanın.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="f7fa2-269">Kod kullanan sonra `db.Execute` çalıştırmak için `Update` deyimi, burada görebilirsiniz değişiklikleri geri listeleme sayfasına yönlendirir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="f7fa2-270">**Farklı SQL deyimlerini, farklı yöntemler**</span><span class="sxs-lookup"><span data-stu-id="f7fa2-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="f7fa2-271">Farklı SQL deyimlerini çalıştırmak için biraz farklı yöntemler kullanın fark etmiş olabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="f7fa2-272">Çalıştırmak için bir `Select` sorgu bu büyük olasılıkla döndürür birden çok kayıt, kullandığınız `Query` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="f7fa2-273">Çalıştırmak için bir `Select` bildiğiniz sorgu yalnızca bir veritabanı öğe döndürür, kullandığınız `QuerySingle` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="f7fa2-274">Değişiklikler yapmak, ancak veritabanı öğeleri dönüş yok komutları çalıştırmak için kullandığınız `Execute` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="f7fa2-275">Bunların her biri farklı sonuçlar döndürür çünkü zaten arasındaki farkı içinde anlatıldığı gibi farklı yöntemler sahip olmanız `Query` ve `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="f7fa2-276">( `Execute` Yöntemi gerçekte değeri döndürür de &mdash; yani, komut tarafından etkilenen veritabanı satır sayısını &mdash; ancak, o ana kadarki yoksayılıyor.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="f7fa2-277">Elbette, `Query` yöntemi yalnızca bir veritabanı satır döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="f7fa2-278">Ancak, ASP.NET sonuçlarını her zaman davranır `Query` yöntemi bir koleksiyon olarak.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="f7fa2-279">Yöntem yalnızca tek bir satır döndürüyorsa bile koleksiyondan tek satır ayıklamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="f7fa2-280">Bu nedenle, durumlarda Burada, *bilmeniz* yalnızca bir satır geri alırsınız, kullanmak daha kullanışlı bir bit `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="f7fa2-281">Belirli türlerdeki veritabanı işlemleri gerçekleştirmek birkaç yöntem vardır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="f7fa2-282">Veritabanı yöntemleri listesini bulabilirsiniz [ASP.NET Web sayfaları API hızlı başvuru](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="f7fa2-283">Doğrulama Kimliği daha fazla bilgi için sağlam hale getirme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="f7fa2-284">Bu film veritabanından alma çalışabilmeniz için sayfa çalıştığında, ilk kez, film kimliği Sorgu dizesinden alırsınız.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="f7fa2-285">Aslında, olduğunu bu kodu kullanarak yaptığınız arayın, gitmek için bir değer emin olun:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="f7fa2-286">Bu kod bir kullanıcı için alırsa emin olmak için kullanılan *EditMovies* bir filmi ilk seçmeden sayfa *filmler* sayfası, sayfayı kolay hata iletisi görüntüler.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="f7fa2-287">(Aksi halde, kullanıcılar muhtemelen yalnızca bunları işlemlerini birbirine karıştırmaktadır bir hata görürsünüz.)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="f7fa2-288">Ancak, bu doğrulama çok sağlam değil.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="f7fa2-289">Sayfa aynı zamanda bu hatalarla çağrılan:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="f7fa2-290">Kimliği bir sayı değil.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-290">The ID isn't a number.</span></span> <span data-ttu-id="f7fa2-291">Örneğin, sayfa gibi bir URL ile çağırılabilir `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="f7fa2-292">Kimlik numarasıdır, ancak mevcut olmayan bir filmi başvurduğu (örneğin, `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="f7fa2-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="f7fa2-293">Çalıştırma bu URL'lerden neden hataları görmek merak ediyorsanız *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="f7fa2-294">Düzenlemek için bir filmi seçin ve ardından URL'sini değiştirme *EditMovie* kimliği veya var olmayan film Kimliğini bir alfabetik içeren bir URL için sayfa.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="f7fa2-295">Bu nedenle neler?</span><span class="sxs-lookup"><span data-stu-id="f7fa2-295">So what should you do?</span></span> <span data-ttu-id="f7fa2-296">İlk düzeltmeyi yalnızca bir kimlik bu sayfaya geçirilir, ancak kimliği tamsayı olduğunu emin olmaktır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="f7fa2-297">Kodunu değiştirmek `!IsPost` test bu örneğe:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="f7fa2-298">İkinci bir koşul eklediğiniz `IsEmpty` ile bağlantılı bir test `&&` (mantıksal AND):</span><span class="sxs-lookup"><span data-stu-id="f7fa2-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="f7fa2-299">Unutmayın [ASP.NET Web sayfaları programlama giriş](../introducing-razor-syntax-c.md) yöntemleri gibi öğretici `AsBool` bir `AsInt` bir karakter dizesi başka bazı veri türüne dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="f7fa2-300">`IsInt` Yöntemi (ve diğer `IsBool` ve `IsDateTime`) benzer.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="f7fa2-301">Ancak, bunlar yalnızca sınama olup olmadığını, *için* dize dönüştürme işlemini gerçekleştirmeden Dönüştür.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="f7fa2-302">Bu nedenle Burada, aslında belirten *sorgu dizesi değeri tamsayıya dönüştürülebilir ise...* .</span><span class="sxs-lookup"><span data-stu-id="f7fa2-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="f7fa2-303">Olası bir sorunu mevcut olmayan bir film arıyor.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="f7fa2-304">Bir filmi alma kodu bu kod gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="f7fa2-305">Geçirirseniz bir `movieId` değeri `QuerySingle` gerçek bir filmi karşılık gelmiyor yöntemi, hiçbir şey döndürülür ve deyimleri izleyin (örneğin, `title=row.Title`) neden hatalar.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="f7fa2-306">Yeniden kolay bir düzeltme yoktur.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-306">Again there's an easy fix.</span></span> <span data-ttu-id="f7fa2-307">Varsa `db.QuerySingle` yöntemi, hiçbir sonuç döndürür `row` değişkeni boş olacaktır.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="f7fa2-308">Böylece kontrol olup olmadığını `row` değerleri elde denemeden önce değişkeni null.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="f7fa2-309">Aşağıdaki kod ekler bir `if` değerlerini kullanıma alın deyimleri geçici blok `row` nesnesi:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="f7fa2-310">Bu iki ek doğrulama testleri sayfası daha madde işareti kanıt haline gelir.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="f7fa2-311">İçin tam kod `!IsPost` şube şimdi aşağıdaki gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="f7fa2-312">Biz kez daha bu görev için iyi bir kullanım olduğuna dikkat edin bir `else` bloğu.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="f7fa2-313">Testleri geçirmezseniz `else` blokları hata iletileri ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="f7fa2-314">Film sayfasına geri dönmek için bağlantı ekleme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="f7fa2-315">Bağlantı eklemek için son ve yararlı bir ayrıntısı şöyle geri *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="f7fa2-316">Kullanıcıların olayları sıradan akışında başlangıç *filmler* sayfasında ve tıklayın bir **Düzenle** bağlantı.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="f7fa2-317">Getirir bunları *EditMovie* sayfasında, burada film düzenleme yapıp düğmesini tıklatın.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="f7fa2-318">Kod değişikliği işledikten sonra geri yönlendiren *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="f7fa2-319">Ancak:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-319">However:</span></span>

- <span data-ttu-id="f7fa2-320">Kullanıcı değişikliği değil karar verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="f7fa2-321">Kullanıcı bu sayfaya ilk tıklamadan getirildiğini bir **Düzenle** bağlamak *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="f7fa2-322">Her iki durumda da, bunları ana listesine geri dönmek daha kolay hale getirmek istediğiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="f7fa2-323">Kolay bir düzeltme durumdadır &mdash; kapattıktan sonra aşağıdaki biçimlendirme eklemek `</form>` işaretleme etiketinde:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="f7fa2-324">Bu biçimlendirme için aynı sözdizimini kullanan bir `<a>` başka bir yerde gördünüz öğesi.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="f7fa2-325">URL içeren `~` "Web sitesinin kök." anlamına gelir</span><span class="sxs-lookup"><span data-stu-id="f7fa2-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="f7fa2-326">Film güncelleştirme işlemini test etme</span><span class="sxs-lookup"><span data-stu-id="f7fa2-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="f7fa2-327">Artık test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-327">Now you can test.</span></span> <span data-ttu-id="f7fa2-328">Çalıştırma *filmler* sayfasında ve tıklayın **Düzenle** film yanındaki.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="f7fa2-329">Zaman *EditMovie* sayfası görüntülenirse, tıklatın ve film değişiklik **gönderme değişiklikleri**.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="f7fa2-330">Film listesi göründüğünde, değişikliklerinizi gösterildiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="f7fa2-331">Doğrulama çalıştığından emin olmak için tıklatın **Düzenle** başka bir filmi için.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="f7fa2-332">Ulaştığınızda *EditMovie* sayfasında, Temizle **Tarz** alan (veya **yıl** alan veya her ikisi de) ve değişikliklerinizi göndermeyi deneyin.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="f7fa2-333">Beklediğiniz gibi bir hata görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="f7fa2-333">You'll see an error, as you'd expect:</span></span>

![Doğrulama hataları gösteren film sayfasını Düzenle](updating-data/_static/image4.png)

<span data-ttu-id="f7fa2-335">Tıklatın **film dökümüne dönün** yaptığınız değişiklikleri iptal ve dönmek için bağlantı *filmler* sayfası.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="f7fa2-336">Sıradaki gelen</span><span class="sxs-lookup"><span data-stu-id="f7fa2-336">Coming Up Next</span></span>

<span data-ttu-id="f7fa2-337">Sonraki öğreticide film kaydını silmek nasıl görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="f7fa2-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="f7fa2-338">Tam listesi için (düzenleme bağlantıları ile güncelleştirilmiş) film sayfası</span><span class="sxs-lookup"><span data-stu-id="f7fa2-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="f7fa2-339">Sayfa listesi düzenleme film sayfa için tamamlayın</span><span class="sxs-lookup"><span data-stu-id="f7fa2-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="f7fa2-340">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="f7fa2-340">Additional Resources</span></span>

- [<span data-ttu-id="f7fa2-341">Razor sözdizimini kullanarak ASP.NET Web programlamaya giriş</span><span class="sxs-lookup"><span data-stu-id="f7fa2-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](introducing-razor-syntax-c.md)
- <span data-ttu-id="f7fa2-342">[SQL güncelleştirme deyimini](http://www.w3schools.com/sql/sql_update.asp) W3Schools sitesinde</span><span class="sxs-lookup"><span data-stu-id="f7fa2-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="f7fa2-343">[Önceki](entering-data.md)
[sonraki](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="f7fa2-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
